x
1,numeric or complex vectors or objects which can becoerced to such, or other objects for which methods have been written.
2,date objects
3,date objects or character vectors.  (Charactervectors are converted by <code>as.Date</code>.)
4,a numeric vector (in days) <em>or</em> an object of class<code>"difftime"</code>, rounded to the nearest whole day.
5,One of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
6,an object to be printed or summarized from one of thedate-time classes.
7,for timezone formatting, passed to <code>format.POSIXct</code>.
8,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
9,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
10,further arguments to be passed from or to other methods.
11,date-time objects
12,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
13,a numeric vector (in seconds)
14,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
15,starting value of sequence.
16,(maximal) end value of the sequence.
17,<code>factor</code>s of the same length.
18,package name: symbol or literal character string.
19,variable name: symbol or literal character string.
20,package name: symbol or literal character string.
21,variable name: symbol or literal character string.
22,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
23,An object, for the methods inheriting from class <code>"hexmode"</code>.
24,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
25,a logical indicating whether to use upper-caseletters or lower-case letters (default).
26,further arguments passed to or from other methods.
27,An object, for the methods inheriting from class <code>"octmode"</code>.
28,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
29,further arguments passed to or from other methods.
30,atomic vectors, symbols, calls, or other objects for whichmethods have been written.
31,object from which to extract element(s) or in which to replace element(s).
32,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

33,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

34,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

35,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
36,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
37,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
38,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
39,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
40,an object
41,data frame.
42,elements to extract or replace.  For <code>[</code> and<code>[[</code>, these are <code>numeric</code> or <code>character</code> or, for<code>[</code> only, empty or <code>logical</code>.  Numeric values are coerced to integer as ifby <code>as.integer</code>.  For replacement by <code>[</code>, a logicalmatrix is allowed.
43,A literal character string or a name (possibly backtickquoted).
44,logical.  If <code>TRUE</code> the result is coerced to thelowest possible dimension.  The default is to drop if only onecolumn is left, but <b>not</b> to drop if only one row is left.
45,A suitable replacement value: it will be repeated a wholenumber of times if necessary and it may be coerced: see theCoercion section.  If <code>NULL</code>, deletes the column if a singlecolumn is selected.
46,logical: see <code>[</code>, and applies to column names.
47,a <code>Date</code> object to be summarized or printed.
48,number of significant digits for the computations.
49,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
50,further arguments to be passed from or to other methods.
51,date-time or date objects.
52,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
53,character string.  Units in which the results aredesired.  Can be abbreviated.
54,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
55,character string or numeric value specifying a time interval.
56,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
57,an object inheriting from class <code>"difftime"</code>.
58,arguments to be passed to or from other methods.
59,object from which to extract element(s) or in which to replace element(s).
60,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

61,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

62,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

63,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
64,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
65,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
66,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
67,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
68,a factor
69,a specification of indices – see <code>Extract</code>.
70,logical.  If true, unused levels are dropped.
71,character: a set of levels.  Factor values are coerced tocharacter.
72,An object, for the methods inheriting from class <code>"hexmode"</code>.
73,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
74,a logical indicating whether to use upper-caseletters or lower-case letters (default).
75,further arguments passed to or from other methods.
76,object from which to extract element(s) or in which to replace element(s).
77,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

78,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

79,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

80,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
81,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
82,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
83,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
84,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
85,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, typically a vector of<code>character</code> strings.
86,optional <code>logical</code> eventually to be passed to<code>print()</code>, used by <code>print.default()</code>, indicatingwhether or not strings should be right aligned.
87,an object of class <code>"noquote"</code>.
88,further options passed to next methods, such as <code>print</code>.
89,for compatibility with the generic <code>c</code> function.
90,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
91,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
92,An object, for the methods inheriting from class <code>"octmode"</code>.
93,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
94,further arguments passed to or from other methods.
95,an object to be printed or summarized from one of thedate-time classes.
96,for timezone formatting, passed to <code>format.POSIXct</code>.
97,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
98,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
99,further arguments to be passed from or to other methods.
100,date-time objects
101,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
102,a numeric vector (in seconds)
103,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
104,an object to be printed or summarized from one of thedate-time classes.
105,for timezone formatting, passed to <code>format.POSIXct</code>.
106,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
107,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
108,further arguments to be passed from or to other methods.
109,date-time objects
110,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
111,a numeric vector (in seconds)
112,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
113,object from which to extract element(s) or in which to replace element(s).
114,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

115,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

116,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

117,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
118,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
119,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
120,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
121,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
122,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
123,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
124,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
125,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
126,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
127,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
128,a character vector giving the row names for the dataframe.
129,The name to be used for the column of tableentries, usually counts.
130,logical: should the classifying factors bereturned as factors (the default) or character vectors?
131,passed to <code>provideDimnames</code>.
132,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
133,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
134,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
135,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
136,a character string <code>cat()</code>ed before themessages are printed.
137,object from which to extract element(s) or in which to replace element(s).
138,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

139,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

140,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

141,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
142,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
143,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
144,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
145,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
146,data frame.
147,elements to extract or replace.  For <code>[</code> and<code>[[</code>, these are <code>numeric</code> or <code>character</code> or, for<code>[</code> only, empty or <code>logical</code>.  Numeric values are coerced to integer as ifby <code>as.integer</code>.  For replacement by <code>[</code>, a logicalmatrix is allowed.
148,A literal character string or a name (possibly backtickquoted).
149,logical.  If <code>TRUE</code> the result is coerced to thelowest possible dimension.  The default is to drop if only onecolumn is left, but <b>not</b> to drop if only one row is left.
150,A suitable replacement value: it will be repeated a wholenumber of times if necessary and it may be coerced: see theCoercion section.  If <code>NULL</code>, deletes the column if a singlecolumn is selected.
151,logical: see <code>[</code>, and applies to column names.
152,a <code>Date</code> object to be summarized or printed.
153,number of significant digits for the computations.
154,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
155,further arguments to be passed from or to other methods.
156,a factor
157,a specification of indices – see <code>Extract</code>.
158,logical.  If true, unused levels are dropped.
159,character: a set of levels.  Factor values are coerced tocharacter.
160,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
161,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
162,an object to be printed or summarized from one of thedate-time classes.
163,for timezone formatting, passed to <code>format.POSIXct</code>.
164,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
165,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
166,further arguments to be passed from or to other methods.
167,date-time objects
168,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
169,a numeric vector (in seconds)
170,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
171,an object to be printed or summarized from one of thedate-time classes.
172,for timezone formatting, passed to <code>format.POSIXct</code>.
173,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
174,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
175,further arguments to be passed from or to other methods.
176,date-time objects
177,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
178,a numeric vector (in seconds)
179,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
180,object from which to extract element(s) or in which to replace element(s).
181,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

182,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

183,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

184,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
185,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
186,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
187,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
188,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
189,data frame.
190,elements to extract or replace.  For <code>[</code> and<code>[[</code>, these are <code>numeric</code> or <code>character</code> or, for<code>[</code> only, empty or <code>logical</code>.  Numeric values are coerced to integer as ifby <code>as.integer</code>.  For replacement by <code>[</code>, a logicalmatrix is allowed.
191,A literal character string or a name (possibly backtickquoted).
192,logical.  If <code>TRUE</code> the result is coerced to thelowest possible dimension.  The default is to drop if only onecolumn is left, but <b>not</b> to drop if only one row is left.
193,A suitable replacement value: it will be repeated a wholenumber of times if necessary and it may be coerced: see theCoercion section.  If <code>NULL</code>, deletes the column if a singlecolumn is selected.
194,logical: see <code>[</code>, and applies to column names.
195,a factor
196,a specification of indices – see <code>Extract</code>.
197,logical.  If true, unused levels are dropped.
198,character: a set of levels.  Factor values are coerced tocharacter.
199,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
200,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
201,an object to be printed or summarized from one of thedate-time classes.
202,for timezone formatting, passed to <code>format.POSIXct</code>.
203,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
204,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
205,further arguments to be passed from or to other methods.
206,date-time objects
207,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
208,a numeric vector (in seconds)
209,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
210,object from which to extract element(s) or in which to replace element(s).
211,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

212,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

213,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

214,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
215,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
216,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
217,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
218,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
219,data frame.
220,elements to extract or replace.  For <code>[</code> and<code>[[</code>, these are <code>numeric</code> or <code>character</code> or, for<code>[</code> only, empty or <code>logical</code>.  Numeric values are coerced to integer as ifby <code>as.integer</code>.  For replacement by <code>[</code>, a logicalmatrix is allowed.
221,A literal character string or a name (possibly backtickquoted).
222,logical.  If <code>TRUE</code> the result is coerced to thelowest possible dimension.  The default is to drop if only onecolumn is left, but <b>not</b> to drop if only one row is left.
223,A suitable replacement value: it will be repeated a wholenumber of times if necessary and it may be coerced: see theCoercion section.  If <code>NULL</code>, deletes the column if a singlecolumn is selected.
224,logical: see <code>[</code>, and applies to column names.
225,a <code>Date</code> object to be summarized or printed.
226,number of significant digits for the computations.
227,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
228,further arguments to be passed from or to other methods.
229,date-time or date objects.
230,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
231,character string.  Units in which the results aredesired.  Can be abbreviated.
232,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
233,character string or numeric value specifying a time interval.
234,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
235,an object inheriting from class <code>"difftime"</code>.
236,arguments to be passed to or from other methods.
237,a factor
238,a specification of indices – see <code>Extract</code>.
239,logical.  If true, unused levels are dropped.
240,character: a set of levels.  Factor values are coerced tocharacter.
241,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
242,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
243,an object to be printed or summarized from one of thedate-time classes.
244,for timezone formatting, passed to <code>format.POSIXct</code>.
245,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
246,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
247,further arguments to be passed from or to other methods.
248,date-time objects
249,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
250,a numeric vector (in seconds)
251,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
252,an object to be printed or summarized from one of thedate-time classes.
253,for timezone formatting, passed to <code>format.POSIXct</code>.
254,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
255,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
256,further arguments to be passed from or to other methods.
257,date-time objects
258,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
259,a numeric vector (in seconds)
260,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
261,An object from a formally defined (S4) class.
262,The character-string name of the slot, quoted ornot.  Must be the name of a slot in the definition of the classof <code>object</code>.
263,A replacement value for the slot, which must be from aclass compatible with the class defined for this slot in thedefinition of the class of  <code>object</code>.
264,An object from a formally defined (S4) class.
265,The character-string name of the slot, quoted ornot.  Must be the name of a slot in the definition of the classof <code>object</code>.
266,A replacement value for the slot, which must be from aclass compatible with the class defined for this slot in thedefinition of the class of  <code>object</code>.
267,numeric or complex vectors or objects which can becoerced to such, or other objects for which methods have been written.
268,date-time or date objects.
269,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
270,character string.  Units in which the results aredesired.  Can be abbreviated.
271,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
272,character string or numeric value specifying a time interval.
273,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
274,an object inheriting from class <code>"difftime"</code>.
275,arguments to be passed to or from other methods.
276,numeric or complex vectors or objects which can becoerced to such, or other objects for which methods have been written.
277,date-time or date objects.
278,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
279,character string.  Units in which the results aredesired.  Can be abbreviated.
280,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
281,character string or numeric value specifying a time interval.
282,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
283,an object inheriting from class <code>"difftime"</code>.
284,arguments to be passed to or from other methods.
285,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
286,An object, for the methods inheriting from class <code>"hexmode"</code>.
287,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
288,a logical indicating whether to use upper-caseletters or lower-case letters (default).
289,further arguments passed to or from other methods.
290,An object, for the methods inheriting from class <code>"octmode"</code>.
291,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
292,further arguments passed to or from other methods.
293,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
294,numeric or complex matrices or vectors.
295,numeric or complex vectors or objects which can becoerced to such, or other objects for which methods have been written.
296,numeric or complex vectors or objects which can becoerced to such, or other objects for which methods have been written.
297,vector or <code>NULL</code>: the values to be matched.Long vectors are supported.
298,vector or <code>NULL</code>: the values to be matched against.Long vectors are not supported.
299,the value to be returned in the case when no match isfound.  Note that it is coerced to <code>integer</code>.
300,a vector of values that cannot be matched.  Anyvalue in <code>x</code> matching a value in this vector is assigned the<code>nomatch</code> value.  For historical reasons, <code>FALSE</code> isequivalent to <code>NULL</code>.
301,First and second arguments for function <code>FUN</code>.Typically a vector or array.
302,a function to use on the outer products, found <em>via</em><code>match.fun</code> (except for the special case <code>"*"</code>).
303,optional arguments to be passed to <code>FUN</code>.
304,A vector or array.
305,A vector or array.
306,a function; it may be a quoted string.
307,Provide dimnames that are the product of thedimnames of <code>X</code> and <code>Y</code>.
308,optional arguments to be passed to <code>FUN</code>.
309,numeric or complex vectors or objects which can becoerced to such, or other objects for which methods have been written.
310,numeric or complex vectors or objects which can becoerced to such, or other objects for which methods have been written.
311,date objects
312,date objects or character vectors.  (Charactervectors are converted by <code>as.Date</code>.)
313,a numeric vector (in days) <em>or</em> an object of class<code>"difftime"</code>, rounded to the nearest whole day.
314,One of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
315,an object to be printed or summarized from one of thedate-time classes.
316,for timezone formatting, passed to <code>format.POSIXct</code>.
317,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
318,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
319,further arguments to be passed from or to other methods.
320,date-time objects
321,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
322,a numeric vector (in seconds)
323,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
324,atomic vectors, symbols, calls, or other objects for whichmethods have been written.
325,a variable name (possibly quoted).
326,a value to be assigned to <code>x</code>.
327,a variable name (possibly quoted).
328,a value to be assigned to <code>x</code>.
329,atomic vectors, symbols, calls, or other objects for whichmethods have been written.
330,a variable name (possibly quoted).
331,a value to be assigned to <code>x</code>.
332,atomic vectors, symbols, calls, or other objects for whichmethods have been written.
333,atomic vectors, symbols, calls, or other objects for whichmethods have been written.
334,atomic vectors, symbols, calls, or other objects for whichmethods have been written.
335,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
336,An object, for the methods inheriting from class <code>"hexmode"</code>.
337,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
338,a logical indicating whether to use upper-caseletters or lower-case letters (default).
339,further arguments passed to or from other methods.
340,An object, for the methods inheriting from class <code>"octmode"</code>.
341,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
342,further arguments passed to or from other methods.
343,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
344,symbolic expressions.
345,object from which to extract element(s) or in which to replace element(s).
346,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

347,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

348,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

349,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
350,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
351,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
352,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
353,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
354,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
355,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
356,object from which to extract element(s) or in which to replace element(s).
357,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

358,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

359,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

360,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
361,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
362,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
363,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
364,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
365,data frame.
366,elements to extract or replace.  For <code>[</code> and<code>[[</code>, these are <code>numeric</code> or <code>character</code> or, for<code>[</code> only, empty or <code>logical</code>.  Numeric values are coerced to integer as ifby <code>as.integer</code>.  For replacement by <code>[</code>, a logicalmatrix is allowed.
367,A literal character string or a name (possibly backtickquoted).
368,logical.  If <code>TRUE</code> the result is coerced to thelowest possible dimension.  The default is to drop if only onecolumn is left, but <b>not</b> to drop if only one row is left.
369,A suitable replacement value: it will be repeated a wholenumber of times if necessary and it may be coerced: see theCoercion section.  If <code>NULL</code>, deletes the column if a singlecolumn is selected.
370,logical: see <code>[</code>, and applies to column names.
371,a character vector of names to be abbreviated, or anobject to be coerced to a character vector by <code>as.character</code>.
372,the minimum length of the abbreviations.
373,logical: should lowercase characters be removed first?
374,logical: should a dot (<code>"."</code>) be appended?
375,logical: should <code>minlength</code> be observed strictly?Note that setting <code>strict = TRUE</code> may return <em>non</em>-uniquestrings.
376,a character string specifying the method used with default<code>"left.kept"</code>, see ‘Details’ below.  Partial matchesallowed.
377,logical: should <code>names</code> (with original vector) be returned.
378,a numeric or <code>complex</code> vector or array.
379,numeric or complex vectors.
380,a numeric or complex vector
381,an environment.
382,logical specifying whether bindings should be locked.
383,a name object or character string.
384,a function taking zero or one arguments.
385,a vector of data, usually taking a small number of distinctvalues.
386,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
387,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
388,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
389,logical flag to determine if the levels should be regardedas ordered (in the order given).
390,an upper bound on the number of levels; see ‘Details’.
391,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
392,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
393,the function that is to be invoked each time a top-level taskis successfully completed.  This is called with 5 or 4 argumentsdepending on whether <code>data</code> is specified or not, respectively.The return value should be a logical value indicating whether tokeep the callback in the list of active callbacks or discard it.
394,if specified, this is the 5-th argument in the call to thecallback function <code>f</code>.
395,a string or an integer identifying the element in theinternal callback list to be removed.Integer indices are 1-based, i.e the first element is 1.The names of currently registered handlers is availableusing <code>getTaskCallbackNames</code>and is also returned in a call to <code>addTaskCallback</code>.
396,character: names to be used.
397,a non-empty character string to be matched.  For<code>fixed = FALSE</code> this should contain an extendedregular expression. Coerced by <code>as.character</code> to a string if possible.
398,character vector where matches are sought.Coerced by <code>as.character</code> to a character vector ifpossible.
399,Maximum distance allowed for a match.  Expressedeither as integer, or as a fraction of the <em>pattern</em> lengthtimes the maximal transformation cost (will be replaced by thesmallest integer not less than the corresponding fraction), or alist with possible components

400,maximum number/fraction of match cost(generalized Levenshtein distance)
401,maximal number/fraction of <em>all</em>transformations (insertions, deletions and substitutions)
402,maximum number/fraction of insertions
403,maximum number/fraction of deletions
404,maximum number/fraction ofsubstitutions
405,If <code>cost</code> is not given, <code>all</code> defaults to 10%, and theother transformation number bounds default to <code>all</code>.The component names can be abbreviated.
406,a numeric vector or list with names partially matching<span class="samp">insertions</span>, <span class="samp">deletions</span> and <span class="samp">substitutions</span> givingthe respective costs for computing the generalized Levenshteindistance, or <code>NULL</code> (default) indicating using unit cost forall three possible transformations.Coerced to integer via <code>as.integer</code> if possible.
407,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
408,if <code>FALSE</code>, a vector containing the (integer)indices of the matches determined is returned and if <code>TRUE</code>, avector containing the matching elements themselves is returned.
409,logical.  If <code>TRUE</code> (default), the pattern ismatched literally (as is).  Otherwise, it is matched as a regularexpression.
410,logical. in a multibyte locale, should the comparisonbe character-by-character (the default) or byte-by-byte.
411,a non-empty character string to be matched.  For<code>fixed = FALSE</code> this should contain an extendedregular expression. Coerced by <code>as.character</code> to a string if possible.
412,character vector where matches are sought.Coerced by <code>as.character</code> to a character vector ifpossible.
413,Maximum distance allowed for a match.  Expressedeither as integer, or as a fraction of the <em>pattern</em> lengthtimes the maximal transformation cost (will be replaced by thesmallest integer not less than the corresponding fraction), or alist with possible components

414,maximum number/fraction of match cost(generalized Levenshtein distance)
415,maximal number/fraction of <em>all</em>transformations (insertions, deletions and substitutions)
416,maximum number/fraction of insertions
417,maximum number/fraction of deletions
418,maximum number/fraction ofsubstitutions
419,If <code>cost</code> is not given, <code>all</code> defaults to 10%, and theother transformation number bounds default to <code>all</code>.The component names can be abbreviated.
420,a numeric vector or list with names partially matching<span class="samp">insertions</span>, <span class="samp">deletions</span> and <span class="samp">substitutions</span> givingthe respective costs for computing the generalized Levenshteindistance, or <code>NULL</code> (default) indicating using unit cost forall three possible transformations.Coerced to integer via <code>as.integer</code> if possible.
421,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
422,if <code>FALSE</code>, a vector containing the (integer)indices of the matches determined is returned and if <code>TRUE</code>, avector containing the matching elements themselves is returned.
423,logical.  If <code>TRUE</code> (default), the pattern ismatched literally (as is).  Otherwise, it is matched as a regularexpression.
424,logical. in a multibyte locale, should the comparisonbe character-by-character (the default) or byte-by-byte.
425,objects, possibly named.
426,object to be coerced or tested.
427,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
428,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
429,zero or more logical vectors.  Other objects of zerolength are ignored, and the rest are coerced to logical ignoringany class.
430,logical.  If true <code>NA</code> values are removed beforethe result is computed.
431,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
432,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
433,further arguments for different methods, notably thefollowing two, for numerical comparison:
434,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
435,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
436,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
437,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
438,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
439,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
440,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
441,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
442,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
443,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
444,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
445,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
446,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
447,further arguments for different methods, notably thefollowing two, for numerical comparison:
448,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
449,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
450,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
451,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
452,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
453,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
454,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
455,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
456,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
457,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
458,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
459,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
460,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
461,further arguments for different methods, notably thefollowing two, for numerical comparison:
462,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
463,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
464,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
465,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
466,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
467,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
468,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
469,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
470,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
471,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
472,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
473,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
474,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
475,further arguments for different methods, notably thefollowing two, for numerical comparison:
476,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
477,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
478,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
479,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
480,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
481,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
482,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
483,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
484,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
485,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
486,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
487,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
488,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
489,further arguments for different methods, notably thefollowing two, for numerical comparison:
490,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
491,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
492,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
493,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
494,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
495,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
496,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
497,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
498,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
499,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
500,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
501,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
502,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
503,further arguments for different methods, notably thefollowing two, for numerical comparison:
504,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
505,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
506,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
507,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
508,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
509,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
510,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
511,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
512,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
513,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
514,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
515,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
516,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
517,further arguments for different methods, notably thefollowing two, for numerical comparison:
518,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
519,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
520,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
521,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
522,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
523,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
524,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
525,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
526,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
527,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
528,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
529,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
530,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
531,further arguments for different methods, notably thefollowing two, for numerical comparison:
532,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
533,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
534,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
535,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
536,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
537,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
538,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
539,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
540,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
541,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
542,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
543,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
544,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
545,further arguments for different methods, notably thefollowing two, for numerical comparison:
546,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
547,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
548,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
549,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
550,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
551,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
552,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
553,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
554,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
555,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
556,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
557,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
558,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
559,further arguments for different methods, notably thefollowing two, for numerical comparison:
560,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
561,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
562,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
563,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
564,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
565,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
566,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
567,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
568,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
569,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
570,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
571,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
572,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
573,further arguments for different methods, notably thefollowing two, for numerical comparison:
574,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
575,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
576,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
577,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
578,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
579,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
580,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
581,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
582,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
583,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
584,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
585,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
586,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
587,further arguments for different methods, notably thefollowing two, for numerical comparison:
588,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
589,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
590,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
591,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
592,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
593,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
594,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
595,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
596,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
597,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
598,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
599,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
600,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
601,further arguments for different methods, notably thefollowing two, for numerical comparison:
602,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
603,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
604,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
605,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
606,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
607,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
608,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
609,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
610,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
611,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
612,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
613,an expression or call from which the namesare to be extracted.
614,a logical value indicating whether function namesshould be included in the result.
615,the maximum number of names to be returned.  <code>-1</code>indicates no limit (other than vector size limits).
616,a logical value which indicates whether duplicate namesshould be removed from the value.
617,an expression or call from which the namesare to be extracted.
618,a logical value indicating whether function namesshould be included in the result.
619,the maximum number of names to be returned.  <code>-1</code>indicates no limit (other than vector size limits).
620,a logical value which indicates whether duplicate namesshould be removed from the value.
621,a condition object.
622,call expression.
623,a condition object.
624,expression to be evaluated.
625,expression to be evaluated before returning or exiting.
626,function.
627,character string.
628,character string.
629,character string naming a restart.
630,restart object.
631,object.
632,character string naming a condition class.
633,additional arguments; see details below.
634,zero or more logical vectors.  Other objects of zerolength are ignored, and the rest are coerced to logical ignoringany class.
635,logical.  If true <code>NA</code> values are removed beforethe result is computed.
636,a vector or a data frame or an array or <code>NULL</code>.
637,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
638,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
639,the maximum number of unique items expected (greater than one).
640,arguments for particular methods.
641,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
642,a vector or a data frame or an array or <code>NULL</code>.
643,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
644,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
645,the maximum number of unique items expected (greater than one).
646,arguments for particular methods.
647,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
648,a vector or a data frame or an array or <code>NULL</code>.
649,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
650,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
651,the maximum number of unique items expected (greater than one).
652,arguments for particular methods.
653,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
654,a vector or a data frame or an array or <code>NULL</code>.
655,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
656,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
657,the maximum number of unique items expected (greater than one).
658,arguments for particular methods.
659,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
660,a vector or a data frame or an array or <code>NULL</code>.
661,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
662,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
663,the maximum number of unique items expected (greater than one).
664,arguments for particular methods.
665,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
666,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default method for<code>is.na</code> and <code>anyNA</code> handle atomic vectors, lists,pairlists, and <code>NULL</code>.
667,logical: should <code>anyNA</code> be applied recursivelyto lists and pairlists?
668,a suitable index vector for use with <code>x</code>.
669,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default method for<code>is.na</code> and <code>anyNA</code> handle atomic vectors, lists,pairlists, and <code>NULL</code>.
670,logical: should <code>anyNA</code> be applied recursivelyto lists and pairlists?
671,a suitable index vector for use with <code>x</code>.
672,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
673,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
674,an object to be printed or summarized from one of thedate-time classes.
675,for timezone formatting, passed to <code>format.POSIXct</code>.
676,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
677,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
678,further arguments to be passed from or to other methods.
679,date-time objects
680,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
681,a numeric vector (in seconds)
682,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
683,the array to be transposed.
684,the subscript permutation vector, usually a permutation ofthe integers <code>1:n</code>, where <code>n</code> is the number of dimensionsof <code>a</code>.  When <code>a</code> has named dimnames, it can be acharacter vector of length <code>n</code> giving a permutation of thosenames. The default (used whenever <code>perm</code> has zero length) is toreverse the order of the dimensions.
685,a flag indicating whether the vector should beresized as well as having its elements reordered (default <code>TRUE</code>).
686,logical indicating if the result should be of thesame class as <code>a</code>.
687,potential further arguments of methods.
688,the array to be transposed.
689,the subscript permutation vector, usually a permutation ofthe integers <code>1:n</code>, where <code>n</code> is the number of dimensionsof <code>a</code>.  When <code>a</code> has named dimnames, it can be acharacter vector of length <code>n</code> giving a permutation of thosenames. The default (used whenever <code>perm</code> has zero length) is toreverse the order of the dimensions.
690,a flag indicating whether the vector should beresized as well as having its elements reordered (default <code>TRUE</code>).
691,logical indicating if the result should be of thesame class as <code>a</code>.
692,potential further arguments of methods.
693,the array to be transposed.
694,the subscript permutation vector, usually a permutation ofthe integers <code>1:n</code>, where <code>n</code> is the number of dimensionsof <code>a</code>.  When <code>a</code> has named dimnames, it can be acharacter vector of length <code>n</code> giving a permutation of thosenames. The default (used whenever <code>perm</code> has zero length) is toreverse the order of the dimensions.
695,a flag indicating whether the vector should beresized as well as having its elements reordered (default <code>TRUE</code>).
696,logical indicating if the result should be of thesame class as <code>a</code>.
697,potential further arguments of methods.
698,the vector the values are to be appended to.
699,to be included in the modified vector.
700,a subscript, after which the values are to be appended.
701,an array, including a matrix.
702,a vector giving the subscripts which the function willbe applied over.  E.g., for a matrix <code>1</code> indicates rows,<code>2</code> indicates columns, <code>c(1, 2)</code> indicates rows andcolumns. Where <code>X</code> has named dimnames, it can be a charactervector selecting dimension names.
703,the function to be applied: see ‘Details’.In the case of functions like <code>+</code>, <code>%*%</code>, etc., thefunction name must be backquoted or quoted.
704,optional arguments to <code>FUN</code>.
705,a logical indicating whether results should besimplified if possible.
706,numeric.  Desired length of the output vector,inputs being recycled as needed.
707,numeric vector.
708,numeric vector.
709,numeric vector.
710,numeric vector.
711,an object, probably of mode <code>complex</code>.
712,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
713,further arguments passed to or from other methods.
714,a function (a closure or a primitive).If <code>name</code> is a character string then the function with thatname is found and used.
715,a vector (including a list or <code>expression</code>vector) giving data to fill the array.  Non-atomic classed objectsare coerced by <code>as.vector</code>.
716,the dim attribute for the array to be created, that is aninteger vector of length one or more giving the maximal indices ineach dimension.
717,either <code>NULL</code> or the names for the dimensions.This must a list (or it will be ignored) with one component for eachdimension, either <code>NULL</code> or a character vector of the lengthgiven by <code>dim</code> for that dimension.  The list can be named, andthe list names will be used as names for the dimensions.  If thelist is shorter than the number of dimensions, it is extended by<code>NULL</code>s to the length required.
718,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
719,additional arguments to be passed to or from methods.
720,a <code>logical</code> vector or array.  <code>NA</code>sare allowed and omitted (treated as if <code>FALSE</code>).
721,logical; should <b>arr</b>ay <b>ind</b>ices be returnedwhen <code>x</code> is an array?
722,integer-valued index vector, as resulting from<code>which(x)</code>.
723,<code>dim(.)</code> integer vector
724,optional list of character <code>dimnames(.)</code>.If <code>useNames</code> is true, to be used for constructing dimnames for<code>arrayInd()</code> (and hence, <code>which(*, arr.ind=TRUE)</code>).If <code>names(.dimnames)</code> is not empty, these are used ascolumn names.  <code>.dimnames[[1]]</code> is used as row names.
725,logical indicating if the value of <code>arrayInd()</code>should have (non-null) dimnames at all.
726,a vector (including a list or <code>expression</code>vector) giving data to fill the array.  Non-atomic classed objectsare coerced by <code>as.vector</code>.
727,the dim attribute for the array to be created, that is aninteger vector of length one or more giving the maximal indices ineach dimension.
728,either <code>NULL</code> or the names for the dimensions.This must a list (or it will be ignored) with one component for eachdimension, either <code>NULL</code> or a character vector of the lengthgiven by <code>dim</code> for that dimension.  The list can be named, andthe list names will be used as names for the dimensions.  If thelist is shorter than the number of dimensions, it is extended by<code>NULL</code>s to the length required.
729,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
730,additional arguments to be passed to or from methods.
731,a vector (including a list or <code>expression</code>vector) giving data to fill the array.  Non-atomic classed objectsare coerced by <code>as.vector</code>.
732,the dim attribute for the array to be created, that is aninteger vector of length one or more giving the maximal indices ineach dimension.
733,either <code>NULL</code> or the names for the dimensions.This must a list (or it will be ignored) with one component for eachdimension, either <code>NULL</code> or a character vector of the lengthgiven by <code>dim</code> for that dimension.  The list can be named, andthe list names will be used as names for the dimensions.  If thelist is shorter than the number of dimensions, it is extended by<code>NULL</code>s to the length required.
734,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
735,additional arguments to be passed to or from methods.
736,a non-empty character string naming the function to be called.
737,arguments to be part of the call.
738,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
739,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
740,object to be coerced or tested.
741,further arguments passed to or from other methods.
742,a condition object.
743,call expression.
744,a condition object.
745,expression to be evaluated.
746,expression to be evaluated before returning or exiting.
747,function.
748,character string.
749,character string.
750,character string naming a restart.
751,restart object.
752,object.
753,character string naming a condition class.
754,additional arguments; see details below.
755,an object to be converted.
756,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
757,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
758,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
759,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
760,a time zone name.
761,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
762,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
763,object to be coerced or tested.
764,further arguments passed to or from other methods.
765,a condition object.
766,call expression.
767,a condition object.
768,expression to be evaluated.
769,expression to be evaluated before returning or exiting.
770,function.
771,character string.
772,character string.
773,character string naming a restart.
774,restart object.
775,object.
776,character string naming a condition class.
777,additional arguments; see details below.
778,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
779,object to be coerced or tested.
780,further arguments passed to or from other methods.
781,An object, for the methods inheriting from class <code>"hexmode"</code>.
782,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
783,a logical indicating whether to use upper-caseletters or lower-case letters (default).
784,further arguments passed to or from other methods.
785,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
786,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
787,An object, for the methods inheriting from class <code>"octmode"</code>.
788,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
789,further arguments passed to or from other methods.
790,An object to be converted: a character vector for<code>strptime</code>, an object which can be converted to<code>"POSIXlt"</code> for <code>strftime</code>.
791,A character string specifying the time zone to be used forthe conversion.  System-specific (see <code>as.POSIXlt</code>), but<code>""</code> is the current time zone, and <code>"GMT"</code> is UTC.Invalid values are most commonly treated as UTC, on some platforms witha warning.
792,A character string.  The default for the <code>format</code>methods is<code>"%Y-%m-%d %H:%M:%S"</code> if any element has a timecomponent which is not midnight, and <code>"%Y-%m-%d"</code>otherwise.  If <code>options("digits.secs")</code> is set, up tothe specified number of digits will be printed for seconds.
793,Further arguments to be passed from or to other methods.
794,logical.  Should the time zone abbreviation be appendedto the output?  This is used in printing times, and more reliablethan using <code>"%Z"</code>.
795, The name of a file. 
796, The character encoding to assume for the file. 
797, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
798, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
799, The timestamp to use on a copy of a file. 
800, Is this <code>srcfilecopy</code> known to come from a file system file? 
801, A <code>srcfile</code> object. 
802, Line numbers. 
803, A vector of four, six or eight values giving a source location; see‘Details’.
804, An object of the appropriate class. 
805, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
806, An optional second <code>srcref</code> object to mark the endof the character range. 
807, Additional arguments to the methods; these will be ignored. 
808,numeric.  Desired length of the output vector,inputs being recycled as needed.
809,numeric vector.
810,numeric vector.
811,numeric vector.
812,numeric vector.
813,an object, probably of mode <code>complex</code>.
814,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
815,further arguments passed to or from other methods.
816,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
817,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
818,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
819,additional arguments to be passed to or from methods.
820,logical: should the character vector be convertedto a factor?
821,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
822,(optional) character vector of column names.
823,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
824,logical; passed to the <code>data.frame()</code> call.
825,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
826,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
827,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
828,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
829,additional arguments to be passed to or from methods.
830,logical: should the character vector be convertedto a factor?
831,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
832,(optional) character vector of column names.
833,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
834,logical; passed to the <code>data.frame()</code> call.
835,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
836,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
837,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
838,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
839,additional arguments to be passed to or from methods.
840,logical: should the character vector be convertedto a factor?
841,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
842,(optional) character vector of column names.
843,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
844,logical; passed to the <code>data.frame()</code> call.
845,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
846,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
847,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
848,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
849,additional arguments to be passed to or from methods.
850,logical: should the character vector be convertedto a factor?
851,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
852,(optional) character vector of column names.
853,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
854,logical; passed to the <code>data.frame()</code> call.
855,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
856,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
857,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
858,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
859,additional arguments to be passed to or from methods.
860,logical: should the character vector be convertedto a factor?
861,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
862,(optional) character vector of column names.
863,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
864,logical; passed to the <code>data.frame()</code> call.
865,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
866,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
867,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
868,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
869,additional arguments to be passed to or from methods.
870,logical: should the character vector be convertedto a factor?
871,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
872,(optional) character vector of column names.
873,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
874,logical; passed to the <code>data.frame()</code> call.
875,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
876,a <code>Date</code> object to be summarized or printed.
877,number of significant digits for the computations.
878,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
879,further arguments to be passed from or to other methods.
880,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
881,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
882,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
883,additional arguments to be passed to or from methods.
884,logical: should the character vector be convertedto a factor?
885,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
886,(optional) character vector of column names.
887,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
888,logical; passed to the <code>data.frame()</code> call.
889,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
890,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
891,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
892,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
893,additional arguments to be passed to or from methods.
894,logical: should the character vector be convertedto a factor?
895,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
896,(optional) character vector of column names.
897,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
898,logical; passed to the <code>data.frame()</code> call.
899,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
900,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
901,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
902,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
903,additional arguments to be passed to or from methods.
904,logical: should the character vector be convertedto a factor?
905,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
906,(optional) character vector of column names.
907,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
908,logical; passed to the <code>data.frame()</code> call.
909,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
910,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
911,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
912,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
913,additional arguments to be passed to or from methods.
914,logical: should the character vector be convertedto a factor?
915,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
916,(optional) character vector of column names.
917,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
918,logical; passed to the <code>data.frame()</code> call.
919,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
920,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
921,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
922,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
923,additional arguments to be passed to or from methods.
924,logical: should the character vector be convertedto a factor?
925,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
926,(optional) character vector of column names.
927,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
928,logical; passed to the <code>data.frame()</code> call.
929,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
930,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
931,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
932,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
933,additional arguments to be passed to or from methods.
934,logical: should the character vector be convertedto a factor?
935,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
936,(optional) character vector of column names.
937,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
938,logical; passed to the <code>data.frame()</code> call.
939,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
940,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
941,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
942,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
943,additional arguments to be passed to or from methods.
944,logical: should the character vector be convertedto a factor?
945,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
946,(optional) character vector of column names.
947,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
948,logical; passed to the <code>data.frame()</code> call.
949,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
950,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
951,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
952,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
953,additional arguments to be passed to or from methods.
954,logical: should the character vector be convertedto a factor?
955,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
956,(optional) character vector of column names.
957,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
958,logical; passed to the <code>data.frame()</code> call.
959,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
960,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
961,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
962,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
963,additional arguments to be passed to or from methods.
964,logical: should the character vector be convertedto a factor?
965,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
966,(optional) character vector of column names.
967,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
968,logical; passed to the <code>data.frame()</code> call.
969,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
970,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
971,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
972,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
973,additional arguments to be passed to or from methods.
974,logical: should the character vector be convertedto a factor?
975,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
976,(optional) character vector of column names.
977,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
978,logical; passed to the <code>data.frame()</code> call.
979,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
980,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
981,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
982,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
983,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
984,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
985,additional arguments to be passed to or from methods.
986,logical: should the character vector be convertedto a factor?
987,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
988,(optional) character vector of column names.
989,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
990,logical; passed to the <code>data.frame()</code> call.
991,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
992,an object to be printed or summarized from one of thedate-time classes.
993,for timezone formatting, passed to <code>format.POSIXct</code>.
994,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
995,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
996,further arguments to be passed from or to other methods.
997,date-time objects
998,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
999,a numeric vector (in seconds)
1000,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1001,an object to be printed or summarized from one of thedate-time classes.
1002,for timezone formatting, passed to <code>format.POSIXct</code>.
1003,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1004,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
1005,further arguments to be passed from or to other methods.
1006,date-time objects
1007,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
1008,a numeric vector (in seconds)
1009,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1010,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1011,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
1012,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
1013,additional arguments to be passed to or from methods.
1014,logical: should the character vector be convertedto a factor?
1015,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
1016,(optional) character vector of column names.
1017,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
1018,logical; passed to the <code>data.frame()</code> call.
1019,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
1020,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
1021,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
1022,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
1023,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
1024,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
1025,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
1026,a character vector giving the row names for the dataframe.
1027,The name to be used for the column of tableentries, usually counts.
1028,logical: should the classifying factors bereturned as factors (the default) or character vectors?
1029,passed to <code>provideDimnames</code>.
1030,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1031,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
1032,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
1033,additional arguments to be passed to or from methods.
1034,logical: should the character vector be convertedto a factor?
1035,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
1036,(optional) character vector of column names.
1037,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
1038,logical; passed to the <code>data.frame()</code> call.
1039,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
1040,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1041,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
1042,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
1043,additional arguments to be passed to or from methods.
1044,logical: should the character vector be convertedto a factor?
1045,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
1046,(optional) character vector of column names.
1047,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
1048,logical; passed to the <code>data.frame()</code> call.
1049,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
1050,an object to be converted.
1051,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
1052,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1053,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1054,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
1055,a time zone name.
1056,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
1057,an object to be converted.
1058,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
1059,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1060,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1061,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
1062,a time zone name.
1063,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
1064,an object to be converted.
1065,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
1066,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1067,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1068,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
1069,a time zone name.
1070,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
1071,an object to be converted.
1072,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
1073,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1074,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1075,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
1076,a time zone name.
1077,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
1078,an object to be converted.
1079,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
1080,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1081,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1082,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
1083,a time zone name.
1084,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
1085,an object to be converted.
1086,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
1087,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1088,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1089,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
1090,a time zone name.
1091,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
1092,an object to be converted.
1093,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
1094,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1095,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1096,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
1097,a time zone name.
1098,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
1099,date-time or date objects.
1100,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
1101,character string.  Units in which the results aredesired.  Can be abbreviated.
1102,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
1103,character string or numeric value specifying a time interval.
1104,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
1105,an object inheriting from class <code>"difftime"</code>.
1106,arguments to be passed to or from other methods.
1107,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1108,object to be coerced or tested.
1109,further arguments passed to or from other methods.
1110,date-time or date objects.
1111,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
1112,character string.  Units in which the results aredesired.  Can be abbreviated.
1113,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
1114,character string or numeric value specifying a time interval.
1115,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
1116,an object inheriting from class <code>"difftime"</code>.
1117,arguments to be passed to or from other methods.
1118,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1119,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1120,further arguments to be passed to or from other methods.
1121,character string giving a date-time format as usedby <code>strptime</code>.
1122,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1123,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1124,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1125,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to convert.  If it is already anenvironment, just return it.  If it is a positive number, return theenvironment corresponding to that position on the search list. If itis <code>-1</code>, the environment it is called from.  Ifit is a character string, match the string to the names on thesearch list.

1126,If it is a list, the equivalent of <code>list2env(x,      parent = emptyenv())</code> is returned.

1127,If <code>is.object(x)</code> is true and it has a <code>class</code>for which an <code>as.environment</code> method is found, that is used.
1128,<code>expression</code>: <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects, typically calls, symbolsor constants.<br><code>as.expression</code>: arguments to be passed to methods.
1129,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1130,<code>expression</code>: <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects, typically calls, symbolsor constants.<br><code>as.expression</code>: arguments to be passed to methods.
1131,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1132,a vector of data, usually taking a small number of distinctvalues.
1133,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
1134,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
1135,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
1136,logical flag to determine if the levels should be regardedas ordered (in the order given).
1137,an upper bound on the number of levels; see ‘Details’.
1138,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
1139,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
1140,object to convert, a list for the default method.
1141,additional arguments, depending on object
1142,environment in which the function should be defined
1143,object to convert, a list for the default method.
1144,additional arguments, depending on object
1145,environment in which the function should be defined
1146,An object, for the methods inheriting from class <code>"hexmode"</code>.
1147,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
1148,a logical indicating whether to use upper-caseletters or lower-case letters (default).
1149,further arguments passed to or from other methods.
1150,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1151,object to be coerced or tested.
1152,further arguments passed to or from other methods.
1153,objects, possibly named.
1154,object to be coerced or tested.
1155,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
1156,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
1157,objects, possibly named.
1158,object to be coerced or tested.
1159,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
1160,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
1161,a <code>Date</code> object to be summarized or printed.
1162,number of significant digits for the computations.
1163,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1164,further arguments to be passed from or to other methods.
1165,objects, possibly named.
1166,object to be coerced or tested.
1167,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
1168,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
1169,date-time or date objects.
1170,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
1171,character string.  Units in which the results aredesired.  Can be abbreviated.
1172,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
1173,character string or numeric value specifying a time interval.
1174,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
1175,an object inheriting from class <code>"difftime"</code>.
1176,arguments to be passed to or from other methods.
1177,objects, possibly named.
1178,object to be coerced or tested.
1179,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
1180,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
1181,objects, possibly named.
1182,object to be coerced or tested.
1183,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
1184,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
1185,objects, possibly named.
1186,object to be coerced or tested.
1187,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
1188,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
1189,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
1190,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
1191,an object to be printed or summarized from one of thedate-time classes.
1192,for timezone formatting, passed to <code>format.POSIXct</code>.
1193,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1194,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
1195,further arguments to be passed from or to other methods.
1196,date-time objects
1197,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
1198,a numeric vector (in seconds)
1199,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1200,an object to be printed or summarized from one of thedate-time classes.
1201,for timezone formatting, passed to <code>format.POSIXct</code>.
1202,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1203,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
1204,further arguments to be passed from or to other methods.
1205,date-time objects
1206,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
1207,a numeric vector (in seconds)
1208,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1209,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1210,object to be coerced or tested.
1211,further arguments passed to or from other methods.
1212,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1213,object to be coerced or tested.
1214,further arguments passed to or from other methods.
1215,an optional data vector (including a list or<code>expression</code> vector).  Non-atomic classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects arecoerced by <code>as.vector</code> and all attributes discarded.
1216,the desired number of rows.
1217,the desired number of columns.
1218,logical. If <code>FALSE</code> (the default) the matrix isfilled by columns, otherwise the matrix is filled by rows.
1219,A <code>dimnames</code> attribute for the matrix:<code>NULL</code> or a <code>list</code> of length 2 giving the row and columnnames respectively.  An empty list is treated as <code>NULL</code>, and alist of length one as row names.  The list can be named, and thelist names will be used as names for the dimensions.
1220,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1221,additional arguments to be passed to or from methods.
1222,logical indicating if the resulting matrixshould have character (rather than <code>NULL</code>)<code>rownames</code>.  The default, <code>NA</code>, uses <code>NULL</code>rownames if the data frame has ‘automatic’ row.names or for azero-row data frame.
1223,an optional data vector (including a list or<code>expression</code> vector).  Non-atomic classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects arecoerced by <code>as.vector</code> and all attributes discarded.
1224,the desired number of rows.
1225,the desired number of columns.
1226,logical. If <code>FALSE</code> (the default) the matrix isfilled by columns, otherwise the matrix is filled by rows.
1227,A <code>dimnames</code> attribute for the matrix:<code>NULL</code> or a <code>list</code> of length 2 giving the row and columnnames respectively.  An empty list is treated as <code>NULL</code>, and alist of length one as row names.  The list can be named, and thelist names will be used as names for the dimensions.
1228,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1229,additional arguments to be passed to or from methods.
1230,logical indicating if the resulting matrixshould have character (rather than <code>NULL</code>)<code>rownames</code>.  The default, <code>NA</code>, uses <code>NULL</code>rownames if the data frame has ‘automatic’ row.names or for azero-row data frame.
1231,an optional data vector (including a list or<code>expression</code> vector).  Non-atomic classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects arecoerced by <code>as.vector</code> and all attributes discarded.
1232,the desired number of rows.
1233,the desired number of columns.
1234,logical. If <code>FALSE</code> (the default) the matrix isfilled by columns, otherwise the matrix is filled by rows.
1235,A <code>dimnames</code> attribute for the matrix:<code>NULL</code> or a <code>list</code> of length 2 giving the row and columnnames respectively.  An empty list is treated as <code>NULL</code>, and alist of length one as row names.  The list can be named, and thelist names will be used as names for the dimensions.
1236,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1237,additional arguments to be passed to or from methods.
1238,logical indicating if the resulting matrixshould have character (rather than <code>NULL</code>)<code>rownames</code>.  The default, <code>NA</code>, uses <code>NULL</code>rownames if the data frame has ‘automatic’ row.names or for azero-row data frame.
1239,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, typically a vector of<code>character</code> strings.
1240,optional <code>logical</code> eventually to be passed to<code>print()</code>, used by <code>print.default()</code>, indicatingwhether or not strings should be right aligned.
1241,an object of class <code>"noquote"</code>.
1242,further options passed to next methods, such as <code>print</code>.
1243,for compatibility with the generic <code>c</code> function.
1244,an object to be printed or summarized from one of thedate-time classes.
1245,for timezone formatting, passed to <code>format.POSIXct</code>.
1246,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1247,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
1248,further arguments to be passed from or to other methods.
1249,date-time objects
1250,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
1251,a numeric vector (in seconds)
1252,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1253,object to be coerced or tested.
1254,an object to be tested or coerced.
1255,ignored.
1256,an object to be tested or coerced.
1257,ignored.
1258,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1259,object to be coerced or tested.
1260,further arguments passed to or from other methods.
1261,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
1262,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
1263,An object, for the methods inheriting from class <code>"octmode"</code>.
1264,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
1265,further arguments passed to or from other methods.
1266,a vector of data, usually taking a small number of distinctvalues.
1267,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
1268,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
1269,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
1270,logical flag to determine if the levels should be regardedas ordered (in the order given).
1271,an upper bound on the number of levels; see ‘Details’.
1272,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
1273,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
1274,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
1275,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
1276,objects, possibly named.
1277,object to be coerced or tested.
1278,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
1279,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
1280,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1281,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1282,further arguments to be passed to or from other methods.
1283,character string giving a date-time format as usedby <code>strptime</code>.
1284,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1285,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1286,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1287,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1288,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1289,further arguments to be passed to or from other methods.
1290,character string giving a date-time format as usedby <code>strptime</code>.
1291,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1292,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1293,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1294,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1295,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1296,further arguments to be passed to or from other methods.
1297,character string giving a date-time format as usedby <code>strptime</code>.
1298,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1299,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1300,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1301,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1302,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1303,further arguments to be passed to or from other methods.
1304,character string giving a date-time format as usedby <code>strptime</code>.
1305,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1306,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1307,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1308,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1309,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1310,further arguments to be passed to or from other methods.
1311,character string giving a date-time format as usedby <code>strptime</code>.
1312,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1313,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1314,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1315,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1316,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1317,further arguments to be passed to or from other methods.
1318,character string giving a date-time format as usedby <code>strptime</code>.
1319,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1320,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1321,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1322,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1323,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1324,further arguments to be passed to or from other methods.
1325,character string giving a date-time format as usedby <code>strptime</code>.
1326,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1327,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1328,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1329,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1330,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1331,further arguments to be passed to or from other methods.
1332,character string giving a date-time format as usedby <code>strptime</code>.
1333,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1334,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1335,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1336,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1337,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1338,further arguments to be passed to or from other methods.
1339,character string giving a date-time format as usedby <code>strptime</code>.
1340,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1341,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1342,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1343,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1344,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1345,further arguments to be passed to or from other methods.
1346,character string giving a date-time format as usedby <code>strptime</code>.
1347,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1348,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1349,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1350,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1351,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1352,further arguments to be passed to or from other methods.
1353,character string giving a date-time format as usedby <code>strptime</code>.
1354,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1355,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1356,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1357,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>  object to be converted.
1358,time zone specification to be used for the conversion,<em>if one is required</em>.  System-specific (see time zones),but <code>""</code> is the current time zone, and <code>"GMT"</code> is UTC(Universal Time, Coordinated).  Invalid values are most commonlytreated as UTC, on some platforms with a warning.
1359,further arguments to be passed to or from other methods.
1360,character string giving a date-time format as usedby <code>strptime</code>.
1361,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
1362,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
1363,a date-time object, or something which can be coerced by<code>as.POSIXct(tz = "GMT")</code> to such an object.
1364,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
1365,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
1366,a QR decomposition of the type computed by <code>qr</code>.
1367,a vector or matrix of right-hand sides of equations.
1368,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
1369,effective rank.
1370,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
1371,further arguments passed to or from other methods
1372,desired length.
1373,object to be coerced.
1374,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1375,object to be coerced or tested.
1376,further arguments passed to or from other methods.
1377,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1378,object to be coerced or tested.
1379,further arguments passed to or from other methods.
1380,object to be coerced or tested.
1381,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
1382,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
1383,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
1384,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
1385,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
1386,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
1387,a character vector giving the row names for the dataframe.
1388,The name to be used for the column of tableentries, usually counts.
1389,logical: should the classifying factors bereturned as factors (the default) or character vectors?
1390,passed to <code>provideDimnames</code>.
1391,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
1392,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
1393,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
1394,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
1395,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
1396,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
1397,a character vector giving the row names for the dataframe.
1398,The name to be used for the column of tableentries, usually counts.
1399,logical: should the classifying factors bereturned as factors (the default) or character vectors?
1400,passed to <code>provideDimnames</code>.
1401,character string naming an atomic mode or<code>"list"</code> or <code>"expression"</code> or (except for <code>vector</code>)<code>"any"</code>.  Currently, <code>is.vector()</code> allows any type (see<code>typeof</code>) for <code>mode</code>, and when mode is not<code>"any"</code>, <code>is.vector(x, mode)</code> is almost the same as<code>typeof(x) == mode</code>.
1402,a non-negative integer specifying the desired length.  Fora long vector, i.e., <code>length &gt; .Machine$integer.max</code>, ithas to be of type <code>"double"</code>.  Supplying an argument of lengthother than one is an error.
1403,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1404,character string naming an atomic mode or<code>"list"</code> or <code>"expression"</code> or (except for <code>vector</code>)<code>"any"</code>.  Currently, <code>is.vector()</code> allows any type (see<code>typeof</code>) for <code>mode</code>, and when mode is not<code>"any"</code>, <code>is.vector(x, mode)</code> is almost the same as<code>typeof(x) == mode</code>.
1405,a non-negative integer specifying the desired length.  Fora long vector, i.e., <code>length &gt; .Machine$integer.max</code>, ithas to be of type <code>"double"</code>.  Supplying an argument of lengthother than one is an error.
1406,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1407,numeric or complex vectors.
1408,a numeric or complex vector
1409,string or namespace environment.
1410,logical.
1411,environment.
1412,namespace environment.
1413,character vector.
1414,optional character vector.
1415,namespace environment.
1416,string naming the package/namespace to load.
1417,vector of package names parallel to <code>generics</code>.
1418,character vector specifying library.
1419,logical.
1420,character.
1421,character.
1422,environment.
1423,a 3-column character matrix.
1424,character.
1425,any object.
1426,character arguments.
1427,the methods table name.
1428,symbol: name of namespace
1429,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
1430,an array, including a matrix.
1431,a vector giving the margins to split by.E.g., for a matrix <code>1</code> indicates rows, <code>2</code> indicatescolumns, <code>c(1, 2)</code> indicates rows and columns.Where <code>x</code> has named dimnames, it can be a character vectorselecting dimension names.
1432,Any R object.
1433,Optional, logical: indicate direction of conversion.
1434,Optional, logical: whether conversion to S3 iscompleted.  Not usually needed, but see the details section.
1435,Any R object.
1436,Optional, logical: indicate direction of conversion.
1437,Optional, logical: whether conversion to S3 iscompleted.  Not usually needed, but see the details section.
1438,a variable name, given as a character string.  No coercion isdone, and the first element of a character vector of length greaterthan one will be used, with a warning.
1439,a value to be assigned to <code>x</code>.
1440,where to do the assignment.  By default, assigns into thecurrent environment.  See ‘Details’ for other possibilities.
1441,the <code>environment</code> to use.  See ‘Details’.
1442,should the enclosing frames of the environment beinspected?
1443,an ignored compatibility feature.
1444,numeric or complex vectors.
1445,numeric or complex vectors.
1446,a numeric or complex vector
1447,‘database’.  This can be a<code>data.frame</code> or a <code>list</code> or a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> data file created with<code>save</code> or <code>NULL</code> or an environment.  See also‘Details’.
1448,integer specifying position in <code>search()</code> whereto attach.
1449,name to use for the attached database. Names starting with<code>package:</code> are reserved for <code>library</code>.
1450,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the database,unless that database contains an object <code>.conflicts.OK</code>.  Aconflict is a function masking a function, or a non-function maskinga non-function.
1451,string or name space object.
1452,integer specifying position to attach.
1453,<code>NULL</code> or a character vector of dependencies to berecorded in object <code>.Depends</code> in the package.
1454,string naming the package/name space to load.
1455,character vector specifying library search path.
1456,Now ignored except during package installation.
1457,Ignored except during package installation.
1458,logical; if true, stop just after loading code.
1459,<code>NULL</code> or a version specification (a listwith components <code>op</code> and <code>version</code>).
1460,logical: should progress and error messages be suppressed?
1461,string or ‘name’, see <code>as.symbol</code>,of a package, e.g., <code>"stats"</code>.
1462,character vectors; see <code>library</code>.
1463,further arguments to be passed to <code>loadNamespace</code>.
1464,an object whose attributes are to be accessed.
1465,a non-empty character string specifying which attributeis to be accessed.
1466,logical: should <code>which</code> be matched exactly?
1467,an object, the new value of the attribute, or <code>NULL</code>to remove the attribute.
1468,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1469,other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be compared with <code>target</code>.
1470,further arguments for different methods, notably thefollowing two, for numerical comparison:
1471,numeric <i>≥</i> 0.  Differences smaller than<code>tolerance</code> are not reported.  The default value is close to<code>1.5e-8</code>.
1472,<code>NULL</code> or numeric &gt; 0, typically of length 1 or<code>length(target)</code>.  See ‘Details’.
1473,logical indicating if the <code>target == current</code>cases should be counted when computing the mean (absolute orrelative) differences.  The default, <code>FALSE</code> may seemmisleading in cases where <code>target</code> and <code>current</code> onlydiffer in a few places; see the extensive example.
1474,a <code>function</code> of two arguments,<code>err</code>, the relative, absolute or scaled error, and<code>what</code>, a character string indicating the <em>kind</em> of error;may be used, e.g., to format relative and absolute errors differently.
1475,logical indicating if the<code>attributes</code> of <code>target</code> and <code>current</code>(other than the names) should be compared.
1476,logical indicating if <code>list</code> comparisonshould report differing components by name (if matching) instead ofinteger index.  Note that this comes after <code>...</code> and so mustbe specified by its full name.
1477,logical passed to <code>ls</code> indicating if“hidden” objects should also be considered in the environments.
1478,for the <code>environment</code> method: <code>logical</code> indicating if“promises should be forced”, i.e., typically formal function argumentsbe evaluated for comparison.  If false, only the <code>names</code> ofthe objects in the two environments are checked for equality.
1479,logical requiring that the<code>environment()</code>s of functions should be compared, too.You may need to set <code>check.environment=FALSE</code> in unexpectedcases, such as when comparing two <code>nls()</code> fits.
1480,logical indicating if the <code>"tzone"</code> attributesof <code>target</code> and <code>current</code> should be compared.
1481,logical indicating if the <code>names(.)</code>of <code>target</code> and <code>current</code> should be compared.
1482,an object whose attributes are to be accessed.
1483,a non-empty character string specifying which attributeis to be accessed.
1484,logical: should <code>which</code> be matched exactly?
1485,an object, the new value of the attribute, or <code>NULL</code>to remove the attribute.
1486,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
1487,an appropriate named <code>list</code> of attributes, or<code>NULL</code>.
1488,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
1489,an appropriate named <code>list</code> of attributes, or<code>NULL</code>.
1490,string giving the name of an object.
1491,string giving the name of a package containing the object.
1492,logical: for internal use by <code>autoloader</code>.
1493,other arguments to <code>library</code>.
1494,string giving the name of an object.
1495,string giving the name of a package containing the object.
1496,logical: for internal use by <code>autoloader</code>.
1497,other arguments to <code>library</code>.
1498,an upper (or lower) triangular matrix giving thecoefficients for the system to be solved.  Values below (above)the diagonal are ignored.
1499,a matrix whose columns give the right-hand sides forthe equations.
1500,The number of columns of <code>r</code> and rows of <code>x</code> to use.
1501,logical; if <code>TRUE</code> (default), the <em>upper</em><em>tri</em>angular part of <code>r</code> is used.  Otherwise, the lower one.
1502,logical; if <code>TRUE</code>, solve <i>r' * y = x</i> for<i>y</i>, i.e., <code>t(r) %*% y == x</code>.
1503,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
1504,an environment to associate with the function
1505,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1506,a logical, if <code>TRUE</code> the environment will use a hash table.
1507,an environment to be used as the enclosure of theenvironment created.
1508,an environment
1509,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
1510,character vector, containing path names.
1511,numeric, <i>≥ 0</i>.
1512,numeric; The <em>order</em> (maybe fractional and negative) ofthe corresponding Bessel function.
1513,logical; if <code>TRUE</code>, the results areexponentially scaled in order to avoid overflow(<i>I(nu)</i>) or underflow (<i>K(nu)</i>),respectively.
1514,numeric, <i>≥ 0</i>.
1515,numeric; The <em>order</em> (maybe fractional and negative) ofthe corresponding Bessel function.
1516,logical; if <code>TRUE</code>, the results areexponentially scaled in order to avoid overflow(<i>I(nu)</i>) or underflow (<i>K(nu)</i>),respectively.
1517,numeric, <i>≥ 0</i>.
1518,numeric; The <em>order</em> (maybe fractional and negative) ofthe corresponding Bessel function.
1519,logical; if <code>TRUE</code>, the results areexponentially scaled in order to avoid overflow(<i>I(nu)</i>) or underflow (<i>K(nu)</i>),respectively.
1520,numeric, <i>≥ 0</i>.
1521,numeric; The <em>order</em> (maybe fractional and negative) ofthe corresponding Bessel function.
1522,logical; if <code>TRUE</code>, the results areexponentially scaled in order to avoid overflow(<i>I(nu)</i>) or underflow (<i>K(nu)</i>),respectively.
1523,non-negative numeric vectors.
1524,numeric vectors.
1525,integer vectors.
1526,an environment.
1527,logical specifying whether bindings should be locked.
1528,a name object or character string.
1529,a function taking zero or one arguments.
1530,an environment.
1531,logical specifying whether bindings should be locked.
1532,a name object or character string.
1533,a function taking zero or one arguments.
1534,One or more character vectors.
1535,The ‘domain’ for the translation.
1536,a non-negative integer.
1537,the message to be used in English for <code>n = 1</code>.
1538,the message to be used in English for <code>n = 0, 2, 3, ...</code>.
1539,The directory in which to find translated messagecatalogs for the domain.
1540,integer vectors; numeric vectors are coerced to integer vectors.
1541,non-negative integer vector of values up to 31.
1542,integer vectors; numeric vectors are coerced to integer vectors.
1543,non-negative integer vector of values up to 31.
1544,integer vectors; numeric vectors are coerced to integer vectors.
1545,non-negative integer vector of values up to 31.
1546,integer vectors; numeric vectors are coerced to integer vectors.
1547,non-negative integer vector of values up to 31.
1548,integer vectors; numeric vectors are coerced to integer vectors.
1549,non-negative integer vector of values up to 31.
1550,integer vectors; numeric vectors are coerced to integer vectors.
1551,non-negative integer vector of values up to 31.
1552,a function object, or see ‘Details’.
1553,environment in which the function should be defined.
1554,an object, usually a language object: see section‘Value’.
1555,a function object, or see ‘Details’.
1556,environment in which the function should be defined.
1557,an object, usually a language object: see section‘Value’.
1558,A language object.
1559,An environment.
1560,Logical; if <code>TRUE</code> splicing is enabled.
1561,A length-one logical vector that is not <code>NA</code>.Conditions of length greater than one are currently accepted with awarning, but only the first element is used.  An error is signalledinstead when the environment variable <span class="env">_R_CHECK_LENGTH_1_CONDITION_</span>is set to true.  Other types are coerced to logicalif possible, ignoring any class.
1562,A syntactical name for a variable.
1563,An expression evaluating to a vector (including a list andan expression) or to a pairlist or <code>NULL</code>.  Afactor value will be coerced to a character vector.  As from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>4.0.0 this can be a long vector.
1564,An <em>expression</em> in a formal sense.  This is either asimple expression or a so-called <em>compound expression</em>, usuallyof the form <code>{ expr1 ; expr2 }</code>.
1565,a text string that can be retrieved once the browser is invoked.
1566,a condition that can be retrieved once the browser isinvoked.
1567,a “condition”.  By default, and whenever not falseafter being coerced to <code>logical</code>, thedebugger will be invoked, otherwise control is returned directly.
1568,how many previous calls to skip when reporting thecalling context.
1569,The number of contexts to skip over, it must be non-negative.
1570,The number of contexts to skip over, it must be non-negative.
1571,The number of contexts to skip over, it must be non-negative.
1572,a logical indicating whether only ‘internal’functions (which can be called via <code>.Internal</code>) shouldbe returned.
1573,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, normally a data frame, possibly a matrix.
1574,a factor or a list of factors, each of length<code>nrow(data)</code>.
1575,a function to be applied to (usually data-frame) subsets of<code>data</code>.
1576,further arguments to <code>FUN</code>.
1577,logical: see <code>tapply</code>.
1578,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, normally a data frame, possibly a matrix.
1579,a factor or a list of factors, each of length<code>nrow(data)</code>.
1580,a function to be applied to (usually data-frame) subsets of<code>data</code>.
1581,further arguments to <code>FUN</code>.
1582,logical: see <code>tapply</code>.
1583,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, normally a data frame, possibly a matrix.
1584,a factor or a list of factors, each of length<code>nrow(data)</code>.
1585,a function to be applied to (usually data-frame) subsets of<code>data</code>.
1586,further arguments to <code>FUN</code>.
1587,logical: see <code>tapply</code>.
1588,character string.  A description of the connection:see ‘Details’.
1589,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
1590,logical.  See the ‘Blocking’ section.
1591,The name of the encoding to be assumed.  See the‘Encoding’ section.
1592,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
1593,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
1594,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
1595,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
1596,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
1597,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
1598,a filename within a zip file.
1599,character string.  Host name for the port.
1600,integer.  The TCP port number.
1601,logical.  Should the socket be a client or a server?
1602,a server socket listening for connections.
1603,a connection.
1604,character string.  Currently ignored.
1605,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
1606,arguments passed to or from other methods.
1607,objects to be concatenated.  All <code>NULL</code> entriesare dropped before method dispatch unless at the very beginning of theargument list.
1608,logical.  If <code>recursive = TRUE</code>, the functionrecursively descends through lists (and pairlists) combining alltheir elements into a vector.
1609,logical indicating if <code>names</code> should bepreserved.
1610,a <code>Date</code> object to be summarized or printed.
1611,number of significant digits for the computations.
1612,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1613,further arguments to be passed from or to other methods.
1614,date-time or date objects.
1615,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
1616,character string.  Units in which the results aredesired.  Can be abbreviated.
1617,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
1618,character string or numeric value specifying a time interval.
1619,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
1620,an object inheriting from class <code>"difftime"</code>.
1621,arguments to be passed to or from other methods.
1622,a vector of data, usually taking a small number of distinctvalues.
1623,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
1624,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
1625,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
1626,logical flag to determine if the levels should be regardedas ordered (in the order given).
1627,an upper bound on the number of levels; see ‘Details’.
1628,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
1629,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
1630,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, typically a vector of<code>character</code> strings.
1631,optional <code>logical</code> eventually to be passed to<code>print()</code>, used by <code>print.default()</code>, indicatingwhether or not strings should be right aligned.
1632,an object of class <code>"noquote"</code>.
1633,further options passed to next methods, such as <code>print</code>.
1634,for compatibility with the generic <code>c</code> function.
1635,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
1636,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
1637,an object to be printed or summarized from one of thedate-time classes.
1638,for timezone formatting, passed to <code>format.POSIXct</code>.
1639,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1640,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
1641,further arguments to be passed from or to other methods.
1642,date-time objects
1643,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
1644,a numeric vector (in seconds)
1645,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1646,an object to be printed or summarized from one of thedate-time classes.
1647,for timezone formatting, passed to <code>format.POSIXct</code>.
1648,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1649,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
1650,further arguments to be passed from or to other methods.
1651,date-time objects
1652,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
1653,a numeric vector (in seconds)
1654,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1655,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
1656,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
1657,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
1658,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
1659,a character string <code>cat()</code>ed before themessages are printed.
1660,a non-empty character string naming the function to be called.
1661,arguments to be part of the call.
1662,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1663,function of one argument, the exit procedure.
1664,character vector or <code>NULL</code>, specifyingrequired components.  <code>NULL</code> implies that all are required.
1665,<code>logical</code> with a smart default, indicating ifX11-related capabilities should be fully checked, notably on macOS.If set to false, may avoid a warning “No protocol specified”and e.g., the "X11" capability may be returned as <code>NA</code>.
1666,a character vector, or an object that can be coerced tocharacter by <code>as.character</code>.
1667,a character string specifying the characters to betranslated.  If a character vector of length 2 or more is supplied,the first element is used with a warning.
1668,a character string specifying the translations. If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.
1669,logical: translate to upper or lower case?.
1670,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects (see ‘Details’ for the types of objectsallowed).
1671,A connection, or a character string naming the fileto print to.  If <code>""</code> (the default), <code>cat</code> prints to thestandard output connection, the console unless redirected by<code>sink</code>.If it is <code>"|cmd"</code>, the output is piped to the command givenby ‘<span class="file">cmd</span>’, by opening a pipe connection.
1672,a character vector of strings to append after each element.
1673,a logical or (positive) numeric controlling how the output isbroken into successive lines.  If <code>FALSE</code> (default), only newlinescreated explicitly by <span class="samp">"\n"</span> are printed.  Otherwise, theoutput is broken into lines with print width equal to the option<code>width</code> if <code>fill</code> is <code>TRUE</code>, or the value of<code>fill</code> if this is numeric.  Linefeeds are only inserted<em>between</em> elements, strings wider than <code>fill</code> are notwrapped.  Non-positive <code>fill</code> values areignored, with a warning.
1674,character vector of labels for the lines printed.Ignored if <code>fill</code> is <code>FALSE</code>.
1675,logical. Only used if the argument <code>file</code> is thename of file (and not a connection or <code>"|cmd"</code>).If <code>TRUE</code> output will be appended to<code>file</code>; otherwise, it will overwrite the contents of<code>file</code>.
1676,(generalized) vectors or matrices.  These can be given as namedarguments.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects may be coerced as appropriate, or S4methods may be used: see sections ‘Details’ and‘Value’.  (For the <code>"data.frame"</code> method of <code>cbind</code>these can be further arguments to <code>data.frame</code> such as<code>stringsAsFactors</code>.)
1677,integer controlling the construction of labels inthe case of non-matrix-like arguments (for the default method):<br><code>deparse.level = 0</code> constructs no labels; the default,<br><code>deparse.level = 1 or 2</code> constructs labels from the argumentnames, see the ‘Value’ section below.
1678,(only for data frame method:) logicalindicating if unique and valid <code>row.names</code> should beconstructed from the arguments.
1679,logical, passed to <code>as.data.frame</code>;only has an effect when the <code>...</code> arguments contain a(non-<code>data.frame</code>) <code>character</code>.
1680,if the data frames contain factors, the default<code>TRUE</code> ensures that <code>NA</code> levels of factors are kept, see<a href="https://bugs.R-project.org/show_bug.cgi?id=17562">PR#17562</a> and the ‘Data frame methods’.  In <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> versions upto 3.6.x, <code>factor.exclude = NA</code> has been implicitly hardcoded(<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &lt;= 3.6.0) or the default (<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> = 3.6.x, x &gt;= 1).
1681,(generalized) vectors or matrices.  These can be given as namedarguments.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects may be coerced as appropriate, or S4methods may be used: see sections ‘Details’ and‘Value’.  (For the <code>"data.frame"</code> method of <code>cbind</code>these can be further arguments to <code>data.frame</code> such as<code>stringsAsFactors</code>.)
1682,integer controlling the construction of labels inthe case of non-matrix-like arguments (for the default method):<br><code>deparse.level = 0</code> constructs no labels; the default,<br><code>deparse.level = 1 or 2</code> constructs labels from the argumentnames, see the ‘Value’ section below.
1683,(only for data frame method:) logicalindicating if unique and valid <code>row.names</code> should beconstructed from the arguments.
1684,logical, passed to <code>as.data.frame</code>;only has an effect when the <code>...</code> arguments contain a(non-<code>data.frame</code>) <code>character</code>.
1685,if the data frames contain factors, the default<code>TRUE</code> ensures that <code>NA</code> levels of factors are kept, see<a href="https://bugs.R-project.org/show_bug.cgi?id=17562">PR#17562</a> and the ‘Data frame methods’.  In <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> versions upto 3.6.x, <code>factor.exclude = NA</code> has been implicitly hardcoded(<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &lt;= 3.6.0) or the default (<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> = 3.6.x, x &gt;= 1).
1686,a numeric vector.  Or, for <code>round</code> and <code>signif</code>, acomplex vector.
1687,integer indicating the number of decimal places(<code>round</code>) or significant digits (<code>signif</code>) to be used.Negative values are allowed (see ‘Details’).
1688,arguments to be passed to methods.
1689,a character string to be expanded.
1690,a character vector with the values to be matchedagainst.
1691,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression to be evaluated in case expansion wasnot possible.
1692,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
1693,object to be coerced or tested.
1694,further arguments passed to or from other methods.
1695,the values to be matched: converted to a character vector by<code>as.character</code>.  Long vectors are supported.
1696,the values to be matched against: converted to a charactervector.  Long vectors are not supported.
1697,the (integer) value to be returned at non-matchingpositions.
1698,object to be converted or shifted.
1699,logical: should the conversion be to a singlecharacter string or multiple individual characters?
1700,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
1701,the result type, partially matched.
1702,a character vector, or an object that can be coerced tocharacter by <code>as.character</code>.
1703,a character string specifying the characters to betranslated.  If a character vector of length 2 or more is supplied,the first element is used with a warning.
1704,a character string specifying the translations. If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.
1705,logical: translate to upper or lower case?.
1706,an object to be printed or summarized from one of thedate-time classes.
1707,for timezone formatting, passed to <code>format.POSIXct</code>.
1708,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
1709,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
1710,further arguments to be passed from or to other methods.
1711,date-time objects
1712,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
1713,a numeric vector (in seconds)
1714,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
1715,“the dots”, as passed from the caller.
1716,passed to <code>sys.call()</code>.  A caller mayuse -2 if the message should mention <em>its</em> caller.
1717,not yet implemented: character vector of <em>named</em>elements in <code>...</code> which are “allowed” and hence notwarned about.
1718,an object for which a method exists.  The default methodapplies to numeric (or logical) symmetric, positive-definite matrices.
1719,arguments to be based to or from methods.
1720,Should pivoting be used?
1721,logical.  Should LINPACK be used (now an error)?
1722,A numeric tolerance for use with <code>pivot = TRUE</code>.
1723,an object for which a method exists.  The default methodapplies to numeric (or logical) symmetric, positive-definite matrices.
1724,arguments to be based to or from methods.
1725,Should pivoting be used?
1726,logical.  Should LINPACK be used (now an error)?
1727,A numeric tolerance for use with <code>pivot = TRUE</code>.
1728,a matrix.  The first <code>size</code> columns of the upper trianglecontain the Choleski decomposition of the matrix to be inverted.
1729,the number of columns of <code>x</code> containing theCholeski decomposition.
1730,logical.  Defunct and gives an error.
1731,non-negative numeric vectors.
1732,numeric vectors.
1733,integer vectors.
1734,a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
1735,a character vector naming classes.  <code>value</code>can also be <code>NULL</code>.
1736,logical affecting return value: see ‘Details’.
1737,a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
1738,a character vector naming classes.  <code>value</code>can also be <code>NULL</code>.
1739,logical affecting return value: see ‘Details’.
1740,a character vector.
1741,A connection.
1742,logical.  If true, a newline is appended to each stringpushed back.
1743,character string, partially matched.  See details.
1744,character string.  A description of the connection:see ‘Details’.
1745,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
1746,logical.  See the ‘Blocking’ section.
1747,The name of the encoding to be assumed.  See the‘Encoding’ section.
1748,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
1749,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
1750,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
1751,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
1752,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
1753,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
1754,a filename within a zip file.
1755,character string.  Host name for the port.
1756,integer.  The TCP port number.
1757,logical.  Should the socket be a client or a server?
1758,a server socket listening for connections.
1759,a connection.
1760,character string.  Currently ignored.
1761,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
1762,arguments passed to or from other methods.
1763,character string.  A description of the connection:see ‘Details’.
1764,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
1765,logical.  See the ‘Blocking’ section.
1766,The name of the encoding to be assumed.  See the‘Encoding’ section.
1767,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
1768,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
1769,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
1770,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
1771,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
1772,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
1773,a filename within a zip file.
1774,character string.  Host name for the port.
1775,integer.  The TCP port number.
1776,logical.  Should the socket be a client or a server?
1777,a server socket listening for connections.
1778,a connection.
1779,character string.  Currently ignored.
1780,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
1781,arguments passed to or from other methods.
1782, The name of a file. 
1783, The character encoding to assume for the file. 
1784, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
1785, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
1786, The timestamp to use on a copy of a file. 
1787, Is this <code>srcfilecopy</code> known to come from a file system file? 
1788, A <code>srcfile</code> object. 
1789, Line numbers. 
1790, A vector of four, six or eight values giving a source location; see‘Details’.
1791, An object of the appropriate class. 
1792, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
1793, An optional second <code>srcref</code> object to mark the endof the character range. 
1794, Additional arguments to the methods; these will be ignored. 
1795, The name of a file. 
1796, The character encoding to assume for the file. 
1797, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
1798, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
1799, The timestamp to use on a copy of a file. 
1800, Is this <code>srcfilecopy</code> known to come from a file system file? 
1801, A <code>srcfile</code> object. 
1802, Line numbers. 
1803, A vector of four, six or eight values giving a source location; see‘Details’.
1804, An object of the appropriate class. 
1805, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
1806, An optional second <code>srcref</code> object to mark the endof the character range. 
1807, Additional arguments to the methods; these will be ignored. 
1808,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
1809,integer: a row number of the table given by<code>showConnections</code>.
1810,a connection.
1811,a matrix-like object, that is one with a two-dimensional<code>dim</code>.
1812,a matrix dimension, i.e., an integer valued numeric vector oflength two (with non-negative entries).
1813,a logical value indicating whether the value shouldbe returned as a factor of column labels (created if necessary)rather than as numbers.
1814,an array of two or more dimensions, containing numeric,complex, integer or logical values, or a numeric data frame.  For<code>.colSums()</code> etc, a numeric, integer or logical matrix (orvector of length <code>m * n</code>).
1815,logical.  Should missing values (including <code>NaN</code>)be omitted from the calculations?
1816,integer: Which dimensions are regarded as ‘rows’ or‘columns’ to sum over.  For <code>row*</code>, the sum or mean isover dimensions <code>dims+1, ...</code>; for <code>col*</code> it is overdimensions <code>1:dims</code>.
1817,the dimensions of the matrix <code>x</code> for<code>.colSums()</code> etc.
1818,a matrix-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, with at least two dimensions for<code>colnames</code>.
1819,logical. If <code>FALSE</code> and names are <code>NULL</code>, names are created.
1820,for created names.
1821,a valid value for that component of<code>dimnames(x)</code>.  For a matrix or array this is either<code>NULL</code> or a character vector of non-zero length equal to theappropriate dimension.
1822,a matrix-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, with at least two dimensions for<code>colnames</code>.
1823,logical. If <code>FALSE</code> and names are <code>NULL</code>, names are created.
1824,for created names.
1825,a valid value for that component of<code>dimnames(x)</code>.  For a matrix or array this is either<code>NULL</code> or a character vector of non-zero length equal to theappropriate dimension.
1826,an array of two or more dimensions, containing numeric,complex, integer or logical values, or a numeric data frame.  For<code>.colSums()</code> etc, a numeric, integer or logical matrix (orvector of length <code>m * n</code>).
1827,logical.  Should missing values (including <code>NaN</code>)be omitted from the calculations?
1828,integer: Which dimensions are regarded as ‘rows’ or‘columns’ to sum over.  For <code>row*</code>, the sum or mean isover dimensions <code>dims+1, ...</code>; for <code>col*</code> it is overdimensions <code>1:dims</code>.
1829,the dimensions of the matrix <code>x</code> for<code>.colSums()</code> etc.
1830,logical.  Should only arguments after<span class="option">--args</span> be returned?
1831,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
1832,a <code>character</code> vector, or <code>NULL</code>.
1833,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
1834,a <code>character</code> vector, or <code>NULL</code>.
1835,numeric.  Desired length of the output vector,inputs being recycled as needed.
1836,numeric vector.
1837,numeric vector.
1838,numeric vector.
1839,numeric vector.
1840,an object, probably of mode <code>complex</code>.
1841,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
1842,further arguments passed to or from other methods.
1843,a condition object.
1844,call expression.
1845,a condition object.
1846,expression to be evaluated.
1847,expression to be evaluated before returning or exiting.
1848,function.
1849,character string.
1850,character string.
1851,character string naming a restart.
1852,restart object.
1853,object.
1854,character string naming a condition class.
1855,additional arguments; see details below.
1856,a condition object.
1857,call expression.
1858,a condition object.
1859,expression to be evaluated.
1860,expression to be evaluated before returning or exiting.
1861,function.
1862,character string.
1863,character string.
1864,character string naming a restart.
1865,restart object.
1866,object.
1867,character string naming a condition class.
1868,additional arguments; see details below.
1869,a condition object.
1870,call expression.
1871,a condition object.
1872,expression to be evaluated.
1873,expression to be evaluated before returning or exiting.
1874,function.
1875,character string.
1876,character string.
1877,character string naming a restart.
1878,restart object.
1879,object.
1880,character string naming a condition class.
1881,additional arguments; see details below.
1882,a condition object.
1883,call expression.
1884,a condition object.
1885,expression to be evaluated.
1886,expression to be evaluated before returning or exiting.
1887,function.
1888,character string.
1889,character string.
1890,character string naming a restart.
1891,restart object.
1892,object.
1893,character string naming a condition class.
1894,additional arguments; see details below.
1895,a condition object.
1896,call expression.
1897,a condition object.
1898,expression to be evaluated.
1899,expression to be evaluated before returning or exiting.
1900,function.
1901,character string.
1902,character string.
1903,character string naming a restart.
1904,restart object.
1905,object.
1906,character string naming a condition class.
1907,additional arguments; see details below.
1908,the name of a package, given as a name orliteral character string, or a character string, depending onwhether <code>character.only</code> is <code>FALSE</code> (default) or<code>TRUE</code>.
1909,the position on the search list at which to attach theloaded namespace.  Can also be the name of a position on the currentsearch list as given by <code>search()</code>.
1910,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.Non-existent library trees are silently ignored.
1911,a logical indicating whether <code>package</code> or<code>help</code> can be assumed to be character strings.
1912,logical.  If it is <code>TRUE</code>,  <code>FALSE</code> or<code>TRUE</code> is returned to indicate success.
1913,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the newpackage.  A conflict is a function masking a function,or a non-function masking a non-function. The default is <code>TRUE</code>unless specified as <code>FALSE</code> in the <code>conflicts.policy</code> option.
1914,a logical.  If <code>TRUE</code>, additional diagnostics areprinted.
1915,a logical.  If <code>TRUE</code>, no message confirmingpackage attaching is printed, and most often, no errors/warnings areprinted if package attaching fails.
1916,character string naming a package.
1917,character vector of names of objects that can maskobjects on the search path without signaling an error when strictconflict checking is enabled
1918,character vector of names of objects toexclude or include in the attached frame. Only one of these argumentsmay be used in a call to <code>library</code> or <code>require</code>.
1919,logical specifying whether required packageslisted in the <code>Depends</code> clause of the <code>DESCRIPTION</code> fileshould be attached automatically.
1920,A subset of the search path, by default the whole search path.
1921,If <code>TRUE</code>, give the masked or masking functions forall members of the search path.
1922,numeric.  Desired length of the output vector,inputs being recycled as needed.
1923,numeric vector.
1924,numeric vector.
1925,numeric vector.
1926,numeric vector.
1927,an object, probably of mode <code>complex</code>.
1928,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
1929,further arguments passed to or from other methods.
1930,numeric or complex vectors.
1931,a numeric or complex vector
1932,numeric or complex vectors.
1933,numeric or complex matrices (or vectors): <code>y = NULL</code>is taken to be the same matrix as <code>x</code>.  Vectors are promoted tosingle-column or single-row matrices, depending on the context.
1934,a numeric or complex (not <code>cummin</code> or <code>cummax</code>)object, or an object that can be coerced to one of these.
1935,a numeric or complex (not <code>cummin</code> or <code>cummax</code>)object, or an object that can be coerced to one of these.
1936,a numeric or complex (not <code>cummin</code> or <code>cummax</code>)object, or an object that can be coerced to one of these.
1937,a numeric or complex (not <code>cummin</code> or <code>cummax</code>)object, or an object that can be coerced to one of these.
1938,character string specifying the URL.
1939,logical: should redirections be followed?
1940,logical: should certificates be verified as validand applying to that host?
1941,integer: the maximum time in seconds the request isallowed to take.  Non-positive and invalid values are ignored(including the default).  (Added in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.1.0.)
1942,character: the minimum version of the TLS protocol to be usedfor <code>https://</code> URLs: the default (<code>""</code>) is no restrictionbeyond that of the underlying <code>libcurl</code> (usually 1.0).  Othervalid values are <code>"1.1"</code>, <code>"1.2"</code> (both for <code>libcurl</code>7.34.0 and later)  and <code>"1.3"</code> (7.52.0 and later), if supportedby the underlying version of <code>libcurl</code> and the SSL library it uses.
1943,a numeric vector which is to be converted to a factor by cutting.
1944,either a numeric vector of two or more unique cut points or asingle number (greater than or equal to 2) giving the number ofintervals into which <code>x</code> is to be cut.
1945,labels for the levels of the resulting category.  By default,labels are constructed using <code>"(a,b]"</code> interval notation.  If<code>labels = FALSE</code>, simple integer codes are returned instead ofa factor.
1946,logical, indicating if an ‘x[i]’ equal tothe lowest (or highest, for <code>right = FALSE</code>) ‘breaks’value should be included.
1947,logical, indicating if the intervals should be closed onthe right (and open on the left) or vice versa.
1948,integer which is used when labels are not given.  Itdetermines the number of digits used in formatting the break numbers.
1949,logical: should the result be an ordered factor?
1950,further arguments passed to or from other methods.
1951,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
1952,a vector of cut points <em>or</em> number giving the number ofintervals which <code>x</code> is to be cut into <em>or</em> aninterval specification, one of <code>"sec"</code>, <code>"min"</code>,<code>"hour"</code>, <code>"day"</code>, <code>"DSTday"</code>, <code>"week"</code>,<code>"month"</code>, <code>"quarter"</code> or <code>"year"</code>, optionallypreceded by an integer and a space, or followed by <code>"s"</code>.(For <code>"Date"</code> objects only interval specifications using<code>"day"</code>, <code>"week"</code>, <code>"month"</code>, <code>"quarter"</code> and<code>"year"</code> are allowed.)
1953,labels for the levels of the resulting category.  By default,labels are constructed from the left-hand end of the intervals(which are included for the default value of <code>right</code>).  If<code>labels = FALSE</code>, simple integer codes are returned insteadof a factor.
1954,logical.  If <code>breaks = "weeks"</code>, should theweek start on Mondays or Sundays?
1955,arguments to be passed to or from other methods.
1956,a numeric vector which is to be converted to a factor by cutting.
1957,either a numeric vector of two or more unique cut points or asingle number (greater than or equal to 2) giving the number ofintervals into which <code>x</code> is to be cut.
1958,labels for the levels of the resulting category.  By default,labels are constructed using <code>"(a,b]"</code> interval notation.  If<code>labels = FALSE</code>, simple integer codes are returned instead ofa factor.
1959,logical, indicating if an ‘x[i]’ equal tothe lowest (or highest, for <code>right = FALSE</code>) ‘breaks’value should be included.
1960,logical, indicating if the intervals should be closed onthe right (and open on the left) or vice versa.
1961,integer which is used when labels are not given.  Itdetermines the number of digits used in formatting the break numbers.
1962,logical: should the result be an ordered factor?
1963,further arguments passed to or from other methods.
1964,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
1965,a vector of cut points <em>or</em> number giving the number ofintervals which <code>x</code> is to be cut into <em>or</em> aninterval specification, one of <code>"sec"</code>, <code>"min"</code>,<code>"hour"</code>, <code>"day"</code>, <code>"DSTday"</code>, <code>"week"</code>,<code>"month"</code>, <code>"quarter"</code> or <code>"year"</code>, optionallypreceded by an integer and a space, or followed by <code>"s"</code>.(For <code>"Date"</code> objects only interval specifications using<code>"day"</code>, <code>"week"</code>, <code>"month"</code>, <code>"quarter"</code> and<code>"year"</code> are allowed.)
1966,labels for the levels of the resulting category.  By default,labels are constructed from the left-hand end of the intervals(which are included for the default value of <code>right</code>).  If<code>labels = FALSE</code>, simple integer codes are returned insteadof a factor.
1967,logical.  If <code>breaks = "weeks"</code>, should theweek start on Mondays or Sundays?
1968,arguments to be passed to or from other methods.
1969,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
1970,these arguments are of either the form <code>value</code> or<code>tag = value</code>.  Component names are created based on the tag (ifpresent) or the deparsed argument itself.
1971,<code>NULL</code> or a single integer or character stringspecifying a column to be used as row names, or a character orinteger vector giving the row names for the data frame.
1972,if <code>TRUE</code> then the rows are checked forconsistency of length and names.
1973,logical.  If <code>TRUE</code> then the names of thevariables in the data frame are checked to ensure that they aresyntactically valid variable names and are not duplicated.If necessary they are adjusted (by <code>make.names</code>)so that they are.
1974,logical indicating if arguments which are“unnamed” (in the sense of not being formally called as<code>someName = arg</code>) get an automatically constructed name orrather name <code>""</code>.  Needs to be set to <code>FALSE</code> even when<code>check.names</code> is false if <code>""</code> names should be kept.
1975,logical: should character vectors be convertedto factors?  The ‘factory-fresh’ default has been <code>TRUE</code>previously but has been changed to <code>FALSE</code> for <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0.
1976,a data frame whose components are logical vectors,factors or numeric or character vectors.
1977,logical indicating if the resulting matrixshould have character (rather than <code>NULL</code>)<code>rownames</code>.  The default, <code>NA</code>, uses <code>NULL</code>rownames if the data frame has ‘automatic’ row.names or for azero-row data frame.
1978,any interpreted <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> function.
1979,a text string that can be retrieved when the browser is entered.
1980,a condition that can be retrieved when the browser isentered.
1981,an optional method signature. If specified, themethod is debugged, rather than its generic.
1982,logical; a call to the support function<code>debuggingState</code> returns <code>TRUE</code> if debugging is globallyturned on, <code>FALSE</code> otherwise.  An argument of one or the otherof those values sets the state.  If the debugging state is<code>FALSE</code>, none of the debugging actions will occur (but explicit<code>browser</code> calls in functions will continue to work).
1983,any interpreted <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> function.
1984,a text string that can be retrieved when the browser is entered.
1985,a condition that can be retrieved when the browser isentered.
1986,an optional method signature. If specified, themethod is debugged, rather than its generic.
1987,logical; a call to the support function<code>debuggingState</code> returns <code>TRUE</code> if debugging is globallyturned on, <code>FALSE</code> otherwise.  An argument of one or the otherof those values sets the state.  If the debugging state is<code>FALSE</code>, none of the debugging actions will occur (but explicit<code>browser</code> calls in functions will continue to work).
1988,any interpreted <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> function.
1989,a text string that can be retrieved when the browser is entered.
1990,a condition that can be retrieved when the browser isentered.
1991,an optional method signature. If specified, themethod is debugged, rather than its generic.
1992,logical; a call to the support function<code>debuggingState</code> returns <code>TRUE</code> if debugging is globallyturned on, <code>FALSE</code> otherwise.  An argument of one or the otherof those values sets the state.  If the debugging state is<code>FALSE</code>, none of the debugging actions will occur (but explicit<code>browser</code> calls in functions will continue to work).
1993,these arguments are of either the form <code>value</code> or<code>tag = value</code>.  Component names are created based on the tag (ifpresent) or the deparsed argument itself.
1994,<code>NULL</code> or a single integer or character stringspecifying a column to be used as row names, or a character orinteger vector giving the row names for the data frame.
1995,if <code>TRUE</code> then the rows are checked forconsistency of length and names.
1996,logical.  If <code>TRUE</code> then the names of thevariables in the data frame are checked to ensure that they aresyntactically valid variable names and are not duplicated.If necessary they are adjusted (by <code>make.names</code>)so that they are.
1997,logical indicating if arguments which are“unnamed” (in the sense of not being formally called as<code>someName = arg</code>) get an automatically constructed name orrather name <code>""</code>.  Needs to be set to <code>FALSE</code> even when<code>check.names</code> is false if <code>""</code> names should be kept.
1998,logical: should character vectors be convertedto factors?  The ‘factory-fresh’ default has been <code>TRUE</code>previously but has been changed to <code>FALSE</code> for <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0.
1999,a variable name (given as a quoted string in the function call)
2000,an expression to be assigned to <code>x</code>
2001,an environment in which to evaluate <code>value</code>
2002,an environment in which to assign <code>x</code>
2003,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression.
2004,integer in <i>[20, 500]</i> determining the cutoff(in bytes) at which line-breaking is tried.
2005,logical indicating whether symbolic names should beenclosed in backticks if they do not follow the standard syntax.
2006,character vector (or <code>NULL</code>) of deparsing options.See <code>.deparseOpts</code>.
2007,integer: the maximum number of lines to produce.Negative values indicate no limit.
2008,a string, passed to <code>paste()</code>.
2009,further arguments passed to <code>deparse()</code>.
2010,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression.
2011,integer in <i>[20, 500]</i> determining the cutoff(in bytes) at which line-breaking is tried.
2012,logical indicating whether symbolic names should beenclosed in backticks if they do not follow the standard syntax.
2013,character vector (or <code>NULL</code>) of deparsing options.See <code>.deparseOpts</code>.
2014,integer: the maximum number of lines to produce.Negative values indicate no limit.
2015,a string, passed to <code>paste()</code>.
2016,further arguments passed to <code>deparse()</code>.
2017,numeric matrix: logical matrices are coerced to numeric.
2018,logical; if <code>TRUE</code> (default) return thelogarithm of the modulus of the determinant.
2019,Optional arguments.  At present none are used.  Previousversions of <code>det</code> allowed an optional <code>method</code> argument.This argument will be ignored but will not produce an error.
2020,The object to detach.  Defaults to <code>search()[pos]</code>.This can be an unquoted name or a character string but <em>not</em> acharacter vector.  If a number is supplied this is taken as <code>pos</code>.
2021,Index position in <code>search()</code> of the database todetach.  When <code>name</code> is a number, <code>pos = name</code>is used.
2022,A logical value indicating whether or not to attempt tounload the namespace when a package is being detached.  If thepackage has a namespace and <code>unload</code> is <code>TRUE</code>, then<code>detach</code> will attempt to unload the namespace <em>via</em><code>unloadNamespace</code>: if the namespace is imported byanother namespace or <code>unload</code> is <code>FALSE</code>, no unloadingwill occur.
2023,a logical indicating whether <code>name</code>can be assumed to be a character string.
2024,logical: should a package be detached even though otherattached packages depend on it?
2025,numeric matrix: logical matrices are coerced to numeric.
2026,logical; if <code>TRUE</code> (default) return thelogarithm of the modulus of the determinant.
2027,Optional arguments.  At present none are used.  Previousversions of <code>det</code> allowed an optional <code>method</code> argument.This argument will be ignored but will not produce an error.
2028,numeric matrix: logical matrices are coerced to numeric.
2029,logical; if <code>TRUE</code> (default) return thelogarithm of the modulus of the determinant.
2030,Optional arguments.  At present none are used.  Previousversions of <code>det</code> allowed an optional <code>method</code> argument.This argument will be ignored but will not produce an error.
2031,an object.
2032,either a character string naming a file or aconnection. <code>""</code> indicates output to the console.
2033,character vector indicating deparsing options.See <code>.deparseOpts</code> for their description.
2034,logical: should the source formatting be retained whenparsing functions, if possible?
2035,a matrix, vector or 1D <code>array</code>, or missing.
2036,optional dimensions for the result when <code>x</code> isnot a matrix.
2037,(when <code>x</code> is a matrix) logical indicating if theresulting vector, the diagonal of <code>x</code>, should inherit<code>names</code> from <code>dimnames(x)</code> if available.
2038,either a single value or a vector of length equal to thatof the current diagonal.  Should be of a mode which can be coercedto that of <code>x</code>.
2039,a matrix, vector or 1D <code>array</code>, or missing.
2040,optional dimensions for the result when <code>x</code> isnot a matrix.
2041,(when <code>x</code> is a matrix) logical indicating if theresulting vector, the diagonal of <code>x</code>, should inherit<code>names</code> from <code>dimnames(x)</code> if available.
2042,either a single value or a vector of length equal to thatof the current diagonal.  Should be of a mode which can be coercedto that of <code>x</code>.
2043,a numeric vector or matrix containing the values to bedifferenced.
2044,an integer indicating which lag to use.
2045,an integer indicating the order of the difference.
2046,further arguments to be passed to or from methods.
2047,a numeric vector or matrix containing the values to bedifferenced.
2048,an integer indicating which lag to use.
2049,an integer indicating the order of the difference.
2050,further arguments to be passed to or from methods.
2051,a numeric vector or matrix containing the values to bedifferenced.
2052,an integer indicating which lag to use.
2053,an integer indicating the order of the difference.
2054,further arguments to be passed to or from methods.
2055,date-time or date objects.
2056,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
2057,character string.  Units in which the results aredesired.  Can be abbreviated.
2058,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
2059,character string or numeric value specifying a time interval.
2060,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
2061,an object inheriting from class <code>"difftime"</code>.
2062,arguments to be passed to or from other methods.
2063,a numeric vector or matrix containing the values to bedifferenced.
2064,an integer indicating which lag to use.
2065,an integer indicating the order of the difference.
2066,further arguments to be passed to or from methods.
2067,date-time or date objects.
2068,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
2069,character string.  Units in which the results aredesired.  Can be abbreviated.
2070,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
2071,character string or numeric value specifying a time interval.
2072,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
2073,an object inheriting from class <code>"difftime"</code>.
2074,arguments to be passed to or from other methods.
2075,non-negative numeric vectors.
2076,numeric vectors.
2077,integer vectors.
2078,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
2079,For the default method, either <code>NULL</code> ora numeric vector, which is coerced to integer (by truncation).
2080,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
2081,For the default method, either <code>NULL</code> ora numeric vector, which is coerced to integer (by truncation).
2082,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
2083,For the default method, either <code>NULL</code> ora numeric vector, which is coerced to integer (by truncation).
2084,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
2085,a possible value for <code>dimnames(x)</code>: see the‘Value’ section.
2086,a character string, used to separate <code>base</code>symbols and digits in the constructed dimnames.
2087,a non-empty <code>list</code> of character vectors.  Thelist components are used in turn (and recycled when needed) toconstruct replacements for empty dimnames components.  See also theexamples.
2088,logical indicating that the dimnames constructed areunique within each dimension in the sense of <code>make.unique</code>.
2089,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
2090,a possible value for <code>dimnames(x)</code>: see the‘Value’ section.
2091,a character string, used to separate <code>base</code>symbols and digits in the constructed dimnames.
2092,a non-empty <code>list</code> of character vectors.  Thelist components are used in turn (and recycled when needed) toconstruct replacements for empty dimnames components.  See also theexamples.
2093,logical indicating that the dimnames constructed areunique within each dimension in the sense of <code>make.unique</code>.
2094,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
2095,a possible value for <code>dimnames(x)</code>: see the‘Value’ section.
2096,a character string, used to separate <code>base</code>symbols and digits in the constructed dimnames.
2097,a non-empty <code>list</code> of character vectors.  Thelist components are used in turn (and recycled when needed) toconstruct replacements for empty dimnames components.  See also theexamples.
2098,logical indicating that the dimnames constructed areunique within each dimension in the sense of <code>make.unique</code>.
2099,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
2100,a possible value for <code>dimnames(x)</code>: see the‘Value’ section.
2101,a character string, used to separate <code>base</code>symbols and digits in the constructed dimnames.
2102,a non-empty <code>list</code> of character vectors.  Thelist components are used in turn (and recycled when needed) toconstruct replacements for empty dimnames components.  See also theexamples.
2103,logical indicating that the dimnames constructed areunique within each dimension in the sense of <code>make.unique</code>.
2104,a character vector of full path names; the defaultcorresponds to the working directory, <code>getwd()</code>.  Tildeexpansion (see <code>path.expand</code>) is performed.  Missingvalues will be ignored.  Elements with a marked encoding willbe converted to the native encoding (and if that fails, considerednon-existent).
2105,an optional regular expression.  Only file nameswhich match the regular expression will be returned.
2106,a logical value.  If <code>FALSE</code>, only thenames of visible files are returned (following Unix-style visibility,that is files whose name does not start with a dot).  If <code>TRUE</code>,all file names will be returned.
2107,a logical value.  If <code>TRUE</code>, the directorypath is prepended to the file names to give a relative file path.If <code>FALSE</code>, the file names (rather than paths) are returned.
2108,logical.  Should the listing recurse into directories?
2109,logical.  Should pattern-matching be case-insensitive?
2110,logical.  Should subdirectory names be included inrecursive listings?  (They always are in non-recursive ones).
2111,logical.  Should both <code>"."</code> and <code>".."</code> be excludedalso from non-recursive listings?
2112,a character vector containing a single path name.  Tildeexpansion (see <code>path.expand</code>) is done.
2113,character vectors containing file or directory paths.  Tildeexpansion (see <code>path.expand</code>) is done.
2114,logical; should the warnings on failure be shown?
2115,logical. Should elements of the path other than thelast be created?  If true, like the Unix command <code>mkdir -p</code>.
2116,the mode to be used on Unix-alikes: it will becoerced by <code>as.octmode</code>.  For <code>Sys.chmod</code> it isrecycled along <code>paths</code>.
2117,logical: should the mode be restricted by the<code>umask</code> setting?
2118,a character vector containing a single path name.  Tildeexpansion (see <code>path.expand</code>) is done.
2119,character vectors containing file or directory paths.  Tildeexpansion (see <code>path.expand</code>) is done.
2120,logical; should the warnings on failure be shown?
2121,logical. Should elements of the path other than thelast be created?  If true, like the Unix command <code>mkdir -p</code>.
2122,the mode to be used on Unix-alikes: it will becoerced by <code>as.octmode</code>.  For <code>Sys.chmod</code> it isrecycled along <code>paths</code>.
2123,logical: should the mode be restricted by the<code>umask</code> setting?
2124,character vector, containing path names.
2125,either a function or a non-empty character string naming thefunction to be called.
2126,a <em>list</em> of arguments to the function call.  The<code>names</code> attribute of <code>args</code> gives the argument names.
2127,a logical value indicating whether to quote thearguments.
2128,an environment within which to evaluate the call.  Thiswill be most useful if <code>what</code> is a character string andthe arguments are symbols or quoted expressions.
2129,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2130,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
2131,object to be coerced or tested.
2132,further arguments passed to or from other methods.
2133,an object.
2134,either a character string naming a file or aconnection. <code>""</code> indicates output to the console.
2135,character vector indicating deparsing options.See <code>.deparseOpts</code> for their description.
2136,logical: should the source formatting be retained whenparsing functions, if possible?
2137,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be coerced to a character vector.
2138,the kind of quotes to be used, see ‘Details’.
2139,an array (including a matrix).
2140,an object from which to drop unused factor levels.
2141,passed to <code>factor()</code>; factor levels whichshould be excluded from the result even if present.  Note that thiswas <em>implicitly</em> <code>NA</code> in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &lt;= 3.3.1 which did drop<code>NA</code> levels even when present in <code>x</code>, contrary to thedocumentation.  The current default is compatible with <code>x[ , drop=TRUE]</code>.
2142,further arguments passed to methods
2143,indices of columns from which <em>not</em> to drop levels
2144,an object from which to drop unused factor levels.
2145,passed to <code>factor()</code>; factor levels whichshould be excluded from the result even if present.  Note that thiswas <em>implicitly</em> <code>NA</code> in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &lt;= 3.3.1 which did drop<code>NA</code> levels even when present in <code>x</code>, contrary to thedocumentation.  The current default is compatible with <code>x[ , drop=TRUE]</code>.
2146,further arguments passed to methods
2147,indices of columns from which <em>not</em> to drop levels
2148,an object from which to drop unused factor levels.
2149,passed to <code>factor()</code>; factor levels whichshould be excluded from the result even if present.  Note that thiswas <em>implicitly</em> <code>NA</code> in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &lt;= 3.3.1 which did drop<code>NA</code> levels even when present in <code>x</code>, contrary to thedocumentation.  The current default is compatible with <code>x[ , drop=TRUE]</code>.
2150,further arguments passed to methods
2151,indices of columns from which <em>not</em> to drop levels
2152,character vector.  The names of one or more <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects to be dumped.
2153,either a character string naming a file or aconnection.  <code>""</code> indicates output to the console.
2154,if <code>TRUE</code> and <code>file</code> is a character string,output will be appended to <code>file</code>; otherwise, it will overwritethe contents of <code>file</code>.
2155,character vector indicating deparsing options.See <code>.deparseOpts</code> for their description.
2156,the environment to search for objects.
2157,logical.  Should promises be evaluated?
2158,a vector or a data frame or an array or <code>NULL</code>.
2159,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
2160,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
2161,the maximum number of unique items expected (greater than one).
2162,arguments for particular methods.
2163,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
2164,a vector or a data frame or an array or <code>NULL</code>.
2165,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
2166,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
2167,the maximum number of unique items expected (greater than one).
2168,arguments for particular methods.
2169,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
2170,a vector or a data frame or an array or <code>NULL</code>.
2171,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
2172,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
2173,the maximum number of unique items expected (greater than one).
2174,arguments for particular methods.
2175,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
2176,a vector or a data frame or an array or <code>NULL</code>.
2177,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
2178,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
2179,the maximum number of unique items expected (greater than one).
2180,arguments for particular methods.
2181,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
2182,a vector or a data frame or an array or <code>NULL</code>.
2183,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
2184,logical indicating if duplication should be consideredfrom the reverse side, i.e., the last (or rightmost) of identicalelements would correspond to <code>duplicated = FALSE</code>.
2185,the maximum number of unique items expected (greater than one).
2186,arguments for particular methods.
2187,the array margin to be held fixed: see<code>apply</code>, and note that <code>MARGIN = 0</code> may be useful.
2188,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
2189,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
2190,an object to be printed or summarized from one of thedate-time classes.
2191,for timezone formatting, passed to <code>format.POSIXct</code>.
2192,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
2193,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
2194,further arguments to be passed from or to other methods.
2195,date-time objects
2196,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
2197,a numeric vector (in seconds)
2198,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
2199,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
2200,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
2201,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
2202,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
2203,a character string <code>cat()</code>ed before themessages are printed.
2204,a character string giving the pathname to a DLL, also knownas a dynamic shared object.  (See ‘Details’ for what theseterms mean.)
2205,a logical value controlling whether the symbols in theDLL are stored in their own local table and not sharedacross DLLs, or added to the global symbol table.  Whether this hasany effect is system-dependent.
2206,a logical controlling whether all symbols are resolved (andrelocated) immediately the library is loaded or deferred until theyare used.  This control is useful for developers testing whether alibrary is complete and has all the necessary symbols, and for usersto ignore missing symbols.  Whether this has any effect is system-dependent.
2207,other arguments for future expansion.
2208,a character string giving a symbol name.
2209,if supplied, confine the search for the <code>name</code> tothe DLL given by this argument (plus the conventional extension,‘<span class="file">.so</span>’, ‘<span class="file">.sl</span>’, ‘<span class="file">.dll</span>’, ...).  This is intended toadd safety for packages, which can ensure by using this argumentthat no other package can override their external symbols.  This isused in the same way as in <code>.C</code>, <code>.Call</code>,<code>.Fortran</code> and <code>.External</code> functions.
2210,The type of symbol to look for: can be any (<code>""</code>, thedefault), <code>"Fortran"</code>, <code>"Call"</code> or <code>"External"</code>.
2211,a character string giving the pathname to a DLL, also knownas a dynamic shared object.  (See ‘Details’ for what theseterms mean.)
2212,a logical value controlling whether the symbols in theDLL are stored in their own local table and not sharedacross DLLs, or added to the global symbol table.  Whether this hasany effect is system-dependent.
2213,a logical controlling whether all symbols are resolved (andrelocated) immediately the library is loaded or deferred until theyare used.  This control is useful for developers testing whether alibrary is complete and has all the necessary symbols, and for usersto ignore missing symbols.  Whether this has any effect is system-dependent.
2214,other arguments for future expansion.
2215,a character string giving a symbol name.
2216,if supplied, confine the search for the <code>name</code> tothe DLL given by this argument (plus the conventional extension,‘<span class="file">.so</span>’, ‘<span class="file">.sl</span>’, ‘<span class="file">.dll</span>’, ...).  This is intended toadd safety for packages, which can ensure by using this argumentthat no other package can override their external symbols.  This isused in the same way as in <code>.C</code>, <code>.Call</code>,<code>.Fortran</code> and <code>.External</code> functions.
2217,The type of symbol to look for: can be any (<code>""</code>, thedefault), <code>"Fortran"</code>, <code>"Call"</code> or <code>"External"</code>.
2218,For <code>get</code>, an object name (given as a characterstring or a symbol).<br>For <code>mget</code>, a character vector of object names.
2219,where to look for the object (see ‘Details’); ifomitted search as if the name of the object appeared unquoted in anexpression.
2220,the mode or type of object sought: see the‘Details’ section.
2221,should the enclosing frames of the environment besearched?
2222,For <code>mget</code>, a <code>list</code> of values tobe used if the item is not found: it will be coerced to a list ifnecessary.<br>For <code>dynGet</code> any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, e.g., a call to<code>stop()</code>.
2223,integer specifying the minimal frame number to lookinto.
2224,environment to be used.
2225,the function to be applied, found <em>via</em><code>match.fun</code>.In the case of functions like <code>+</code>, <code>%*%</code>, etc., thefunction name must be backquoted or quoted.
2226,optional arguments to <code>FUN</code>.
2227,a logical indicating whether to apply the function toall values.
2228,logical indicating whether the resulting list shouldhave <code>names</code>.
2229,a numeric or complex matrix whose spectral decomposition is tobe computed.  Logical matrices are coerced to numeric.
2230,if <code>TRUE</code>, the matrix is assumed to be symmetric(or Hermitian if complex) and only its lower triangle (diagonalincluded) is used.  If <code>symmetric</code> is not specified,<code>isSymmetric(x)</code> is used.
2231,if <code>TRUE</code>, only the eigenvalues are computedand returned, otherwise both eigenvalues and eigenvectors arereturned.
2232,logical. Defunct and ignored.
2233,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
2234,an environment to associate with the function
2235,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2236,a logical, if <code>TRUE</code> the environment will use a hash table.
2237,an environment to be used as the enclosure of theenvironment created.
2238,an environment
2239,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
2240,A character vector.
2241,A character vector of positive length.
2242,A character vector.
2243,A character vector of positive length.
2244,A character vector, or an object that can be coerced to oneby <code>as.character</code>.
2245,integer: the minimum field width.  If <code>NULL</code> or<code>NA</code>, this is taken to be the largest field width needed forany element of <code>x</code>.
2246,character: quoting character, if any.
2247,logical: should <code>NA</code> strings be encoded?
2248,character: partial matches are allowed.  If padding tothe minimum field width is needed, how should spaces be inserted?<code>justify == "none"</code> is equivalent to <code>width = 0</code>, forconsistency with <code>format.default</code>.
2249,A character vector.
2250,A character vector of positive length.
2251,A character vector.
2252,A character vector of positive length.
2253,vector of <code>character</code> string whose “starts”are considered.
2254,<code>character</code> vector (often of length one).
2255,any syntactically valid <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression
2256,a <code>call</code>, i.e., an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of<code>class</code> (and <code>mode</code>) <code>"call"</code>.
2257,an environment or a list object.  Defaults to thecurrent evaluation environment.
2258,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
2259,an environment to associate with the function
2260,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2261,a logical, if <code>TRUE</code> the environment will use a hash table.
2262,an environment to be used as the enclosure of theenvironment created.
2263,an environment
2264,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
2265,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
2266,an environment to associate with the function
2267,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2268,a logical, if <code>TRUE</code> the environment will use a hash table.
2269,an environment to be used as the enclosure of theenvironment created.
2270,an environment
2271,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
2272,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
2273,an environment to associate with the function
2274,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2275,a logical, if <code>TRUE</code> the environment will use a hash table.
2276,an environment to be used as the enclosure of theenvironment created.
2277,an environment
2278,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
2279,an environment.
2280,logical specifying whether bindings should be locked.
2281,a name object or character string.
2282,a function taking zero or one arguments.
2283,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
2284,an environment to associate with the function
2285,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2286,a logical, if <code>TRUE</code> the environment will use a hash table.
2287,an environment to be used as the enclosure of theenvironment created.
2288,an environment
2289,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
2290,a condition object.
2291,call expression.
2292,a condition object.
2293,expression to be evaluated.
2294,expression to be evaluated before returning or exiting.
2295,function.
2296,character string.
2297,character string.
2298,character string naming a restart.
2299,restart object.
2300,object.
2301,character string naming a condition class.
2302,additional arguments; see details below.
2303,an object to be evaluated.  See ‘Details’.
2304,the <code>environment</code> in which <code>expr</code> is tobe evaluated.  May also be <code>NULL</code>, a list, a data frame,a pairlist or an integer as specified to <code>sys.call</code>.
2305,Relevant when <code>envir</code> is a (pair)list or a data frame.Specifies the enclosure, i.e., where <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> looks for objects not foundin <code>envir</code>.  This can be <code>NULL</code> (interpreted as the basepackage environment, <code>baseenv()</code>) or an environment.
2306,number of parent generations to go back
2307,an object to be evaluated.  See ‘Details’.
2308,the <code>environment</code> in which <code>expr</code> is tobe evaluated.  May also be <code>NULL</code>, a list, a data frame,a pairlist or an integer as specified to <code>sys.call</code>.
2309,Relevant when <code>envir</code> is a (pair)list or a data frame.Specifies the enclosure, i.e., where <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> looks for objects not foundin <code>envir</code>.  This can be <code>NULL</code> (interpreted as the basepackage environment, <code>baseenv()</code>) or an environment.
2310,number of parent generations to go back
2311,an object to be evaluated.  See ‘Details’.
2312,the <code>environment</code> in which <code>expr</code> is tobe evaluated.  May also be <code>NULL</code>, a list, a data frame,a pairlist or an integer as specified to <code>sys.call</code>.
2313,Relevant when <code>envir</code> is a (pair)list or a data frame.Specifies the enclosure, i.e., where <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> looks for objects not foundin <code>envir</code>.  This can be <code>NULL</code> (interpreted as the basepackage environment, <code>baseenv()</code>) or an environment.
2314,number of parent generations to go back
2315,a variable name (given as a character string or a symbol).
2316,where to look for the object (see the details section); ifomitted, the function will search as if the name of the objectappeared unquoted in an expression.
2317,an alternative way to specify an environment to look in,but it is usually simpler to just use the <code>where</code> argument.
2318,a frame in the calling list.  Equivalent to giving<code>where</code> as <code>sys.frame(frame)</code>.
2319,the mode or type of object sought: see the‘Details’ section.
2320,should the enclosing frames of the environment besearched?
2321,the return value of <code>get0(x, *)</code> when<code>x</code> does not exist.
2322,a numeric or complex vector.
2323,a positive or complex number: the base with respect to whichlogarithms are computed.  Defaults to <i>e</i>=<code>exp(1)</code>.
2324,vectors, factors or a list containing these.
2325,a logical indicating the <code>"out.attrs"</code>attribute (see below) should be computed and returned.
2326,logical specifying if character vectors areconverted to factors.
2327,a numeric or complex vector.
2328,a positive or complex number: the base with respect to whichlogarithms are computed.  Defaults to <i>e</i>=<code>exp(1)</code>.
2329,<code>expression</code>: <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects, typically calls, symbolsor constants.<br><code>as.expression</code>: arguments to be passed to methods.
2330,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2331,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
2332,object to be coerced or tested.
2333,further arguments passed to or from other methods.
2334,a vector of data, usually taking a small number of distinctvalues.
2335,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
2336,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
2337,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
2338,logical flag to determine if the levels should be regardedas ordered (in the order given).
2339,an upper bound on the number of levels; see ‘Details’.
2340,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
2341,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
2342,non-negative numeric vectors.
2343,numeric vectors.
2344,integer vectors.
2345,character string.  A description of the connection:see ‘Details’.
2346,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
2347,logical.  See the ‘Blocking’ section.
2348,The name of the encoding to be assumed.  See the‘Encoding’ section.
2349,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
2350,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
2351,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
2352,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
2353,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
2354,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
2355,a filename within a zip file.
2356,character string.  Host name for the port.
2357,integer.  The TCP port number.
2358,logical.  Should the socket be a client or a server?
2359,a server socket listening for connections.
2360,a connection.
2361,character string.  Currently ignored.
2362,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
2363,arguments passed to or from other methods.
2364,character string.  A description of the connection:see ‘Details’.
2365,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
2366,logical.  See the ‘Blocking’ section.
2367,The name of the encoding to be assumed.  See the‘Encoding’ section.
2368,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
2369,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
2370,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
2371,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
2372,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
2373,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
2374,a filename within a zip file.
2375,character string.  Host name for the port.
2376,integer.  The TCP port number.
2377,logical.  Should the socket be a client or a server?
2378,a server socket listening for connections.
2379,a connection.
2380,character string.  Currently ignored.
2381,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
2382,arguments passed to or from other methods.
2383,character vector containing file names.Tilde-expansion will be done: see <code>path.expand</code>.
2384,integer specifying access mode required: see ‘Details’.
2385,character vectors, containing file names or paths.
2386,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2387,logical; should existing destination files be overwritten?
2388,logical; should the warnings on failure be shown?
2389,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2390,logical:  should file permission bits be copied wherepossible?
2391,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2392,Logical: choose the style of dialog boxpresented to the user: at present only new = FALSE is used.
2393,character vectors, containing file names or paths.
2394,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2395,logical; should existing destination files be overwritten?
2396,logical; should the warnings on failure be shown?
2397,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2398,logical:  should file permission bits be copied wherepossible?
2399,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2400,character vectors, containing file names or paths.
2401,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2402,logical; should existing destination files be overwritten?
2403,logical; should the warnings on failure be shown?
2404,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2405,logical:  should file permission bits be copied wherepossible?
2406,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2407,character vectors, containing file names or paths.
2408,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2409,logical; should existing destination files be overwritten?
2410,logical; should the warnings on failure be shown?
2411,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2412,logical:  should file permission bits be copied wherepossible?
2413,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2414,character vectors containing file paths.  Tilde-expansionis done: see <code>path.expand</code>.
2415,Logical: return all cols rather than just thefirst six.
2416,character vectors, containing file names or paths.
2417,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2418,logical; should existing destination files be overwritten?
2419,logical; should the warnings on failure be shown?
2420,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2421,logical:  should file permission bits be copied wherepossible?
2422,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2423,character vectors containing file paths.  Tilde-expansionis done: see <code>path.expand</code>.
2424,Logical: return all cols rather than just thefirst six.
2425,character vectors containing file paths.  Tilde-expansionis done: see <code>path.expand</code>.
2426,Logical: return all cols rather than just thefirst six.
2427,character vectors.  Long vectors are not supported.
2428,the path separator to use (assumed to be ASCII).
2429,character vectors, containing file names or paths.
2430,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2431,logical; should existing destination files be overwritten?
2432,logical; should the warnings on failure be shown?
2433,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2434,logical:  should file permission bits be copied wherepossible?
2435,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2436,character vectors, containing file names or paths.
2437,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2438,logical; should existing destination files be overwritten?
2439,logical; should the warnings on failure be shown?
2440,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2441,logical:  should file permission bits be copied wherepossible?
2442,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2443,one or more character vectors containing the names of thefiles to be displayed.  Paths with have tilde expansion.
2444,character vector (of the same length as the number of filesspecified in <code>...</code>) giving a header for each file beingdisplayed.  Defaults to empty strings.
2445,an overall title for the display.  If a single separatewindow is used for the display, <code>title</code> will be used as the windowtitle.  If multiple windows are used, their titles should combine the titleand the file-specific header.
2446,should the files be deleted after display?  Usedfor temporary files.
2447,the pager to be used: not used on all platforms
2448,character string giving the encoding to be assumed forthe file(s).
2449,character vectors containing file paths.  Tilde-expansionis done: see <code>path.expand</code>.
2450,Logical: return all cols rather than just thefirst six.
2451,character vectors, containing file names or paths.
2452,character vectors, containing file names or paths.For <code>file.copy</code> and <code>file.symlink</code><code>to</code> can alternatively be the path to a single existing directory.
2453,logical; should existing destination files be overwritten?
2454,logical; should the warnings on failure be shown?
2455,logical.  If <code>to</code> is a directory, shoulddirectories in <code>from</code> be copied (and their contents)?  (Like<code>cp -R</code> on POSIX OSes.)
2456,logical:  should file permission bits be copied wherepossible?
2457,logical:  should file dates be preserved wherepossible? See <code>Sys.setFileTime</code>.
2458,a function of the appropriate arity (binary for<code>Reduce</code>, unary for <code>Filter</code>, <code>Find</code> and<code>Position</code>, <i>k</i>-ary for <code>Map</code> if this is called with<i>k</i> arguments).  An arbitrary predicate function for<code>Negate</code>.
2459,a vector.
2460,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of the same kind as the elements of<code>x</code>.
2461,a logical indicating whether to proceed from left toright (default) or from right to left.
2462,a logical indicating whether the successive reducecombinations should be accumulated.  By default, only the finalcombination is used.
2463,the value to be returned in the case when“no match” (no element satisfying the predicate) is found.
2464,vectors.
2465,a function of the appropriate arity (binary for<code>Reduce</code>, unary for <code>Filter</code>, <code>Find</code> and<code>Position</code>, <i>k</i>-ary for <code>Map</code> if this is called with<i>k</i> arguments).  An arbitrary predicate function for<code>Negate</code>.
2466,a vector.
2467,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of the same kind as the elements of<code>x</code>.
2468,a logical indicating whether to proceed from left toright (default) or from right to left.
2469,a logical indicating whether the successive reducecombinations should be accumulated.  By default, only the finalcombination is used.
2470,the value to be returned in the case when“no match” (no element satisfying the predicate) is found.
2471,vectors.
2472,character vector: the names of packages.
2473,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to checking the loaded namespace, thenall libraries currently known in <code>.libPaths()</code>.
2474,logical.  Should this not give warnings or an errorif the package is not found?
2475,a logical.  If <code>TRUE</code>, additional diagnostics areprinted, notably when a package is found more than once.
2476,call expression.
2477,numeric.
2478,numeric, sorted (weakly) increasingly, of length <code>N</code>,say.
2479,logical; if true, the rightmost interval,<code>vec[N-1] .. vec[N]</code> is treated as <em>closed</em>, see below.
2480,logical; if true, the returned indices are coercedinto <code>1,...,N-1</code>, i.e., <code>0</code> is mapped to <code>1</code>and <code>N</code> to <code>N-1</code>.
2481,logical; if true all the intervals are open at leftand closed at right; in the formulas below, <i>≤</i> should beswapped with <i>&lt;</i> (and <i>&gt;</i> with <i>≥</i>), and<code>rightmost.closed</code> means ‘leftmost is closed’.  This maybe useful, e.g., in survival analysis computations.
2482,an object

2483,from which to extract elements.
2484,with a <code>"row.names"</code> attribute, typically adata frame.
2485,typically S3- or S4-classed.
2486,The full path to the‘<span class="file">DESCRIPTION</span>’ file of a package.
2487,the filepath to a lazyload database.
2488,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.
2489,logical.  Should this not give warnings or an errorif the package(s) are not found?
2490,logical.  Should  the <code>Imports</code> field be processed?
2491,logical: should message(s) be printed.
2492,A processed ‘<span class="file">DESCRIPTION</span>’ file of class<code>"packageDescription2"</code>.
2493,platform strings of the form <code>"cpu-vendor-os"</code>.
2494,name of image file to save or load.
2495,integer.  Currently <code>type = 0</code> returns the internal<code>"row.names"</code> attribute (possibly <code>NULL</code>), <code>type = 2</code>the number of rows implied by the attribute, and <code>type = 1</code> thelatter with a negative sign for ‘automatic’ row names.
2496,integer.  The number of rows.
2497,character string such as <code>"package:MASS"</code>.
2498,the object name in a lazyload database.
2499,logical: are the values in the database compressed?
2500,a load hook: see <code>unserialize</code>.
2501,a length-one integer vector.
2502,indices.
2503,logical.
2504,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2505,a classless object to be placed into the class.
2506,an optional time zone.
2507,an optional character vector of classes <em>containing</em>the default classes.
2508,see <code>difftime</code>.
2509,Arguments passed to non-dot name versions.
2510,an integer.
2511,character vector of <em>same</em> objects, masking others.
2512,character string naming the package which is masked from or by.
2513,logical indicating if the masking happens <em>by</em><code>pkg</code>, or (<code>by = FALSE</code>) from <code>pkg</code>.
2514,a condition object.
2515,call expression.
2516,a condition object.
2517,expression to be evaluated.
2518,expression to be evaluated before returning or exiting.
2519,function.
2520,character string.
2521,character string.
2522,character string naming a restart.
2523,restart object.
2524,object.
2525,character string naming a condition class.
2526,additional arguments; see details below.
2527,a numeric vector.  Or, for <code>round</code> and <code>signif</code>, acomplex vector.
2528,integer indicating the number of decimal places(<code>round</code>) or significant digits (<code>signif</code>) to be used.Negative values are allowed (see ‘Details’).
2529,arguments to be passed to methods.
2530,character string.  A description of the connection:see ‘Details’.
2531,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
2532,logical.  See the ‘Blocking’ section.
2533,The name of the encoding to be assumed.  See the‘Encoding’ section.
2534,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
2535,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
2536,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
2537,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
2538,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
2539,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
2540,a filename within a zip file.
2541,character string.  Host name for the port.
2542,integer.  The TCP port number.
2543,logical.  Should the socket be a client or a server?
2544,a server socket listening for connections.
2545,a connection.
2546,character string.  Currently ignored.
2547,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
2548,arguments passed to or from other methods.
2549,character string.  A description of the connection:see ‘Details’.
2550,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
2551,logical.  See the ‘Blocking’ section.
2552,The name of the encoding to be assumed.  See the‘Encoding’ section.
2553,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
2554,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
2555,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
2556,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
2557,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
2558,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
2559,a filename within a zip file.
2560,character string.  Host name for the port.
2561,integer.  The TCP port number.
2562,logical.  Should the socket be a client or a server?
2563,a server socket listening for connections.
2564,a connection.
2565,character string.  Currently ignored.
2566,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
2567,arguments passed to or from other methods.
2568,A length-one logical vector that is not <code>NA</code>.Conditions of length greater than one are currently accepted with awarning, but only the first element is used.  An error is signalledinstead when the environment variable <span class="env">_R_CHECK_LENGTH_1_CONDITION_</span>is set to true.  Other types are coerced to logicalif possible, ignoring any class.
2569,A syntactical name for a variable.
2570,An expression evaluating to a vector (including a list andan expression) or to a pairlist or <code>NULL</code>.  Afactor value will be coerced to a character vector.  As from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>4.0.0 this can be a long vector.
2571,An <em>expression</em> in a formal sense.  This is either asimple expression or a so-called <em>compound expression</em>, usuallyof the form <code>{ expr1 ; expr2 }</code>.
2572,a formal argument of the enclosing function.
2573,number of leading arguments to force.
2574,function to call.
2575,arguments to <code>FUN</code>.
2576,a <code>function</code>, or see ‘Details’.
2577,<code>environment</code> in which the function should bedefined (or found via <code>get()</code> in the first case and when<code>fun</code> a character string).
2578,a <code>list</code> (or <code>pairlist</code>) of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expressions.
2579,a <code>function</code>, or see ‘Details’.
2580,<code>environment</code> in which the function should bedefined (or found via <code>get()</code> in the first case and when<code>fun</code> a character string).
2581,a <code>list</code> (or <code>pairlist</code>) of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expressions.
2582,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object (conceptually); typically numeric.
2583,logical; if <code>FALSE</code>, logical, numeric and complexvalues are right-justified to a common width: if <code>TRUE</code> theleading blanks for justification are suppressed.
2584,how many significant digits are to be used fornumeric and complex <code>x</code>.  The default, <code>NULL</code>, uses<code>getOption("digits")</code>.  This is a suggestion: enough decimalplaces will be used so that the smallest (in magnitude) number hasthis many significant digits, and also to satisfy <code>nsmall</code>.(For the interpretation for complex numbers see <code>signif</code>.)
2585,the minimum number of digits to the right of the decimalpoint in formatting real/complex numbers in non-scientific formats.Allowed values are <code>0 &lt;= nsmall &lt;= 20</code>.
2586,should a <em>character</em> vector be left-justified (thedefault), right-justified, centred or left alone.  Can be abbreviated.
2587,<code>default</code> method: the <em>minimum</em> field width or<code>NULL</code> or <code>0</code> for no restriction.

2588,<code>AsIs</code> method: the <em>maximum</em> field width for non-characterobjects.  <code>NULL</code> corresponds to the default <code>12</code>.
2589,logical: should <code>NA</code> strings be encoded?  Notethis only applies to elements of character vectors, not to numerical,complex nor logical <code>NA</code>s, which are always encoded as <code>"NA"</code>.
2590,Either a logical specifying whetherelements of a real or complex vector should be encoded in scientificformat, or an integer penalty (see <code>options("scipen")</code>).Missing values correspond to the current default penalty.
2591,further arguments passed to or from other methods.
2592,used for prettying (longish) numerical and complex sequences.Passed to <code>prettyNum</code>: that help page explains the details.
2593,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object (conceptually); typically numeric.
2594,logical; if <code>FALSE</code>, logical, numeric and complexvalues are right-justified to a common width: if <code>TRUE</code> theleading blanks for justification are suppressed.
2595,how many significant digits are to be used fornumeric and complex <code>x</code>.  The default, <code>NULL</code>, uses<code>getOption("digits")</code>.  This is a suggestion: enough decimalplaces will be used so that the smallest (in magnitude) number hasthis many significant digits, and also to satisfy <code>nsmall</code>.(For the interpretation for complex numbers see <code>signif</code>.)
2596,the minimum number of digits to the right of the decimalpoint in formatting real/complex numbers in non-scientific formats.Allowed values are <code>0 &lt;= nsmall &lt;= 20</code>.
2597,should a <em>character</em> vector be left-justified (thedefault), right-justified, centred or left alone.  Can be abbreviated.
2598,<code>default</code> method: the <em>minimum</em> field width or<code>NULL</code> or <code>0</code> for no restriction.

2599,<code>AsIs</code> method: the <em>maximum</em> field width for non-characterobjects.  <code>NULL</code> corresponds to the default <code>12</code>.
2600,logical: should <code>NA</code> strings be encoded?  Notethis only applies to elements of character vectors, not to numerical,complex nor logical <code>NA</code>s, which are always encoded as <code>"NA"</code>.
2601,Either a logical specifying whetherelements of a real or complex vector should be encoded in scientificformat, or an integer penalty (see <code>options("scipen")</code>).Missing values correspond to the current default penalty.
2602,further arguments passed to or from other methods.
2603,used for prettying (longish) numerical and complex sequences.Passed to <code>prettyNum</code>: that help page explains the details.
2604,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object (conceptually); typically numeric.
2605,logical; if <code>FALSE</code>, logical, numeric and complexvalues are right-justified to a common width: if <code>TRUE</code> theleading blanks for justification are suppressed.
2606,how many significant digits are to be used fornumeric and complex <code>x</code>.  The default, <code>NULL</code>, uses<code>getOption("digits")</code>.  This is a suggestion: enough decimalplaces will be used so that the smallest (in magnitude) number hasthis many significant digits, and also to satisfy <code>nsmall</code>.(For the interpretation for complex numbers see <code>signif</code>.)
2607,the minimum number of digits to the right of the decimalpoint in formatting real/complex numbers in non-scientific formats.Allowed values are <code>0 &lt;= nsmall &lt;= 20</code>.
2608,should a <em>character</em> vector be left-justified (thedefault), right-justified, centred or left alone.  Can be abbreviated.
2609,<code>default</code> method: the <em>minimum</em> field width or<code>NULL</code> or <code>0</code> for no restriction.

2610,<code>AsIs</code> method: the <em>maximum</em> field width for non-characterobjects.  <code>NULL</code> corresponds to the default <code>12</code>.
2611,logical: should <code>NA</code> strings be encoded?  Notethis only applies to elements of character vectors, not to numerical,complex nor logical <code>NA</code>s, which are always encoded as <code>"NA"</code>.
2612,Either a logical specifying whetherelements of a real or complex vector should be encoded in scientificformat, or an integer penalty (see <code>options("scipen")</code>).Missing values correspond to the current default penalty.
2613,further arguments passed to or from other methods.
2614,used for prettying (longish) numerical and complex sequences.Passed to <code>prettyNum</code>: that help page explains the details.
2615,an object to be converted.
2616,<code>character</code> string.  If not specified, it will try<code>tryFormats</code> one by one on the first non-<code>NA</code> element, andgive an error if none works.  Otherwise, the processing is via<code>strptime()</code> whose help page describes availableconversion specifications.
2617,<code>character</code> vector of <code>format</code>strings to try if <code>format</code> is not specified.
2618,<code>logical</code> indicating to return <code>NA</code>(instead of signalling an error) if the format guessing does not succeed.
2619,a <code>Date</code> object, or something which can be coerced by<code>as.Date(origin, ...)</code> to such an object.
2620,a time zone name.
2621,further arguments to be passed from or to other methods,including <code>format</code> for <code>as.character</code> and <code>as.Date</code>methods.
2622,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object (conceptually); typically numeric.
2623,logical; if <code>FALSE</code>, logical, numeric and complexvalues are right-justified to a common width: if <code>TRUE</code> theleading blanks for justification are suppressed.
2624,how many significant digits are to be used fornumeric and complex <code>x</code>.  The default, <code>NULL</code>, uses<code>getOption("digits")</code>.  This is a suggestion: enough decimalplaces will be used so that the smallest (in magnitude) number hasthis many significant digits, and also to satisfy <code>nsmall</code>.(For the interpretation for complex numbers see <code>signif</code>.)
2625,the minimum number of digits to the right of the decimalpoint in formatting real/complex numbers in non-scientific formats.Allowed values are <code>0 &lt;= nsmall &lt;= 20</code>.
2626,should a <em>character</em> vector be left-justified (thedefault), right-justified, centred or left alone.  Can be abbreviated.
2627,<code>default</code> method: the <em>minimum</em> field width or<code>NULL</code> or <code>0</code> for no restriction.

2628,<code>AsIs</code> method: the <em>maximum</em> field width for non-characterobjects.  <code>NULL</code> corresponds to the default <code>12</code>.
2629,logical: should <code>NA</code> strings be encoded?  Notethis only applies to elements of character vectors, not to numerical,complex nor logical <code>NA</code>s, which are always encoded as <code>"NA"</code>.
2630,Either a logical specifying whetherelements of a real or complex vector should be encoded in scientificformat, or an integer penalty (see <code>options("scipen")</code>).Missing values correspond to the current default penalty.
2631,further arguments passed to or from other methods.
2632,used for prettying (longish) numerical and complex sequences.Passed to <code>prettyNum</code>: that help page explains the details.
2633,date-time or date objects.
2634,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
2635,character string.  Units in which the results aredesired.  Can be abbreviated.
2636,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
2637,character string or numeric value specifying a time interval.
2638,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
2639,an object inheriting from class <code>"difftime"</code>.
2640,arguments to be passed to or from other methods.
2641,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object (conceptually); typically numeric.
2642,logical; if <code>FALSE</code>, logical, numeric and complexvalues are right-justified to a common width: if <code>TRUE</code> theleading blanks for justification are suppressed.
2643,how many significant digits are to be used fornumeric and complex <code>x</code>.  The default, <code>NULL</code>, uses<code>getOption("digits")</code>.  This is a suggestion: enough decimalplaces will be used so that the smallest (in magnitude) number hasthis many significant digits, and also to satisfy <code>nsmall</code>.(For the interpretation for complex numbers see <code>signif</code>.)
2644,the minimum number of digits to the right of the decimalpoint in formatting real/complex numbers in non-scientific formats.Allowed values are <code>0 &lt;= nsmall &lt;= 20</code>.
2645,should a <em>character</em> vector be left-justified (thedefault), right-justified, centred or left alone.  Can be abbreviated.
2646,<code>default</code> method: the <em>minimum</em> field width or<code>NULL</code> or <code>0</code> for no restriction.

2647,<code>AsIs</code> method: the <em>maximum</em> field width for non-characterobjects.  <code>NULL</code> corresponds to the default <code>12</code>.
2648,logical: should <code>NA</code> strings be encoded?  Notethis only applies to elements of character vectors, not to numerical,complex nor logical <code>NA</code>s, which are always encoded as <code>"NA"</code>.
2649,Either a logical specifying whetherelements of a real or complex vector should be encoded in scientificformat, or an integer penalty (see <code>options("scipen")</code>).Missing values correspond to the current default penalty.
2650,further arguments passed to or from other methods.
2651,used for prettying (longish) numerical and complex sequences.Passed to <code>prettyNum</code>: that help page explains the details.
2652,An object, for the methods inheriting from class <code>"hexmode"</code>.
2653,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
2654,a logical indicating whether to use upper-caseletters or lower-case letters (default).
2655,further arguments passed to or from other methods.
2656,an atomic vector; a potential argument of<code>format(x, ...)</code>.
2657,how many significant digits are to be used fornumeric and complex <code>x</code>.  The default, <code>NULL</code>, uses<code>getOption("digits")</code>.
2658, (see <code>format(..., nsmall)</code>).
2659,the name of a package, given as a name orliteral character string, or a character string, depending onwhether <code>character.only</code> is <code>FALSE</code> (default) or<code>TRUE</code>.
2660,the position on the search list at which to attach theloaded namespace.  Can also be the name of a position on the currentsearch list as given by <code>search()</code>.
2661,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.Non-existent library trees are silently ignored.
2662,a logical indicating whether <code>package</code> or<code>help</code> can be assumed to be character strings.
2663,logical.  If it is <code>TRUE</code>,  <code>FALSE</code> or<code>TRUE</code> is returned to indicate success.
2664,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the newpackage.  A conflict is a function masking a function,or a non-function masking a non-function. The default is <code>TRUE</code>unless specified as <code>FALSE</code> in the <code>conflicts.policy</code> option.
2665,a logical.  If <code>TRUE</code>, additional diagnostics areprinted.
2666,a logical.  If <code>TRUE</code>, no message confirmingpackage attaching is printed, and most often, no errors/warnings areprinted if package attaching fails.
2667,character string naming a package.
2668,character vector of names of objects that can maskobjects on the search path without signaling an error when strictconflict checking is enabled
2669,character vector of names of objects toexclude or include in the attached frame. Only one of these argumentsmay be used in a call to <code>library</code> or <code>require</code>.
2670,logical specifying whether required packageslisted in the <code>Depends</code> clause of the <code>DESCRIPTION</code> fileshould be attached automatically.
2671,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
2672,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
2673,An object, for the methods inheriting from class <code>"octmode"</code>.
2674,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
2675,further arguments passed to or from other methods.
2676,the name of a package, given as a name orliteral character string, or a character string, depending onwhether <code>character.only</code> is <code>FALSE</code> (default) or<code>TRUE</code>.
2677,the position on the search list at which to attach theloaded namespace.  Can also be the name of a position on the currentsearch list as given by <code>search()</code>.
2678,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.Non-existent library trees are silently ignored.
2679,a logical indicating whether <code>package</code> or<code>help</code> can be assumed to be character strings.
2680,logical.  If it is <code>TRUE</code>,  <code>FALSE</code> or<code>TRUE</code> is returned to indicate success.
2681,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the newpackage.  A conflict is a function masking a function,or a non-function masking a non-function. The default is <code>TRUE</code>unless specified as <code>FALSE</code> in the <code>conflicts.policy</code> option.
2682,a logical.  If <code>TRUE</code>, additional diagnostics areprinted.
2683,a logical.  If <code>TRUE</code>, no message confirmingpackage attaching is printed, and most often, no errors/warnings areprinted if package attaching fails.
2684,character string naming a package.
2685,character vector of names of objects that can maskobjects on the search path without signaling an error when strictconflict checking is enabled
2686,character vector of names of objects toexclude or include in the attached frame. Only one of these argumentsmay be used in a call to <code>library</code> or <code>require</code>.
2687,logical specifying whether required packageslisted in the <code>Depends</code> clause of the <code>DESCRIPTION</code> fileshould be attached automatically.
2688,An object to be converted: a character vector for<code>strptime</code>, an object which can be converted to<code>"POSIXlt"</code> for <code>strftime</code>.
2689,A character string specifying the time zone to be used forthe conversion.  System-specific (see <code>as.POSIXlt</code>), but<code>""</code> is the current time zone, and <code>"GMT"</code> is UTC.Invalid values are most commonly treated as UTC, on some platforms witha warning.
2690,A character string.  The default for the <code>format</code>methods is<code>"%Y-%m-%d %H:%M:%S"</code> if any element has a timecomponent which is not midnight, and <code>"%Y-%m-%d"</code>otherwise.  If <code>options("digits.secs")</code> is set, up tothe specified number of digits will be printed for seconds.
2691,Further arguments to be passed from or to other methods.
2692,logical.  Should the time zone abbreviation be appendedto the output?  This is used in printing times, and more reliablethan using <code>"%Z"</code>.
2693,An object to be converted: a character vector for<code>strptime</code>, an object which can be converted to<code>"POSIXlt"</code> for <code>strftime</code>.
2694,A character string specifying the time zone to be used forthe conversion.  System-specific (see <code>as.POSIXlt</code>), but<code>""</code> is the current time zone, and <code>"GMT"</code> is UTC.Invalid values are most commonly treated as UTC, on some platforms witha warning.
2695,A character string.  The default for the <code>format</code>methods is<code>"%Y-%m-%d %H:%M:%S"</code> if any element has a timecomponent which is not midnight, and <code>"%Y-%m-%d"</code>otherwise.  If <code>options("digits.secs")</code> is set, up tothe specified number of digits will be printed for seconds.
2696,Further arguments to be passed from or to other methods.
2697,logical.  Should the time zone abbreviation be appendedto the output?  This is used in printing times, and more reliablethan using <code>"%Z"</code>.
2698,a numeric vector.
2699,how many significant digits are to be used.
2700,a numerical tolerance: see ‘Details’.
2701,character representation of <code>NA</code>s.
2702,further arguments to be passed to <code>format</code>such as <code>nsmall</code>.
2703,an object for which a summary is desired.
2704,a result of the <em>default</em> method of <code>summary()</code>.
2705,integer, indicating how many levels should be shown for<code>factor</code>s.
2706,integer, used for number formatting with<code>signif()</code> (for <code>summary.default</code>) or<code>format()</code> (for <code>summary.data.frame</code>).  In<code>summary.default</code>, if not specified (i.e.,<code>missing(.)</code>), <code>signif()</code> will <em>not</em> be calledanymore (since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &gt;= 3.4.0, where the default has been changed toonly round in the <code>print</code> and <code>format</code> methods).
2707,integer code used in <code>quantile(*, type=quantile.type)</code>for the default method.
2708,additional arguments affecting the summary produced.
2709,an atomic numerical or character object, possibly<code>complex</code> only for <code>prettyNum()</code>, typically avector of real numbers.  Any class is discarded, with a warning.
2710,the desired number of digits after the decimalpoint (<code>format = "f"</code>) or <em>significant</em> digits(<code>format = "g"</code>, <code>= "e"</code> or <code>= "fg"</code>).

2711,Default: 2 for integer, 4 for real numbers.  If less than 0,the C default of 6 digits is used.  If specified as more than 50, 50will be used with a warning unless <code>format = "f"</code> where it islimited to typically 324. (Not more than 15–21 digits need beaccurate, depending on the OS and compiler used.  This limit isjust a precaution against segfaults in the underlying C runtime.)
2712,the total field width; if both <code>digits</code> and<code>width</code> are unspecified, <code>width</code> defaults to 1,otherwise to <code>digits + 1</code>.  <code>width = 0</code> will use<code>width = digits</code>, <code>width &lt; 0</code> means leftjustify the number in this field (equivalent to <code>flag = "-"</code>).If necessary, the result will have more characters than<code>width</code>.  For character data this is interpreted in characters(not bytes nor display width).
2713,equal to <code>"d"</code>  (for integers), <code>"f"</code>,<code>"e"</code>, <code>"E"</code>, <code>"g"</code>, <code>"G"</code>, <code>"fg"</code> (forreals), or <code>"s"</code> (for strings).  Default is <code>"d"</code> forintegers, <code>"g"</code> for reals.

2714,<code>"f"</code> gives numbers in the usual<code>xxx.xxx</code> format;  <code>"e"</code> and <code>"E"</code> give <code>n.ddde+nn</code> or<code>n.dddE+nn</code> (scientific format); <code>"g"</code> and <code>"G"</code> put<code>x[i]</code> into scientific format only if it saves space to do so<em>and</em> drop trailing zeros and decimal point - unless <code>flag</code>contains <code>"#"</code> which keeps trailing zeros for the <code>"g", "G"</code>formats.

2715,<code>"fg"</code> (our own hybrid format) uses fixed format as <code>"f"</code>,but <code>digits</code> as the minimum number of <em>significant</em> digits.This can lead to quite long result strings, see examples below.  Notethat unlike <code>signif</code> this prints large numbers withmore significant digits than <code>digits</code>.  Trailing zeros are<em>dropped</em> in this format, unless <code>flag</code> contains<code>"#"</code>.
2716,for <code>formatC</code>, a character string giving aformat modifier as in Kernighan and Ritchie (1988, page 243) or theC+99 standard.

2717,pads leading zeros;
2718,does left adjustment,
2719,ensures a sign in all cases, i.e., <code>"+"</code> forpositive numbers ,
2720,if the first character is not a sign, the spacecharacter <code>" "</code> will be used instead.
2721,specifies “an alternative output form”,specifically depending on <code>format</code>.
2722,on some platform–locale combination, activates“thousands' grouping” for decimal conversion,
2723,in some versions of ‘<span class="file">glibc</span>’ allow for integerconversion to use the locale's alternative output digits, if any.
2724,There can be more than one of these flags, in any order.  Other charactersused to have no effect for <code>character</code> formatting, but signalan error since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.4.0.
2725,<code>"double"</code> (or <code>"real"</code>), <code>"integer"</code> or<code>"character"</code>.Default: Determined from the storage mode of <code>x</code>.
2726,character; if not empty used as mark between every<code>big.interval</code> decimals <em>before</em> (hence <code>big</code>) thedecimal point.
2727,see <code>big.mark</code> above; defaults to 3.
2728,character; if not empty used as mark between every<code>small.interval</code> decimals <em>after</em> (hence <code>small</code>) thedecimal point.
2729,see <code>small.mark</code> above; defaults to 5.
2730,the character to be used to indicate the numericdecimal point.
2731,if <code>x</code> is <code>character</code>, thecharacter known to have been used as the numeric decimal point in<code>x</code>.
2732,string specifying if the string widths shouldbe preserved where possible in those cases where marks(<code>big.mark</code> or <code>small.mark</code>) are added.  <code>"common"</code>,the default, corresponds to <code>format</code>-like behaviorwhereas <code>"individual"</code> is the default in<code>formatC()</code>. Value can be abbreviated.
2733,logical, character string or <code>NULL</code> specifyingif and how <em>zeros</em> should be formatted specially.  Useful forpretty printing ‘sparse’ objects.
2734,logical; if <code>zero.print</code> is acharacter string, indicates if the exact zero entries in <code>x</code>should be simply replaced by <code>zero.print</code>.  Otherwise,depending on the widths of the respective strings, the (formatted)zeroes are <em>partly</em> replaced by <code>zero.print</code> and thenpadded with <code>" "</code> to the right were applicable.  In that case(false <code>replace[.zero]</code>), if the <code>zero.print</code> string doesnot fit, a warning is produced (if <code>warn.non.fitting</code> is true).

2735,This works via <code>prettyNum()</code>, which calls <code>.format.zeros(*,    replace=replace.zero)</code> three times in this case, see the ‘Details’.
2736,logical; if it is true, <code>replace[.zero]</code> isfalse and the <code>zero.print</code> string does not fit, a<code>warning</code> is signalled.
2737,logical, indicating if trailing zeros,i.e., <code>"0"</code> <em>after</em> the decimal mark, should be removed;also drops <code>"e+00"</code> in exponential formats.  This is simply passedto <code>prettyNum()</code>, see the ‘Details’.
2738,optional logical, to be used when <code>x</code> is<code>"character"</code> to indicate if it stems from<code>complex</code> vector or not.  By default (<code>NA</code>),<code>x</code> is checked to ‘look like’ complex.
2739,arguments passed to <code>format</code>.
2740,numeric vector of the same length as <code>x</code>, typically thenumbers of which the character vector <code>x</code> is the pre-format.
2741,a vector giving the items to be described, or a list oflength 2 or a matrix with 2 columns giving both items anddescriptions.
2742,a vector of the same length as <code>x</code> with thecorresponding descriptions.  Only used if <code>x</code> does not alreadygive the descriptions.
2743,a character string specifying the rendering style of thedescription information.  Can be abbreviated.If <code>"table"</code>, a two-column table withitems and descriptions as columns is produced (similar to Texinfo's<code style="white-space: pre;">@table</code> environment).  If <code>"list"</code>, a LaTeX-style taggeddescription list is obtained.
2744,a positive integer giving the target column for wrappinglines in the output.
2745,a positive integer specifying the indentation of thesecond column in table style, and the indentation of continuationlines in list style.  Must not be greater than <code>width/2</code>, anddefaults to <code>width/3</code> for table style and <code>width/9</code> forlist style.
2746,an upper (or lower) triangular matrix giving thecoefficients for the system to be solved.  Values below (above)the diagonal are ignored.
2747,a matrix whose columns give the right-hand sides forthe equations.
2748,The number of columns of <code>r</code> and rows of <code>x</code> to use.
2749,logical; if <code>TRUE</code> (default), the <em>upper</em><em>tri</em>angular part of <code>r</code> is used.  Otherwise, the lower one.
2750,logical; if <code>TRUE</code>, solve <i>r' * y = x</i> for<i>y</i>, i.e., <code>t(r) %*% y == x</code>.
2751,Empty or one or more name or name=expression terms.
2752,An expression.
2753,An expression.
2754,non-negative numeric vectors.
2755,numeric vectors.
2756,integer vectors.
2757,logical; if <code>TRUE</code>, the garbage collection printsstatistics about cons cells and the space allocated for vectors.
2758,logical; if <code>TRUE</code> the values for maximum space usedare reset to the current values.
2759,logical; if <code>TRUE</code> a full collection is performed;otherwise only more recently allocated objects may be collected.
2760,logical; if <code>TRUE</code>, GC timing is enabled.
2761,logical; if <code>TRUE</code>, the garbage collection printsstatistics about cons cells and the space allocated for vectors.
2762,logical; if <code>TRUE</code> the values for maximum space usedare reset to the current values.
2763,logical; if <code>TRUE</code> a full collection is performed;otherwise only more recently allocated objects may be collected.
2764,logical; turning it on/off.
2765,integer; run GC every <code>step</code> allocations; <code>step      = 0</code> turns the GC torture off.
2766,integer; number of allocations to wait before startingGC torture.
2767,logical; do not release free objects forre-use: use with caution.
2768,logical; turning it on/off.
2769,integer; run GC every <code>step</code> allocations; <code>step      = 0</code> turns the GC torture off.
2770,integer; number of allocations to wait before startingGC torture.
2771,logical; do not release free objects forre-use: use with caution.
2772,For <code>get</code>, an object name (given as a characterstring or a symbol).<br>For <code>mget</code>, a character vector of object names.
2773,where to look for the object (see ‘Details’); ifomitted search as if the name of the object appeared unquoted in anexpression.
2774,the mode or type of object sought: see the‘Details’ section.
2775,should the enclosing frames of the environment besearched?
2776,For <code>mget</code>, a <code>list</code> of values tobe used if the item is not found: it will be coerced to a list ifnecessary.<br>For <code>dynGet</code> any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, e.g., a call to<code>stop()</code>.
2777,integer specifying the minimal frame number to lookinto.
2778,a variable name (given as a character string or a symbol).
2779,where to look for the object (see the details section); ifomitted, the function will search as if the name of the objectappeared unquoted in an expression.
2780,an alternative way to specify an environment to look in,but it is usually simpler to just use the <code>where</code> argument.
2781,a frame in the calling list.  Equivalent to giving<code>where</code> as <code>sys.frame(frame)</code>.
2782,the mode or type of object sought: see the‘Details’ section.
2783,should the enclosing frames of the environment besearched?
2784,the return value of <code>get0(x, *)</code> when<code>x</code> does not exist.
2785,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
2786,integer: a row number of the table given by<code>showConnections</code>.
2787,a connection.
2788,the function whose namespace and DLL are to be found.By default, this is the current function being called whichis the one in which the native routine is being invoked.
2789,a logical value indicating whether failure to finda namespace and/or DLL is an error (<code>TRUE</code>) or not(<code>FALSE</code>).  The default is <code>FALSE</code> so that when this iscalled because there is no <code>PACKAGE</code> argument in a<code>.C</code>, <code>.Call</code>, <code>.Fortran</code>,<code>.External</code> call,  no error occurs and the regular lookupis performed by searching all DLLs in order.
2790,an environment.
2791,the function whose namespace and DLL are to be found.By default, this is the current function being called whichis the one in which the native routine is being invoked.
2792,a logical value indicating whether failure to finda namespace and/or DLL is an error (<code>TRUE</code>) or not(<code>FALSE</code>).  The default is <code>FALSE</code> so that when this iscalled because there is no <code>PACKAGE</code> argument in a<code>.C</code>, <code>.Call</code>, <code>.Fortran</code>,<code>.External</code> call,  no error occurs and the regular lookupis performed by searching all DLLs in order.
2793,an environment.
2794,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
2795,integer: a row number of the table given by<code>showConnections</code>.
2796,a connection.
2797,a character string or <code>DLLInfo</code> object.The character string specifies the file name of the DLLof interest, and is given without the file name extension (e.g., the‘<span class="file">.dll</span>’ or ‘<span class="file">.so</span>’) and with no directory/path information.So a file ‘<span class="file">MyPackage/libs/MyPackage.so</span>’ would be specified as<span class="samp">MyPackage</span>.

2798,The <code>DLLInfo</code> objects can be obtained directlyin calls to <code>dyn.load</code> and  <code>library.dynam</code>,or can be found after the DLL has been loaded using<code>getLoadedDLLs</code>, which returns a list of<code>DLLInfo</code> objects (index-able by DLL file name).

2799,The <code>DLLInfo</code> approach avoids any ambiguities related to twoDLLs having the same name but corresponding to files in differentdirectories.
2800,a logical value.  If this is <code>TRUE</code>, the elementsof the returned lists are named using the names of the routines (asseen by R via registration or raw name).  If <code>FALSE</code>,these names are not computed and assigned to the lists.  As aresult, the call should be quicker.  The name information is alsoavailable in the <code>NativeSymbolInfo</code> objects in the lists.
2801,a character string or <code>DLLInfo</code> object.The character string specifies the file name of the DLLof interest, and is given without the file name extension (e.g., the‘<span class="file">.dll</span>’ or ‘<span class="file">.so</span>’) and with no directory/path information.So a file ‘<span class="file">MyPackage/libs/MyPackage.so</span>’ would be specified as<span class="samp">MyPackage</span>.

2802,The <code>DLLInfo</code> objects can be obtained directlyin calls to <code>dyn.load</code> and  <code>library.dynam</code>,or can be found after the DLL has been loaded using<code>getLoadedDLLs</code>, which returns a list of<code>DLLInfo</code> objects (index-able by DLL file name).

2803,The <code>DLLInfo</code> approach avoids any ambiguities related to twoDLLs having the same name but corresponding to files in differentdirectories.
2804,a logical value.  If this is <code>TRUE</code>, the elementsof the returned lists are named using the names of the routines (asseen by R via registration or raw name).  If <code>FALSE</code>,these names are not computed and assigned to the lists.  As aresult, the call should be quicker.  The name information is alsoavailable in the <code>NativeSymbolInfo</code> objects in the lists.
2805,a character string or <code>DLLInfo</code> object.The character string specifies the file name of the DLLof interest, and is given without the file name extension (e.g., the‘<span class="file">.dll</span>’ or ‘<span class="file">.so</span>’) and with no directory/path information.So a file ‘<span class="file">MyPackage/libs/MyPackage.so</span>’ would be specified as<span class="samp">MyPackage</span>.

2806,The <code>DLLInfo</code> objects can be obtained directlyin calls to <code>dyn.load</code> and  <code>library.dynam</code>,or can be found after the DLL has been loaded using<code>getLoadedDLLs</code>, which returns a list of<code>DLLInfo</code> objects (index-able by DLL file name).

2807,The <code>DLLInfo</code> approach avoids any ambiguities related to twoDLLs having the same name but corresponding to files in differentdirectories.
2808,a logical value.  If this is <code>TRUE</code>, the elementsof the returned lists are named using the names of the routines (asseen by R via registration or raw name).  If <code>FALSE</code>,these names are not computed and assigned to the lists.  As aresult, the call should be quicker.  The name information is alsoavailable in the <code>NativeSymbolInfo</code> objects in the lists.
2809,object from which to extract element(s) or in which to replace element(s).
2810,indices specifying elements to extract or replace.  Indices are<code>numeric</code> or <code>character</code> vectors or empty (missing) or<code>NULL</code>.  Numeric values are coerced to integer as by<code>as.integer</code> (and hence truncated towards zero).Character vectors will be matched to the <code>names</code> of theobject (or for matrices/arrays, the <code>dimnames</code>):see ‘Character indices’ below for further details.

2811,For <code>[</code>-indexing only: <code>i</code>, <code>j</code>, <code>...</code> can belogical vectors, indicating elements/slices to select.  Such vectorsare recycled if necessary to match the corresponding extent.<code>i</code>, <code>j</code>, <code>...</code> can also be negative integers,indicating elements/slices to leave out of the selection.

2812,When indexing arrays by <code>[</code> a single argument <code>i</code> can be amatrix with as many columns as there are dimensions of <code>x</code>; theresult is then a vector with elements corresponding to the sets ofindices in each row of <code>i</code>.

2813,An index value of <code>NULL</code> is treated as if it were <code>integer(0)</code>.
2814,A literal character string or a name (possibly backtickquoted).  For extraction, this is normally (see under‘Environments’) partially matched to the <code>names</code>of the object.
2815,For matrices and arrays.  If <code>TRUE</code> the result iscoerced to the lowest possible dimension (see the examples).  Thisonly works for extracting elements, not for the replacement.  See<code>drop</code> for further details.
2816,Controls possible partial matching of <code>[[</code> whenextracting by a character vector (for most objects, but see under‘Environments’).  The default is no partial matching.  Value<code>NA</code> allows partial matching but issues a warning when itoccurs.  Value <code>FALSE</code> allows partial matching without anywarning.
2817,typically an array-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of a similar class as<code>x</code>.
2818,zero or more objects which can be coerced to character(and which are pasted together with no separator) or a singlecondition object.
2819,logical, indicating if the call should become part of theerror message.
2820,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated.
2821,string or namespace object.
2822,string or name.
2823,character string: the hook name
2824,character string: the package/namespace name
2825,character string: an event for the package.  Can be abbreviated.
2826,A function or a list of functions, or for <code>action = "replace"</code>, <code>NULL</code> 
2827,The action to be taken.  Can be abbreviated.
2828,string or namespace object.
2829,string or name.
2830,string or namespace object.
2831,string or name.
2832,string or namespace object.
2833,string or name.
2834,string or namespace environment.
2835,logical.
2836,environment.
2837,namespace environment.
2838,character vector.
2839,optional character vector.
2840,namespace environment.
2841,string naming the package/namespace to load.
2842,vector of package names parallel to <code>generics</code>.
2843,character vector specifying library.
2844,logical.
2845,character.
2846,character.
2847,environment.
2848,a 3-column character matrix.
2849,character.
2850,any object.
2851,character arguments.
2852,the methods table name.
2853,symbol: name of namespace
2854,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
2855,string or namespace object.
2856,string or name.
2857,string or namespace object.
2858,string or name.
2859,string or namespace object.
2860,string or name.
2861,the name(s) of the native symbol(s).
2862,an optional argument that specifies to whichDLL to restrict the search for this symbol.  If this is<code>"base"</code>, we search in the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> executable itself.
2863,a logical value which controls how the result isreturned if the function is called with the name of a single symbol.If <code>unlist</code> is <code>TRUE</code> and the number of symbol names in<code>name</code> is one, then the <code>NativeSymbolInfo</code> objectis returned.  If it is <code>FALSE</code>, then a listof <code>NativeSymbolInfo</code> objects is returned.This is ignored if the number of symbols passed in <code>name</code> ismore than one.To be compatible with earlier versions of this function, thisdefaults to <code>TRUE</code>.
2864,a logical value indicating whether, if<code>TRUE</code>, to return information that was registered with <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> aboutthe symbol and its parameter types if such information is available,or if <code>FALSE</code> to return just the address of the symbol.
2865,any options can be defined, using <code>name = value</code>.However, only the ones below are used in base <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>.

2866,Options can also be passed by giving a single unnamed argument whichis a named list.
2867,a character string holding an option name.
2868,if the specified option is not set in the options list,this value is returned.  This facilitates retrieving an option andchecking whether it is set and setting it separately if not.
2869,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
2870,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
2871, The name of a file. 
2872, The character encoding to assume for the file. 
2873, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
2874, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
2875, The timestamp to use on a copy of a file. 
2876, Is this <code>srcfilecopy</code> known to come from a file system file? 
2877, A <code>srcfile</code> object. 
2878, Line numbers. 
2879, A vector of four, six or eight values giving a source location; see‘Details’.
2880, An object of the appropriate class. 
2881, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
2882, An optional second <code>srcref</code> object to mark the endof the character range. 
2883, Additional arguments to the methods; these will be ignored. 
2884,One or more character vectors.
2885,The ‘domain’ for the translation.
2886,a non-negative integer.
2887,the message to be used in English for <code>n = 1</code>.
2888,the message to be used in English for <code>n = 0, 2, 3, ...</code>.
2889,The directory in which to find translated messagecatalogs for the domain.
2890,a character vector of format strings, each of up to 8192 bytes.
2891,values to be passed into <code>fmt</code>.  Only logical,integer, real and character vectors are supported, but some coercionwill be done: see the ‘Details’ section. Up to 100.
2892,see <code>gettext</code>.
2893,A character string: tilde expansion will be done.
2894,an integer giving the number of levels.
2895,an integer giving the number of replications.
2896,an integer giving the length of the result.
2897,an optional vector of labels for the resulting factorlevels.
2898,a logical indicating whether the result should beordered or not.
2899,a condition object.
2900,call expression.
2901,a condition object.
2902,expression to be evaluated.
2903,expression to be evaluated before returning or exiting.
2904,function.
2905,character string.
2906,character string.
2907,character string naming a restart.
2908,restart object.
2909,object.
2910,character string naming a condition class.
2911,additional arguments; see details below.
2912,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
2913,an environment to associate with the function
2914,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2915,a logical, if <code>TRUE</code> the environment will use a hash table.
2916,an environment to be used as the enclosure of theenvironment created.
2917,an environment
2918,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
2919,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
2920,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
2921,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
2922,logical.  Should Perl-compatible regexps be used?
2923,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
2924,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
2925,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
2926,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
2927,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
2928,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
2929,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
2930,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
2931,logical.  Should Perl-compatible regexps be used?
2932,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
2933,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
2934,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
2935,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
2936,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
2937,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
2938,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
2939,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
2940,logical.  Should Perl-compatible regexps be used?
2941,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
2942,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
2943,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
2944,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
2945,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
2946,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
2947,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
2948,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
2949,logical.  Should Perl-compatible regexps be used?
2950,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
2951,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
2952,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
2953,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
2954,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
2955,raw vector containing a regular expression(or fixed pattern for <code>fixed = TRUE</code>) to be matched in thegiven raw vector.  Coerced by <code>charToRaw</code> to a characterstring if possible.
2956,a raw vector where matches are sought, or an object which canbe coerced by <code>charToRaw</code> to a raw vector.  Long vectorsare not supported.
2957,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
2958,An integer specifying the offset fromwhich the search should start.  Must be positive.  The beginning ofline is defined to be at that offset so <code>"^"</code> will match there.
2959,logical.  Determines the return value: see ‘Value’.
2960,logical.  If <code>TRUE</code>, <code>pattern</code> is a pattern to bematched as is.
2961,logical.  If <code>TRUE</code> all matches are returned,otherwise just the first one.
2962,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.  Ignored (with a warning) unless<code>value = TRUE</code>.
2963,a sequence of numeric, character or logicalvectors, all of the same length, or a classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
2964,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
2965,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
2966,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
2967,logical.  Should Perl-compatible regexps be used?
2968,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
2969,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
2970,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
2971,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
2972,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
2973,a connection.
2974,integer between 0 and 9, the compression level when writing.
2975,logical.  When reading, shouldnon-compressed input be allowed?
2976,logical. Should the connection be text-oriented? This isdistinct from the mode of the connection (must always be binary).If <code>TRUE</code>, <code>pushBack</code> works on the connection,otherwise <code>readBin</code> and friends apply.
2977,character string.  A description of the connection:see ‘Details’.
2978,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
2979,logical.  See the ‘Blocking’ section.
2980,The name of the encoding to be assumed.  See the‘Encoding’ section.
2981,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
2982,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
2983,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
2984,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
2985,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
2986,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
2987,a filename within a zip file.
2988,character string.  Host name for the port.
2989,integer.  The TCP port number.
2990,logical.  Should the socket be a client or a server?
2991,a server socket listening for connections.
2992,a connection.
2993,character string.  Currently ignored.
2994,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
2995,arguments passed to or from other methods.
2996,an object
2997,A character vector, or an object to be converted to a charactervector by <code>as.character</code>, or a list with <code>NULL</code> and<code>raw</code> elements as returned by <code>iconv(toRaw = TRUE)</code>.
2998,A character string describing the current encoding.
2999,A character string describing the target encoding.
3000,character string.  If not <code>NA</code> it is used to replaceany non-convertible bytes in the input.  (This would normally be asingle character, but can be more.)  If <code>"byte"</code>, the indication is<code>"&lt;xx&gt;"</code> with the hex code of the byte.  If <code>"Unicode"</code>and converting from UTF-8, the Unicode point in the form <code>"&lt;U+xxxx&gt;"</code>.
3001,logical, for expert use.  Should encodings be marked?
3002,logical.  Should a list of raw vectors be returned ratherthan a character vector?
3003,A character vector, or an object to be converted to a charactervector by <code>as.character</code>, or a list with <code>NULL</code> and<code>raw</code> elements as returned by <code>iconv(toRaw = TRUE)</code>.
3004,A character string describing the current encoding.
3005,A character string describing the target encoding.
3006,character string.  If not <code>NA</code> it is used to replaceany non-convertible bytes in the input.  (This would normally be asingle character, but can be more.)  If <code>"byte"</code>, the indication is<code>"&lt;xx&gt;"</code> with the hex code of the byte.  If <code>"Unicode"</code>and converting from UTF-8, the Unicode point in the form <code>"&lt;U+xxxx&gt;"</code>.
3007,logical, for expert use.  Should encodings be marked?
3008,logical.  Should a list of raw vectors be returned ratherthan a character vector?
3009,Named arguments, see ‘Details’.
3010,character string: can be abbreviated.  Either the actual localein use for collation or the most specific locale which would be valid.
3011,Named arguments, see ‘Details’.
3012,character string: can be abbreviated.  Either the actual localein use for collation or the most specific locale which would be valid.
3013,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects.
3014,logical indicating if (<code>double</code> and<code>complex</code> non-<code>NA</code>) numbers should becompared using <code>==</code> (‘equal’), or by bitwisecomparison.  The latter (non-default) differentiates between<code>-0</code> and <code>+0</code>.
3015,logical indicating if there is conceptually just one numeric<code>NA</code> and one <code>NaN</code>;  <code>single.NA = FALSE</code>differentiates bit patterns.
3016,logical indicating if <code>attributes</code> of<code>x</code> and <code>y</code> should be treated as <em>unordered</em> taggedpairlists (“sets”); this currently also applies to<code>slot</code>s of S4 objects.  It may well be too strict to set<code>attrib.as.set = FALSE</code>.
3017,logical indicating if byte code should beignored when comparing closures.
3018,logical indicating if their environmentsshould be ignored when comparing closures.
3019,logical indicating if their <code>"srcref"</code>attributes should be ignored when comparing closures.
3020,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3021,A length-one logical vector that is not <code>NA</code>.Conditions of length greater than one are currently accepted with awarning, but only the first element is used.  An error is signalledinstead when the environment variable <span class="env">_R_CHECK_LENGTH_1_CONDITION_</span>is set to true.  Other types are coerced to logicalif possible, ignoring any class.
3022,A syntactical name for a variable.
3023,An expression evaluating to a vector (including a list andan expression) or to a pairlist or <code>NULL</code>.  Afactor value will be coerced to a character vector.  As from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>4.0.0 this can be a long vector.
3024,An <em>expression</em> in a formal sense.  This is either asimple expression or a so-called <em>compound expression</em>, usuallyof the form <code>{ expr1 ; expr2 }</code>.
3025,an object which can be coerced to logical mode.
3026,return values for true elements of <code>test</code>.
3027,return values for false elements of <code>test</code>.
3028,numeric.  Desired length of the output vector,inputs being recycled as needed.
3029,numeric vector.
3030,numeric vector.
3031,numeric vector.
3032,numeric vector.
3033,an object, probably of mode <code>complex</code>.
3034,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
3035,further arguments passed to or from other methods.
3036,string or namespace environment.
3037,logical.
3038,environment.
3039,namespace environment.
3040,character vector.
3041,optional character vector.
3042,namespace environment.
3043,string naming the package/namespace to load.
3044,vector of package names parallel to <code>generics</code>.
3045,character vector specifying library.
3046,logical.
3047,character.
3048,character.
3049,environment.
3050,a 3-column character matrix.
3051,character.
3052,any object.
3053,character arguments.
3054,the methods table name.
3055,symbol: name of namespace
3056,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
3057,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to serialize.
3058,a connection or the name of the file where the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectis saved to or read from.
3059,a logical.  If <code>TRUE</code> or <code>NA</code>, an ASCIIrepresentation is written; otherwise (default), a binary one is used.See the comments in the help for <code>save</code>.
3060,the workspace format version to use.  <code>NULL</code>specifies the current default version (3). The only other supportedvalue is 2, the default from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.4.0 to <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.5.0.
3061,a logical specifying whether saving to a named file isto use <code>"gzip"</code> compression, or one of <code>"gzip"</code>,<code>"bzip2"</code> or <code>"xz"</code> to indicate the type of compression tobe used.  Ignored if <code>file</code> is a connection.
3062,a hook function for handling reference objects.
3063,a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
3064,a character vector naming classes.  <code>value</code>can also be <code>NULL</code>.
3065,logical affecting return value: see ‘Details’.
3066,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3067,object to be coerced or tested.
3068,further arguments passed to or from other methods.
3069,the factors for which interaction is to be computed, or asingle list giving those factors.
3070,if <code>drop</code> is <code>TRUE</code>, unused factor levelsare dropped from the result.  The default is to retain allfactor levels.
3071,string to construct the new level labels by joining theconstituent ones.
3072,logical indicating if the order of factor concatenationshould be lexically ordered.
3073,vectors (of the same mode) containing a sequenceof items (conceptually) with no duplicated values.
3074,object to be converted or shifted.
3075,logical: should the conversion be to a singlecharacter string or multiple individual characters?
3076,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
3077,the result type, partially matched.
3078,object to be converted.
3079,logical: should the conversion be to a singlecharacter string or multiple individual characters?
3080,logical: should interpretation ofsurrogate pairs be attempted?  (See ‘Details’.)Only supported for <code>multiple = FALSE</code>.
3081,a vector (atomic, not a list) for <code>rle()</code>;an object of class <code>"rle"</code> for <code>inverse.rle()</code>.
3082,further arguments; ignored here.
3083,number of significant digits for printing, see<code>print.default</code>.
3084,character string, prepended to each printed line.
3085,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3086,a condition object.
3087,call expression.
3088,a condition object.
3089,expression to be evaluated.
3090,expression to be evaluated before returning or exiting.
3091,function.
3092,character string.
3093,character string.
3094,character string naming a restart.
3095,restart object.
3096,object.
3097,character string naming a condition class.
3098,additional arguments; see details below.
3099,a condition object.
3100,call expression.
3101,a condition object.
3102,expression to be evaluated.
3103,expression to be evaluated before returning or exiting.
3104,function.
3105,character string.
3106,character string.
3107,character string naming a restart.
3108,restart object.
3109,object.
3110,character string naming a condition class.
3111,additional arguments; see details below.
3112,a vector (including a list or <code>expression</code>vector) giving data to fill the array.  Non-atomic classed objectsare coerced by <code>as.vector</code>.
3113,the dim attribute for the array to be created, that is aninteger vector of length one or more giving the maximal indices ineach dimension.
3114,either <code>NULL</code> or the names for the dimensions.This must a list (or it will be ignored) with one component for eachdimension, either <code>NULL</code> or a character vector of the lengthgiven by <code>dim</code> for that dimension.  The list can be named, andthe list names will be used as names for the dimensions.  If thelist is shorter than the number of dimensions, it is extended by<code>NULL</code>s to the length required.
3115,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3116,additional arguments to be passed to or from methods.
3117,object to be tested.
3118,a non-empty character string naming the function to be called.
3119,arguments to be part of the call.
3120,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3121,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3122,object to be coerced or tested.
3123,further arguments passed to or from other methods.
3124,numeric.  Desired length of the output vector,inputs being recycled as needed.
3125,numeric vector.
3126,numeric vector.
3127,numeric vector.
3128,numeric vector.
3129,an object, probably of mode <code>complex</code>.
3130,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
3131,further arguments passed to or from other methods.
3132,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3133,<code>NULL</code> or a character vector giving the rownames for the data frame.  Missing values are not allowed.
3134,logical. If <code>TRUE</code>, setting row names andconverting column names (to syntactic names: see<code>make.names</code>) is optional.  Note that all of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s<span class="pkg">base</span> package <code>as.data.frame()</code> methods use<code>optional</code> only for column names treatment, basically with themeaning of <code>data.frame(*, check.names = !optional)</code>.See also the <code>make.names</code> argument of the <code>matrix</code> method.
3135,additional arguments to be passed to or from methods.
3136,logical: should the character vector be convertedto a factor?
3137,logical or integer; indicating if column names withmore than 256 (or <code>cut.names</code> if that is numeric) charactersshould be shortened (and the last 6 characters replaced by <code>" ..."</code>).
3138,(optional) character vector of column names.
3139,logical indicating if empty column names, i.e.,<code>""</code> should be fixed up (in <code>data.frame</code>) or not.
3140,logical; passed to the <code>data.frame()</code> call.
3141,a <code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the row names (of the matrix<code>x</code>) are invalid.  If they are invalid, the default,<code>TRUE</code>, calls <code>make.names(*, unique=TRUE)</code>;<code>make.names=NA</code> will use “automatic” row names and a<code>FALSE</code> value will signal an error for invalid row names.
3142,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3143,object to be coerced or tested.
3144,further arguments passed to or from other methods.
3145,vectors (of the same mode) containing a sequenceof items (conceptually) with no duplicated values.
3146,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
3147,an environment to associate with the function
3148,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3149,a logical, if <code>TRUE</code> the environment will use a hash table.
3150,an environment to be used as the enclosure of theenvironment created.
3151,an environment
3152,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
3153,<code>expression</code>: <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects, typically calls, symbolsor constants.<br><code>as.expression</code>: arguments to be passed to methods.
3154,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3155,a vector of data, usually taking a small number of distinctvalues.
3156,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
3157,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
3158,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
3159,logical flag to determine if the levels should be regardedas ordered (in the order given).
3160,an upper bound on the number of levels; see ‘Details’.
3161,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
3162,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
3163,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default methods handle atomicvectors.
3164,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3165,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default methods handle atomicvectors.
3166,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3167,object to be coerced or tested.
3168,further arguments passed to or from other methods.
3169,object to be tested.
3170,objects, possibly named.
3171,object to be coerced or tested.
3172,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
3173,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
3174,a character string giving the pathname to a DLL, also knownas a dynamic shared object.  (See ‘Details’ for what theseterms mean.)
3175,a logical value controlling whether the symbols in theDLL are stored in their own local table and not sharedacross DLLs, or added to the global symbol table.  Whether this hasany effect is system-dependent.
3176,a logical controlling whether all symbols are resolved (andrelocated) immediately the library is loaded or deferred until theyare used.  This control is useful for developers testing whether alibrary is complete and has all the necessary symbols, and for usersto ignore missing symbols.  Whether this has any effect is system-dependent.
3177,other arguments for future expansion.
3178,a character string giving a symbol name.
3179,if supplied, confine the search for the <code>name</code> tothe DLL given by this argument (plus the conventional extension,‘<span class="file">.so</span>’, ‘<span class="file">.sl</span>’, ‘<span class="file">.dll</span>’, ...).  This is intended toadd safety for packages, which can ensure by using this argumentthat no other package can override their external symbols.  This isused in the same way as in <code>.C</code>, <code>.Call</code>,<code>.Fortran</code> and <code>.External</code> functions.
3180,The type of symbol to look for: can be any (<code>""</code>, thedefault), <code>"Fortran"</code>, <code>"Call"</code> or <code>"External"</code>.
3181,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3182,object to be coerced or tested.
3183,further arguments passed to or from other methods.
3184,an optional data vector (including a list or<code>expression</code> vector).  Non-atomic classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects arecoerced by <code>as.vector</code> and all attributes discarded.
3185,the desired number of rows.
3186,the desired number of columns.
3187,logical. If <code>FALSE</code> (the default) the matrix isfilled by columns, otherwise the matrix is filled by rows.
3188,A <code>dimnames</code> attribute for the matrix:<code>NULL</code> or a <code>list</code> of length 2 giving the row and columnnames respectively.  An empty list is treated as <code>NULL</code>, and alist of length one as row names.  The list can be named, and thelist names will be used as names for the dimensions.
3189,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3190,additional arguments to be passed to or from methods.
3191,logical indicating if the resulting matrixshould have character (rather than <code>NULL</code>)<code>rownames</code>.  The default, <code>NA</code>, uses <code>NULL</code>rownames if the data frame has ‘automatic’ row.names or for azero-row data frame.
3192,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default method for<code>is.na</code> and <code>anyNA</code> handle atomic vectors, lists,pairlists, and <code>NULL</code>.
3193,logical: should <code>anyNA</code> be applied recursivelyto lists and pairlists?
3194,a suitable index vector for use with <code>x</code>.
3195,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default method for<code>is.na</code> and <code>anyNA</code> handle atomic vectors, lists,pairlists, and <code>NULL</code>.
3196,logical: should <code>anyNA</code> be applied recursivelyto lists and pairlists?
3197,a suitable index vector for use with <code>x</code>.
3198,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
3199,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
3200,an object to be printed or summarized from one of thedate-time classes.
3201,for timezone formatting, passed to <code>format.POSIXct</code>.
3202,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3203,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3204,further arguments to be passed from or to other methods.
3205,date-time objects
3206,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3207,a numeric vector (in seconds)
3208,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3209,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default method for<code>is.na</code> and <code>anyNA</code> handle atomic vectors, lists,pairlists, and <code>NULL</code>.
3210,logical: should <code>anyNA</code> be applied recursivelyto lists and pairlists?
3211,a suitable index vector for use with <code>x</code>.
3212,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default method for<code>is.na</code> and <code>anyNA</code> handle atomic vectors, lists,pairlists, and <code>NULL</code>.
3213,logical: should <code>anyNA</code> be applied recursivelyto lists and pairlists?
3214,a suitable index vector for use with <code>x</code>.
3215,a vector of data, usually taking a small number of distinctvalues.
3216,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
3217,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
3218,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
3219,logical flag to determine if the levels should be regardedas ordered (in the order given).
3220,an upper bound on the number of levels; see ‘Details’.
3221,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
3222,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
3223,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
3224,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
3225,object to be coerced or tested.
3226,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be tested: the default methods handle atomicvectors.
3227,an object to be tested or coerced.
3228,ignored.
3229,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3230,object to be coerced or tested.
3231,further arguments passed to or from other methods.
3232,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
3233,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
3234,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3235,object to be coerced or tested.
3236,further arguments passed to or from other methods.
3237,date-time or date objects.
3238,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
3239,character string.  Units in which the results aredesired.  Can be abbreviated.
3240,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
3241,character string or numeric value specifying a time interval.
3242,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
3243,an object inheriting from class <code>"difftime"</code>.
3244,arguments to be passed to or from other methods.
3245,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3246,object to be coerced or tested.
3247,further arguments passed to or from other methods.
3248,object to be tested.
3249,a vector of data, usually taking a small number of distinctvalues.
3250,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
3251,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
3252,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
3253,logical flag to determine if the levels should be regardedas ordered (in the order given).
3254,an upper bound on the number of levels; see ‘Details’.
3255,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
3256,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
3257,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
3258,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
3259,objects, possibly named.
3260,object to be coerced or tested.
3261,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
3262,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
3263,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3264,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
3265,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
3266,a QR decomposition of the type computed by <code>qr</code>.
3267,a vector or matrix of right-hand sides of equations.
3268,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
3269,effective rank.
3270,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
3271,further arguments passed to or from other methods
3272,desired length.
3273,object to be coerced.
3274,object to be tested.
3275,object to be tested.
3276,object to be coerced or tested.
3277,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
3278,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
3279,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
3280,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
3281,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
3282,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
3283,a character vector giving the row names for the dataframe.
3284,The name to be used for the column of tableentries, usually counts.
3285,logical: should the classifying factors bereturned as factors (the default) or character vectors?
3286,passed to <code>provideDimnames</code>.
3287,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with a class or a numeric, complex, character,logical or raw vector.
3288,logical.  Should missing values be removed before checking?
3289,logical indicating if the check should be for<em>strictly</em> increasing values.
3290,character string naming an atomic mode or<code>"list"</code> or <code>"expression"</code> or (except for <code>vector</code>)<code>"any"</code>.  Currently, <code>is.vector()</code> allows any type (see<code>typeof</code>) for <code>mode</code>, and when mode is not<code>"any"</code>, <code>is.vector(x, mode)</code> is almost the same as<code>typeof(x) == mode</code>.
3291,a non-negative integer specifying the desired length.  Fora long vector, i.e., <code>length &gt; .Machine$integer.max</code>, ithas to be of type <code>"double"</code>.  Supplying an argument of lengthother than one is an error.
3292,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3293,a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
3294,a character vector naming classes.  <code>value</code>can also be <code>NULL</code>.
3295,logical affecting return value: see ‘Details’.
3296,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
3297,integer: a row number of the table given by<code>showConnections</code>.
3298,a connection.
3299,string or namespace environment.
3300,logical.
3301,environment.
3302,namespace environment.
3303,character vector.
3304,optional character vector.
3305,namespace environment.
3306,string naming the package/namespace to load.
3307,vector of package names parallel to <code>generics</code>.
3308,character vector specifying library.
3309,logical.
3310,character.
3311,character.
3312,environment.
3313,a 3-column character matrix.
3314,character.
3315,any object.
3316,character arguments.
3317,the methods table name.
3318,symbol: name of namespace
3319,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
3320,any interpreted <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> function.
3321,a text string that can be retrieved when the browser is entered.
3322,a condition that can be retrieved when the browser isentered.
3323,an optional method signature. If specified, themethod is debugged, rather than its generic.
3324,logical; a call to the support function<code>debuggingState</code> returns <code>TRUE</code> if debugging is globallyturned on, <code>FALSE</code> otherwise.  An argument of one or the otherof those values sets the state.  If the debugging state is<code>FALSE</code>, none of the debugging actions will occur (but explicit<code>browser</code> calls in functions will continue to work).
3325,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
3326,character string.  A description of the connection:see ‘Details’.
3327,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
3328,logical.  See the ‘Blocking’ section.
3329,The name of the encoding to be assumed.  See the‘Encoding’ section.
3330,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
3331,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
3332,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
3333,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
3334,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
3335,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
3336,a filename within a zip file.
3337,character string.  Host name for the port.
3338,integer.  The TCP port number.
3339,logical.  Should the socket be a client or a server?
3340,a server socket listening for connections.
3341,a connection.
3342,character string.  Currently ignored.
3343,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
3344,arguments passed to or from other methods.
3345,string or namespace environment.
3346,logical.
3347,environment.
3348,namespace environment.
3349,character vector.
3350,optional character vector.
3351,namespace environment.
3352,string naming the package/namespace to load.
3353,vector of package names parallel to <code>generics</code>.
3354,character vector specifying library.
3355,logical.
3356,character.
3357,character.
3358,environment.
3359,a 3-column character matrix.
3360,character.
3361,any object.
3362,character arguments.
3363,the methods table name.
3364,symbol: name of namespace
3365,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
3366,string or name space object.
3367,integer specifying position to attach.
3368,<code>NULL</code> or a character vector of dependencies to berecorded in object <code>.Depends</code> in the package.
3369,string naming the package/name space to load.
3370,character vector specifying library search path.
3371,Now ignored except during package installation.
3372,Ignored except during package installation.
3373,logical; if true, stop just after loading code.
3374,<code>NULL</code> or a version specification (a listwith components <code>op</code> and <code>version</code>).
3375,logical: should progress and error messages be suppressed?
3376,string or ‘name’, see <code>as.symbol</code>,of a package, e.g., <code>"stats"</code>.
3377,character vectors; see <code>library</code>.
3378,further arguments to be passed to <code>loadNamespace</code>.
3379,numerical values to specify a day.
3380,numerical values for a time within a day.Fractional seconds are allowed.
3381,A time zone specification to be used for the conversion.<code>""</code> is the current time zone and <code>"GMT"</code> is UTC.  Invalidvalues are most commonly treated as UTC, on some platforms with a warning.
3382,numerical values to specify a day.
3383,numerical values for a time within a day.Fractional seconds are allowed.
3384,A time zone specification to be used for the conversion.<code>""</code> is the current time zone and <code>"GMT"</code> is UTC.  Invalidvalues are most commonly treated as UTC, on some platforms with a warning.
3385,character string.  A description of the connection:see ‘Details’.
3386,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
3387,logical.  See the ‘Blocking’ section.
3388,The name of the encoding to be assumed.  See the‘Encoding’ section.
3389,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
3390,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
3391,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
3392,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
3393,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
3394,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
3395,a filename within a zip file.
3396,character string.  Host name for the port.
3397,integer.  The TCP port number.
3398,logical.  Should the socket be a client or a server?
3399,a server socket listening for connections.
3400,a connection.
3401,character string.  Currently ignored.
3402,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
3403,arguments passed to or from other methods.
3404,a condition object.
3405,call expression.
3406,a condition object.
3407,expression to be evaluated.
3408,expression to be evaluated before returning or exiting.
3409,function.
3410,character string.
3411,character string.
3412,character string naming a restart.
3413,restart object.
3414,object.
3415,character string naming a condition class.
3416,additional arguments; see details below.
3417,Any R object.
3418,Optional, logical: indicate direction of conversion.
3419,Optional, logical: whether conversion to S3 iscompleted.  Not usually needed, but see the details section.
3420,a connection.
3421,numeric.  A file position (relative to the originspecified by <code>origin</code>), or <code>NA</code>.
3422,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
3423,character string.  One of <code>"start"</code>, <code>"current"</code>,<code>"end"</code>: see ‘Details’.
3424,further arguments passed to or from other methods.
3425,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object; a <code>matrix</code> for the matrix method.
3426,numeric scalar &gt;= 0.  Smaller differences are notconsidered, see <code>all.equal.numeric</code>.
3427,numeric scalar &gt;= 0.  <code>isSymmetric.matrix()</code>‘pre-tests’ the first and last few rows for fast detection of‘obviously’ asymmetric cases with this tolerance.  Setting itto length zero will skip the pre-tests.
3428,further arguments passed to methods; the matrix methodpasses these to <code>all.equal</code>.  If the row and columnnames of <code>object</code> are allowed to differ for the symmetry checkdo use <code>check.attributes = FALSE</code>!
3429,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object; a <code>matrix</code> for the matrix method.
3430,numeric scalar &gt;= 0.  Smaller differences are notconsidered, see <code>all.equal.numeric</code>.
3431,numeric scalar &gt;= 0.  <code>isSymmetric.matrix()</code>‘pre-tests’ the first and last few rows for fast detection of‘obviously’ asymmetric cases with this tolerance.  Setting itto length zero will skip the pre-tests.
3432,further arguments passed to methods; the matrix methodpasses these to <code>all.equal</code>.  If the row and columnnames of <code>object</code> are allowed to differ for the symmetry checkdo use <code>check.attributes = FALSE</code>!
3433,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
3434,numeric vector to which <em>jitter</em> should be added.
3435,numeric.
3436,numeric; if positive, used as <em>amount</em> (see below),otherwise, if <code>= 0</code> the default is <code>factor * z/50</code>.

3437,Default (<code>NULL</code>): <code>factor * d/5</code> where <code>d</code> is aboutthe smallest difference between <code>x</code> values.
3438,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
3439,logical vector (possibly recycled).  Should the names beabbreviated?
3440,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
3441,arguments for other methods.
3442,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
3443,logical vector (possibly recycled).  Should the names beabbreviated?
3444,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
3445,arguments for other methods.
3446,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
3447,logical vector (possibly recycled).  Should the names beabbreviated?
3448,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
3449,arguments for other methods.
3450,A matrix or a the result of <code>qr</code> or a fit froma class inheriting from <code>"lm"</code>.
3451,logical.  Should the result be exact?
3452,character string, specifying the matrix norm with respectto which the condition number is to be computed, see also<code>norm</code>.  For <code>rcond</code>, the default is <code>"O"</code>,meaning the <b>O</b>ne- or 1-norm.  The (currently only) otherpossible value is <code>"I"</code> for the infinity norm.
3453,a partially matched character string specifying the method to be used;<code>"qr"</code> is the default for back-compatibility, mainly.
3454,logical.  If true, the matrix used is just the lowertriangular part of <code>z</code>.
3455,logical.  If true and <code>z</code> is not complex, theLINPACK routine <code>dtrco()</code> is called; otherwise the relevantLAPACK routine is.
3456,further arguments passed to or from other methods;for <code>kappa.*()</code>, notably <code>LINPACK</code> when <code>norm</code> is not<code>"2"</code>.
3457,A matrix or a the result of <code>qr</code> or a fit froma class inheriting from <code>"lm"</code>.
3458,logical.  Should the result be exact?
3459,character string, specifying the matrix norm with respectto which the condition number is to be computed, see also<code>norm</code>.  For <code>rcond</code>, the default is <code>"O"</code>,meaning the <b>O</b>ne- or 1-norm.  The (currently only) otherpossible value is <code>"I"</code> for the infinity norm.
3460,a partially matched character string specifying the method to be used;<code>"qr"</code> is the default for back-compatibility, mainly.
3461,logical.  If true, the matrix used is just the lowertriangular part of <code>z</code>.
3462,logical.  If true and <code>z</code> is not complex, theLINPACK routine <code>dtrco()</code> is called; otherwise the relevantLAPACK routine is.
3463,further arguments passed to or from other methods;for <code>kappa.*()</code>, notably <code>LINPACK</code> when <code>norm</code> is not<code>"2"</code>.
3464,A matrix or a the result of <code>qr</code> or a fit froma class inheriting from <code>"lm"</code>.
3465,logical.  Should the result be exact?
3466,character string, specifying the matrix norm with respectto which the condition number is to be computed, see also<code>norm</code>.  For <code>rcond</code>, the default is <code>"O"</code>,meaning the <b>O</b>ne- or 1-norm.  The (currently only) otherpossible value is <code>"I"</code> for the infinity norm.
3467,a partially matched character string specifying the method to be used;<code>"qr"</code> is the default for back-compatibility, mainly.
3468,logical.  If true, the matrix used is just the lowertriangular part of <code>z</code>.
3469,logical.  If true and <code>z</code> is not complex, theLINPACK routine <code>dtrco()</code> is called; otherwise the relevantLAPACK routine is.
3470,further arguments passed to or from other methods;for <code>kappa.*()</code>, notably <code>LINPACK</code> when <code>norm</code> is not<code>"2"</code>.
3471,A matrix or a the result of <code>qr</code> or a fit froma class inheriting from <code>"lm"</code>.
3472,logical.  Should the result be exact?
3473,character string, specifying the matrix norm with respectto which the condition number is to be computed, see also<code>norm</code>.  For <code>rcond</code>, the default is <code>"O"</code>,meaning the <b>O</b>ne- or 1-norm.  The (currently only) otherpossible value is <code>"I"</code> for the infinity norm.
3474,a partially matched character string specifying the method to be used;<code>"qr"</code> is the default for back-compatibility, mainly.
3475,logical.  If true, the matrix used is just the lowertriangular part of <code>z</code>.
3476,logical.  If true and <code>z</code> is not complex, theLINPACK routine <code>dtrco()</code> is called; otherwise the relevantLAPACK routine is.
3477,further arguments passed to or from other methods;for <code>kappa.*()</code>, notably <code>LINPACK</code> when <code>norm</code> is not<code>"2"</code>.
3478,A vector or array.
3479,A vector or array.
3480,a function; it may be a quoted string.
3481,Provide dimnames that are the product of thedimnames of <code>X</code> and <code>Y</code>.
3482,optional arguments to be passed to <code>FUN</code>.
3483,a numeric or complex matrix whose SVD decompositionis to be computed.  Logical matrices are coerced to numeric.
3484,the number of left  singular vectors to be computed.This must between <code>0</code> and <code>n = nrow(x)</code>.
3485,the number of right singular vectors to be computed.This must be between <code>0</code> and <code>p = ncol(x)</code>.
3486,logical.  Defunct and an error.
3487,Any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object: the function is generic.
3488,further arguments passed to or from other methods.
3489,Any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object: the function is generic.
3490,further arguments passed to or from other methods.
3491,a vector (atomic or list) or an <code>expression</code>object.  Other objects (including classed objects) will be coercedby <code>base::as.list</code>.
3492,the function to be applied to each element of <code>X</code>:see ‘Details’.  In the case of functions like<code>+</code>, <code>%*%</code>, the function name must be backquoted or quoted.
3493,optional arguments to <code>FUN</code>.
3494,logical or character string; should the result besimplified to a vector, matrix or higher dimensional array ifpossible?  For <code>sapply</code> it must be named and not abbreviated.The default value, <code>TRUE</code>, returns a vector or matrix if appropriate,whereas if <code>simplify = "array"</code> the result may be an<code>array</code> of “rank”(<i>=</i><code>length(dim(.))</code>) one higher than the resultof <code>FUN(X[[i]])</code>.
3495,logical; if <code>TRUE</code> and if <code>X</code> is character,use <code>X</code> as <code>names</code> for the result unless it had namesalready.  Since this argument follows <code>...</code> its name cannotbe abbreviated.
3496,a (generalized) vector; a template for the returnvalue from FUN.  See ‘Details’.
3497,integer: the number of replications.
3498,the expression (a language object, usually a call)to evaluate repeatedly.
3499,a list, typically returned from <code>lapply()</code>.
3500,logical; if true, <code>simplify2array()</code> will produce a(“higher rank”) array when appropriate, whereas<code>higher = FALSE</code> would return a matrix (or vector) only.These two cases correspond to <code>sapply(*, simplify = "array")</code> or<code>simplify = TRUE</code>, respectively.
3501,The file path to the database, with no extension.
3502,The environment into which objects are loaded.
3503,An optional function which when called on a a charactervector of object names returns a logical vector: only objects forwhich this is true will be loaded.
3504,Function of one argument, an environment.
3505,The file path to the database, with no extension.
3506,The environment into which objects are loaded.
3507,An optional function which when called on a a charactervector of object names returns a logical vector: only objects forwhich this is true will be loaded.
3508,Function of one argument, an environment.
3509,an object

3510,from which to extract elements.
3511,with a <code>"row.names"</code> attribute, typically adata frame.
3512,typically S3- or S4-classed.
3513,The full path to the‘<span class="file">DESCRIPTION</span>’ file of a package.
3514,the filepath to a lazyload database.
3515,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.
3516,logical.  Should this not give warnings or an errorif the package(s) are not found?
3517,logical.  Should  the <code>Imports</code> field be processed?
3518,logical: should message(s) be printed.
3519,A processed ‘<span class="file">DESCRIPTION</span>’ file of class<code>"packageDescription2"</code>.
3520,platform strings of the form <code>"cpu-vendor-os"</code>.
3521,name of image file to save or load.
3522,integer.  Currently <code>type = 0</code> returns the internal<code>"row.names"</code> attribute (possibly <code>NULL</code>), <code>type = 2</code>the number of rows implied by the attribute, and <code>type = 1</code> thelatter with a negative sign for ‘automatic’ row names.
3523,integer.  The number of rows.
3524,character string such as <code>"package:MASS"</code>.
3525,the object name in a lazyload database.
3526,logical: are the values in the database compressed?
3527,a load hook: see <code>unserialize</code>.
3528,a length-one integer vector.
3529,indices.
3530,logical.
3531,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3532,a classless object to be placed into the class.
3533,an optional time zone.
3534,an optional character vector of classes <em>containing</em>the default classes.
3535,see <code>difftime</code>.
3536,Arguments passed to non-dot name versions.
3537,an integer.
3538,character vector of <em>same</em> objects, masking others.
3539,character string naming the package which is masked from or by.
3540,logical indicating if the masking happens <em>by</em><code>pkg</code>, or (<code>by = FALSE</code>) from <code>pkg</code>.
3541,non-negative numeric vectors.
3542,numeric vectors.
3543,integer vectors.
3544,non-negative numeric vectors.
3545,numeric vectors.
3546,integer vectors.
3547,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.  For replacement, a vector or factor.
3548,a non-negative integer or double (which will be rounded down).
3549,an object to be printed or summarized from one of thedate-time classes.
3550,for timezone formatting, passed to <code>format.POSIXct</code>.
3551,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3552,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3553,further arguments to be passed from or to other methods.
3554,date-time objects
3555,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3556,a numeric vector (in seconds)
3557,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3558,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.  For replacement, a vector or factor.
3559,a non-negative integer or double (which will be rounded down).
3560,a <code>Date</code> object to be summarized or printed.
3561,number of significant digits for the computations.
3562,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3563,further arguments to be passed from or to other methods.
3564,date-time or date objects.
3565,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
3566,character string.  Units in which the results aredesired.  Can be abbreviated.
3567,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
3568,character string or numeric value specifying a time interval.
3569,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
3570,an object inheriting from class <code>"difftime"</code>.
3571,arguments to be passed to or from other methods.
3572,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.  For replacement, a vector or factor.
3573,a non-negative integer or double (which will be rounded down).
3574,an object to be printed or summarized from one of thedate-time classes.
3575,for timezone formatting, passed to <code>format.POSIXct</code>.
3576,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3577,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3578,further arguments to be passed from or to other methods.
3579,date-time objects
3580,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3581,a numeric vector (in seconds)
3582,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3583,an object to be printed or summarized from one of thedate-time classes.
3584,for timezone formatting, passed to <code>format.POSIXct</code>.
3585,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3586,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3587,further arguments to be passed from or to other methods.
3588,date-time objects
3589,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3590,a numeric vector (in seconds)
3591,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3592,a <code>list</code>, list-like such as an<code>expression</code> or an atomic vector (for which the resultis trivial).
3593,logical indicating if the result should inherit the<code>names</code> from <code>x</code>.
3594,an object, for example a factor.
3595,A valid value for <code>levels(x)</code>.For the default method, <code>NULL</code> or a character vector.  For the<code>factor</code> method, a vector of character strings with length atleast the number of levels of <code>x</code>, or a named list specifying how torename the levels.
3596,an object, for example a factor.
3597,A valid value for <code>levels(x)</code>.For the default method, <code>NULL</code> or a character vector.  For the<code>factor</code> method, a vector of character strings with length atleast the number of levels of <code>x</code>, or a named list specifying how torename the levels.
3598,an object, for example a factor.
3599,A valid value for <code>levels(x)</code>.For the default method, <code>NULL</code> or a character vector.  For the<code>factor</code> method, a vector of character strings with length atleast the number of levels of <code>x</code>, or a named list specifying how torename the levels.
3600,an object, for example a factor.
3601,A valid value for <code>levels(x)</code>.For the default method, <code>NULL</code> or a character vector.  For the<code>factor</code> method, a vector of character strings with length atleast the number of levels of <code>x</code>, or a named list specifying how torename the levels.
3602,non-negative numeric vectors.
3603,numeric vectors.
3604,integer vectors.
3605,non-negative numeric vectors.
3606,numeric vectors.
3607,integer vectors.
3608,the name of a package, given as a name orliteral character string, or a character string, depending onwhether <code>character.only</code> is <code>FALSE</code> (default) or<code>TRUE</code>.
3609,the position on the search list at which to attach theloaded namespace.  Can also be the name of a position on the currentsearch list as given by <code>search()</code>.
3610,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.Non-existent library trees are silently ignored.
3611,a logical indicating whether <code>package</code> or<code>help</code> can be assumed to be character strings.
3612,logical.  If it is <code>TRUE</code>,  <code>FALSE</code> or<code>TRUE</code> is returned to indicate success.
3613,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the newpackage.  A conflict is a function masking a function,or a non-function masking a non-function. The default is <code>TRUE</code>unless specified as <code>FALSE</code> in the <code>conflicts.policy</code> option.
3614,a logical.  If <code>TRUE</code>, additional diagnostics areprinted.
3615,a logical.  If <code>TRUE</code>, no message confirmingpackage attaching is printed, and most often, no errors/warnings areprinted if package attaching fails.
3616,character string naming a package.
3617,character vector of names of objects that can maskobjects on the search path without signaling an error when strictconflict checking is enabled
3618,character vector of names of objects toexclude or include in the attached frame. Only one of these argumentsmay be used in a call to <code>library</code> or <code>require</code>.
3619,logical specifying whether required packageslisted in the <code>Depends</code> clause of the <code>DESCRIPTION</code> fileshould be attached automatically.
3620,a character string naming a DLL (also known as a dynamicshared object or library) to load.
3621,a character vector with the name of package.
3622,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through.
3623,the path to the loaded package whose DLL is to be unloaded.
3624,a logical value indicating whether an announcementis printed on the console before loading the DLL.  Thedefault value is taken from the verbose entry in the system<code>options</code>.
3625,the extension (including <span class="samp">.</span> if used) to appendto the file name to specify the library to be loaded.  This defaultsto the appropriate value for the operating system.
3626,additional arguments needed by some libraries thatare passed to the call to <code>dyn.load</code> to controlhow the library and its dependencies are loaded.
3627,a list of <code>"DLLInfo"</code> objects corresponding to theDLLs loaded by packages.  Can be missing.
3628,a character string naming a DLL (also known as a dynamicshared object or library) to load.
3629,a character vector with the name of package.
3630,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through.
3631,the path to the loaded package whose DLL is to be unloaded.
3632,a logical value indicating whether an announcementis printed on the console before loading the DLL.  Thedefault value is taken from the verbose entry in the system<code>options</code>.
3633,the extension (including <span class="samp">.</span> if used) to appendto the file name to specify the library to be loaded.  This defaultsto the appropriate value for the operating system.
3634,additional arguments needed by some libraries thatare passed to the call to <code>dyn.load</code> to controlhow the library and its dependencies are loaded.
3635,a list of <code>"DLLInfo"</code> objects corresponding to theDLLs loaded by packages.  Can be missing.
3636,objects, possibly named.
3637,object to be coerced or tested.
3638,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
3639,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
3640,a character vector of full path names; the defaultcorresponds to the working directory, <code>getwd()</code>.  Tildeexpansion (see <code>path.expand</code>) is performed.  Missingvalues will be ignored.  Elements with a marked encoding willbe converted to the native encoding (and if that fails, considerednon-existent).
3641,an optional regular expression.  Only file nameswhich match the regular expression will be returned.
3642,a logical value.  If <code>FALSE</code>, only thenames of visible files are returned (following Unix-style visibility,that is files whose name does not start with a dot).  If <code>TRUE</code>,all file names will be returned.
3643,a logical value.  If <code>TRUE</code>, the directorypath is prepended to the file names to give a relative file path.If <code>FALSE</code>, the file names (rather than paths) are returned.
3644,logical.  Should the listing recurse into directories?
3645,logical.  Should pattern-matching be case-insensitive?
3646,logical.  Should subdirectory names be included inrecursive listings?  (They always are in non-recursive ones).
3647,logical.  Should both <code>"."</code> and <code>".."</code> be excludedalso from non-recursive listings?
3648,a character vector of full path names; the defaultcorresponds to the working directory, <code>getwd()</code>.  Tildeexpansion (see <code>path.expand</code>) is performed.  Missingvalues will be ignored.  Elements with a marked encoding willbe converted to the native encoding (and if that fails, considerednon-existent).
3649,an optional regular expression.  Only file nameswhich match the regular expression will be returned.
3650,a logical value.  If <code>FALSE</code>, only thenames of visible files are returned (following Unix-style visibility,that is files whose name does not start with a dot).  If <code>TRUE</code>,all file names will be returned.
3651,a logical value.  If <code>TRUE</code>, the directorypath is prepended to the file names to give a relative file path.If <code>FALSE</code>, the file names (rather than paths) are returned.
3652,logical.  Should the listing recurse into directories?
3653,logical.  Should pattern-matching be case-insensitive?
3654,logical.  Should subdirectory names be included inrecursive listings?  (They always are in non-recursive ones).
3655,logical.  Should both <code>"."</code> and <code>".."</code> be excludedalso from non-recursive listings?
3656,A list of variables for the data frame.
3657,An integer giving the desired number of rows for the data frame, or<code>NULL</code> (default), in which case the maximal length of theelements of the list will be used.  If necessary, list elements willbe replicated to the same length given by the number of rows.
3658,a <code>list</code>, where <code>names(x)</code> mustnot contain empty (<code>""</code>) elements.
3659,an <code>environment</code> or <code>NULL</code>.
3660,(for the case <code>envir = NULL</code>): a parent frame akaenclosing environment, see <code>new.env</code>.
3661,(for the case <code>envir = NULL</code>): logical indicatingif the created environment should use hashing, see <code>new.env</code>.
3662,(in the case <code>envir = NULL, hash = TRUE</code>): hash size,see <code>new.env</code>.
3663,a (readable binary-mode) connection or a character stringgiving the name of the file to load (when tilde expansionis done).
3664,the environment where the data should be loaded.
3665,should item names be printed during loading?
3666,string or name space object.
3667,integer specifying position to attach.
3668,<code>NULL</code> or a character vector of dependencies to berecorded in object <code>.Depends</code> in the package.
3669,string naming the package/name space to load.
3670,character vector specifying library search path.
3671,Now ignored except during package installation.
3672,Ignored except during package installation.
3673,logical; if true, stop just after loading code.
3674,<code>NULL</code> or a version specification (a listwith components <code>op</code> and <code>version</code>).
3675,logical: should progress and error messages be suppressed?
3676,string or ‘name’, see <code>as.symbol</code>,of a package, e.g., <code>"stats"</code>.
3677,character vectors; see <code>library</code>.
3678,further arguments to be passed to <code>loadNamespace</code>.
3679,string or namespace environment.
3680,logical.
3681,environment.
3682,namespace environment.
3683,character vector.
3684,optional character vector.
3685,namespace environment.
3686,string naming the package/namespace to load.
3687,vector of package names parallel to <code>generics</code>.
3688,character vector specifying library.
3689,logical.
3690,character.
3691,character.
3692,environment.
3693,a 3-column character matrix.
3694,character.
3695,any object.
3696,character arguments.
3697,the methods table name.
3698,symbol: name of namespace
3699,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
3700,string or name space object.
3701,integer specifying position to attach.
3702,<code>NULL</code> or a character vector of dependencies to berecorded in object <code>.Depends</code> in the package.
3703,string naming the package/name space to load.
3704,character vector specifying library search path.
3705,Now ignored except during package installation.
3706,Ignored except during package installation.
3707,logical; if true, stop just after loading code.
3708,<code>NULL</code> or a version specification (a listwith components <code>op</code> and <code>version</code>).
3709,logical: should progress and error messages be suppressed?
3710,string or ‘name’, see <code>as.symbol</code>,of a package, e.g., <code>"stats"</code>.
3711,character vectors; see <code>library</code>.
3712,further arguments to be passed to <code>loadNamespace</code>.
3713,an object to be evaluated.  See ‘Details’.
3714,the <code>environment</code> in which <code>expr</code> is tobe evaluated.  May also be <code>NULL</code>, a list, a data frame,a pairlist or an integer as specified to <code>sys.call</code>.
3715,Relevant when <code>envir</code> is a (pair)list or a data frame.Specifies the enclosure, i.e., where <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> looks for objects not foundin <code>envir</code>.  This can be <code>NULL</code> (interpreted as the basepackage environment, <code>baseenv()</code>) or an environment.
3716,number of parent generations to go back
3717,an environment.
3718,logical specifying whether bindings should be locked.
3719,a name object or character string.
3720,a function taking zero or one arguments.
3721,an environment.
3722,logical specifying whether bindings should be locked.
3723,a name object or character string.
3724,a function taking zero or one arguments.
3725,a numeric or complex vector.
3726,a positive or complex number: the base with respect to whichlogarithms are computed.  Defaults to <i>e</i>=<code>exp(1)</code>.
3727,a numeric or complex vector.
3728,a positive or complex number: the base with respect to whichlogarithms are computed.  Defaults to <i>e</i>=<code>exp(1)</code>.
3729,a numeric or complex vector.
3730,a positive or complex number: the base with respect to whichlogarithms are computed.  Defaults to <i>e</i>=<code>exp(1)</code>.
3731,a numeric or complex vector.
3732,a positive or complex number: the base with respect to whichlogarithms are computed.  Defaults to <i>e</i>=<code>exp(1)</code>.
3733,a numeric or complex vector.
3734,a positive or complex number: the base with respect to whichlogarithms are computed.  Defaults to <i>e</i>=<code>exp(1)</code>.
3735,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
3736,object to be coerced or tested.
3737,further arguments passed to or from other methods.
3738,a matrix or other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with <code>length(dim(x)) == 2</code>.For back compatibility reasons, when the above is not fulfilled,<code>as.matrix(x)</code> is called first.
3739,logical.  Should the diagonal be included?
3740,which environment to use in listing the available objects.Defaults to the <em>current</em> environment.  Although called<code>name</code> for back compatibility, in fact this argument canspecify the environment in any form; see the ‘Details’ section.
3741,an alternative argument to <code>name</code> for specifying theenvironment as a position in the search list.  Mostly there forback compatibility.
3742,an alternative argument to <code>name</code> for specifying theenvironment.  Mostly there for back compatibility.
3743,a logical value.  If <code>TRUE</code>, allobject names are returned.  If <code>FALSE</code>, names which begin with a<span class="samp">.</span> are omitted.
3744,an optional regular expression.  Only namesmatching <code>pattern</code> are returned.  <code>glob2rx</code> can beused to convert wildcard patterns to regular expressions.
3745,logical indicating if the resulting<code>character</code> should be sorted alphabetically.  Note thatthis is part of <code>ls()</code> may take most of the time.
3746,character vector to be coerced to syntactically validnames.  This is coerced to character if necessary.
3747,logical; if <code>TRUE</code>, the resulting elements areunique.  This may be desired for, e.g., column names.
3748,logical.  For compatibility with <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> prior to 1.9.0.
3749,a character vector
3750,a character string used to separate a duplicate name fromits sequence number.
3751,an environment.
3752,logical specifying whether bindings should be locked.
3753,a name object or character string.
3754,a function taking zero or one arguments.
3755,a function of the appropriate arity (binary for<code>Reduce</code>, unary for <code>Filter</code>, <code>Find</code> and<code>Position</code>, <i>k</i>-ary for <code>Map</code> if this is called with<i>k</i> arguments).  An arbitrary predicate function for<code>Negate</code>.
3756,a vector.
3757,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of the same kind as the elements of<code>x</code>.
3758,a logical indicating whether to proceed from left toright (default) or from right to left.
3759,a logical indicating whether the successive reducecombinations should be accumulated.  By default, only the finalcombination is used.
3760,the value to be returned in the case when“no match” (no element satisfying the predicate) is found.
3761,vectors.
3762,function to apply, found via <code>match.fun</code>.
3763,arguments to vectorize over (vectors or lists of strictlypositive length, or all of zero length).  See also ‘Details’.
3764,<code>list</code> or <code>pairlist</code> of arguments tovectorize over, see <code>...</code> above.
3765,a list of other arguments to <code>FUN</code>.
3766,logical or character string; attempt to reduce theresult to a vector, matrix or higher dimensional array; seethe <code>simplify</code> argument of <code>sapply</code>.
3767,logical; use the names of the first ... argument, orif that is an unnamed character vector, use that vector as the names.
3768,an array
3769,a vector giving the margins to compute sums for.E.g., for a matrix <code>1</code> indicates rows, <code>2</code> indicatescolumns, <code>c(1, 2)</code> indicates rows and columns.When <code>x</code> has named dimnames, it can be a character vectorselecting dimension names.
3770,an array
3771,a vector giving the margins to compute sums for.E.g., for a matrix <code>1</code> indicates rows, <code>2</code> indicatescolumns, <code>c(1, 2)</code> indicates rows and columns.When <code>x</code> has named dimnames, it can be a character vectorselecting dimension names.
3772,numbers of rows and columns.
3773,vector or <code>NULL</code>: the values to be matched.Long vectors are supported.
3774,vector or <code>NULL</code>: the values to be matched against.Long vectors are not supported.
3775,the value to be returned in the case when no match isfound.  Note that it is coerced to <code>integer</code>.
3776,a vector of values that cannot be matched.  Anyvalue in <code>x</code> matching a value in this vector is assigned the<code>nomatch</code> value.  For historical reasons, <code>FALSE</code> isequivalent to <code>NULL</code>.
3777,a character vector (of length one unless <code>several.ok</code>is <code>TRUE</code>) or <code>NULL</code>.
3778,a character vector of candidate values
3779,logical specifying if <code>arg</code> should be allowedto have more than one element.
3780,a function, by default the function from which<code>match.call</code> is called.  See details.
3781,an unevaluated call to the function specified by<code>definition</code>, as generated by <code>call</code>.
3782,logical.  Should arguments matching <code>...</code>in the call be included or left as a <code>...</code> argument?
3783,an environment, from which the <code>...</code> in <code>call</code>are retrieved, if any.
3784,item to match as function: a function, symbol orcharacter string.  See ‘Details’.
3785,logical; control whether to search past non-functionobjects.
3786,objects.
3787,further arguments passed to methods.
3788,logical: should missing values be removed?
3789,a <code>Date</code> object to be summarized or printed.
3790,number of significant digits for the computations.
3791,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3792,further arguments to be passed from or to other methods.
3793,date-time or date objects.
3794,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
3795,character string.  Units in which the results aredesired.  Can be abbreviated.
3796,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
3797,character string or numeric value specifying a time interval.
3798,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
3799,an object inheriting from class <code>"difftime"</code>.
3800,arguments to be passed to or from other methods.
3801,a vector of data, usually taking a small number of distinctvalues.
3802,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
3803,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
3804,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
3805,logical flag to determine if the levels should be regardedas ordered (in the order given).
3806,an upper bound on the number of levels; see ‘Details’.
3807,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
3808,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
3809,an object to be printed or summarized from one of thedate-time classes.
3810,for timezone formatting, passed to <code>format.POSIXct</code>.
3811,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3812,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3813,further arguments to be passed from or to other methods.
3814,date-time objects
3815,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3816,a numeric vector (in seconds)
3817,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3818,an optional data vector (including a list or<code>expression</code> vector).  Non-atomic classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects arecoerced by <code>as.vector</code> and all attributes discarded.
3819,the desired number of rows.
3820,the desired number of columns.
3821,logical. If <code>FALSE</code> (the default) the matrix isfilled by columns, otherwise the matrix is filled by rows.
3822,A <code>dimnames</code> attribute for the matrix:<code>NULL</code> or a <code>list</code> of length 2 giving the row and columnnames respectively.  An empty list is treated as <code>NULL</code>, and alist of length one as row names.  The list can be named, and thelist names will be used as names for the dimensions.
3823,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3824,additional arguments to be passed to or from methods.
3825,logical indicating if the resulting matrixshould have character (rather than <code>NULL</code>)<code>rownames</code>.  The default, <code>NA</code>, uses <code>NULL</code>rownames if the data frame has ‘automatic’ row.names or for azero-row data frame.
3826,numeric or character arguments (see Note).
3827,a logical indicating whether missing values should beremoved.
3828,numerical matrix
3829,a character string specifying how ties arehandled, <code>"random"</code> by default; can be abbreviated; see‘Details’.
3830,An <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.  Currently there are methods fornumeric/logical vectors and date,date-time and time interval objects.  Complex vectorsare allowed for <code>trim = 0</code>, only.
3831,the fraction (0 to 0.5) of observations to betrimmed from each end of <code>x</code> before the mean is computed.Values of trim outside that range are taken as the nearest endpoint.
3832,a logical value indicating whether <code>NA</code>values should be stripped before the computation proceeds.
3833,further arguments passed to or from other methods.
3834,a <code>Date</code> object to be summarized or printed.
3835,number of significant digits for the computations.
3836,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3837,further arguments to be passed from or to other methods.
3838,An <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.  Currently there are methods fornumeric/logical vectors and date,date-time and time interval objects.  Complex vectorsare allowed for <code>trim = 0</code>, only.
3839,the fraction (0 to 0.5) of observations to betrimmed from each end of <code>x</code> before the mean is computed.Values of trim outside that range are taken as the nearest endpoint.
3840,a logical value indicating whether <code>NA</code>values should be stripped before the computation proceeds.
3841,further arguments passed to or from other methods.
3842,date-time or date objects.
3843,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
3844,character string.  Units in which the results aredesired.  Can be abbreviated.
3845,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
3846,character string or numeric value specifying a time interval.
3847,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
3848,an object inheriting from class <code>"difftime"</code>.
3849,arguments to be passed to or from other methods.
3850,an object to be printed or summarized from one of thedate-time classes.
3851,for timezone formatting, passed to <code>format.POSIXct</code>.
3852,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3853,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3854,further arguments to be passed from or to other methods.
3855,date-time objects
3856,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3857,a numeric vector (in seconds)
3858,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3859,an object to be printed or summarized from one of thedate-time classes.
3860,for timezone formatting, passed to <code>format.POSIXct</code>.
3861,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3862,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3863,further arguments to be passed from or to other methods.
3864,date-time objects
3865,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3866,a numeric vector (in seconds)
3867,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3868,numeric; new size limit in Mb.
3869,numeric; new maximal node number.
3870,numeric; new size limit in Mb.
3871,numeric; new maximal node number.
3872,A raw vector.  For <code>memCompress</code> a character vectorwill be converted to a raw vector with character strings separatedby <code>"\n"</code>.  Types <code>"gzip"</code> and <code>"xz"</code> support longraw vectors as from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0.
3873,character string, the type of compression.  May beabbreviated to a single letter, defaults to the first of the alternatives.
3874,logical: should the result be converted to a characterstring?  NB: character strings have a limit of<i>2^31 - 1</i> bytes, so raw vectors should be used forlarge inputs.
3875,A raw vector.  For <code>memCompress</code> a character vectorwill be converted to a raw vector with character strings separatedby <code>"\n"</code>.  Types <code>"gzip"</code> and <code>"xz"</code> support longraw vectors as from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0.
3876,character string, the type of compression.  May beabbreviated to a single letter, defaults to the first of the alternatives.
3877,logical: should the result be converted to a characterstring?  NB: character strings have a limit of<i>2^31 - 1</i> bytes, so raw vectors should be used forlarge inputs.
3878,data frames, or objects to be coerced to one.
3879,specifications of the columns used for merging.See ‘Details’.
3880,logical; <code>all = L</code> is shorthand for <code>all.x = L</code> and<code>all.y = L</code>, where <code>L</code> is either <code>TRUE</code> or<code>FALSE</code>.
3881,logical; if <code>TRUE</code>, then extra rows will be added tothe output, one for each row in <code>x</code> that has no matching row in<code>y</code>.  These rows will have <code>NA</code>s in those columns that areusually filled with values from <code>y</code>.  The default is<code>FALSE</code>, so that only rows with data from both <code>x</code> and<code>y</code> are included in the output.
3882,logical; analogous to <code>all.x</code>.
3883,logical.  Should the result be sorted on the <code>by</code>columns?
3884,a character vector of length 2 specifying the suffixesto be used for making unique the names of columns in the resultwhich are not used for merging (appearing in <code>by</code> etc).
3885,logical indicating that <code>suffixes</code> are appended inmore cases to avoid duplicated column names in the result.  Thiswas implicitly false before <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version 3.5.0.
3886,values which cannot be matched.  See<code>match</code>.  This is intended to be used for merging on onecolumn, so these are incomparable values of that column.
3887,arguments to be passed to or from methods.
3888,data frames, or objects to be coerced to one.
3889,specifications of the columns used for merging.See ‘Details’.
3890,logical; <code>all = L</code> is shorthand for <code>all.x = L</code> and<code>all.y = L</code>, where <code>L</code> is either <code>TRUE</code> or<code>FALSE</code>.
3891,logical; if <code>TRUE</code>, then extra rows will be added tothe output, one for each row in <code>x</code> that has no matching row in<code>y</code>.  These rows will have <code>NA</code>s in those columns that areusually filled with values from <code>y</code>.  The default is<code>FALSE</code>, so that only rows with data from both <code>x</code> and<code>y</code> are included in the output.
3892,logical; analogous to <code>all.x</code>.
3893,logical.  Should the result be sorted on the <code>by</code>columns?
3894,a character vector of length 2 specifying the suffixesto be used for making unique the names of columns in the resultwhich are not used for merging (appearing in <code>by</code> etc).
3895,logical indicating that <code>suffixes</code> are appended inmore cases to avoid duplicated column names in the result.  Thiswas implicitly false before <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version 3.5.0.
3896,values which cannot be matched.  See<code>match</code>.  This is intended to be used for merging on onecolumn, so these are incomparable values of that column.
3897,arguments to be passed to or from methods.
3898,data frames, or objects to be coerced to one.
3899,specifications of the columns used for merging.See ‘Details’.
3900,logical; <code>all = L</code> is shorthand for <code>all.x = L</code> and<code>all.y = L</code>, where <code>L</code> is either <code>TRUE</code> or<code>FALSE</code>.
3901,logical; if <code>TRUE</code>, then extra rows will be added tothe output, one for each row in <code>x</code> that has no matching row in<code>y</code>.  These rows will have <code>NA</code>s in those columns that areusually filled with values from <code>y</code>.  The default is<code>FALSE</code>, so that only rows with data from both <code>x</code> and<code>y</code> are included in the output.
3902,logical; analogous to <code>all.x</code>.
3903,logical.  Should the result be sorted on the <code>by</code>columns?
3904,a character vector of length 2 specifying the suffixesto be used for making unique the names of columns in the resultwhich are not used for merging (appearing in <code>by</code> etc).
3905,logical indicating that <code>suffixes</code> are appended inmore cases to avoid duplicated column names in the result.  Thiswas implicitly false before <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version 3.5.0.
3906,values which cannot be matched.  See<code>match</code>.  This is intended to be used for merging on onecolumn, so these are incomparable values of that column.
3907,arguments to be passed to or from methods.
3908,zero or more objects which can be coerced to character(and which are pasted together with no separator) or (for<code>message</code> only) a single condition object.
3909,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated, see also the note in <code>stop</code>.
3910,logical: should messages given as a character stringhave a newline appended?
3911,expression to evaluate.
3912,character, indicating which classes of messages shouldbe suppressed.
3913,For <code>get</code>, an object name (given as a characterstring or a symbol).<br>For <code>mget</code>, a character vector of object names.
3914,where to look for the object (see ‘Details’); ifomitted search as if the name of the object appeared unquoted in anexpression.
3915,the mode or type of object sought: see the‘Details’ section.
3916,should the enclosing frames of the environment besearched?
3917,For <code>mget</code>, a <code>list</code> of values tobe used if the item is not found: it will be coerced to a list ifnecessary.<br>For <code>dynGet</code> any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, e.g., a call to<code>stop()</code>.
3918,integer specifying the minimal frame number to lookinto.
3919,numeric or character arguments (see Note).
3920,a logical indicating whether missing values should beremoved.
3921,a formal argument.
3922,numeric.  Desired length of the output vector,inputs being recycled as needed.
3923,numeric vector.
3924,numeric vector.
3925,numeric vector.
3926,numeric vector.
3927,an object, probably of mode <code>complex</code>.
3928,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
3929,further arguments passed to or from other methods.
3930,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3931,a character string giving the desired mode or‘storage mode’ (type) of the object.
3932,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3933,a character string giving the desired mode or‘storage mode’ (type) of the object.
3934,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
3935,logical vector (possibly recycled).  Should the names beabbreviated?
3936,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
3937,arguments for other methods.
3938,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
3939,logical vector (possibly recycled).  Should the names beabbreviated?
3940,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
3941,arguments for other methods.
3942,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
3943,logical vector (possibly recycled).  Should the names beabbreviated?
3944,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
3945,arguments for other methods.
3946,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
3947,an appropriate named <code>list</code> of attributes, or<code>NULL</code>.
3948,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3949,a character vector of up to the same length as <code>x</code>, or<code>NULL</code>.
3950,an object to be printed or summarized from one of thedate-time classes.
3951,for timezone formatting, passed to <code>format.POSIXct</code>.
3952,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3953,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3954,further arguments to be passed from or to other methods.
3955,date-time objects
3956,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3957,a numeric vector (in seconds)
3958,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3959,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
3960,a character vector of up to the same length as <code>x</code>, or<code>NULL</code>.
3961,an object to be printed or summarized from one of thedate-time classes.
3962,for timezone formatting, passed to <code>format.POSIXct</code>.
3963,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
3964,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
3965,further arguments to be passed from or to other methods.
3966,date-time objects
3967,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
3968,a numeric vector (in seconds)
3969,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
3970,string or namespace environment.
3971,logical.
3972,environment.
3973,namespace environment.
3974,character vector.
3975,optional character vector.
3976,namespace environment.
3977,string naming the package/namespace to load.
3978,vector of package names parallel to <code>generics</code>.
3979,character vector specifying library.
3980,logical.
3981,character.
3982,character.
3983,environment.
3984,a 3-column character matrix.
3985,character.
3986,any object.
3987,character arguments.
3988,the methods table name.
3989,symbol: name of namespace
3990,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
3991,string or namespace environment.
3992,logical.
3993,environment.
3994,namespace environment.
3995,character vector.
3996,optional character vector.
3997,namespace environment.
3998,string naming the package/namespace to load.
3999,vector of package names parallel to <code>generics</code>.
4000,character vector specifying library.
4001,logical.
4002,character.
4003,character.
4004,environment.
4005,a 3-column character matrix.
4006,character.
4007,any object.
4008,character arguments.
4009,the methods table name.
4010,symbol: name of namespace
4011,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
4012,string or namespace environment.
4013,logical.
4014,environment.
4015,namespace environment.
4016,character vector.
4017,optional character vector.
4018,namespace environment.
4019,string naming the package/namespace to load.
4020,vector of package names parallel to <code>generics</code>.
4021,character vector specifying library.
4022,logical.
4023,character.
4024,character.
4025,environment.
4026,a 3-column character matrix.
4027,character.
4028,any object.
4029,character arguments.
4030,the methods table name.
4031,symbol: name of namespace
4032,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
4033,string or namespace environment.
4034,logical.
4035,environment.
4036,namespace environment.
4037,character vector.
4038,optional character vector.
4039,namespace environment.
4040,string naming the package/namespace to load.
4041,vector of package names parallel to <code>generics</code>.
4042,character vector specifying library.
4043,logical.
4044,character.
4045,character.
4046,environment.
4047,a 3-column character matrix.
4048,character.
4049,any object.
4050,character arguments.
4051,the methods table name.
4052,symbol: name of namespace
4053,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
4054,string or namespace environment.
4055,logical.
4056,environment.
4057,namespace environment.
4058,character vector.
4059,optional character vector.
4060,namespace environment.
4061,string naming the package/namespace to load.
4062,vector of package names parallel to <code>generics</code>.
4063,character vector specifying library.
4064,logical.
4065,character.
4066,character.
4067,environment.
4068,a 3-column character matrix.
4069,character.
4070,any object.
4071,character arguments.
4072,the methods table name.
4073,symbol: name of namespace
4074,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
4075,character vector, or a vector to be coerced to a charactervector.  Giving a factor is an error.
4076,character string: partial matching to one of<code>c("bytes", "chars", "width")</code>.  See ‘Details’.
4077,logical: should <code>NA</code> be returned for invalidmultibyte strings or <code>"bytes"</code>-encoded strings (rather thanthrowing an error)?
4078,logical: should <code>NA</code> be returned when<code>x</code> is <code>NA</code>?  If false, <code>nchar()</code> returns<code>2</code>, as that is the number of printing characters used whenstrings are written to output, and <code>nzchar()</code> is <code>TRUE</code>.  Thedefault for <code>nchar()</code>, <code>NA</code>, means to use <code>keepNA = TRUE</code>unless <code>type</code> is <code>"width"</code>.
4079,a vector, array, data frame, or <code>NULL</code>.
4080,a vector, array, data frame, or <code>NULL</code>.
4081,a function of the appropriate arity (binary for<code>Reduce</code>, unary for <code>Filter</code>, <code>Find</code> and<code>Position</code>, <i>k</i>-ary for <code>Map</code> if this is called with<i>k</i> arguments).  An arbitrary predicate function for<code>Negate</code>.
4082,a vector.
4083,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of the same kind as the elements of<code>x</code>.
4084,a logical indicating whether to proceed from left toright (default) or from right to left.
4085,a logical indicating whether the successive reducecombinations should be accumulated.  By default, only the finalcombination is used.
4086,the value to be returned in the case when“no match” (no element satisfying the predicate) is found.
4087,vectors.
4088,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
4089,an environment to associate with the function
4090,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
4091,a logical, if <code>TRUE</code> the environment will use a hash table.
4092,an environment to be used as the enclosure of theenvironment created.
4093,an environment
4094,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
4095,A length-one logical vector that is not <code>NA</code>.Conditions of length greater than one are currently accepted with awarning, but only the first element is used.  An error is signalledinstead when the environment variable <span class="env">_R_CHECK_LENGTH_1_CONDITION_</span>is set to true.  Other types are coerced to logicalif possible, ignoring any class.
4096,A syntactical name for a variable.
4097,An expression evaluating to a vector (including a list andan expression) or to a pairlist or <code>NULL</code>.  Afactor value will be coerced to a character vector.  As from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>4.0.0 this can be a long vector.
4098,An <em>expression</em> in a formal sense.  This is either asimple expression or a so-called <em>compound expression</em>, usuallyof the form <code>{ expr1 ; expr2 }</code>.
4099,a character string naming a function (and not abuilt-in operator).  Required for <code>UseMethod</code>.
4100,for <code>UseMethod</code>: an object whose class willdetermine the method to be dispatched.  Defaults to the firstargument of the enclosing function.
4101,further arguments to be passed to the next method.
4102,One or more character vectors.
4103,The ‘domain’ for the translation.
4104,a non-negative integer.
4105,the message to be used in English for <code>n = 1</code>.
4106,the message to be used in English for <code>n = 0, 2, 3, ...</code>.
4107,The directory in which to find translated messagecatalogs for the domain.
4108,an object, usually a factor.
4109,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, typically a vector of<code>character</code> strings.
4110,optional <code>logical</code> eventually to be passed to<code>print()</code>, used by <code>print.default()</code>, indicatingwhether or not strings should be right aligned.
4111,an object of class <code>"noquote"</code>.
4112,further options passed to next methods, such as <code>print</code>.
4113,for compatibility with the generic <code>c</code> function.
4114,numeric matrix; note that packages such as <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>define more <code>norm()</code> methods.
4115,character string, specifying the <em>type</em> of matrixnorm to be computed.A character indicating the type of norm desired.

4116,specifies the <b>o</b>ne norm,(maximum absolute column sum);
4117,specifies the <b>i</b>nfinity norm (maximumabsolute row sum);
4118,specifies the <b>F</b>robenius norm (theEuclidean norm of <code>x</code> treated as if it were a vector);
4119,specifies the <b>m</b>aximum modulus ofall the elements in <code>x</code>; and
4120,specifies the “spectral” or 2-norm, whichis the largest singular value (<code>svd</code>) of <code>x</code>.
4121,The default is <code>"O"</code>.  Only the first character of<code>type[1]</code> is used.
4122,character vector of file paths.
4123,the separator to be used on Windows – ignoredelsewhere.  Must be one of <code>c("/", "\\")</code>.
4124,logical: if <code>TRUE</code> then an error is given if the resultcannot be determined; if <code>NA</code> then a warning.
4125,a vector, array, data frame, or <code>NULL</code>.
4126,a vector, array, data frame, or <code>NULL</code>.
4127,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
4128,integer: a row number of the table given by<code>showConnections</code>.
4129,a connection.
4130,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
4131,object to be coerced or tested.
4132,further arguments passed to or from other methods.
4133,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
4134,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
4135,object to be converted or shifted.
4136,logical: should the conversion be to a singlecharacter string or multiple individual characters?
4137,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
4138,the result type, partially matched.
4139,object to be converted or shifted.
4140,logical: should the conversion be to a singlecharacter string or multiple individual characters?
4141,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
4142,the result type, partially matched.
4143,character vector, or a vector to be coerced to a charactervector.  Giving a factor is an error.
4144,character string: partial matching to one of<code>c("bytes", "chars", "width")</code>.  See ‘Details’.
4145,logical: should <code>NA</code> be returned for invalidmultibyte strings or <code>"bytes"</code>-encoded strings (rather thanthrowing an error)?
4146,logical: should <code>NA</code> be returned when<code>x</code> is <code>NA</code>?  If false, <code>nchar()</code> returns<code>2</code>, as that is the number of printing characters used whenstrings are written to output, and <code>nzchar()</code> is <code>TRUE</code>.  Thedefault for <code>nchar()</code>, <code>NA</code>, means to use <code>keepNA = TRUE</code>unless <code>type</code> is <code>"width"</code>.
4147,which environment to use in listing the available objects.Defaults to the <em>current</em> environment.  Although called<code>name</code> for back compatibility, in fact this argument canspecify the environment in any form; see the ‘Details’ section.
4148,an alternative argument to <code>name</code> for specifying theenvironment as a position in the search list.  Mostly there forback compatibility.
4149,an alternative argument to <code>name</code> for specifying theenvironment.  Mostly there for back compatibility.
4150,a logical value.  If <code>TRUE</code>, allobject names are returned.  If <code>FALSE</code>, names which begin with a<span class="samp">.</span> are omitted.
4151,an optional regular expression.  Only namesmatching <code>pattern</code> are returned.  <code>glob2rx</code> can beused to convert wildcard patterns to regular expressions.
4152,logical indicating if the resulting<code>character</code> should be sorted alphabetically.  Note thatthis is part of <code>ls()</code> may take most of the time.
4153,a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
4154,a character vector naming classes.  <code>value</code>can also be <code>NULL</code>.
4155,logical affecting return value: see ‘Details’.
4156,a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
4157,a character vector naming classes.  <code>value</code>can also be <code>NULL</code>.
4158,logical affecting return value: see ‘Details’.
4159,logical: defunct: ignored, with a warning for false values.
4160,The time-zone database to be used: the default is to tryknown locations until one is found.
4161,an expression to be executed.
4162,if TRUE, add <code>expr</code> to be executed after any previouslyset expressions (or before if <code>after</code> is FALSE); otherwise (thedefault) <code>expr</code> will overwrite any previously set expressions.
4163,if <code>add</code> is TRUE and <code>after</code> is FALSE, then<code>expr</code> will be added on top of the expressions that were alreadyregistered. The resulting last in first out order is useful for freeingor closing resources in reverse order.
4164,character string.  A description of the connection:see ‘Details’.
4165,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
4166,logical.  See the ‘Blocking’ section.
4167,The name of the encoding to be assumed.  See the‘Encoding’ section.
4168,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
4169,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
4170,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
4171,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
4172,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
4173,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
4174,a filename within a zip file.
4175,character string.  Host name for the port.
4176,integer.  The TCP port number.
4177,logical.  Should the socket be a client or a server?
4178,a server socket listening for connections.
4179,a connection.
4180,character string.  Currently ignored.
4181,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
4182,arguments passed to or from other methods.
4183,character string.  A description of the connection:see ‘Details’.
4184,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
4185,logical.  See the ‘Blocking’ section.
4186,The name of the encoding to be assumed.  See the‘Encoding’ section.
4187,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
4188,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
4189,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
4190,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
4191,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
4192,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
4193,a filename within a zip file.
4194,character string.  Host name for the port.
4195,integer.  The TCP port number.
4196,logical.  Should the socket be a client or a server?
4197,a server socket listening for connections.
4198,a connection.
4199,character string.  Currently ignored.
4200,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
4201,arguments passed to or from other methods.
4202, The name of a file. 
4203, The character encoding to assume for the file. 
4204, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
4205, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
4206, The timestamp to use on a copy of a file. 
4207, Is this <code>srcfilecopy</code> known to come from a file system file? 
4208, A <code>srcfile</code> object. 
4209, Line numbers. 
4210, A vector of four, six or eight values giving a source location; see‘Details’.
4211, An object of the appropriate class. 
4212, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
4213, An optional second <code>srcref</code> object to mark the endof the character range. 
4214, Additional arguments to the methods; these will be ignored. 
4215, The name of a file. 
4216, The character encoding to assume for the file. 
4217, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
4218, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
4219, The timestamp to use on a copy of a file. 
4220, Is this <code>srcfilecopy</code> known to come from a file system file? 
4221, A <code>srcfile</code> object. 
4222, Line numbers. 
4223, A vector of four, six or eight values giving a source location; see‘Details’.
4224, An object of the appropriate class. 
4225, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
4226, An optional second <code>srcref</code> object to mark the endof the character range. 
4227, Additional arguments to the methods; these will be ignored. 
4228, The name of a file. 
4229, The character encoding to assume for the file. 
4230, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
4231, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
4232, The timestamp to use on a copy of a file. 
4233, Is this <code>srcfilecopy</code> known to come from a file system file? 
4234, A <code>srcfile</code> object. 
4235, Line numbers. 
4236, A vector of four, six or eight values giving a source location; see‘Details’.
4237, An object of the appropriate class. 
4238, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
4239, An optional second <code>srcref</code> object to mark the endof the character range. 
4240, Additional arguments to the methods; these will be ignored. 
4241,objects.
4242,further arguments passed to methods.
4243,logical: should missing values be removed?
4244,date objects
4245,date objects or character vectors.  (Charactervectors are converted by <code>as.Date</code>.)
4246,a numeric vector (in days) <em>or</em> an object of class<code>"difftime"</code>, rounded to the nearest whole day.
4247,One of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
4248,date-time or date objects.
4249,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
4250,character string.  Units in which the results aredesired.  Can be abbreviated.
4251,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
4252,character string or numeric value specifying a time interval.
4253,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
4254,an object inheriting from class <code>"difftime"</code>.
4255,arguments to be passed to or from other methods.
4256,a vector of data, usually taking a small number of distinctvalues.
4257,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
4258,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
4259,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
4260,logical flag to determine if the levels should be regardedas ordered (in the order given).
4261,an upper bound on the number of levels; see ‘Details’.
4262,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
4263,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
4264,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
4265,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
4266,a vector of data, usually taking a small number of distinctvalues.
4267,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
4268,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
4269,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
4270,logical flag to determine if the levels should be regardedas ordered (in the order given).
4271,an upper bound on the number of levels; see ‘Details’.
4272,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
4273,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
4274,an object to be printed or summarized from one of thedate-time classes.
4275,for timezone formatting, passed to <code>format.POSIXct</code>.
4276,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
4277,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
4278,further arguments to be passed from or to other methods.
4279,date-time objects
4280,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
4281,a numeric vector (in seconds)
4282,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
4283,any options can be defined, using <code>name = value</code>.However, only the ones below are used in base <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>.

4284,Options can also be passed by giving a single unnamed argument whichis a named list.
4285,a character string holding an option name.
4286,if the specified option is not set in the options list,this value is returned.  This facilitates retrieving an option andchecking whether it is set and setting it separately if not.
4287,a sequence of numeric, complex, character or logicalvectors, all of the same length, or a classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
4288,an atomic vector for <code>method</code>s <code>"shell"</code> and<code>"quick"</code>.  When <code>x</code> is a non-atomic <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, the default<code>"auto"</code> and <code>"radix"</code> methods may work if <code>order(x,..)</code>does.
4289,vector of indices for partial sorting.(Non-<code>NULL</code> values are not implemented.)
4290,logical.  Should the sort order be increasing ordecreasing? For the <code>"radix"</code> method, this can be a vector oflength equal to the number of arguments in <code>...</code>. For theother methods, it must be length one.
4291,for controlling the treatment of <code>NA</code>s.If <code>TRUE</code>, missing values in the data are put last; if<code>FALSE</code>, they are put first; if <code>NA</code>, they are removed(see ‘Note’.)
4292,the method to be used: partial matches are allowed.  Thedefault (<code>"auto"</code>) implies <code>"radix"</code> for short numeric vectors,integer vectors, logical vectors and factors.Otherwise, it implies <code>"shell"</code>.For details of methods <code>"shell"</code>, <code>"quick"</code>, and <code>"radix"</code>,see the help for <code>sort</code>.
4293,a vector of data, usually taking a small number of distinctvalues.
4294,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
4295,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
4296,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
4297,logical flag to determine if the levels should be regardedas ordered (in the order given).
4298,an upper bound on the number of levels; see ‘Details’.
4299,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
4300,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
4301,First and second arguments for function <code>FUN</code>.Typically a vector or array.
4302,a function to use on the outer products, found <em>via</em><code>match.fun</code> (except for the special case <code>"*"</code>).
4303,optional arguments to be passed to <code>FUN</code>.
4304,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
4305,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
4306,character string: the hook name
4307,character string: the package/namespace name
4308,character string: an event for the package.  Can be abbreviated.
4309,A function or a list of functions, or for <code>action = "replace"</code>, <code>NULL</code> 
4310,The action to be taken.  Can be abbreviated.
4311,string or namespace environment.
4312,logical.
4313,environment.
4314,namespace environment.
4315,character vector.
4316,optional character vector.
4317,namespace environment.
4318,string naming the package/namespace to load.
4319,vector of package names parallel to <code>generics</code>.
4320,character vector specifying library.
4321,logical.
4322,character.
4323,character.
4324,environment.
4325,a 3-column character matrix.
4326,character.
4327,any object.
4328,character arguments.
4329,the methods table name.
4330,symbol: name of namespace
4331,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
4332,character vector: the names of packages.
4333,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to checking the loaded namespace, thenall libraries currently known in <code>.libPaths()</code>.
4334,logical.  Should this not give warnings or an errorif the package is not found?
4335,a logical.  If <code>TRUE</code>, additional diagnostics areprinted, notably when a package is found more than once.
4336,call expression.
4337,zero or more objects which can be coerced to character(and which are pasted together with no separator) or (for<code>message</code> only) a single condition object.
4338,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated, see also the note in <code>stop</code>.
4339,logical: should messages given as a character stringhave a newline appended?
4340,expression to evaluate.
4341,character, indicating which classes of messages shouldbe suppressed.
4342,object to be converted or shifted.
4343,logical: should the conversion be to a singlecharacter string or multiple individual characters?
4344,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
4345,the result type, partially matched.
4346,objects, possibly named.
4347,object to be coerced or tested.
4348,a logical indicating whether to copy all values or(default) only those whose names do not begin with a dot.
4349,a logical indicating whether the <code>names</code> ofthe resulting list should be sorted (increasingly).  Note that thisis somewhat costly, but may be useful for comparison of environments.
4350,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
4351,an environment to associate with the function
4352,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
4353,a logical, if <code>TRUE</code> the environment will use a hash table.
4354,an environment to be used as the enclosure of theenvironment created.
4355,an environment
4356,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
4357,a <code>function</code>, a <code>formula</code>, or<code>NULL</code>, which is the default.
4358,an environment to associate with the function
4359,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
4360,a logical, if <code>TRUE</code> the environment will use a hash table.
4361,an environment to be used as the enclosure of theenvironment created.
4362,an environment
4363,an integer specifying the initial size for a hashedenvironment.  An internal default value will be used if<code>size</code> is <code>NA</code> or zero.  This argument is ignored if<code>hash</code> is <code>FALSE</code>.
4364,the frame number if non-negative, the number of framesto go back if negative.
4365,the number of generations to go back.  (See the‘Details’ section.)
4366,a connection, or a character string giving the name of afile or a URL to read the expressions from.If <code>file</code> is <code>""</code> and <code>text</code> is missing or <code>NULL</code>then input is taken from the console.
4367,integer (or coerced to integer).  The maximum number ofexpressions to parse.  If <code>n</code> is <code>NULL</code> or negative or<code>NA</code> the input is parsed in its entirety.
4368,character vector.  The text to parse.  Elements are treatedas if they were lines of a file.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects will be coercedto character if possible.
4369,the prompt to print when parsing from the keyboard.<code>NULL</code> means to use <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s prompt, <code>getOption("prompt")</code>.
4370,a logical value; if <code>TRUE</code>, keepsource reference information.
4371,<code>NULL</code>, a character vector, or a<code>srcfile</code> object.  See the ‘Details’ section.
4372,encoding to be assumed for input strings.  If thevalue is <code>"latin1"</code> or <code>"UTF-8"</code> it is used to markcharacter strings as known to be in Latin-1 or UTF-8: it is not usedto re-encode the input.  To do the latter, specify the encoding aspart of the connection <code>con</code> or <em>via</em><code>options(encoding=)</code>: see the example under<code>file</code>. Arguments <code>encoding = "latin1"</code> and <code>encoding = "UTF-8"</code> are ignored with a warning when runningin a MBCS locale.
4373,a <code>character</code> vector of length <code>1</code>, i.e., a“string”.
4374,string or namespace environment.
4375,logical.
4376,environment.
4377,namespace environment.
4378,character vector.
4379,optional character vector.
4380,namespace environment.
4381,string naming the package/namespace to load.
4382,vector of package names parallel to <code>generics</code>.
4383,character vector specifying library.
4384,logical.
4385,character.
4386,character.
4387,environment.
4388,a 3-column character matrix.
4389,character.
4390,any object.
4391,character arguments.
4392,the methods table name.
4393,symbol: name of namespace
4394,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
4395,one or more <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects, to be converted to character vectors.
4396,a character string to separate the terms.  Not<code>NA_character_</code>.
4397,an optional character string to separate the results.  Not<code>NA_character_</code>.
4398,<code>logical</code> indicating if zero-lengthcharacter arguments should lead to the zero-length<code>character(0)</code> after the <code>sep</code>-phase (which turns into<code>""</code> in the <code>collapse</code>-phase, i.e., when <code>collapse</code> isnot <code>NULL</code>).
4399,one or more <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects, to be converted to character vectors.
4400,a character string to separate the terms.  Not<code>NA_character_</code>.
4401,an optional character string to separate the results.  Not<code>NA_character_</code>.
4402,<code>logical</code> indicating if zero-lengthcharacter arguments should lead to the zero-length<code>character(0)</code> after the <code>sep</code>-phase (which turns into<code>""</code> in the <code>collapse</code>-phase, i.e., when <code>collapse</code> isnot <code>NULL</code>).
4403,character vector containing one or more path names.
4404,character vector: the names of packages.
4405,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to checking the loaded namespace, thenall libraries currently known in <code>.libPaths()</code>.
4406,logical.  Should this not give warnings or an errorif the package is not found?
4407,a logical.  If <code>TRUE</code>, additional diagnostics areprinted, notably when a package is found more than once.
4408,call expression.
4409,character string.  A description of the connection:see ‘Details’.
4410,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
4411,logical.  See the ‘Blocking’ section.
4412,The name of the encoding to be assumed.  See the‘Encoding’ section.
4413,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
4414,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
4415,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
4416,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
4417,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
4418,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
4419,a filename within a zip file.
4420,character string.  Host name for the port.
4421,integer.  The TCP port number.
4422,logical.  Should the socket be a client or a server?
4423,a server socket listening for connections.
4424,a connection.
4425,character string.  Currently ignored.
4426,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
4427,arguments passed to or from other methods.
4428,the coordinates of points in the plot. Alternatively, asingle plotting structure, function or <em>any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with a<code>plot</code> method</em> can be provided.
4429,the y coordinates of points in the plot, <em>optional</em>if <code>x</code> is an appropriate structure.
4430,Arguments to be passed to methods, such asgraphical parameters (see <code>par</code>).Many methods will accept the following arguments:

4431,what type of plot should be drawn.  Possible types are

4432,<code>"p"</code> for <b>p</b>oints,
4433,<code>"l"</code> for <b>l</b>ines,
4434,<code>"b"</code> for <b>b</b>oth,
4435,<code>"c"</code> for the lines part alone of <code>"b"</code>,
4436,<code>"o"</code> for both ‘<b>o</b>verplotted’,
4437,<code>"h"</code> for ‘<b>h</b>istogram’ like (or‘high-density’) vertical lines,
4438,<code>"s"</code> for stair <b>s</b>teps,
4439,<code>"S"</code> for other <b>s</b>teps, see ‘Details’ below,
4440,<code>"n"</code> for no plotting.
4441,All other <code>type</code>s give a warning or an error; using, e.g.,<code>type = "punkte"</code> being equivalent to <code>type = "p"</code> for Scompatibility.  Note that some methods,e.g. <code>plot.factor</code>, do not accept this.
4442,an overall title for the plot: see <code>title</code>.
4443,a sub title for the plot: see <code>title</code>.
4444,a title for the x axis: see <code>title</code>.
4445,a title for the y axis: see <code>title</code>.
4446,the <i>y/x</i> aspect ratio,see <code>plot.window</code>.
4447,the values to be matched: converted to a character vector by<code>as.character</code>.  Long vectors are supported.
4448,the values to be matched against: converted to a charactervector.  Long vectors are not supported.
4449,the value to be returned at non-matching or multiplypartially matching positions.  Note that it is coerced to <code>integer</code>.
4450,should elements be in <code>table</code> be used morethan once?
4451,numeric or character arguments (see Note).
4452,a logical indicating whether missing values should beremoved.
4453,numeric or character arguments (see Note).
4454,a logical indicating whether missing values should beremoved.
4455,numeric or character arguments (see Note).
4456,a logical indicating whether missing values should beremoved.
4457,numeric or character arguments (see Note).
4458,a logical indicating whether missing values should beremoved.
4459,the vector of polynomial coefficients in increasing order.
4460,an integer between <code>1</code> and <code>length(search())</code>, the lengthof the search path, or <code>-1</code>.
4461,a function of the appropriate arity (binary for<code>Reduce</code>, unary for <code>Filter</code>, <code>Find</code> and<code>Position</code>, <i>k</i>-ary for <code>Map</code> if this is called with<i>k</i> arguments).  An arbitrary predicate function for<code>Negate</code>.
4462,a vector.
4463,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of the same kind as the elements of<code>x</code>.
4464,a logical indicating whether to proceed from left toright (default) or from right to left.
4465,a logical indicating whether the successive reducecombinations should be accumulated.  By default, only the finalcombination is used.
4466,the value to be returned in the case when“no match” (no element satisfying the predicate) is found.
4467,vectors.
4468,an object coercible to numeric by <code>as.numeric</code>.
4469,integer giving the <em>desired</em> number ofintervals.  Non-integer values are rounded down.
4470,nonnegative integer giving the <em>minimal</em> number ofintervals.  If <code>min.n == 0</code>, <code>pretty(.)</code> may return asingle value.
4471,positive number, a factor (smaller than one)by which a default scale is shrunk in the case when<code>range(x)</code> is very small (usually 0).
4472,non-negative numeric, typically <i>&gt; 1</i>.The interval unit is determined as {1,2,5,10} times <code>b</code>, apower of 10.  Larger <code>high.u.bias</code> values favor larger units.
4473,non-negative numericmultiplier favoring factor 5 over 2.  Default and ‘optimal’:<code>u5.bias = .5 + 1.5*high.u.bias</code>.
4474,integer code, one of {0,1,2}. If non-0, an<em>epsilon correction</em> is made at the boundaries such thatthe result boundaries will be outside <code>range(x)</code>; in the<em>small</em> case, the correction is only done if <code>eps.correct       &gt;= 2</code>.
4475,further arguments for methods.
4476,an object coercible to numeric by <code>as.numeric</code>.
4477,integer giving the <em>desired</em> number ofintervals.  Non-integer values are rounded down.
4478,nonnegative integer giving the <em>minimal</em> number ofintervals.  If <code>min.n == 0</code>, <code>pretty(.)</code> may return asingle value.
4479,positive number, a factor (smaller than one)by which a default scale is shrunk in the case when<code>range(x)</code> is very small (usually 0).
4480,non-negative numeric, typically <i>&gt; 1</i>.The interval unit is determined as {1,2,5,10} times <code>b</code>, apower of 10.  Larger <code>high.u.bias</code> values favor larger units.
4481,non-negative numericmultiplier favoring factor 5 over 2.  Default and ‘optimal’:<code>u5.bias = .5 + 1.5*high.u.bias</code>.
4482,integer code, one of {0,1,2}. If non-0, an<em>epsilon correction</em> is made at the boundaries such thatthe result boundaries will be outside <code>range(x)</code>; in the<em>small</em> case, the correction is only done if <code>eps.correct       &gt;= 2</code>.
4483,further arguments for methods.
4484,an atomic numerical or character object, possibly<code>complex</code> only for <code>prettyNum()</code>, typically avector of real numbers.  Any class is discarded, with a warning.
4485,the desired number of digits after the decimalpoint (<code>format = "f"</code>) or <em>significant</em> digits(<code>format = "g"</code>, <code>= "e"</code> or <code>= "fg"</code>).

4486,Default: 2 for integer, 4 for real numbers.  If less than 0,the C default of 6 digits is used.  If specified as more than 50, 50will be used with a warning unless <code>format = "f"</code> where it islimited to typically 324. (Not more than 15–21 digits need beaccurate, depending on the OS and compiler used.  This limit isjust a precaution against segfaults in the underlying C runtime.)
4487,the total field width; if both <code>digits</code> and<code>width</code> are unspecified, <code>width</code> defaults to 1,otherwise to <code>digits + 1</code>.  <code>width = 0</code> will use<code>width = digits</code>, <code>width &lt; 0</code> means leftjustify the number in this field (equivalent to <code>flag = "-"</code>).If necessary, the result will have more characters than<code>width</code>.  For character data this is interpreted in characters(not bytes nor display width).
4488,equal to <code>"d"</code>  (for integers), <code>"f"</code>,<code>"e"</code>, <code>"E"</code>, <code>"g"</code>, <code>"G"</code>, <code>"fg"</code> (forreals), or <code>"s"</code> (for strings).  Default is <code>"d"</code> forintegers, <code>"g"</code> for reals.

4489,<code>"f"</code> gives numbers in the usual<code>xxx.xxx</code> format;  <code>"e"</code> and <code>"E"</code> give <code>n.ddde+nn</code> or<code>n.dddE+nn</code> (scientific format); <code>"g"</code> and <code>"G"</code> put<code>x[i]</code> into scientific format only if it saves space to do so<em>and</em> drop trailing zeros and decimal point - unless <code>flag</code>contains <code>"#"</code> which keeps trailing zeros for the <code>"g", "G"</code>formats.

4490,<code>"fg"</code> (our own hybrid format) uses fixed format as <code>"f"</code>,but <code>digits</code> as the minimum number of <em>significant</em> digits.This can lead to quite long result strings, see examples below.  Notethat unlike <code>signif</code> this prints large numbers withmore significant digits than <code>digits</code>.  Trailing zeros are<em>dropped</em> in this format, unless <code>flag</code> contains<code>"#"</code>.
4491,for <code>formatC</code>, a character string giving aformat modifier as in Kernighan and Ritchie (1988, page 243) or theC+99 standard.

4492,pads leading zeros;
4493,does left adjustment,
4494,ensures a sign in all cases, i.e., <code>"+"</code> forpositive numbers ,
4495,if the first character is not a sign, the spacecharacter <code>" "</code> will be used instead.
4496,specifies “an alternative output form”,specifically depending on <code>format</code>.
4497,on some platform–locale combination, activates“thousands' grouping” for decimal conversion,
4498,in some versions of ‘<span class="file">glibc</span>’ allow for integerconversion to use the locale's alternative output digits, if any.
4499,There can be more than one of these flags, in any order.  Other charactersused to have no effect for <code>character</code> formatting, but signalan error since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.4.0.
4500,<code>"double"</code> (or <code>"real"</code>), <code>"integer"</code> or<code>"character"</code>.Default: Determined from the storage mode of <code>x</code>.
4501,character; if not empty used as mark between every<code>big.interval</code> decimals <em>before</em> (hence <code>big</code>) thedecimal point.
4502,see <code>big.mark</code> above; defaults to 3.
4503,character; if not empty used as mark between every<code>small.interval</code> decimals <em>after</em> (hence <code>small</code>) thedecimal point.
4504,see <code>small.mark</code> above; defaults to 5.
4505,the character to be used to indicate the numericdecimal point.
4506,if <code>x</code> is <code>character</code>, thecharacter known to have been used as the numeric decimal point in<code>x</code>.
4507,string specifying if the string widths shouldbe preserved where possible in those cases where marks(<code>big.mark</code> or <code>small.mark</code>) are added.  <code>"common"</code>,the default, corresponds to <code>format</code>-like behaviorwhereas <code>"individual"</code> is the default in<code>formatC()</code>. Value can be abbreviated.
4508,logical, character string or <code>NULL</code> specifyingif and how <em>zeros</em> should be formatted specially.  Useful forpretty printing ‘sparse’ objects.
4509,logical; if <code>zero.print</code> is acharacter string, indicates if the exact zero entries in <code>x</code>should be simply replaced by <code>zero.print</code>.  Otherwise,depending on the widths of the respective strings, the (formatted)zeroes are <em>partly</em> replaced by <code>zero.print</code> and thenpadded with <code>" "</code> to the right were applicable.  In that case(false <code>replace[.zero]</code>), if the <code>zero.print</code> string doesnot fit, a warning is produced (if <code>warn.non.fitting</code> is true).

4510,This works via <code>prettyNum()</code>, which calls <code>.format.zeros(*,    replace=replace.zero)</code> three times in this case, see the ‘Details’.
4511,logical; if it is true, <code>replace[.zero]</code> isfalse and the <code>zero.print</code> string does not fit, a<code>warning</code> is signalled.
4512,logical, indicating if trailing zeros,i.e., <code>"0"</code> <em>after</em> the decimal mark, should be removed;also drops <code>"e+00"</code> in exponential formats.  This is simply passedto <code>prettyNum()</code>, see the ‘Details’.
4513,optional logical, to be used when <code>x</code> is<code>"character"</code> to indicate if it stems from<code>complex</code> vector or not.  By default (<code>NA</code>),<code>x</code> is checked to ‘look like’ complex.
4514,arguments passed to <code>format</code>.
4515,numeric vector of the same length as <code>x</code>, typically thenumbers of which the character vector <code>x</code> is the pre-format.
4516,an object used to select a method.
4517,further arguments passed to or from other methods.
4518,logical, indicating whether or not strings should beprinted with surrounding quotes.
4519,integer, indicating how many levels should beprinted for a factor; if <code>0</code>, no extra "Levels" line will beprinted.  The default, <code>NULL</code>, entails choosing <code>max.levels</code>such that the levels print on one line of width <code>width</code>.
4520,only used when <code>max.levels</code> is NULL, see above.
4521,minimal number of <em>significant</em> digits, see<code>print.default</code>.
4522,character string (or <code>NULL</code>) indicating<code>NA</code> values in printed output, see<code>print.default</code>.
4523,character specifying how zeros (<code>0</code>) should beprinted; for sparse tables, using <code>"."</code> can produce morereadable results, similar to printing sparse matrices in <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>.
4524,logical, indicating whether or not strings should beright aligned.
4525,character indicating if strings should left- orright-justified or left alone, passed to <code>format</code>.
4526,logical indicating if internally stored sourceshould be used for printing when present, e.g., if<code>options(keep.source = TRUE)</code> has been in use.
4527,an object
4528,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, normally a data frame, possibly a matrix.
4529,a factor or a list of factors, each of length<code>nrow(data)</code>.
4530,a function to be applied to (usually data-frame) subsets of<code>data</code>.
4531,further arguments to <code>FUN</code>.
4532,logical: see <code>tapply</code>.
4533,a condition object.
4534,call expression.
4535,a condition object.
4536,expression to be evaluated.
4537,expression to be evaluated before returning or exiting.
4538,function.
4539,character string.
4540,character string.
4541,character string naming a restart.
4542,restart object.
4543,object.
4544,character string naming a condition class.
4545,additional arguments; see details below.
4546,character string.  A description of the connection:see ‘Details’.
4547,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
4548,logical.  See the ‘Blocking’ section.
4549,The name of the encoding to be assumed.  See the‘Encoding’ section.
4550,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
4551,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
4552,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
4553,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
4554,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
4555,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
4556,a filename within a zip file.
4557,character string.  Host name for the port.
4558,integer.  The TCP port number.
4559,logical.  Should the socket be a client or a server?
4560,a server socket listening for connections.
4561,a connection.
4562,character string.  Currently ignored.
4563,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
4564,arguments passed to or from other methods.
4565,object of class <code>data.frame</code>.
4566,optional arguments to <code>print</code> methods.
4567,the minimum number of significant digits to be used: see<code>print.default</code>.
4568,logical, indicating whether or not entries should beprinted with surrounding quotes.
4569,logical, indicating whether or not strings should beright-aligned.  The default is right-alignment.
4570,logical (or character vector), indicating whether (orwhat) row names should be printed.
4571,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
4572,a <code>Date</code> object to be summarized or printed.
4573,number of significant digits for the computations.
4574,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
4575,further arguments to be passed from or to other methods.
4576,the object to be printed.
4577,a non-null value for <code>digits</code> specifies the minimumnumber of significant digits to be printed in values.  The default,<code>NULL</code>, uses <code>getOption("digits")</code>.  (For theinterpretation for complex numbers see <code>signif</code>.)Non-integer values will be rounded down, and only valuesgreater than or equal to 1 and no greater than 22 are accepted.
4578,logical, indicating whether or not strings(<code>character</code>s) should be printed with surrounding quotes.
4579,a character string which is used to indicate<code>NA</code> values in printed output, or <code>NULL</code>(see ‘Details’).
4580,a non-negative integer <i>≤ 1024</i>,or <code>NULL</code> (meaning 1), giving the spacing between adjacentcolumns in printed vectors, matrices and arrays.
4581,logical, indicating whether or not strings should beright aligned.  The default is left alignment.
4582,a non-null value for <code>max</code> specifies the approximatemaximum number of entries to be printed.  The default, <code>NULL</code>,uses <code>getOption("max.print")</code>: see that help page for moredetails.
4583,controls the maximum number of columns on a line used inprinting vectors, matrices, etc.  The default, <code>NULL</code>, uses<code>getOption("width")</code>: see that help page for moredetails including allowed values.
4584,logical, indicating whether to use sourcereferences or copies rather than deparsing language objects.The default is to use the original source if it is available.
4585,further arguments to be passed to or from othermethods.  They are ignored in this function.
4586,date-time or date objects.
4587,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
4588,character string.  Units in which the results aredesired.  Can be abbreviated.
4589,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
4590,character string or numeric value specifying a time interval.
4591,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
4592,an object inheriting from class <code>"difftime"</code>.
4593,arguments to be passed to or from other methods.
4594,an object used to select a method.
4595,further arguments passed to or from other methods.
4596,logical, indicating whether or not strings should beprinted with surrounding quotes.
4597,integer, indicating how many levels should beprinted for a factor; if <code>0</code>, no extra "Levels" line will beprinted.  The default, <code>NULL</code>, entails choosing <code>max.levels</code>such that the levels print on one line of width <code>width</code>.
4598,only used when <code>max.levels</code> is NULL, see above.
4599,minimal number of <em>significant</em> digits, see<code>print.default</code>.
4600,character string (or <code>NULL</code>) indicating<code>NA</code> values in printed output, see<code>print.default</code>.
4601,character specifying how zeros (<code>0</code>) should beprinted; for sparse tables, using <code>"."</code> can produce morereadable results, similar to printing sparse matrices in <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>.
4602,logical, indicating whether or not strings should beright aligned.
4603,character indicating if strings should left- orright-justified or left alone, passed to <code>format</code>.
4604,logical indicating if internally stored sourceshould be used for printing when present, e.g., if<code>options(keep.source = TRUE)</code> has been in use.
4605,a character string or <code>DLLInfo</code> object.The character string specifies the file name of the DLLof interest, and is given without the file name extension (e.g., the‘<span class="file">.dll</span>’ or ‘<span class="file">.so</span>’) and with no directory/path information.So a file ‘<span class="file">MyPackage/libs/MyPackage.so</span>’ would be specified as<span class="samp">MyPackage</span>.

4606,The <code>DLLInfo</code> objects can be obtained directlyin calls to <code>dyn.load</code> and  <code>library.dynam</code>,or can be found after the DLL has been loaded using<code>getLoadedDLLs</code>, which returns a list of<code>DLLInfo</code> objects (index-able by DLL file name).

4607,The <code>DLLInfo</code> approach avoids any ambiguities related to twoDLLs having the same name but corresponding to files in differentdirectories.
4608,a logical value.  If this is <code>TRUE</code>, the elementsof the returned lists are named using the names of the routines (asseen by R via registration or raw name).  If <code>FALSE</code>,these names are not computed and assigned to the lists.  As aresult, the call should be quicker.  The name information is alsoavailable in the <code>NativeSymbolInfo</code> objects in the lists.
4609,a numeric or complex matrix whose spectral decomposition is tobe computed.  Logical matrices are coerced to numeric.
4610,if <code>TRUE</code>, the matrix is assumed to be symmetric(or Hermitian if complex) and only its lower triangle (diagonalincluded) is used.  If <code>symmetric</code> is not specified,<code>isSymmetric(x)</code> is used.
4611,if <code>TRUE</code>, only the eigenvalues are computedand returned, otherwise both eigenvalues and eigenvectors arereturned.
4612,logical. Defunct and ignored.
4613,an object used to select a method.
4614,further arguments passed to or from other methods.
4615,logical, indicating whether or not strings should beprinted with surrounding quotes.
4616,integer, indicating how many levels should beprinted for a factor; if <code>0</code>, no extra "Levels" line will beprinted.  The default, <code>NULL</code>, entails choosing <code>max.levels</code>such that the levels print on one line of width <code>width</code>.
4617,only used when <code>max.levels</code> is NULL, see above.
4618,minimal number of <em>significant</em> digits, see<code>print.default</code>.
4619,character string (or <code>NULL</code>) indicating<code>NA</code> values in printed output, see<code>print.default</code>.
4620,character specifying how zeros (<code>0</code>) should beprinted; for sparse tables, using <code>"."</code> can produce morereadable results, similar to printing sparse matrices in <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>.
4621,logical, indicating whether or not strings should beright aligned.
4622,character indicating if strings should left- orright-justified or left alone, passed to <code>format</code>.
4623,logical indicating if internally stored sourceshould be used for printing when present, e.g., if<code>options(keep.source = TRUE)</code> has been in use.
4624,an object used to select a method.
4625,further arguments passed to or from other methods.
4626,logical, indicating whether or not strings should beprinted with surrounding quotes.
4627,integer, indicating how many levels should beprinted for a factor; if <code>0</code>, no extra "Levels" line will beprinted.  The default, <code>NULL</code>, entails choosing <code>max.levels</code>such that the levels print on one line of width <code>width</code>.
4628,only used when <code>max.levels</code> is NULL, see above.
4629,minimal number of <em>significant</em> digits, see<code>print.default</code>.
4630,character string (or <code>NULL</code>) indicating<code>NA</code> values in printed output, see<code>print.default</code>.
4631,character specifying how zeros (<code>0</code>) should beprinted; for sparse tables, using <code>"."</code> can produce morereadable results, similar to printing sparse matrices in <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>.
4632,logical, indicating whether or not strings should beright aligned.
4633,character indicating if strings should left- orright-justified or left alone, passed to <code>format</code>.
4634,logical indicating if internally stored sourceshould be used for printing when present, e.g., if<code>options(keep.source = TRUE)</code> has been in use.
4635,An object, for the methods inheriting from class <code>"hexmode"</code>.
4636,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
4637,a logical indicating whether to use upper-caseletters or lower-case letters (default).
4638,further arguments passed to or from other methods.
4639,the name of a package, given as a name orliteral character string, or a character string, depending onwhether <code>character.only</code> is <code>FALSE</code> (default) or<code>TRUE</code>.
4640,the position on the search list at which to attach theloaded namespace.  Can also be the name of a position on the currentsearch list as given by <code>search()</code>.
4641,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.Non-existent library trees are silently ignored.
4642,a logical indicating whether <code>package</code> or<code>help</code> can be assumed to be character strings.
4643,logical.  If it is <code>TRUE</code>,  <code>FALSE</code> or<code>TRUE</code> is returned to indicate success.
4644,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the newpackage.  A conflict is a function masking a function,or a non-function masking a non-function. The default is <code>TRUE</code>unless specified as <code>FALSE</code> in the <code>conflicts.policy</code> option.
4645,a logical.  If <code>TRUE</code>, additional diagnostics areprinted.
4646,a logical.  If <code>TRUE</code>, no message confirmingpackage attaching is printed, and most often, no errors/warnings areprinted if package attaching fails.
4647,character string naming a package.
4648,character vector of names of objects that can maskobjects on the search path without signaling an error when strictconflict checking is enabled
4649,character vector of names of objects toexclude or include in the attached frame. Only one of these argumentsmay be used in a call to <code>library</code> or <code>require</code>.
4650,logical specifying whether required packageslisted in the <code>Depends</code> clause of the <code>DESCRIPTION</code> fileshould be attached automatically.
4651,an object used to select a method.
4652,further arguments passed to or from other methods.
4653,logical, indicating whether or not strings should beprinted with surrounding quotes.
4654,integer, indicating how many levels should beprinted for a factor; if <code>0</code>, no extra "Levels" line will beprinted.  The default, <code>NULL</code>, entails choosing <code>max.levels</code>such that the levels print on one line of width <code>width</code>.
4655,only used when <code>max.levels</code> is NULL, see above.
4656,minimal number of <em>significant</em> digits, see<code>print.default</code>.
4657,character string (or <code>NULL</code>) indicating<code>NA</code> values in printed output, see<code>print.default</code>.
4658,character specifying how zeros (<code>0</code>) should beprinted; for sparse tables, using <code>"."</code> can produce morereadable results, similar to printing sparse matrices in <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>.
4659,logical, indicating whether or not strings should beright aligned.
4660,character indicating if strings should left- orright-justified or left alone, passed to <code>format</code>.
4661,logical indicating if internally stored sourceshould be used for printing when present, e.g., if<code>options(keep.source = TRUE)</code> has been in use.
4662,a character string or <code>DLLInfo</code> object.The character string specifies the file name of the DLLof interest, and is given without the file name extension (e.g., the‘<span class="file">.dll</span>’ or ‘<span class="file">.so</span>’) and with no directory/path information.So a file ‘<span class="file">MyPackage/libs/MyPackage.so</span>’ would be specified as<span class="samp">MyPackage</span>.

4663,The <code>DLLInfo</code> objects can be obtained directlyin calls to <code>dyn.load</code> and  <code>library.dynam</code>,or can be found after the DLL has been loaded using<code>getLoadedDLLs</code>, which returns a list of<code>DLLInfo</code> objects (index-able by DLL file name).

4664,The <code>DLLInfo</code> approach avoids any ambiguities related to twoDLLs having the same name but corresponding to files in differentdirectories.
4665,a logical value.  If this is <code>TRUE</code>, the elementsof the returned lists are named using the names of the routines (asseen by R via registration or raw name).  If <code>FALSE</code>,these names are not computed and assigned to the lists.  As aresult, the call should be quicker.  The name information is alsoavailable in the <code>NativeSymbolInfo</code> objects in the lists.
4666,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, typically a vector of<code>character</code> strings.
4667,optional <code>logical</code> eventually to be passed to<code>print()</code>, used by <code>print.default()</code>, indicatingwhether or not strings should be right aligned.
4668,an object of class <code>"noquote"</code>.
4669,further options passed to next methods, such as <code>print</code>.
4670,for compatibility with the generic <code>c</code> function.
4671,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
4672,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
4673,An object, for the methods inheriting from class <code>"octmode"</code>.
4674,<code>NULL</code> or a positive integer specifying the minimumfield width to be used, with padding by leading zeroes.
4675,further arguments passed to or from other methods.
4676,the name of a package, given as a name orliteral character string, or a character string, depending onwhether <code>character.only</code> is <code>FALSE</code> (default) or<code>TRUE</code>.
4677,the position on the search list at which to attach theloaded namespace.  Can also be the name of a position on the currentsearch list as given by <code>search()</code>.
4678,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.Non-existent library trees are silently ignored.
4679,a logical indicating whether <code>package</code> or<code>help</code> can be assumed to be character strings.
4680,logical.  If it is <code>TRUE</code>,  <code>FALSE</code> or<code>TRUE</code> is returned to indicate success.
4681,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the newpackage.  A conflict is a function masking a function,or a non-function masking a non-function. The default is <code>TRUE</code>unless specified as <code>FALSE</code> in the <code>conflicts.policy</code> option.
4682,a logical.  If <code>TRUE</code>, additional diagnostics areprinted.
4683,a logical.  If <code>TRUE</code>, no message confirmingpackage attaching is printed, and most often, no errors/warnings areprinted if package attaching fails.
4684,character string naming a package.
4685,character vector of names of objects that can maskobjects on the search path without signaling an error when strictconflict checking is enabled
4686,character vector of names of objects toexclude or include in the attached frame. Only one of these argumentsmay be used in a call to <code>library</code> or <code>require</code>.
4687,logical specifying whether required packageslisted in the <code>Depends</code> clause of the <code>DESCRIPTION</code> fileshould be attached automatically.
4688,an object to be printed or summarized from one of thedate-time classes.
4689,for timezone formatting, passed to <code>format.POSIXct</code>.
4690,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
4691,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
4692,further arguments to be passed from or to other methods.
4693,date-time objects
4694,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
4695,a numeric vector (in seconds)
4696,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
4697,an object to be printed or summarized from one of thedate-time classes.
4698,for timezone formatting, passed to <code>format.POSIXct</code>.
4699,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
4700,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
4701,further arguments to be passed from or to other methods.
4702,date-time objects
4703,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
4704,a numeric vector (in seconds)
4705,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
4706,a condition object.
4707,call expression.
4708,a condition object.
4709,expression to be evaluated.
4710,expression to be evaluated before returning or exiting.
4711,function.
4712,character string.
4713,character string.
4714,character string naming a restart.
4715,restart object.
4716,object.
4717,character string naming a condition class.
4718,additional arguments; see details below.
4719,a vector (atomic, not a list) for <code>rle()</code>;an object of class <code>"rle"</code> for <code>inverse.rle()</code>.
4720,further arguments; ignored here.
4721,number of significant digits for printing, see<code>print.default</code>.
4722,character string, prepended to each printed line.
4723,an object used to select a method.
4724,further arguments passed to or from other methods.
4725,logical, indicating whether or not strings should beprinted with surrounding quotes.
4726,integer, indicating how many levels should beprinted for a factor; if <code>0</code>, no extra "Levels" line will beprinted.  The default, <code>NULL</code>, entails choosing <code>max.levels</code>such that the levels print on one line of width <code>width</code>.
4727,only used when <code>max.levels</code> is NULL, see above.
4728,minimal number of <em>significant</em> digits, see<code>print.default</code>.
4729,character string (or <code>NULL</code>) indicating<code>NA</code> values in printed output, see<code>print.default</code>.
4730,character specifying how zeros (<code>0</code>) should beprinted; for sparse tables, using <code>"."</code> can produce morereadable results, similar to printing sparse matrices in <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>.
4731,logical, indicating whether or not strings should beright aligned.
4732,character indicating if strings should left- orright-justified or left alone, passed to <code>format</code>.
4733,logical indicating if internally stored sourceshould be used for printing when present, e.g., if<code>options(keep.source = TRUE)</code> has been in use.
4734, The name of a file. 
4735, The character encoding to assume for the file. 
4736, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
4737, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
4738, The timestamp to use on a copy of a file. 
4739, Is this <code>srcfilecopy</code> known to come from a file system file? 
4740, A <code>srcfile</code> object. 
4741, Line numbers. 
4742, A vector of four, six or eight values giving a source location; see‘Details’.
4743, An object of the appropriate class. 
4744, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
4745, An optional second <code>srcref</code> object to mark the endof the character range. 
4746, Additional arguments to the methods; these will be ignored. 
4747, The name of a file. 
4748, The character encoding to assume for the file. 
4749, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
4750, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
4751, The timestamp to use on a copy of a file. 
4752, Is this <code>srcfilecopy</code> known to come from a file system file? 
4753, A <code>srcfile</code> object. 
4754, Line numbers. 
4755, A vector of four, six or eight values giving a source location; see‘Details’.
4756, An object of the appropriate class. 
4757, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
4758, An optional second <code>srcref</code> object to mark the endof the character range. 
4759, Additional arguments to the methods; these will be ignored. 
4760,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
4761,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
4762,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
4763,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
4764,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
4765,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
4766,a character vector giving the row names for the dataframe.
4767,The name to be used for the column of tableentries, usually counts.
4768,logical: should the classifying factors bereturned as factors (the default) or character vectors?
4769,passed to <code>provideDimnames</code>.
4770,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
4771,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
4772,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
4773,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
4774,a character string <code>cat()</code>ed before themessages are printed.
4775,an object for which a summary is desired.
4776,a result of the <em>default</em> method of <code>summary()</code>.
4777,integer, indicating how many levels should be shown for<code>factor</code>s.
4778,integer, used for number formatting with<code>signif()</code> (for <code>summary.default</code>) or<code>format()</code> (for <code>summary.data.frame</code>).  In<code>summary.default</code>, if not specified (i.e.,<code>missing(.)</code>), <code>signif()</code> will <em>not</em> be calledanymore (since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &gt;= 3.4.0, where the default has been changed toonly round in the <code>print</code> and <code>format</code> methods).
4779,integer code used in <code>quantile(*, type=quantile.type)</code>for the default method.
4780,additional arguments affecting the summary produced.
4781,an object used to select a method.
4782,further arguments passed to or from other methods.
4783,logical, indicating whether or not strings should beprinted with surrounding quotes.
4784,integer, indicating how many levels should beprinted for a factor; if <code>0</code>, no extra "Levels" line will beprinted.  The default, <code>NULL</code>, entails choosing <code>max.levels</code>such that the levels print on one line of width <code>width</code>.
4785,only used when <code>max.levels</code> is NULL, see above.
4786,minimal number of <em>significant</em> digits, see<code>print.default</code>.
4787,character string (or <code>NULL</code>) indicating<code>NA</code> values in printed output, see<code>print.default</code>.
4788,character specifying how zeros (<code>0</code>) should beprinted; for sparse tables, using <code>"."</code> can produce morereadable results, similar to printing sparse matrices in <a href="https://CRAN.R-project.org/package=Matrix"><span class="pkg">Matrix</span></a>.
4789,logical, indicating whether or not strings should beright aligned.
4790,character indicating if strings should left- orright-justified or left alone, passed to <code>format</code>.
4791,logical indicating if internally stored sourceshould be used for printing when present, e.g., if<code>options(keep.source = TRUE)</code> has been in use.
4792,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
4793,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
4794,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
4795,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
4796,a character string <code>cat()</code>ed before themessages are printed.
4797,numeric or character matrix.
4798,(optional) character vectors giving row or columnnames respectively.  By default, these are taken from<code>dimnames(x)</code>.
4799,logical; if <code>TRUE</code> and <code>x</code> is of mode<code>"character"</code>, <em>quotes</em> (<span class="samp">"</span>) are used.
4800, if <code>TRUE</code> and <code>x</code> is of mode<code>"character"</code>, the output columns are <em>right</em>-justified.
4801,how <code>NA</code>s are printed.  If this is non-null, itsvalue is used to represent <code>NA</code>.
4802,arguments for <code>print</code> methods.
4803,numeric or complex or logical vectors.
4804,logical.  Should missing values be removed?
4805, table 
4806,a vector giving the margins to split by.E.g., for a matrix <code>1</code> indicates rows, <code>2</code> indicatescolumns, <code>c(1, 2)</code> indicates rows and columns.When <code>x</code> has named dimnames, it can be a character vectorselecting dimension names.
4807, table 
4808,a vector giving the margins to split by.E.g., for a matrix <code>1</code> indicates rows, <code>2</code> indicatescolumns, <code>c(1, 2)</code> indicates rows and columns.When <code>x</code> has named dimnames, it can be a character vectorselecting dimension names.
4809,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, for example a matrix, array or data frame.
4810,a possible value for <code>dimnames(x)</code>: see the‘Value’ section.
4811,a character string, used to separate <code>base</code>symbols and digits in the constructed dimnames.
4812,a non-empty <code>list</code> of character vectors.  Thelist components are used in turn (and recycled when needed) toconstruct replacements for empty dimnames components.  See also theexamples.
4813,logical indicating that the dimnames constructed areunique within each dimension in the sense of <code>make.unique</code>.
4814,non-negative numeric vectors.
4815,numeric vectors.
4816,integer vectors.
4817,a character vector.
4818,A connection.
4819,logical.  If true, a newline is appended to each stringpushed back.
4820,character string, partially matched.  See details.
4821,a character vector.
4822,A connection.
4823,logical.  If true, a newline is appended to each stringpushed back.
4824,character string, partially matched.  See details.
4825,a character string indicating whether the environment(workspace) should be saved, one of <code>"no"</code>, <code>"yes"</code>,<code>"ask"</code> or <code>"default"</code>.
4826,the (numerical) error status to be returned to theoperating system, where relevant.  Conventionally <code>0</code>indicates successful completion.
4827,should <code>.Last()</code> be executed?
4828,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4829,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4830,a QR decomposition of the type computed by <code>qr</code>.
4831,a vector or matrix of right-hand sides of equations.
4832,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4833,effective rank.
4834,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4835,further arguments passed to or from other methods
4836,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4837,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4838,a QR decomposition of the type computed by <code>qr</code>.
4839,a vector or matrix of right-hand sides of equations.
4840,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4841,effective rank.
4842,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4843,further arguments passed to or from other methods
4844,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4845,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4846,a QR decomposition of the type computed by <code>qr</code>.
4847,a vector or matrix of right-hand sides of equations.
4848,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4849,effective rank.
4850,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4851,further arguments passed to or from other methods
4852,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4853,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4854,a QR decomposition of the type computed by <code>qr</code>.
4855,a vector or matrix of right-hand sides of equations.
4856,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4857,effective rank.
4858,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4859,further arguments passed to or from other methods
4860,object representing a QR decomposition.  This willtypically have come from a previous call to <code>qr</code> or<code>lsfit</code>.
4861,logical expression of length 1.  Indicates whether anarbitrary  orthogonal completion of the <i>\bold{Q}</i> or<i>\bold{X}</i> matrices is to be made, or whether the <i>\bold{R}</i>matrix is to be completed  by binding zero-value rows beneath thesquare upper triangle.
4862,integer in the range <code>1:nrow(qr$qr)</code>.  The numberof columns to be in the reconstructed <i>\bold{X}</i>.  The defaultwhen <code>complete</code> is <code>FALSE</code> is the first<code>min(ncol(X), nrow(X))</code> columns of the original <i>\bold{X}</i>from which the qr object was constructed.  The default when<code>complete</code> is <code>TRUE</code> is a square matrix with the original<i>\bold{X}</i> in the first <code>ncol(X)</code> columns and an arbitraryorthogonal completion (unitary completion in the complex case) inthe remaining columns.
4863,vector (not matrix) of diagonal values.  Each column ofthe returned <i>\bold{Q}</i> will be multiplied by the correspondingdiagonal value.  Defaults to all <code>1</code>s.
4864,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4865,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4866,a QR decomposition of the type computed by <code>qr</code>.
4867,a vector or matrix of right-hand sides of equations.
4868,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4869,effective rank.
4870,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4871,further arguments passed to or from other methods
4872,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4873,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4874,a QR decomposition of the type computed by <code>qr</code>.
4875,a vector or matrix of right-hand sides of equations.
4876,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4877,effective rank.
4878,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4879,further arguments passed to or from other methods
4880,object representing a QR decomposition.  This willtypically have come from a previous call to <code>qr</code> or<code>lsfit</code>.
4881,logical expression of length 1.  Indicates whether anarbitrary  orthogonal completion of the <i>\bold{Q}</i> or<i>\bold{X}</i> matrices is to be made, or whether the <i>\bold{R}</i>matrix is to be completed  by binding zero-value rows beneath thesquare upper triangle.
4882,integer in the range <code>1:nrow(qr$qr)</code>.  The numberof columns to be in the reconstructed <i>\bold{X}</i>.  The defaultwhen <code>complete</code> is <code>FALSE</code> is the first<code>min(ncol(X), nrow(X))</code> columns of the original <i>\bold{X}</i>from which the qr object was constructed.  The default when<code>complete</code> is <code>TRUE</code> is a square matrix with the original<i>\bold{X}</i> in the first <code>ncol(X)</code> columns and an arbitraryorthogonal completion (unitary completion in the complex case) inthe remaining columns.
4883,vector (not matrix) of diagonal values.  Each column ofthe returned <i>\bold{Q}</i> will be multiplied by the correspondingdiagonal value.  Defaults to all <code>1</code>s.
4884,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4885,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4886,a QR decomposition of the type computed by <code>qr</code>.
4887,a vector or matrix of right-hand sides of equations.
4888,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4889,effective rank.
4890,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4891,further arguments passed to or from other methods
4892,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
4893,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
4894,a QR decomposition of the type computed by <code>qr</code>.
4895,a vector or matrix of right-hand sides of equations.
4896,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
4897,effective rank.
4898,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
4899,further arguments passed to or from other methods
4900,object representing a QR decomposition.  This willtypically have come from a previous call to <code>qr</code> or<code>lsfit</code>.
4901,logical expression of length 1.  Indicates whether anarbitrary  orthogonal completion of the <i>\bold{Q}</i> or<i>\bold{X}</i> matrices is to be made, or whether the <i>\bold{R}</i>matrix is to be completed  by binding zero-value rows beneath thesquare upper triangle.
4902,integer in the range <code>1:nrow(qr$qr)</code>.  The numberof columns to be in the reconstructed <i>\bold{X}</i>.  The defaultwhen <code>complete</code> is <code>FALSE</code> is the first<code>min(ncol(X), nrow(X))</code> columns of the original <i>\bold{X}</i>from which the qr object was constructed.  The default when<code>complete</code> is <code>TRUE</code> is a square matrix with the original<i>\bold{X}</i> in the first <code>ncol(X)</code> columns and an arbitraryorthogonal completion (unitary completion in the complex case) inthe remaining columns.
4903,vector (not matrix) of diagonal values.  Each column ofthe returned <i>\bold{Q}</i> will be multiplied by the correspondingdiagonal value.  Defaults to all <code>1</code>s.
4904,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
4905,logical vector (possibly recycled).  Should the names beabbreviated?
4906,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
4907,arguments for other methods.
4908,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
4909,logical vector (possibly recycled).  Should the names beabbreviated?
4910,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
4911,arguments for other methods.
4912,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
4913,logical vector (possibly recycled).  Should the names beabbreviated?
4914,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
4915,arguments for other methods.
4916,a character string indicating whether the environment(workspace) should be saved, one of <code>"no"</code>, <code>"yes"</code>,<code>"ask"</code> or <code>"default"</code>.
4917,the (numerical) error status to be returned to theoperating system, where relevant.  Conventionally <code>0</code>indicates successful completion.
4918,should <code>.Last()</code> be executed?
4919,any syntactically valid <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression
4920,a <code>call</code>, i.e., an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of<code>class</code> (and <code>mode</code>) <code>"call"</code>.
4921,an environment or a list object.  Defaults to thecurrent evaluation environment.
4922,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
4923,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
4924,As well as <code>"home"</code> which gives the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> homedirectory, other known values are <code>"bin"</code>, <code>"doc"</code>,<code>"etc"</code>, <code>"include"</code>, <code>"modules"</code> and <code>"share"</code>giving the paths to the corresponding parts of an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> installation.
4925,any <code>numeric</code> or character objects.
4926,logical, indicating if <code>NA</code>'s should beomitted.
4927,logical, indicating if all non-finite elements shouldbe omitted.
4928,any <code>numeric</code> or character objects.
4929,logical, indicating if <code>NA</code>'s should beomitted.
4930,logical, indicating if all non-finite elements shouldbe omitted.
4931,a numeric, complex, character or logical vector.
4932,for controlling the treatment of <code>NA</code>s.If <code>TRUE</code>, missing values in the data are put last; if<code>FALSE</code>, they are put first; if <code>NA</code>, they are removed; if<code>"keep"</code> they are kept with rank <code>NA</code>.
4933,a character string specifying how ties are treated,see ‘Details’; can be abbreviated.
4934,a <code>list</code> or <code>expression</code>, i.e., “list-like”.
4935,a <code>function</code> of one “principal” argument,passing further arguments via <code>...</code>.
4936,character vector of <code>class</code> names, or<code>"ANY"</code> to match any class.
4937,The default result (not used if <code>how = "replace"</code>).
4938,character string partially matching the three possibilities given:see ‘Details’.
4939,additional arguments passed to the call to <code>f</code>.
4940,desired length.
4941,object to be coerced.
4942,character or raw vector.  A description of the connection.For an input this is an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> raw vector object, and for an outputconnection the name for the connection.
4943,character.  Any of the standard connection open modes.
4944,An output raw connection.
4945,character or raw vector.  A description of the connection.For an input this is an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> raw vector object, and for an outputconnection the name for the connection.
4946,character.  Any of the standard connection open modes.
4947,An output raw connection.
4948,object to be converted or shifted.
4949,logical: should the conversion be to a singlecharacter string or multiple individual characters?
4950,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
4951,the result type, partially matched.
4952,object to be converted or shifted.
4953,logical: should the conversion be to a singlecharacter string or multiple individual characters?
4954,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
4955,the result type, partially matched.
4956,object to be converted or shifted.
4957,logical: should the conversion be to a singlecharacter string or multiple individual characters?
4958,the number of bits to shift.  Positive numbers shift rightand negative numbers shift left: allowed values are <code>-8 ... 8</code>.
4959,the result type, partially matched.
4960,(generalized) vectors or matrices.  These can be given as namedarguments.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects may be coerced as appropriate, or S4methods may be used: see sections ‘Details’ and‘Value’.  (For the <code>"data.frame"</code> method of <code>cbind</code>these can be further arguments to <code>data.frame</code> such as<code>stringsAsFactors</code>.)
4961,integer controlling the construction of labels inthe case of non-matrix-like arguments (for the default method):<br><code>deparse.level = 0</code> constructs no labels; the default,<br><code>deparse.level = 1 or 2</code> constructs labels from the argumentnames, see the ‘Value’ section below.
4962,(only for data frame method:) logicalindicating if unique and valid <code>row.names</code> should beconstructed from the arguments.
4963,logical, passed to <code>as.data.frame</code>;only has an effect when the <code>...</code> arguments contain a(non-<code>data.frame</code>) <code>character</code>.
4964,if the data frames contain factors, the default<code>TRUE</code> ensures that <code>NA</code> levels of factors are kept, see<a href="https://bugs.R-project.org/show_bug.cgi?id=17562">PR#17562</a> and the ‘Data frame methods’.  In <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> versions upto 3.6.x, <code>factor.exclude = NA</code> has been implicitly hardcoded(<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &lt;= 3.6.0) or the default (<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> = 3.6.x, x &gt;= 1).
4965,(generalized) vectors or matrices.  These can be given as namedarguments.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects may be coerced as appropriate, or S4methods may be used: see sections ‘Details’ and‘Value’.  (For the <code>"data.frame"</code> method of <code>cbind</code>these can be further arguments to <code>data.frame</code> such as<code>stringsAsFactors</code>.)
4966,integer controlling the construction of labels inthe case of non-matrix-like arguments (for the default method):<br><code>deparse.level = 0</code> constructs no labels; the default,<br><code>deparse.level = 1 or 2</code> constructs labels from the argumentnames, see the ‘Value’ section below.
4967,(only for data frame method:) logicalindicating if unique and valid <code>row.names</code> should beconstructed from the arguments.
4968,logical, passed to <code>as.data.frame</code>;only has an effect when the <code>...</code> arguments contain a(non-<code>data.frame</code>) <code>character</code>.
4969,if the data frames contain factors, the default<code>TRUE</code> ensures that <code>NA</code> levels of factors are kept, see<a href="https://bugs.R-project.org/show_bug.cgi?id=17562">PR#17562</a> and the ‘Data frame methods’.  In <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> versions upto 3.6.x, <code>factor.exclude = NA</code> has been implicitly hardcoded(<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &lt;= 3.6.0) or the default (<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> = 3.6.x, x &gt;= 1).
4970,A matrix or a the result of <code>qr</code> or a fit froma class inheriting from <code>"lm"</code>.
4971,logical.  Should the result be exact?
4972,character string, specifying the matrix norm with respectto which the condition number is to be computed, see also<code>norm</code>.  For <code>rcond</code>, the default is <code>"O"</code>,meaning the <b>O</b>ne- or 1-norm.  The (currently only) otherpossible value is <code>"I"</code> for the infinity norm.
4973,a partially matched character string specifying the method to be used;<code>"qr"</code> is the default for back-compatibility, mainly.
4974,logical.  If true, the matrix used is just the lowertriangular part of <code>z</code>.
4975,logical.  If true and <code>z</code> is not complex, theLINPACK routine <code>dtrco()</code> is called; otherwise the relevantLAPACK routine is.
4976,further arguments passed to or from other methods;for <code>kappa.*()</code>, notably <code>LINPACK</code> when <code>norm</code> is not<code>"2"</code>.
4977,numeric.  Desired length of the output vector,inputs being recycled as needed.
4978,numeric vector.
4979,numeric vector.
4980,numeric vector.
4981,numeric vector.
4982,an object, probably of mode <code>complex</code>.
4983,an object of mode <code>complex</code>, or one of a class for whicha methods has been defined.
4984,further arguments passed to or from other methods.
4985,either a character string naming a file or a connection.<code>""</code> indicates output to the console.  For <code>read.dcf</code> thiscan name a compressed file (see <code>gzfile</code>).
4986,Fields to read from the DCF file.  Default is to read allfields.
4987,a logical indicating whether in case of multipleoccurrences of a field in a record, all these should be gathered.If <code>all</code> is false (default), only the last such occurrence isused.
4988,a character string with the names of the fields forwhich whitespace should be kept as is, or <code>NULL</code> (default)indicating that there are no such fields.  Coerced to character ifpossible.  For fields where whitespace is not to be kept as is,<code>read.dcf</code> removes leading and trailing whitespace, and<code>write.dcf</code> folds using <code>strwrap</code>.
4989,the object to be written, typically a data frame.  If not, itis attempted to coerce <code>x</code> to a data frame.
4990,logical.  If <code>TRUE</code>, the output is appended to thefile.  If <code>FALSE</code>, any existing file of the name is destroyed.
4991,logical to be passed to <code>writeLines()</code>,see there: “for expert use”.
4992,a positive integer specifying the indentation forcontinuation lines in output entries.
4993,a positive integer giving the target column for wrappinglines in the output.
4994,A connection object or a character string naming a file ora raw vector.
4995,Either an object whose mode will give the mode of thevector to be read, or a character vector of length one describingthe mode: one of <code>"numeric"</code>, <code>"double"</code>,<code>"integer"</code>, <code>"int"</code>, <code>"logical"</code>, <code>"complex"</code>,<code>"character"</code>, <code>"raw"</code>.
4996,numeric.  The (maximal) number of records to beread.  You can use an over-estimate here, but not too large asstorage is reserved for <code>n</code> items.
4997,integer.  The number of bytes per element in the bytestream.  The default, <code>NA_integer_</code>, uses the natural size.Size changing is not supported for raw and complex vectors.
4998,logical.  Only used for integers of sizes 1 and 2,when it determines if the quantity on fileshould be regarded as a signed or unsigned integer.
4999,The endian-ness (<code>"big"</code> or <code>"little"</code>) of thetarget system for the file.  Using <code>"swap"</code> will force swappingendian-ness.
5000,An <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be written to the connection.
5001,See <code>writeLines</code>.
5002,A connection object, or a character string naming a file,or a raw vector.
5003,integer vector, giving the lengths in characters of(unterminated) character strings to be read or written.  Elementsmust be &gt;= 0 and not <code>NA</code>.
5004,logical: For <code>readChar</code>, should <code>nchars</code> beregarded as a number of bytes not characters in a multi-bytelocale?  For <code>writeChar</code>, see <code>writeLines</code>.
5005,A character vector to be written to the connection, atleast as long as <code>nchars</code>.
5006,‘end of string’: character string.  The terminatorto be written after each string, followed by an ASCII <code>nul</code>;use <code>NULL</code> for no terminator at all.
5007,the string printed when prompting the user for input.Should usually end with a space <code>" "</code>.
5008,a connection object or a character string.
5009,integer.  The (maximal) number of lines toread.  Negative values indicate that one should read up to the end ofinput on the connection.
5010,logical.  Is it OK to reach the end of the connection before<code>n &gt; 0</code> lines are read?  If not, an error will be generated.
5011,logical.  Warn if a text file is missing a final EOL or ifthere are embedded nuls in the file.
5012,encoding to be assumed for input strings.  It isused to mark character strings as known to be inLatin-1 or UTF-8: it is not used to re-encode the input.  To do thelatter, specify the encoding as part of the connection <code>con</code> orvia <code>options(encoding=)</code>: see the examples.
5013,logical: should nuls be skipped?
5014,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to serialize.
5015,a connection or the name of the file where the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectis saved to or read from.
5016,a logical.  If <code>TRUE</code> or <code>NA</code>, an ASCIIrepresentation is written; otherwise (default), a binary one is used.See the comments in the help for <code>save</code>.
5017,the workspace format version to use.  <code>NULL</code>specifies the current default version (3). The only other supportedvalue is 2, the default from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.4.0 to <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.5.0.
5018,a logical specifying whether saving to a named file isto use <code>"gzip"</code> compression, or one of <code>"gzip"</code>,<code>"bzip2"</code> or <code>"xz"</code> to indicate the type of compression tobe used.  Ignored if <code>file</code> is a connection.
5019,a hook function for handling reference objects.
5020,A length-one character vector giving the path to thefile.  Tilde-expansion is performed where supported.
5021,all the arguments to be passed.
5022,a function of the appropriate arity (binary for<code>Reduce</code>, unary for <code>Filter</code>, <code>Find</code> and<code>Position</code>, <i>k</i>-ary for <code>Map</code> if this is called with<i>k</i> arguments).  An arbitrary predicate function for<code>Negate</code>.
5023,a vector.
5024,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of the same kind as the elements of<code>x</code>.
5025,a logical indicating whether to proceed from left toright (default) or from right to left.
5026,a logical indicating whether the successive reducecombinations should be accumulated.  By default, only the finalcombination is used.
5027,the value to be returned in the case when“no match” (no element satisfying the predicate) is found.
5028,vectors.
5029,Object to finalize.  Must be an environment or an external pointer.
5030,Function to call on finalization. Must accept a single argument,which will be the object to finalize.
5031,logical: should the finalizer be run if the object isstill uncollected at the end of the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> session?
5032,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
5033,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
5034,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
5035,logical.  Should Perl-compatible regexps be used?
5036,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
5037,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
5038,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
5039,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
5040,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
5041,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
5042,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
5043,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
5044,logical.  Should Perl-compatible regexps be used?
5045,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
5046,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
5047,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
5048,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
5049,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
5050,string or namespace environment.
5051,logical.
5052,environment.
5053,namespace environment.
5054,character vector.
5055,optional character vector.
5056,namespace environment.
5057,string naming the package/namespace to load.
5058,vector of package names parallel to <code>generics</code>.
5059,character vector specifying library.
5060,logical.
5061,character.
5062,character.
5063,environment.
5064,a 3-column character matrix.
5065,character.
5066,any object.
5067,character arguments.
5068,the methods table name.
5069,symbol: name of namespace
5070,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
5071,string or namespace environment.
5072,logical.
5073,environment.
5074,namespace environment.
5075,character vector.
5076,optional character vector.
5077,namespace environment.
5078,string naming the package/namespace to load.
5079,vector of package names parallel to <code>generics</code>.
5080,character vector specifying library.
5081,logical.
5082,character.
5083,character.
5084,environment.
5085,a 3-column character matrix.
5086,character.
5087,any object.
5088,character arguments.
5089,the methods table name.
5090,symbol: name of namespace
5091,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
5092,a character vector
5093,an object with match data
5094,a logical: if <code>TRUE</code>, extract or replace thenon-matched substrings.
5095,an object with suitable replacement values for thematched or non-matched substrings (see <code>Details</code>).
5096,a character vector
5097,an object with match data
5098,a logical: if <code>TRUE</code>, extract or replace thenon-matched substrings.
5099,an object with suitable replacement values for thematched or non-matched substrings (see <code>Details</code>).
5100,the objects to be removed, as names (unquoted) orcharacter strings (quoted).
5101,a character vector naming objects to be removed.
5102,where to do the removal.  By default, uses thecurrent environment.  See ‘details’ for other possibilities.
5103,the <code>environment</code> to use.  See ‘details’.
5104,should the enclosing frames of the environment beinspected?
5105,the function that is to be invoked each time a top-level taskis successfully completed.  This is called with 5 or 4 argumentsdepending on whether <code>data</code> is specified or not, respectively.The return value should be a logical value indicating whether tokeep the callback in the list of active callbacks or discard it.
5106,if specified, this is the 5-th argument in the call to thecallback function <code>f</code>.
5107,a string or an integer identifying the element in theinternal callback list to be removed.Integer indices are 1-based, i.e the first element is 1.The names of currently registered handlers is availableusing <code>getTaskCallbackNames</code>and is also returned in a call to <code>addTaskCallback</code>.
5108,character: names to be used.
5109,a vector (of any mode including a <code>list</code>) or a factor or (for<code>rep</code> only) a <code>POSIXct</code> or <code>POSIXlt</code> or <code>Date</code>object; or an S4 object containing such an object.
5110,further arguments to be passed to or from other methods.For the internal default method these can include:

5111,an integer-valued vector giving the(non-negative) number of times to repeat each element if oflength <code>length(x)</code>, or to repeat the whole vector if oflength 1.  Negative or <code>NA</code> values are an error.  A<code>double</code> vector is accepted, other inputs being coerced toan integer or double vector.
5112,non-negative integer.  The desired length of theoutput vector.  Other inputs will be coerced to a doublevector and the first element taken.  Ignored if <code>NA</code> or invalid.
5113,non-negative integer.  Each element of <code>x</code>is repeated <code>each</code> times.  Other inputs will be coerced toan integer or double vector and the first element taken.  Treated as<code>1</code> if <code>NA</code> or invalid.
5114,see <code>...</code> above.
5115,a vector (of any mode including a <code>list</code>) or a factor or (for<code>rep</code> only) a <code>POSIXct</code> or <code>POSIXlt</code> or <code>Date</code>object; or an S4 object containing such an object.
5116,further arguments to be passed to or from other methods.For the internal default method these can include:

5117,an integer-valued vector giving the(non-negative) number of times to repeat each element if oflength <code>length(x)</code>, or to repeat the whole vector if oflength 1.  Negative or <code>NA</code> values are an error.  A<code>double</code> vector is accepted, other inputs being coerced toan integer or double vector.
5118,non-negative integer.  The desired length of theoutput vector.  Other inputs will be coerced to a doublevector and the first element taken.  Ignored if <code>NA</code> or invalid.
5119,non-negative integer.  Each element of <code>x</code>is repeated <code>each</code> times.  Other inputs will be coerced toan integer or double vector and the first element taken.  Treated as<code>1</code> if <code>NA</code> or invalid.
5120,see <code>...</code> above.
5121,a vector (of any mode including a <code>list</code>) or a factor or (for<code>rep</code> only) a <code>POSIXct</code> or <code>POSIXlt</code> or <code>Date</code>object; or an S4 object containing such an object.
5122,further arguments to be passed to or from other methods.For the internal default method these can include:

5123,an integer-valued vector giving the(non-negative) number of times to repeat each element if oflength <code>length(x)</code>, or to repeat the whole vector if oflength 1.  Negative or <code>NA</code> values are an error.  A<code>double</code> vector is accepted, other inputs being coerced toan integer or double vector.
5124,non-negative integer.  The desired length of theoutput vector.  Other inputs will be coerced to a doublevector and the first element taken.  Ignored if <code>NA</code> or invalid.
5125,non-negative integer.  Each element of <code>x</code>is repeated <code>each</code> times.  Other inputs will be coerced toan integer or double vector and the first element taken.  Treated as<code>1</code> if <code>NA</code> or invalid.
5126,see <code>...</code> above.
5127,date-time or date objects.
5128,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
5129,character string.  Units in which the results aredesired.  Can be abbreviated.
5130,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
5131,character string or numeric value specifying a time interval.
5132,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
5133,an object inheriting from class <code>"difftime"</code>.
5134,arguments to be passed to or from other methods.
5135,a vector (of any mode including a <code>list</code>) or a factor or (for<code>rep</code> only) a <code>POSIXct</code> or <code>POSIXlt</code> or <code>Date</code>object; or an S4 object containing such an object.
5136,further arguments to be passed to or from other methods.For the internal default method these can include:

5137,an integer-valued vector giving the(non-negative) number of times to repeat each element if oflength <code>length(x)</code>, or to repeat the whole vector if oflength 1.  Negative or <code>NA</code> values are an error.  A<code>double</code> vector is accepted, other inputs being coerced toan integer or double vector.
5138,non-negative integer.  The desired length of theoutput vector.  Other inputs will be coerced to a doublevector and the first element taken.  Ignored if <code>NA</code> or invalid.
5139,non-negative integer.  Each element of <code>x</code>is repeated <code>each</code> times.  Other inputs will be coerced toan integer or double vector and the first element taken.  Treated as<code>1</code> if <code>NA</code> or invalid.
5140,see <code>...</code> above.
5141,a vector (of any mode including a <code>list</code>) or a factor or (for<code>rep</code> only) a <code>POSIXct</code> or <code>POSIXlt</code> or <code>Date</code>object; or an S4 object containing such an object.
5142,further arguments to be passed to or from other methods.For the internal default method these can include:

5143,an integer-valued vector giving the(non-negative) number of times to repeat each element if oflength <code>length(x)</code>, or to repeat the whole vector if oflength 1.  Negative or <code>NA</code> values are an error.  A<code>double</code> vector is accepted, other inputs being coerced toan integer or double vector.
5144,non-negative integer.  The desired length of theoutput vector.  Other inputs will be coerced to a doublevector and the first element taken.  Ignored if <code>NA</code> or invalid.
5145,non-negative integer.  Each element of <code>x</code>is repeated <code>each</code> times.  Other inputs will be coerced toan integer or double vector and the first element taken.  Treated as<code>1</code> if <code>NA</code> or invalid.
5146,see <code>...</code> above.
5147,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
5148,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
5149,a vector (of any mode including a <code>list</code>) or a factor or (for<code>rep</code> only) a <code>POSIXct</code> or <code>POSIXlt</code> or <code>Date</code>object; or an S4 object containing such an object.
5150,further arguments to be passed to or from other methods.For the internal default method these can include:

5151,an integer-valued vector giving the(non-negative) number of times to repeat each element if oflength <code>length(x)</code>, or to repeat the whole vector if oflength 1.  Negative or <code>NA</code> values are an error.  A<code>double</code> vector is accepted, other inputs being coerced toan integer or double vector.
5152,non-negative integer.  The desired length of theoutput vector.  Other inputs will be coerced to a doublevector and the first element taken.  Ignored if <code>NA</code> or invalid.
5153,non-negative integer.  Each element of <code>x</code>is repeated <code>each</code> times.  Other inputs will be coerced toan integer or double vector and the first element taken.  Treated as<code>1</code> if <code>NA</code> or invalid.
5154,see <code>...</code> above.
5155,a vector (of any mode including a <code>list</code>) or a factor or (for<code>rep</code> only) a <code>POSIXct</code> or <code>POSIXlt</code> or <code>Date</code>object; or an S4 object containing such an object.
5156,further arguments to be passed to or from other methods.For the internal default method these can include:

5157,an integer-valued vector giving the(non-negative) number of times to repeat each element if oflength <code>length(x)</code>, or to repeat the whole vector if oflength 1.  Negative or <code>NA</code> values are an error.  A<code>double</code> vector is accepted, other inputs being coerced toan integer or double vector.
5158,non-negative integer.  The desired length of theoutput vector.  Other inputs will be coerced to a doublevector and the first element taken.  Ignored if <code>NA</code> or invalid.
5159,non-negative integer.  Each element of <code>x</code>is repeated <code>each</code> times.  Other inputs will be coerced toan integer or double vector and the first element taken.  Treated as<code>1</code> if <code>NA</code> or invalid.
5160,see <code>...</code> above.
5161,A length-one logical vector that is not <code>NA</code>.Conditions of length greater than one are currently accepted with awarning, but only the first element is used.  An error is signalledinstead when the environment variable <span class="env">_R_CHECK_LENGTH_1_CONDITION_</span>is set to true.  Other types are coerced to logicalif possible, ignoring any class.
5162,A syntactical name for a variable.
5163,An expression evaluating to a vector (including a list andan expression) or to a pairlist or <code>NULL</code>.  Afactor value will be coerced to a character vector.  As from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>4.0.0 this can be a long vector.
5164,An <em>expression</em> in a formal sense.  This is either asimple expression or a so-called <em>compound expression</em>, usuallyof the form <code>{ expr1 ; expr2 }</code>.
5165,vector
5166,an index vector
5167,replacement values
5168,a vector (atomic or list) or an <code>expression</code>object.  Other objects (including classed objects) will be coercedby <code>base::as.list</code>.
5169,the function to be applied to each element of <code>X</code>:see ‘Details’.  In the case of functions like<code>+</code>, <code>%*%</code>, the function name must be backquoted or quoted.
5170,optional arguments to <code>FUN</code>.
5171,logical or character string; should the result besimplified to a vector, matrix or higher dimensional array ifpossible?  For <code>sapply</code> it must be named and not abbreviated.The default value, <code>TRUE</code>, returns a vector or matrix if appropriate,whereas if <code>simplify = "array"</code> the result may be an<code>array</code> of “rank”(<i>=</i><code>length(dim(.))</code>) one higher than the resultof <code>FUN(X[[i]])</code>.
5172,logical; if <code>TRUE</code> and if <code>X</code> is character,use <code>X</code> as <code>names</code> for the result unless it had namesalready.  Since this argument follows <code>...</code> its name cannotbe abbreviated.
5173,a (generalized) vector; a template for the returnvalue from FUN.  See ‘Details’.
5174,integer: the number of replications.
5175,the expression (a language object, usually a call)to evaluate repeatedly.
5176,a list, typically returned from <code>lapply()</code>.
5177,logical; if true, <code>simplify2array()</code> will produce a(“higher rank”) array when appropriate, whereas<code>higher = FALSE</code> would return a matrix (or vector) only.These two cases correspond to <code>sapply(*, simplify = "array")</code> or<code>simplify = TRUE</code>, respectively.
5178,the name of a package, given as a name orliteral character string, or a character string, depending onwhether <code>character.only</code> is <code>FALSE</code> (default) or<code>TRUE</code>.
5179,the position on the search list at which to attach theloaded namespace.  Can also be the name of a position on the currentsearch list as given by <code>search()</code>.
5180,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.Non-existent library trees are silently ignored.
5181,a logical indicating whether <code>package</code> or<code>help</code> can be assumed to be character strings.
5182,logical.  If it is <code>TRUE</code>,  <code>FALSE</code> or<code>TRUE</code> is returned to indicate success.
5183,logical.  If <code>TRUE</code>, warnings areprinted about <code>conflicts</code> from attaching the newpackage.  A conflict is a function masking a function,or a non-function masking a non-function. The default is <code>TRUE</code>unless specified as <code>FALSE</code> in the <code>conflicts.policy</code> option.
5184,a logical.  If <code>TRUE</code>, additional diagnostics areprinted.
5185,a logical.  If <code>TRUE</code>, no message confirmingpackage attaching is printed, and most often, no errors/warnings areprinted if package attaching fails.
5186,character string naming a package.
5187,character vector of names of objects that can maskobjects on the search path without signaling an error when strictconflict checking is enabled
5188,character vector of names of objects toexclude or include in the attached frame. Only one of these argumentsmay be used in a call to <code>library</code> or <code>require</code>.
5189,logical specifying whether required packageslisted in the <code>Depends</code> clause of the <code>DESCRIPTION</code> fileshould be attached automatically.
5190,string or name space object.
5191,integer specifying position to attach.
5192,<code>NULL</code> or a character vector of dependencies to berecorded in object <code>.Depends</code> in the package.
5193,string naming the package/name space to load.
5194,character vector specifying library search path.
5195,Now ignored except during package installation.
5196,Ignored except during package installation.
5197,logical; if true, stop just after loading code.
5198,<code>NULL</code> or a version specification (a listwith components <code>op</code> and <code>version</code>).
5199,logical: should progress and error messages be suppressed?
5200,string or ‘name’, see <code>as.symbol</code>,of a package, e.g., <code>"stats"</code>.
5201,character vectors; see <code>library</code>.
5202,further arguments to be passed to <code>loadNamespace</code>.
5203,a condition object.
5204,call expression.
5205,a condition object.
5206,expression to be evaluated.
5207,expression to be evaluated before returning or exiting.
5208,function.
5209,character string.
5210,character string.
5211,character string naming a restart.
5212,restart object.
5213,object.
5214,character string naming a condition class.
5215,additional arguments; see details below.
5216,a condition object.
5217,call expression.
5218,a condition object.
5219,expression to be evaluated.
5220,expression to be evaluated before returning or exiting.
5221,function.
5222,character string.
5223,character string.
5224,character string naming a restart.
5225,restart object.
5226,object.
5227,character string naming a condition class.
5228,additional arguments; see details below.
5229,An R object, not a function or environment or <code>NULL</code>.
5230,A value as returned by <code>tracemem</code> or <code>retracemem</code>.
5231,Empty or one or more name or name=expression terms.
5232,An expression.
5233,An expression.
5234,the name, possibly <code>quote()</code>d, of a functionto be traced or untraced.  For <code>untrace</code> or for <code>trace</code>with more than one argument, more than one name can be given in thequoted form, and the same action will be applied to each one.  For“hidden” functions such as S3 methods in a namespace,<code>where = *</code> typically needs to be specified as well.
5235,either a function or an unevaluated expression.  Thefunction will be called or the expression will be evaluated eitherat the beginning of the call, or before those steps in the callspecified by the argument <code>at</code>.See the details section.
5236,either a <code>function</code> or an unevaluated expression.  Thefunction will be called or the expression will be evaluated onexiting the function.See the details section.
5237,optional numeric vector or list.  If supplied, <code>tracer</code>will be called just before the corresponding step in the body of thefunction.See the details section. 
5238,If <code>TRUE</code> (as per default), a descriptive line isprinted before any trace expression is evaluated.
5239, If this argument is supplied, it should be asignature for a method for function <code>what</code>.  In this case, themethod, and <em>not</em> the function itself, is traced.
5240, For complicated tracing, such as tracing within a loopinside the function, you will need to insert the desired calls byediting the body of the function.  If so, supply the <code>edit</code>argument either as <code>TRUE</code>, or as the name of the editor youwant to use.  Then <code>trace()</code> will call <code>edit</code> anduse the version of the function after you edit it.  See the detailssection for additional information.
5241,where to look for the function to betraced; by default, the top-level environment of the call to<code>trace</code>.

5242,An important use of this argument is to trace functions from apackage which are “hidden” or called from another package.The namespace mechanism imports the functions to be called (with theexception of functions in the base package).  The functions beingcalled are <em>not</em> the same objects seen from the top-level (ingeneral, the imported packages may not even be attached).Therefore, you must ensure that the correct versions are beingtraced.  The way to do this is to set argument <code>where</code> to afunction in the namespace (or that namespace).  The tracingcomputations will then start looking in the environment of thatfunction (which will be the namespace of the corresponding package).(Yes, it's subtle, but the semantics here are central to hownamespaces work in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>.)
5243,logical; a call to the support function <code>tracingState</code> returns <code>TRUE</code>if tracing is globally turned on, <code>FALSE</code> otherwise.  Anargument of one or the other of those values sets the state.  If thetracing state is <code>FALSE</code>, none of the trace actions willactually occur (used, for example, by debugging functions to shutoff tracing during debugging).
5244,arguments to the support function <code>.doTrace</code>, calls towhich are inserted into the modified function or method:<code>expr</code> is the tracing action (such as a call to<code>browser()</code>), and <code>msg</code> is a string identifying theplace where the trace action occurs.
5245,If <code>returnValue</code> finds no return value (e.g.a function exited because of an error, restart or as a resultof evaluating a return from a caller function), it will return<code>default</code> instead.
5246,a vector or another object for which reversal is defined.
5247,a vector or another object for which reversal is defined.
5248,a vector (atomic, not a list) for <code>rle()</code>;an object of class <code>"rle"</code> for <code>inverse.rle()</code>.
5249,further arguments; ignored here.
5250,number of significant digits for printing, see<code>print.default</code>.
5251,character string, prepended to each printed line.
5252,the objects to be removed, as names (unquoted) orcharacter strings (quoted).
5253,a character vector naming objects to be removed.
5254,where to do the removal.  By default, uses thecurrent environment.  See ‘details’ for other possibilities.
5255,the <code>environment</code> to use.  See ‘details’.
5256,should the enclosing frames of the environment beinspected?
5257,character or <code>NULL</code>.  If <code>kind</code> is a characterstring, set <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s RNG to the kind desired.  Use <code>"default"</code> toreturn to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  See ‘Details’ for theinterpretation of <code>NULL</code>.
5258,character string or <code>NULL</code>.  If it is a characterstring, set the method of Normal generation.  Use <code>"default"</code>to return to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  <code>NULL</code> makes no change.
5259,character string or <code>NULL</code>.  If it is a characterstring, set the method of discrete uniform generation (used in <code>sample</code>, for instance).  Use <code>"default"</code> to return to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  <code>NULL</code> makes no change.
5260,a single value, interpreted as an integer, or <code>NULL</code>(see ‘Details’).
5261,a character string containing a version number,e.g., <code>"1.6.2"</code>.  The default RNG configuration of the current<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version is used if <code>vstr</code> is greater than the current version.
5262,integer code in <code>0:k</code> for the above <code>kind</code>.
5263,integers.  See the details for how many are required(which depends on <code>rng.kind</code>).
5264,character or <code>NULL</code>.  If <code>kind</code> is a characterstring, set <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s RNG to the kind desired.  Use <code>"default"</code> toreturn to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  See ‘Details’ for theinterpretation of <code>NULL</code>.
5265,character string or <code>NULL</code>.  If it is a characterstring, set the method of Normal generation.  Use <code>"default"</code>to return to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  <code>NULL</code> makes no change.
5266,character string or <code>NULL</code>.  If it is a characterstring, set the method of discrete uniform generation (used in <code>sample</code>, for instance).  Use <code>"default"</code> to return to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  <code>NULL</code> makes no change.
5267,a single value, interpreted as an integer, or <code>NULL</code>(see ‘Details’).
5268,a character string containing a version number,e.g., <code>"1.6.2"</code>.  The default RNG configuration of the current<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version is used if <code>vstr</code> is greater than the current version.
5269,integer code in <code>0:k</code> for the above <code>kind</code>.
5270,integers.  See the details for how many are required(which depends on <code>rng.kind</code>).
5271,a numeric vector.  Or, for <code>round</code> and <code>signif</code>, acomplex vector.
5272,integer indicating the number of decimal places(<code>round</code>) or significant digits (<code>signif</code>) to be used.Negative values are allowed (see ‘Details’).
5273,arguments to be passed to methods.
5274,an object inheriting from <code>"POSIXt"</code> or <code>"Date"</code>.
5275,one of the units listed.  Can be abbreviated.
5276,arguments to be passed to or from other methods, notably<code>digits</code> for <code>round</code>.
5277,an object inheriting from <code>"POSIXt"</code> or <code>"Date"</code>.
5278,one of the units listed.  Can be abbreviated.
5279,arguments to be passed to or from other methods, notably<code>digits</code> for <code>round</code>.
5280,a matrix-like object, that is one with a two-dimensional<code>dim</code>.
5281,a matrix dimension, i.e., an integer valued numeric vector oflength two (with non-negative entries).
5282,a logical value indicating whether the value shouldbe returned as a factor of row labels (created if necessary)rather than as numbers.
5283,object of class <code>"data.frame"</code>, or any other class forwhich a method has been defined.
5284,<code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the specified row names, i.e.,<code>value</code>, are invalid, e.g., duplicated or <code>NA</code>.  The default(is back compatible), <code>FALSE</code>, will signal an error, where<code>NA</code> will “automatic” row names and <code>TRUE</code> will call<code>make.names(value, unique=TRUE)</code> for constructing validnames.
5285,an object to be coerced to character unless an integervector.  It should have (after coercion) the same length as thenumber of rows of <code>x</code> with no duplicated nor missing values.<code>NULL</code> is also allowed: see ‘Details’.
5286,object of class <code>"data.frame"</code>, or any other class forwhich a method has been defined.
5287,<code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the specified row names, i.e.,<code>value</code>, are invalid, e.g., duplicated or <code>NA</code>.  The default(is back compatible), <code>FALSE</code>, will signal an error, where<code>NA</code> will “automatic” row names and <code>TRUE</code> will call<code>make.names(value, unique=TRUE)</code> for constructing validnames.
5288,an object to be coerced to character unless an integervector.  It should have (after coercion) the same length as thenumber of rows of <code>x</code> with no duplicated nor missing values.<code>NULL</code> is also allowed: see ‘Details’.
5289,object of class <code>"data.frame"</code>, or any other class forwhich a method has been defined.
5290,<code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the specified row names, i.e.,<code>value</code>, are invalid, e.g., duplicated or <code>NA</code>.  The default(is back compatible), <code>FALSE</code>, will signal an error, where<code>NA</code> will “automatic” row names and <code>TRUE</code> will call<code>make.names(value, unique=TRUE)</code> for constructing validnames.
5291,an object to be coerced to character unless an integervector.  It should have (after coercion) the same length as thenumber of rows of <code>x</code> with no duplicated nor missing values.<code>NULL</code> is also allowed: see ‘Details’.
5292,object of class <code>"data.frame"</code>, or any other class forwhich a method has been defined.
5293,<code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the specified row names, i.e.,<code>value</code>, are invalid, e.g., duplicated or <code>NA</code>.  The default(is back compatible), <code>FALSE</code>, will signal an error, where<code>NA</code> will “automatic” row names and <code>TRUE</code> will call<code>make.names(value, unique=TRUE)</code> for constructing validnames.
5294,an object to be coerced to character unless an integervector.  It should have (after coercion) the same length as thenumber of rows of <code>x</code> with no duplicated nor missing values.<code>NULL</code> is also allowed: see ‘Details’.
5295,object of class <code>"data.frame"</code>, or any other class forwhich a method has been defined.
5296,<code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the specified row names, i.e.,<code>value</code>, are invalid, e.g., duplicated or <code>NA</code>.  The default(is back compatible), <code>FALSE</code>, will signal an error, where<code>NA</code> will “automatic” row names and <code>TRUE</code> will call<code>make.names(value, unique=TRUE)</code> for constructing validnames.
5297,an object to be coerced to character unless an integervector.  It should have (after coercion) the same length as thenumber of rows of <code>x</code> with no duplicated nor missing values.<code>NULL</code> is also allowed: see ‘Details’.
5298,object of class <code>"data.frame"</code>, or any other class forwhich a method has been defined.
5299,<code>logical</code>, i.e., one of <code>FALSE, NA, TRUE</code>,indicating what should happen if the specified row names, i.e.,<code>value</code>, are invalid, e.g., duplicated or <code>NA</code>.  The default(is back compatible), <code>FALSE</code>, will signal an error, where<code>NA</code> will “automatic” row names and <code>TRUE</code> will call<code>make.names(value, unique=TRUE)</code> for constructing validnames.
5300,an object to be coerced to character unless an integervector.  It should have (after coercion) the same length as thenumber of rows of <code>x</code> with no duplicated nor missing values.<code>NULL</code> is also allowed: see ‘Details’.
5301,an array of two or more dimensions, containing numeric,complex, integer or logical values, or a numeric data frame.  For<code>.colSums()</code> etc, a numeric, integer or logical matrix (orvector of length <code>m * n</code>).
5302,logical.  Should missing values (including <code>NaN</code>)be omitted from the calculations?
5303,integer: Which dimensions are regarded as ‘rows’ or‘columns’ to sum over.  For <code>row*</code>, the sum or mean isover dimensions <code>dims+1, ...</code>; for <code>col*</code> it is overdimensions <code>1:dims</code>.
5304,the dimensions of the matrix <code>x</code> for<code>.colSums()</code> etc.
5305,a matrix-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, with at least two dimensions for<code>colnames</code>.
5306,logical. If <code>FALSE</code> and names are <code>NULL</code>, names are created.
5307,for created names.
5308,a valid value for that component of<code>dimnames(x)</code>.  For a matrix or array this is either<code>NULL</code> or a character vector of non-zero length equal to theappropriate dimension.
5309,a matrix-like <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, with at least two dimensions for<code>colnames</code>.
5310,logical. If <code>FALSE</code> and names are <code>NULL</code>, names are created.
5311,for created names.
5312,a valid value for that component of<code>dimnames(x)</code>.  For a matrix or array this is either<code>NULL</code> or a character vector of non-zero length equal to theappropriate dimension.
5313,a matrix, data frame or vector of numeric data.  Missingvalues are allowed.  A numeric vector will be treated as a column vector.
5314,a vector or factor giving the grouping, with one elementper row of <code>x</code>.  Missing values will be treated as anothergroup and a warning will be given.
5315,if <code>TRUE</code>, then the result will be in order of<code>sort(unique(group))</code>, if <code>FALSE</code>, it will be in the orderthat groups were encountered.
5316,logical (<code>TRUE</code> or <code>FALSE</code>).  Should <code>NA</code>(including <code>NaN</code>) values be discarded?
5317,other arguments to be passed to or from methods
5318,a matrix, data frame or vector of numeric data.  Missingvalues are allowed.  A numeric vector will be treated as a column vector.
5319,a vector or factor giving the grouping, with one elementper row of <code>x</code>.  Missing values will be treated as anothergroup and a warning will be given.
5320,if <code>TRUE</code>, then the result will be in order of<code>sort(unique(group))</code>, if <code>FALSE</code>, it will be in the orderthat groups were encountered.
5321,logical (<code>TRUE</code> or <code>FALSE</code>).  Should <code>NA</code>(including <code>NaN</code>) values be discarded?
5322,other arguments to be passed to or from methods
5323,a matrix, data frame or vector of numeric data.  Missingvalues are allowed.  A numeric vector will be treated as a column vector.
5324,a vector or factor giving the grouping, with one elementper row of <code>x</code>.  Missing values will be treated as anothergroup and a warning will be given.
5325,if <code>TRUE</code>, then the result will be in order of<code>sort(unique(group))</code>, if <code>FALSE</code>, it will be in the orderthat groups were encountered.
5326,logical (<code>TRUE</code> or <code>FALSE</code>).  Should <code>NA</code>(including <code>NaN</code>) values be discarded?
5327,other arguments to be passed to or from methods
5328,an array of two or more dimensions, containing numeric,complex, integer or logical values, or a numeric data frame.  For<code>.colSums()</code> etc, a numeric, integer or logical matrix (orvector of length <code>m * n</code>).
5329,logical.  Should missing values (including <code>NaN</code>)be omitted from the calculations?
5330,integer: Which dimensions are regarded as ‘rows’ or‘columns’ to sum over.  For <code>row*</code>, the sum or mean isover dimensions <code>dims+1, ...</code>; for <code>col*</code> it is overdimensions <code>1:dims</code>.
5331,the dimensions of the matrix <code>x</code> for<code>.colSums()</code> etc.
5332,either a vector of one or more elements from which to choose,or a positive integer.  See ‘Details.’
5333,a positive number, the number of items to choose from.  See‘Details.’
5334,a non-negative integer giving the number of items to choose.
5335,should sampling be with replacement?
5336,a vector of probability weights for obtaining the elementsof the vector being sampled.
5337,<code>logical</code> indicating if the hash-version ofthe algorithm should be used.  Can only be used for <code>replace =      FALSE</code>, <code>prob = NULL</code>, and <code>size &lt;= n/2</code>, and reallyshould be used for large <code>n</code>, as <code>useHash=FALSE</code> will usememory proportional to <code>n</code>.
5338,either a vector of one or more elements from which to choose,or a positive integer.  See ‘Details.’
5339,a positive number, the number of items to choose from.  See‘Details.’
5340,a non-negative integer giving the number of items to choose.
5341,should sampling be with replacement?
5342,a vector of probability weights for obtaining the elementsof the vector being sampled.
5343,<code>logical</code> indicating if the hash-version ofthe algorithm should be used.  Can only be used for <code>replace =      FALSE</code>, <code>prob = NULL</code>, and <code>size &lt;= n/2</code>, and reallyshould be used for large <code>n</code>, as <code>useHash=FALSE</code> will usememory proportional to <code>n</code>.
5344,a vector (atomic or list) or an <code>expression</code>object.  Other objects (including classed objects) will be coercedby <code>base::as.list</code>.
5345,the function to be applied to each element of <code>X</code>:see ‘Details’.  In the case of functions like<code>+</code>, <code>%*%</code>, the function name must be backquoted or quoted.
5346,optional arguments to <code>FUN</code>.
5347,logical or character string; should the result besimplified to a vector, matrix or higher dimensional array ifpossible?  For <code>sapply</code> it must be named and not abbreviated.The default value, <code>TRUE</code>, returns a vector or matrix if appropriate,whereas if <code>simplify = "array"</code> the result may be an<code>array</code> of “rank”(<i>=</i><code>length(dim(.))</code>) one higher than the resultof <code>FUN(X[[i]])</code>.
5348,logical; if <code>TRUE</code> and if <code>X</code> is character,use <code>X</code> as <code>names</code> for the result unless it had namesalready.  Since this argument follows <code>...</code> its name cannotbe abbreviated.
5349,a (generalized) vector; a template for the returnvalue from FUN.  See ‘Details’.
5350,integer: the number of replications.
5351,the expression (a language object, usually a call)to evaluate repeatedly.
5352,a list, typically returned from <code>lapply()</code>.
5353,logical; if true, <code>simplify2array()</code> will produce a(“higher rank”) array when appropriate, whereas<code>higher = FALSE</code> would return a matrix (or vector) only.These two cases correspond to <code>sapply(*, simplify = "array")</code> or<code>simplify = TRUE</code>, respectively.
5354,the names of the objects to be saved (as symbols orcharacter strings).
5355,A character vector containing the names of objects to besaved.
5356,a (writable binary-mode) connection or the name of thefile where the data will be saved (when tilde expansionis done).  Must be a file name for <code>save.image</code> or<code>version = 1</code>.
5357,if <code>TRUE</code>, an ASCII representation of the data iswritten.  The default value of <code>ascii</code> is <code>FALSE</code> whichleads to a binary file being written.  If <code>NA</code> and<code>version &gt;= 2</code>, a different ASCII representation is used whichwrites double/complex numbers as binary fractions.
5358,the workspace format version to use.  <code>NULL</code>specifies the current default format (3).  Version 1 was the defaultfrom <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 0.99.0 to <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.3.1 and version 2 from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.4.0 to 3.5.0.Version 3 is supported from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.5.0.
5359,environment to search for objects to be saved.
5360,logical or character string specifying whether savingto a named file is to use compression.  <code>TRUE</code> corresponds to<code>gzip</code> compression, and character strings <code>"gzip"</code>,<code>"bzip2"</code> or <code>"xz"</code> specify the type ofcompression.  Ignored when <code>file</code> is a connection andfor workspace format version 1.
5361,integer: the level of compression to beused.  Defaults to <code>6</code> for <code>gzip</code> compression and to<code>9</code> for <code>bzip2</code> or <code>xz</code> compression.  See thehelp for <code>file</code> for possible values and their merits.
5362,logical: should objects which are promises beforced before saving?
5363,logical: should the existence of the objects bechecked before starting to save (and in particular before openingthe file/connection)?  Does not apply to version 1 saves.
5364,logical.  If <code>TRUE</code>, a temporary file is used forcreating the saved workspace.  The temporary file is renamed to<code>file</code> if the save succeeds.  This preserves an existingworkspace <code>file</code> if the save fails, but at the cost of usingextra disk space during the save.
5365,the names of the objects to be saved (as symbols orcharacter strings).
5366,A character vector containing the names of objects to besaved.
5367,a (writable binary-mode) connection or the name of thefile where the data will be saved (when tilde expansionis done).  Must be a file name for <code>save.image</code> or<code>version = 1</code>.
5368,if <code>TRUE</code>, an ASCII representation of the data iswritten.  The default value of <code>ascii</code> is <code>FALSE</code> whichleads to a binary file being written.  If <code>NA</code> and<code>version &gt;= 2</code>, a different ASCII representation is used whichwrites double/complex numbers as binary fractions.
5369,the workspace format version to use.  <code>NULL</code>specifies the current default format (3).  Version 1 was the defaultfrom <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 0.99.0 to <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.3.1 and version 2 from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.4.0 to 3.5.0.Version 3 is supported from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.5.0.
5370,environment to search for objects to be saved.
5371,logical or character string specifying whether savingto a named file is to use compression.  <code>TRUE</code> corresponds to<code>gzip</code> compression, and character strings <code>"gzip"</code>,<code>"bzip2"</code> or <code>"xz"</code> specify the type ofcompression.  Ignored when <code>file</code> is a connection andfor workspace format version 1.
5372,integer: the level of compression to beused.  Defaults to <code>6</code> for <code>gzip</code> compression and to<code>9</code> for <code>bzip2</code> or <code>xz</code> compression.  See thehelp for <code>file</code> for possible values and their merits.
5373,logical: should objects which are promises beforced before saving?
5374,logical: should the existence of the objects bechecked before starting to save (and in particular before openingthe file/connection)?  Does not apply to version 1 saves.
5375,logical.  If <code>TRUE</code>, a temporary file is used forcreating the saved workspace.  The temporary file is renamed to<code>file</code> if the save succeeds.  This preserves an existingworkspace <code>file</code> if the save fails, but at the cost of usingextra disk space during the save.
5376,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to serialize.
5377,a connection or the name of the file where the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectis saved to or read from.
5378,a logical.  If <code>TRUE</code> or <code>NA</code>, an ASCIIrepresentation is written; otherwise (default), a binary one is used.See the comments in the help for <code>save</code>.
5379,the workspace format version to use.  <code>NULL</code>specifies the current default version (3). The only other supportedvalue is 2, the default from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.4.0 to <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.5.0.
5380,a logical specifying whether saving to a named file isto use <code>"gzip"</code> compression, or one of <code>"gzip"</code>,<code>"bzip2"</code> or <code>"xz"</code> to indicate the type of compression tobe used.  Ignored if <code>file</code> is a connection.
5381,a hook function for handling reference objects.
5382,a numeric matrix(like object).
5383,either a logical value or numeric-alike vector of lengthequal to the number of columns of <code>x</code>, where‘numeric-alike’ means that <code>as.numeric(.)</code> willbe applied successfully if <code>is.numeric(.)</code> is not true.
5384,either a logical value or a numeric-alike vector of lengthequal to the number of columns of <code>x</code>.
5385,a numeric matrix(like object).
5386,either a logical value or numeric-alike vector of lengthequal to the number of columns of <code>x</code>, where‘numeric-alike’ means that <code>as.numeric(.)</code> willbe applied successfully if <code>is.numeric(.)</code> is not true.
5387,either a logical value or a numeric-alike vector of lengthequal to the number of columns of <code>x</code>.
5388,the name of a file to read data values from.  If thespecified file is <code>""</code>, then input is taken from the keyboard(or whatever <code>stdin()</code> reads if input is redirected or<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> is embedded).(In this case input can be terminated by a blank line or an EOFsignal, <span class="samp">Ctrl-D</span> on Unix and <span class="samp">Ctrl-Z</span> on Windows.)

5389,Otherwise, the file name is interpreted <em>relative</em> to thecurrent working directory (given by <code>getwd()</code>),unless it specifies an <em>absolute</em> path.Tilde-expansion is performed where supported.When running <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> from a script, <code>file = "stdin"</code> can be used torefer to the process's <code>stdin</code> file stream.

5390,This can be a compressed file (see <code>file</code>).

5391,Alternatively, <code>file</code> can be a <code>connection</code>,which will be opened if necessary, and if so closed at the end ofthe function call.  Whatever mode the connection is opened in,any of LF, CRLF or CR will be accepted as the EOL marker for a lineand so will match <code>sep = "\n"</code>.

5392,<code>file</code> can also be a complete URL.  (For the supported URLschemes, see the ‘URLs’ section of the help for<code>url</code>.)

5393,To read a data file not in the current encoding (for example aLatin-1 file in a UTF-8 locale or conversely) use a<code>file</code> connection setting its <code>encoding</code> argument(or <code>scan</code>'s <code>fileEncoding</code> argument).
5394,the type of <code>what</code> gives the type of data tobe read.  (Here ‘type’ is used in the sense of<code>typeof</code>.)  The supported types are <code>logical</code>,<code>integer</code>, <code>numeric</code>, <code>complex</code>, <code>character</code>,<code>raw</code> and <code>list</code>.  If <code>what</code> is a list, it isassumed that the lines of the data file are records each containing<code>length(what)</code> items (‘fields’) and the list componentsshould have elements which are one of the first six (atomic)types listed or <code>NULL</code>, see section ‘Details’ below.
5395,the maximum number of data values to be read, or if<code>what</code> is a list, the maximum number of records to be read.  Ifomitted or not positive or an invalid value for an integer(and <code>nlines</code> is not set to a positive value), <code>scan</code> willread to the end of <code>file</code>.
5396,integer: the maximum number of data values to be read, defaulting tono limit.  Invalid values will be ignored.
5397,by default, scan expects to read ‘white-space’delimited input fields.  Alternatively, <code>sep</code> can be used tospecify a character which delimits fields.  A field is alwaysdelimited by an end-of-line marker unless it is quoted.

5398,If specified this should be the empty character string (the default)or <code>NULL</code> or a character string containing just one single-bytecharacter.
5399,the set of quoting characters as a single characterstring or <code>NULL</code>.  In a multibyte locale the quoting charactersmust be ASCII (single-byte).
5400,decimal point character.  This should be a character stringcontaining just one single-byte character.  (<code>NULL</code> and azero-length character vector are also accepted, and taken as thedefault.)
5401,the number of  lines of the input file to skip beforebeginning to read data values.
5402,if positive, the maximum number of lines of data to be read.
5403,character vector.  Elements of this vector are to beinterpreted as missing (<code>NA</code>) values.  Blank fields arealso considered to be missing values in logical, integer, numericand complex fields.  Note that the test happens <em>after</em> white space is stripped from the input, so <code>na.strings</code> values may need their own white space stripped in advance.
5404,logical: if <code>TRUE</code>, <code>scan</code> will flush to theend of the line after reading the last of the fields requested.This allows putting comments after the last field, but precludesputting more that one record on a line.
5405,logical: if <code>TRUE</code>, <code>scan</code> will implicitly addempty fields to any lines with fewer fields than implied by<code>what</code>.
5406,vector of logical value(s) corresponding to itemsin the <code>what</code> argument.  It is used only when <code>sep</code> hasbeen specified, and allows the stripping of leading and trailing‘white space’ from <code>character</code> fields (<code>numeric</code> fieldsare always stripped).  Note: white space inside quoted strings isnot stripped.

5407,If <code>strip.white</code> is of length 1, it applies to all fields;otherwise, if <code>strip.white[i]</code> is <code>TRUE</code> <em>and</em> the<code>i</code>-th field is of mode character (because <code>what[i]</code> is)then the leading and trailing unquoted white space from field <code>i</code> isstripped.
5408,logical: if <code>FALSE</code> (default), scan() will print aline, saying how many items have been read.
5409,logical: if <code>TRUE</code> blank lines in theinput are ignored, except when counting <code>skip</code> and <code>nlines</code>.
5410,logical.  Only used if <code>what</code> is a list.  If<code>FALSE</code>, all of a record must appear on one line (but more thanone record can appear on a single line).  Note that using <code>fill = TRUE</code>implies that a record will be terminated at the end of a line.
5411,character: a character vector of length onecontaining a single character or an empty string.  Use <code>""</code> toturn off the interpretation of comments altogether (the default).
5412,logical.  Should C-style escapes such as<span class="samp">\n</span> be processed (the default) or read verbatim?   Note that ifnot within quotes these could be interpreted as a delimiter (but notas a comment character).

5413,The escapes which are interpreted are the control characters<span class="samp">\a, \b, \f, \n, \r, \t, \v</span> and octal andhexadecimal representations like <span class="samp">\040</span> and <span class="samp">\0x2A</span>.  Anyother escaped character is treated as itself, including backslash.Note that Unicode escapes (starting <span class="samp">\u</span> or <span class="samp">\U</span>: seeQuotes) are never processed.
5414,character string: if non-empty declares theencoding used on a file (not a connection nor the keyboard) so thecharacter data can be re-encoded.  See the ‘Encoding’ sectionof the help for <code>file</code>, and the ‘R DataImport/Export Manual’.
5415,encoding to be assumed for input strings.  If thevalue is <code>"latin1"</code> or <code>"UTF-8"</code> it is used to markcharacter strings as known to be in Latin-1 or UTF-8: it is not usedto re-encode the input (see <code>fileEncoding</code>).See also ‘Details’.
5416,character string: if <code>file</code> is not supplied and this is,then data are read from the value of <code>text</code> via a text connection.
5417,logical: should nuls be skipped when reading characterfields?
5418,a connection.
5419,numeric.  A file position (relative to the originspecified by <code>origin</code>), or <code>NA</code>.
5420,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
5421,character string.  One of <code>"start"</code>, <code>"current"</code>,<code>"end"</code>: see ‘Details’.
5422,further arguments passed to or from other methods.
5423,a connection.
5424,numeric.  A file position (relative to the originspecified by <code>origin</code>), or <code>NA</code>.
5425,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
5426,character string.  One of <code>"start"</code>, <code>"current"</code>,<code>"end"</code>: see ‘Details’.
5427,further arguments passed to or from other methods.
5428,arguments passed to or from methods.
5429,the starting and (maximal) end values of thesequence.  Of length <code>1</code> unless just <code>from</code> is supplied asan unnamed argument.
5430,number: increment of the sequence.
5431,desired length of the sequence.  Anon-negative number, which for <code>seq</code> and <code>seq.int</code> will berounded up if fractional.
5432,take the length from the length of this argument.
5433,arguments passed to or from methods.
5434,the starting and (maximal) end values of thesequence.  Of length <code>1</code> unless just <code>from</code> is supplied asan unnamed argument.
5435,number: increment of the sequence.
5436,desired length of the sequence.  Anon-negative number, which for <code>seq</code> and <code>seq.int</code> will berounded up if fractional.
5437,take the length from the length of this argument.
5438,arguments passed to or from methods.
5439,the starting and (maximal) end values of thesequence.  Of length <code>1</code> unless just <code>from</code> is supplied asan unnamed argument.
5440,number: increment of the sequence.
5441,desired length of the sequence.  Anon-negative number, which for <code>seq</code> and <code>seq.int</code> will berounded up if fractional.
5442,take the length from the length of this argument.
5443,starting date.  Required
5444,end date.  Optional.
5445,increment of the sequence.  Optional.  See ‘Details’.
5446,integer, optional.  Desired length of the sequence.
5447,take the length from the length of this argument.
5448,arguments passed to or from other methods.
5449,arguments passed to or from methods.
5450,the starting and (maximal) end values of thesequence.  Of length <code>1</code> unless just <code>from</code> is supplied asan unnamed argument.
5451,number: increment of the sequence.
5452,desired length of the sequence.  Anon-negative number, which for <code>seq</code> and <code>seq.int</code> will berounded up if fractional.
5453,take the length from the length of this argument.
5454,arguments passed to or from methods.
5455,the starting and (maximal) end values of thesequence.  Of length <code>1</code> unless just <code>from</code> is supplied asan unnamed argument.
5456,number: increment of the sequence.
5457,desired length of the sequence.  Anon-negative number, which for <code>seq</code> and <code>seq.int</code> will berounded up if fractional.
5458,take the length from the length of this argument.
5459,starting date.  Required.
5460,end date.  Optional.
5461,increment of the sequence.  Optional.  See ‘Details’.
5462,integer, optional.  Desired length of the sequence.
5463,take the length from the length of this argument.
5464,arguments passed to or from other methods.
5465,coerced to a non-negative integer vector each element of whichspecifies the length of a sequence.
5466,coerced to an integer vector each element of whichspecifies the first element of a sequence.
5467,coerced to an integer vector each element of whichspecifies the step size between elements of a sequence.
5468,additional arguments passed to methods.
5469,coerced to a non-negative integer vector each element of whichspecifies the length of a sequence.
5470,coerced to an integer vector each element of whichspecifies the first element of a sequence.
5471,coerced to an integer vector each element of whichspecifies the step size between elements of a sequence.
5472,additional arguments passed to methods.
5473,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to serialize.
5474,an open connection or (for <code>serialize</code>)<code>NULL</code> or (for <code>unserialize</code>) a raw vector(see ‘Details’).
5475,a logical.  If <code>TRUE</code> or <code>NA</code>, an ASCIIrepresentation is written; otherwise (default) a binary one.See also the comments in the help for <code>save</code>.
5476,a logical: if a binary representation is used, should abig-endian one (XDR) be used?
5477,the workspace format version to use.  <code>NULL</code>specifies the current default version (3). The only other supportedvalue is 2, the default from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.4.0 to <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.5.0.
5478,a hook function for handling reference objects.
5479,character string.  A description of the connection:see ‘Details’.
5480,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
5481,logical.  See the ‘Blocking’ section.
5482,The name of the encoding to be assumed.  See the‘Encoding’ section.
5483,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
5484,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
5485,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
5486,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
5487,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
5488,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
5489,a filename within a zip file.
5490,character string.  Host name for the port.
5491,integer.  The TCP port number.
5492,logical.  Should the socket be a client or a server?
5493,a server socket listening for connections.
5494,a connection.
5495,character string.  Currently ignored.
5496,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
5497,arguments passed to or from other methods.
5498,character or <code>NULL</code>.  If <code>kind</code> is a characterstring, set <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s RNG to the kind desired.  Use <code>"default"</code> toreturn to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  See ‘Details’ for theinterpretation of <code>NULL</code>.
5499,character string or <code>NULL</code>.  If it is a characterstring, set the method of Normal generation.  Use <code>"default"</code>to return to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  <code>NULL</code> makes no change.
5500,character string or <code>NULL</code>.  If it is a characterstring, set the method of discrete uniform generation (used in <code>sample</code>, for instance).  Use <code>"default"</code> to return to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> default.  <code>NULL</code> makes no change.
5501,a single value, interpreted as an integer, or <code>NULL</code>(see ‘Details’).
5502,a character string containing a version number,e.g., <code>"1.6.2"</code>.  The default RNG configuration of the current<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version is used if <code>vstr</code> is greater than the current version.
5503,integer code in <code>0:k</code> for the above <code>kind</code>.
5504,integers.  See the details for how many are required(which depends on <code>rng.kind</code>).
5505,vectors (of the same mode) containing a sequenceof items (conceptually) with no duplicated values.
5506,vectors (of the same mode) containing a sequenceof items (conceptually) with no duplicated values.
5507,character string: the hook name
5508,character string: the package/namespace name
5509,character string: an event for the package.  Can be abbreviated.
5510,A function or a list of functions, or for <code>action = "replace"</code>, <code>NULL</code> 
5511,The action to be taken.  Can be abbreviated.
5512,string or namespace environment.
5513,logical.
5514,environment.
5515,namespace environment.
5516,character vector.
5517,optional character vector.
5518,namespace environment.
5519,string naming the package/namespace to load.
5520,vector of package names parallel to <code>generics</code>.
5521,character vector specifying library.
5522,logical.
5523,character.
5524,character.
5525,environment.
5526,a 3-column character matrix.
5527,character.
5528,any object.
5529,character arguments.
5530,the methods table name.
5531,symbol: name of namespace
5532,character vector naming symbols to exclude from theimport, particularly useful when <code>vars</code> is missing.
5533,double (of length one).  Set a limit onthe total or elapsed cpu time in seconds, respectively.
5534,logical.  If <code>TRUE</code>, the limits apply only tothe rest of the current computation.
5535,double (of length one).  Set a limit onthe total or elapsed cpu time in seconds, respectively.
5536,logical.  If <code>TRUE</code>, the limits apply only tothe rest of the current computation.
5537,A character string: tilde expansion will be done.
5538,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
5539,integer: a row number of the table given by<code>showConnections</code>.
5540,a connection.
5541,a character vector, usually of length one.
5542,character: the type of shell quoting.  Partial matching issupported.  <code>"cmd"</code> and <code>"cmd2"</code> refer to the Windows shell.<code>"cmd"</code> is the default under Windows.
5543,a numeric vector
5544,a condition object.
5545,call expression.
5546,a condition object.
5547,expression to be evaluated.
5548,expression to be evaluated before returning or exiting.
5549,function.
5550,character string.
5551,character string.
5552,character string naming a restart.
5553,restart object.
5554,object.
5555,character string naming a condition class.
5556,additional arguments; see details below.
5557,a numeric vector.  Or, for <code>round</code> and <code>signif</code>, acomplex vector.
5558,integer indicating the number of decimal places(<code>round</code>) or significant digits (<code>signif</code>) to be used.Negative values are allowed (see ‘Details’).
5559,arguments to be passed to methods.
5560,a condition object.
5561,call expression.
5562,a condition object.
5563,expression to be evaluated.
5564,expression to be evaluated before returning or exiting.
5565,function.
5566,character string.
5567,character string.
5568,character string naming a restart.
5569,restart object.
5570,object.
5571,character string naming a condition class.
5572,additional arguments; see details below.
5573,a condition object.
5574,call expression.
5575,a condition object.
5576,expression to be evaluated.
5577,expression to be evaluated before returning or exiting.
5578,function.
5579,character string.
5580,character string.
5581,character string naming a restart.
5582,restart object.
5583,object.
5584,character string naming a condition class.
5585,additional arguments; see details below.
5586,a condition object.
5587,call expression.
5588,a condition object.
5589,expression to be evaluated.
5590,expression to be evaluated before returning or exiting.
5591,function.
5592,character string.
5593,character string.
5594,character string naming a restart.
5595,restart object.
5596,object.
5597,character string naming a condition class.
5598,additional arguments; see details below.
5599,a condition object.
5600,call expression.
5601,a condition object.
5602,expression to be evaluated.
5603,expression to be evaluated before returning or exiting.
5604,function.
5605,character string.
5606,character string.
5607,character string naming a restart.
5608,restart object.
5609,object.
5610,character string naming a condition class.
5611,additional arguments; see details below.
5612,a vector (atomic or list) or an <code>expression</code>object.  Other objects (including classed objects) will be coercedby <code>base::as.list</code>.
5613,the function to be applied to each element of <code>X</code>:see ‘Details’.  In the case of functions like<code>+</code>, <code>%*%</code>, the function name must be backquoted or quoted.
5614,optional arguments to <code>FUN</code>.
5615,logical or character string; should the result besimplified to a vector, matrix or higher dimensional array ifpossible?  For <code>sapply</code> it must be named and not abbreviated.The default value, <code>TRUE</code>, returns a vector or matrix if appropriate,whereas if <code>simplify = "array"</code> the result may be an<code>array</code> of “rank”(<i>=</i><code>length(dim(.))</code>) one higher than the resultof <code>FUN(X[[i]])</code>.
5616,logical; if <code>TRUE</code> and if <code>X</code> is character,use <code>X</code> as <code>names</code> for the result unless it had namesalready.  Since this argument follows <code>...</code> its name cannotbe abbreviated.
5617,a (generalized) vector; a template for the returnvalue from FUN.  See ‘Details’.
5618,integer: the number of replications.
5619,the expression (a language object, usually a call)to evaluate repeatedly.
5620,a list, typically returned from <code>lapply()</code>.
5621,logical; if true, <code>simplify2array()</code> will produce a(“higher rank”) array when appropriate, whereas<code>higher = FALSE</code> would return a matrix (or vector) only.These two cases correspond to <code>sapply(*, simplify = "array")</code> or<code>simplify = TRUE</code>, respectively.
5622,numeric or complex vectors.
5623,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
5624,object to be coerced or tested.
5625,further arguments passed to or from other methods.
5626,a numeric or complex vector
5627,a writable connection or a character string naming thefile to write to, or <code>NULL</code> to stop sink-ing.
5628,logical.  If <code>TRUE</code>, output will be appended to<code>file</code>; otherwise, it will overwrite the contents of<code>file</code>.
5629,character string.  Either the output stream or the messagesstream.  The name will be partially matched so can be abbreviated.
5630,logical: if <code>TRUE</code>, output will be sent to the newsink and to the current output stream, like the Unix program <code>tee</code>.
5631,a writable connection or a character string naming thefile to write to, or <code>NULL</code> to stop sink-ing.
5632,logical.  If <code>TRUE</code>, output will be appended to<code>file</code>; otherwise, it will overwrite the contents of<code>file</code>.
5633,character string.  Either the output stream or the messagesstream.  The name will be partially matched so can be abbreviated.
5634,logical: if <code>TRUE</code>, output will be sent to the newsink and to the current output stream, like the Unix program <code>tee</code>.
5635,numeric or complex vectors.
5636,an array.  If <code>x</code> has no dimension attribute, it isconsidered a one-dimensional array.
5637,an integer vector giving the dimension numbers to slice by.
5638,character string.  A description of the connection:see ‘Details’.
5639,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
5640,logical.  See the ‘Blocking’ section.
5641,The name of the encoding to be assumed.  See the‘Encoding’ section.
5642,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
5643,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
5644,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
5645,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
5646,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
5647,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
5648,a filename within a zip file.
5649,character string.  Host name for the port.
5650,integer.  The TCP port number.
5651,logical.  Should the socket be a client or a server?
5652,a server socket listening for connections.
5653,a connection.
5654,character string.  Currently ignored.
5655,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
5656,arguments passed to or from other methods.
5657,character string.  A description of the connection:see ‘Details’.
5658,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
5659,logical.  See the ‘Blocking’ section.
5660,The name of the encoding to be assumed.  See the‘Encoding’ section.
5661,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
5662,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
5663,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
5664,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
5665,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
5666,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
5667,a filename within a zip file.
5668,character string.  Host name for the port.
5669,integer.  The TCP port number.
5670,logical.  Should the socket be a client or a server?
5671,a server socket listening for connections.
5672,a connection.
5673,character string.  Currently ignored.
5674,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
5675,arguments passed to or from other methods.
5676,list of open socket connections and server sockets.
5677,logical.  If <code>TRUE</code> wait for corresponding socket tobecome available for writing; otherwise wait for it to becomeavailable for reading or for accepting an incomingconnection (server sockets).
5678,numeric or <code>NULL</code>. Time in seconds to wait for asocket to become available; <code>NULL</code> means waitindefinitely.
5679,character string.  A description of the connection:see ‘Details’.
5680,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
5681,logical.  See the ‘Blocking’ section.
5682,The name of the encoding to be assumed.  See the‘Encoding’ section.
5683,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
5684,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
5685,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
5686,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
5687,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
5688,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
5689,a filename within a zip file.
5690,character string.  Host name for the port.
5691,integer.  The TCP port number.
5692,logical.  Should the socket be a client or a server?
5693,a server socket listening for connections.
5694,a connection.
5695,character string.  Currently ignored.
5696,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
5697,arguments passed to or from other methods.
5698,a square numeric or complex matrix containing the coefficients ofthe linear system.  Logical matrices are coerced to numeric.
5699,a numeric or complex vector or matrix giving the right-handside(s) of the linear system.  If missing, <code>b</code> is taken to bean identity matrix and <code>solve</code> will return the inverse of <code>a</code>.
5700,the tolerance for detecting linear dependencies in thecolumns of <code>a</code>.  The default is <code>.Machine$double.eps</code>. Notcurrently used with complex matrices <code>a</code>.
5701,logical.  Defunct and an error.
5702,further arguments passed to or from other methods
5703,a square numeric or complex matrix containing the coefficients ofthe linear system.  Logical matrices are coerced to numeric.
5704,a numeric or complex vector or matrix giving the right-handside(s) of the linear system.  If missing, <code>b</code> is taken to bean identity matrix and <code>solve</code> will return the inverse of <code>a</code>.
5705,the tolerance for detecting linear dependencies in thecolumns of <code>a</code>.  The default is <code>.Machine$double.eps</code>. Notcurrently used with complex matrices <code>a</code>.
5706,logical.  Defunct and an error.
5707,further arguments passed to or from other methods
5708,a numeric or complex matrix whose QR decomposition is to becomputed.  Logical matrices are coerced to numeric.
5709,the tolerance for detecting linear dependencies in thecolumns of <code>x</code>. Only used if <code>LAPACK</code> is false and<code>x</code> is real.
5710,a QR decomposition of the type computed by <code>qr</code>.
5711,a vector or matrix of right-hand sides of equations.
5712,a QR decomposition or (<code>qr.solve</code> only) a rectangular matrix.
5713,effective rank.
5714,logical.  For real <code>x</code>, if true use LAPACKotherwise use LINPACK (the default).
5715,further arguments passed to or from other methods
5716,for <code>sort</code> an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with a class or a numeric,complex, character or logical vector.  For <code>sort.int</code>, anumeric, complex, character or logical vector, or a factor.
5717,logical.  Should the sort be increasing or decreasing?For the <code>"radix"</code> method, this can be a vector oflength equal to the number of arguments in <code>...</code>. For theother methods, it must be length one. Not available for partial sorting.
5718,arguments to be passed to or from methods or (for thedefault methods and objects without a class) to <code>sort.int</code>.
5719,for controlling the treatment of <code>NA</code>s.If <code>TRUE</code>, missing values in the data are put last; if<code>FALSE</code>, they are put first; if <code>NA</code>, they are removed.
5720,<code>NULL</code> or a  vector of indices for partial sorting.
5721,character string specifying the algorithm used.  Notavailable for partial sorting.  Can be abbreviated.
5722,logical indicating if the ordering index vector shouldbe returned as well. Supported by <code>method == "radix"</code> for any<code>na.last</code> mode and data type, and the other methods when<code>na.last = NA</code> (the default) and fully sorting non-factors.
5723,for <code>sort</code> an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with a class or a numeric,complex, character or logical vector.  For <code>sort.int</code>, anumeric, complex, character or logical vector, or a factor.
5724,logical.  Should the sort be increasing or decreasing?For the <code>"radix"</code> method, this can be a vector oflength equal to the number of arguments in <code>...</code>. For theother methods, it must be length one. Not available for partial sorting.
5725,arguments to be passed to or from methods or (for thedefault methods and objects without a class) to <code>sort.int</code>.
5726,for controlling the treatment of <code>NA</code>s.If <code>TRUE</code>, missing values in the data are put last; if<code>FALSE</code>, they are put first; if <code>NA</code>, they are removed.
5727,<code>NULL</code> or a  vector of indices for partial sorting.
5728,character string specifying the algorithm used.  Notavailable for partial sorting.  Can be abbreviated.
5729,logical indicating if the ordering index vector shouldbe returned as well. Supported by <code>method == "radix"</code> for any<code>na.last</code> mode and data type, and the other methods when<code>na.last = NA</code> (the default) and fully sorting non-factors.
5730,for <code>sort</code> an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with a class or a numeric,complex, character or logical vector.  For <code>sort.int</code>, anumeric, complex, character or logical vector, or a factor.
5731,logical.  Should the sort be increasing or decreasing?For the <code>"radix"</code> method, this can be a vector oflength equal to the number of arguments in <code>...</code>. For theother methods, it must be length one. Not available for partial sorting.
5732,arguments to be passed to or from methods or (for thedefault methods and objects without a class) to <code>sort.int</code>.
5733,for controlling the treatment of <code>NA</code>s.If <code>TRUE</code>, missing values in the data are put last; if<code>FALSE</code>, they are put first; if <code>NA</code>, they are removed.
5734,<code>NULL</code> or a  vector of indices for partial sorting.
5735,character string specifying the algorithm used.  Notavailable for partial sorting.  Can be abbreviated.
5736,logical indicating if the ordering index vector shouldbe returned as well. Supported by <code>method == "radix"</code> for any<code>na.last</code> mode and data type, and the other methods when<code>na.last = NA</code> (the default) and fully sorting non-factors.
5737,a sequence of numeric, complex, character or logicalvectors, all of the same length, or a classed <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
5738,an atomic vector for <code>method</code>s <code>"shell"</code> and<code>"quick"</code>.  When <code>x</code> is a non-atomic <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, the default<code>"auto"</code> and <code>"radix"</code> methods may work if <code>order(x,..)</code>does.
5739,vector of indices for partial sorting.(Non-<code>NULL</code> values are not implemented.)
5740,logical.  Should the sort order be increasing ordecreasing? For the <code>"radix"</code> method, this can be a vector oflength equal to the number of arguments in <code>...</code>. For theother methods, it must be length one.
5741,for controlling the treatment of <code>NA</code>s.If <code>TRUE</code>, missing values in the data are put last; if<code>FALSE</code>, they are put first; if <code>NA</code>, they are removed(see ‘Note’.)
5742,the method to be used: partial matches are allowed.  Thedefault (<code>"auto"</code>) implies <code>"radix"</code> for short numeric vectors,integer vectors, logical vectors and factors.Otherwise, it implies <code>"shell"</code>.For details of methods <code>"shell"</code>, <code>"quick"</code>, and <code>"radix"</code>,see the help for <code>sort</code>.
5743,for <code>sort</code> an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with a class or a numeric,complex, character or logical vector.  For <code>sort.int</code>, anumeric, complex, character or logical vector, or a factor.
5744,logical.  Should the sort be increasing or decreasing?For the <code>"radix"</code> method, this can be a vector oflength equal to the number of arguments in <code>...</code>. For theother methods, it must be length one. Not available for partial sorting.
5745,arguments to be passed to or from methods or (for thedefault methods and objects without a class) to <code>sort.int</code>.
5746,for controlling the treatment of <code>NA</code>s.If <code>TRUE</code>, missing values in the data are put last; if<code>FALSE</code>, they are put first; if <code>NA</code>, they are removed.
5747,<code>NULL</code> or a  vector of indices for partial sorting.
5748,character string specifying the algorithm used.  Notavailable for partial sorting.  Can be abbreviated.
5749,logical indicating if the ordering index vector shouldbe returned as well. Supported by <code>method == "radix"</code> for any<code>na.last</code> mode and data type, and the other methods when<code>na.last = NA</code> (the default) and fully sorting non-factors.
5750,a connection or a character string giving thepathname of the file or URL to read from.  <code>""</code> indicates theconnection <code>stdin()</code>.
5751,<code>TRUE</code>, <code>FALSE</code> or an environment, determiningwhere the parsed expressions are evaluated.  <code>FALSE</code> (thedefault) corresponds to the user's workspace (the globalenvironment) and <code>TRUE</code> to the environment from which<code>source</code> is called.
5752,logical; if <code>TRUE</code>, each expression is printedafter parsing, before evaluation.
5753,logical; if <code>TRUE</code>, the result of<code>eval(i)</code> is printed for each expression <code>i</code>; defaultsto the value of <code>echo</code>.
5754,for <code>source()</code> and <code>withAutoprint(*, evaluated=TRUE)</code>:<em>instead</em> of specifying <code>file</code>, an<code>expression</code>, <code>call</code>, or <code>list</code>of <code>call</code>'s, but <em>not</em> an unevaluated “expression”.

5755,for <code>withAutoprint()</code> (with default <code>evaluated=FALSE</code>):one or more unevaluated “expressions”.
5756,logical indicating that <code>exprs</code> is passed to<code>source(exprs= *)</code> and hence must be evaluated, i.e., a formal<code>expression</code>, <code>call</code> or <code>list</code> of calls.
5757,logical indicating if newline (hence empty line) shouldbe printed before each expression (when <code>echo = TRUE</code>).
5758,if <code>TRUE</code>, more diagnostics (than just<code>echo = TRUE</code>) are printed during parsing and evaluation ofinput, including extra info for <b>each</b> expression.
5759,character; gives the prompt to be used if<code>echo = TRUE</code>.
5760,integer; is used only if <code>echo</code> is<code>TRUE</code> and gives the maximal number of characters output forthe deparse of a single expression.
5761,integer, passed to <code>deparse()</code> whichis used (only) when there are no source references.
5762,<code>character</code> vector, passed as<code>control</code> to <code>deparse()</code>, see also<code>.deparseOpts</code>.  In <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version &lt;= 3.3.x, this washardcoded to <code>"showAttributes"</code>, which is the defaultcurrently; <code>deparseCtrl = "all"</code> may be preferable, when strictback compatibility is not of importance.
5763,logical; if <code>TRUE</code> and <code>file</code> is a pathname,the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> working directory is temporarily changed to the directorycontaining <code>file</code> for evaluating.
5764,character vector.  The encoding(s) to be assumed when<code>file</code> is a character string: see <code>file</code>.  Apossible value is <code>"unknown"</code> when the encoding is guessed: seethe ‘Encodings’ section.
5765,character; gives the prompt to use oncontinuation lines if <code>echo = TRUE</code>.
5766,integer; how many comment lines at the start of thefile to skip if <code>echo = TRUE</code>.
5767,logical: should the source formatting be retainedwhen echoing expressions, if possible?
5768,(for <code>withAutoprint()</code>:) further (non-file related)arguments to be passed to <code>source(.)</code>.
5769,vector or data frame containing values to be divided into groups.
5770,a ‘factor’ in the sense that <code>as.factor(f)</code>defines the grouping, or a list of such factors in which case theirinteraction is used for the grouping. If <code>x</code> is a data frame,<code>f</code> can also be a formula of the form <code> ~ g</code> to split bythe variable <code>g</code>, or more generally of the form <code> ~ g1 +      ... + gk</code> to split by the interaction of the variables<code>g1</code>, ..., <code>gk</code>, where these variables are evaluated inthe data frame <code>x</code> using the usual non-standard evaluationrules.
5771,logical indicating if levels that do not occur should be dropped(if <code>f</code> is a <code>factor</code> or a list).
5772,a list of vectors or data frames compatible with asplitting of <code>x</code>. Recycling applies if the lengths do not match.
5773,character string, passed to <code>interaction</code> in thecase where <code>f</code> is a <code>list</code>.
5774,logical, passed to <code>interaction</code> when<code>f</code> is a list.
5775,further potential arguments passed to methods.
5776,vector or data frame containing values to be divided into groups.
5777,a ‘factor’ in the sense that <code>as.factor(f)</code>defines the grouping, or a list of such factors in which case theirinteraction is used for the grouping. If <code>x</code> is a data frame,<code>f</code> can also be a formula of the form <code> ~ g</code> to split bythe variable <code>g</code>, or more generally of the form <code> ~ g1 +      ... + gk</code> to split by the interaction of the variables<code>g1</code>, ..., <code>gk</code>, where these variables are evaluated inthe data frame <code>x</code> using the usual non-standard evaluationrules.
5778,logical indicating if levels that do not occur should be dropped(if <code>f</code> is a <code>factor</code> or a list).
5779,a list of vectors or data frames compatible with asplitting of <code>x</code>. Recycling applies if the lengths do not match.
5780,character string, passed to <code>interaction</code> in thecase where <code>f</code> is a <code>list</code>.
5781,logical, passed to <code>interaction</code> when<code>f</code> is a list.
5782,further potential arguments passed to methods.
5783,a <code>Date</code> object to be summarized or printed.
5784,number of significant digits for the computations.
5785,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
5786,further arguments to be passed from or to other methods.
5787,vector or data frame containing values to be divided into groups.
5788,a ‘factor’ in the sense that <code>as.factor(f)</code>defines the grouping, or a list of such factors in which case theirinteraction is used for the grouping. If <code>x</code> is a data frame,<code>f</code> can also be a formula of the form <code> ~ g</code> to split bythe variable <code>g</code>, or more generally of the form <code> ~ g1 +      ... + gk</code> to split by the interaction of the variables<code>g1</code>, ..., <code>gk</code>, where these variables are evaluated inthe data frame <code>x</code> using the usual non-standard evaluationrules.
5789,logical indicating if levels that do not occur should be dropped(if <code>f</code> is a <code>factor</code> or a list).
5790,a list of vectors or data frames compatible with asplitting of <code>x</code>. Recycling applies if the lengths do not match.
5791,character string, passed to <code>interaction</code> in thecase where <code>f</code> is a <code>list</code>.
5792,logical, passed to <code>interaction</code> when<code>f</code> is a list.
5793,further potential arguments passed to methods.
5794,an object to be printed or summarized from one of thedate-time classes.
5795,for timezone formatting, passed to <code>format.POSIXct</code>.
5796,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
5797,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
5798,further arguments to be passed from or to other methods.
5799,date-time objects
5800,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
5801,a numeric vector (in seconds)
5802,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
5803,vector or data frame containing values to be divided into groups.
5804,a ‘factor’ in the sense that <code>as.factor(f)</code>defines the grouping, or a list of such factors in which case theirinteraction is used for the grouping. If <code>x</code> is a data frame,<code>f</code> can also be a formula of the form <code> ~ g</code> to split bythe variable <code>g</code>, or more generally of the form <code> ~ g1 +      ... + gk</code> to split by the interaction of the variables<code>g1</code>, ..., <code>gk</code>, where these variables are evaluated inthe data frame <code>x</code> using the usual non-standard evaluationrules.
5805,logical indicating if levels that do not occur should be dropped(if <code>f</code> is a <code>factor</code> or a list).
5806,a list of vectors or data frames compatible with asplitting of <code>x</code>. Recycling applies if the lengths do not match.
5807,character string, passed to <code>interaction</code> in thecase where <code>f</code> is a <code>list</code>.
5808,logical, passed to <code>interaction</code> when<code>f</code> is a list.
5809,further potential arguments passed to methods.
5810,vector or data frame containing values to be divided into groups.
5811,a ‘factor’ in the sense that <code>as.factor(f)</code>defines the grouping, or a list of such factors in which case theirinteraction is used for the grouping. If <code>x</code> is a data frame,<code>f</code> can also be a formula of the form <code> ~ g</code> to split bythe variable <code>g</code>, or more generally of the form <code> ~ g1 +      ... + gk</code> to split by the interaction of the variables<code>g1</code>, ..., <code>gk</code>, where these variables are evaluated inthe data frame <code>x</code> using the usual non-standard evaluationrules.
5812,logical indicating if levels that do not occur should be dropped(if <code>f</code> is a <code>factor</code> or a list).
5813,a list of vectors or data frames compatible with asplitting of <code>x</code>. Recycling applies if the lengths do not match.
5814,character string, passed to <code>interaction</code> in thecase where <code>f</code> is a <code>list</code>.
5815,logical, passed to <code>interaction</code> when<code>f</code> is a list.
5816,further potential arguments passed to methods.
5817,vector or data frame containing values to be divided into groups.
5818,a ‘factor’ in the sense that <code>as.factor(f)</code>defines the grouping, or a list of such factors in which case theirinteraction is used for the grouping. If <code>x</code> is a data frame,<code>f</code> can also be a formula of the form <code> ~ g</code> to split bythe variable <code>g</code>, or more generally of the form <code> ~ g1 +      ... + gk</code> to split by the interaction of the variables<code>g1</code>, ..., <code>gk</code>, where these variables are evaluated inthe data frame <code>x</code> using the usual non-standard evaluationrules.
5819,logical indicating if levels that do not occur should be dropped(if <code>f</code> is a <code>factor</code> or a list).
5820,a list of vectors or data frames compatible with asplitting of <code>x</code>. Recycling applies if the lengths do not match.
5821,character string, passed to <code>interaction</code> in thecase where <code>f</code> is a <code>list</code>.
5822,logical, passed to <code>interaction</code> when<code>f</code> is a list.
5823,further potential arguments passed to methods.
5824,a character vector of format strings, each of up to 8192 bytes.
5825,values to be passed into <code>fmt</code>.  Only logical,integer, real and character vectors are supported, but some coercionwill be done: see the ‘Details’ section. Up to 100.
5826,see <code>gettext</code>.
5827,a numeric or <code>complex</code> vector or array.
5828,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, to be coerced to a character vector.
5829,the kind of quotes to be used, see ‘Details’.
5830, The name of a file. 
5831, The character encoding to assume for the file. 
5832, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
5833, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
5834, The timestamp to use on a copy of a file. 
5835, Is this <code>srcfilecopy</code> known to come from a file system file? 
5836, A <code>srcfile</code> object. 
5837, Line numbers. 
5838, A vector of four, six or eight values giving a source location; see‘Details’.
5839, An object of the appropriate class. 
5840, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
5841, An optional second <code>srcref</code> object to mark the endof the character range. 
5842, Additional arguments to the methods; these will be ignored. 
5843, The name of a file. 
5844, The character encoding to assume for the file. 
5845, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
5846, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
5847, The timestamp to use on a copy of a file. 
5848, Is this <code>srcfilecopy</code> known to come from a file system file? 
5849, A <code>srcfile</code> object. 
5850, Line numbers. 
5851, A vector of four, six or eight values giving a source location; see‘Details’.
5852, An object of the appropriate class. 
5853, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
5854, An optional second <code>srcref</code> object to mark the endof the character range. 
5855, Additional arguments to the methods; these will be ignored. 
5856, The name of a file. 
5857, The character encoding to assume for the file. 
5858, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
5859, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
5860, The timestamp to use on a copy of a file. 
5861, Is this <code>srcfilecopy</code> known to come from a file system file? 
5862, A <code>srcfile</code> object. 
5863, Line numbers. 
5864, A vector of four, six or eight values giving a source location; see‘Details’.
5865, An object of the appropriate class. 
5866, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
5867, An optional second <code>srcref</code> object to mark the endof the character range. 
5868, Additional arguments to the methods; these will be ignored. 
5869, The name of a file. 
5870, The character encoding to assume for the file. 
5871, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
5872, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
5873, The timestamp to use on a copy of a file. 
5874, Is this <code>srcfilecopy</code> known to come from a file system file? 
5875, A <code>srcfile</code> object. 
5876, Line numbers. 
5877, A vector of four, six or eight values giving a source location; see‘Details’.
5878, An object of the appropriate class. 
5879, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
5880, An optional second <code>srcref</code> object to mark the endof the character range. 
5881, Additional arguments to the methods; these will be ignored. 
5882,The name of the generic.
5883,The generic function definition. Never passed whendefining a new generic.
5884,vector of <code>character</code> string whose “starts”are considered.
5885,<code>character</code> vector (often of length one).
5886,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
5887,integer: a row number of the table given by<code>showConnections</code>.
5888,a connection.
5889,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
5890,integer: a row number of the table given by<code>showConnections</code>.
5891,a connection.
5892,logical: if true all connections, including closed onesand the standard ones are displayed.  If false only open user-createdconnections are included.
5893,integer: a row number of the table given by<code>showConnections</code>.
5894,a connection.
5895,zero or more objects which can be coerced to character(and which are pasted together with no separator) or a singlecondition object.
5896,logical, indicating if the call should become part of theerror message.
5897,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated.
5898,any number of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expressions, which should eachevaluate to (a logical vector of all) <code>TRUE</code>.  Use<em>either</em> <code>...</code> <em>or</em><code>exprs</code>, the latter typically an unevaluated expression of theform 

5899,Note that e.g., positive numbers are <em>not</em> <code>TRUE</code>, even whenthey are coerced to <code>TRUE</code>, e.g., inside <code>if(.)</code> or inarithmetic computations in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>.

5900,If names are provided to <code>...</code>, they will be used in lieu ofthe default error message.
5901,alternative to <code>exprs</code> or <code>...</code>:an ‘expression-like’ object, typically an<code>expression</code>, but also a <code>call</code>, a<code>name</code>, or atomic constant such as <code>TRUE</code>.
5902,(only when <code>exprs</code> is used:) indicates the<code>environment</code> in which the expressions should beevaluated; by default the one from where <code>stopifnot()</code> has beencalled.
5903,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
5904,a character string giving the desired mode or‘storage mode’ (type) of the object.
5905,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
5906,a character string giving the desired mode or‘storage mode’ (type) of the object.
5907,a connection, or a character string giving the name of afile or a URL to read the expressions from.If <code>file</code> is <code>""</code> and <code>text</code> is missing or <code>NULL</code>then input is taken from the console.
5908,integer (or coerced to integer).  The maximum number ofexpressions to parse.  If <code>n</code> is <code>NULL</code> or negative or<code>NA</code> the input is parsed in its entirety.
5909,character vector.  The text to parse.  Elements are treatedas if they were lines of a file.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects will be coercedto character if possible.
5910,the prompt to print when parsing from the keyboard.<code>NULL</code> means to use <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s prompt, <code>getOption("prompt")</code>.
5911,a logical value; if <code>TRUE</code>, keepsource reference information.
5912,<code>NULL</code>, a character vector, or a<code>srcfile</code> object.  See the ‘Details’ section.
5913,encoding to be assumed for input strings.  If thevalue is <code>"latin1"</code> or <code>"UTF-8"</code> it is used to markcharacter strings as known to be in Latin-1 or UTF-8: it is not usedto re-encode the input.  To do the latter, specify the encoding aspart of the connection <code>con</code> or <em>via</em><code>options(encoding=)</code>: see the example under<code>file</code>. Arguments <code>encoding = "latin1"</code> and <code>encoding = "UTF-8"</code> are ignored with a warning when runningin a MBCS locale.
5914,a <code>character</code> vector of length <code>1</code>, i.e., a“string”.
5915,a connection, or a character string giving the name of afile or a URL to read the expressions from.If <code>file</code> is <code>""</code> and <code>text</code> is missing or <code>NULL</code>then input is taken from the console.
5916,integer (or coerced to integer).  The maximum number ofexpressions to parse.  If <code>n</code> is <code>NULL</code> or negative or<code>NA</code> the input is parsed in its entirety.
5917,character vector.  The text to parse.  Elements are treatedas if they were lines of a file.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objects will be coercedto character if possible.
5918,the prompt to print when parsing from the keyboard.<code>NULL</code> means to use <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>'s prompt, <code>getOption("prompt")</code>.
5919,a logical value; if <code>TRUE</code>, keepsource reference information.
5920,<code>NULL</code>, a character vector, or a<code>srcfile</code> object.  See the ‘Details’ section.
5921,encoding to be assumed for input strings.  If thevalue is <code>"latin1"</code> or <code>"UTF-8"</code> it is used to markcharacter strings as known to be in Latin-1 or UTF-8: it is not usedto re-encode the input.  To do the latter, specify the encoding aspart of the connection <code>con</code> or <em>via</em><code>options(encoding=)</code>: see the example under<code>file</code>. Arguments <code>encoding = "latin1"</code> and <code>encoding = "UTF-8"</code> are ignored with a warning when runningin a MBCS locale.
5922,a <code>character</code> vector of length <code>1</code>, i.e., a“string”.
5923,An object to be converted: a character vector for<code>strptime</code>, an object which can be converted to<code>"POSIXlt"</code> for <code>strftime</code>.
5924,A character string specifying the time zone to be used forthe conversion.  System-specific (see <code>as.POSIXlt</code>), but<code>""</code> is the current time zone, and <code>"GMT"</code> is UTC.Invalid values are most commonly treated as UTC, on some platforms witha warning.
5925,A character string.  The default for the <code>format</code>methods is<code>"%Y-%m-%d %H:%M:%S"</code> if any element has a timecomponent which is not midnight, and <code>"%Y-%m-%d"</code>otherwise.  If <code>options("digits.secs")</code> is set, up tothe specified number of digits will be printed for seconds.
5926,Further arguments to be passed from or to other methods.
5927,logical.  Should the time zone abbreviation be appendedto the output?  This is used in printing times, and more reliablethan using <code>"%Z"</code>.
5928,An object to be converted: a character vector for<code>strptime</code>, an object which can be converted to<code>"POSIXlt"</code> for <code>strftime</code>.
5929,A character string specifying the time zone to be used forthe conversion.  System-specific (see <code>as.POSIXlt</code>), but<code>""</code> is the current time zone, and <code>"GMT"</code> is UTC.Invalid values are most commonly treated as UTC, on some platforms witha warning.
5930,A character string.  The default for the <code>format</code>methods is<code>"%Y-%m-%d %H:%M:%S"</code> if any element has a timecomponent which is not midnight, and <code>"%Y-%m-%d"</code>otherwise.  If <code>options("digits.secs")</code> is set, up tothe specified number of digits will be printed for seconds.
5931,Further arguments to be passed from or to other methods.
5932,logical.  Should the time zone abbreviation be appendedto the output?  This is used in printing times, and more reliablethan using <code>"%Z"</code>.
5933,a character vector, or an object which can be coerced to acharacter vector using <code>as.character</code>.
5934,an integer vector giving the (non-negative) numbers oftimes to repeat the respective elements of <code>x</code>.
5935,character vector, each element of which is to be split.  Otherinputs, including a factor, will give an error.
5936,character vector (or object which can be coerced to such)containing regular expression(s) (unless <code>fixed = TRUE</code>)to use for splitting.  If empty matches occur, in particular if<code>split</code> has length 0, <code>x</code> is split into single characters.If <code>split</code> has length greater than 1, it is re-cycled along<code>x</code>.
5937,logical.  If <code>TRUE</code> match <code>split</code> exactly, otherwiseuse regular expressions.  Has priority over <code>perl</code>.
5938,logical.  Should Perl-compatible regexps be used?
5939,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character, and inputs withmarked encodings are not converted.  This is forced (with a warning)if any input is found which is marked as <code>"bytes"</code>(see <code>Encoding</code>).
5940,a character vector, or something coercible to this by<code>as.character</code>.
5941,an integer which is between 2 and 36 inclusive, or zero(default).
5942,a character vector, or an object which can be coerced to acharacter vector by <code>as.character</code>.
5943,Positive integer values: recycled to the length of <code>x</code>.
5944,an object which will havevarious attributes attached to it.
5945,attributes, specified in <code>tag = value</code>form, which will be attached to data.
5946,a character vector, or an object which can be converted to acharacter vector by <code>as.character</code>.
5947,a positive integer giving the target column for wrappinglines in the output.
5948,a non-negative integer giving the indentation of thefirst line in a paragraph.
5949,a non-negative integer specifying the indentation ofsubsequent lines in paragraphs.
5950,a character string to be used as prefix foreach line except the first, for which <code>initial</code> is used.
5951,a logical.  If <code>TRUE</code>, the result is a singlecharacter vector of line text; otherwise, it is a list of the samelength as <code>x</code> the elements of which are character vectors ofline text obtained from the corresponding element of <code>x</code>.(Hence, the result in the former case is obtained by unlisting thatof the latter.)
5952,character string containing a regular expression(or character string for <code>fixed = TRUE</code>) to be matchedin the given character vector.  Coerced by<code>as.character</code> to a character string if possible.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  Missing values are allowed except for<code>regexpr</code>, <code>gregexpr</code> and <code>regexec</code>.
5953,a character vector where matches are sought, or anobject which can be coerced by <code>as.character</code> to a charactervector.  Long vectors are supported.
5954,if <code>FALSE</code>, the pattern matching is <em>casesensitive</em> and if <code>TRUE</code>, case is ignored during matching.
5955,logical.  Should Perl-compatible regexps be used?
5956,if <code>FALSE</code>, a vector containing the (<code>integer</code>)indices of the matches determined by <code>grep</code> is returned, and if<code>TRUE</code>, a vector containing the matching elements themselves isreturned.
5957,logical.  If <code>TRUE</code>, <code>pattern</code> is a string to bematched as is.  Overrides all conflicting arguments.
5958,logical.  If <code>TRUE</code> the matching is donebyte-by-byte rather than character-by-character.  See‘Details’.
5959,logical.  If <code>TRUE</code> return indices or values forelements that do <em>not</em> match.
5960,a replacement for matched pattern in <code>sub</code> and<code>gsub</code>.  Coerced to character if possible.  For<code>fixed = FALSE</code> this can include backreferences <code>"\1"</code> to<code>"\9"</code> to parenthesized subexpressions of <code>pattern</code>.  For<code>perl = TRUE</code> only, it can also contain <code>"\U"</code> or<code>"\L"</code> to convert the rest of the replacement to upper orlower case and <code>"\E"</code> to end case conversion.  If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.  If <code>NA</code>, all elements in the resultcorresponding to matches will be set to <code>NA</code>.
5961,object to be subsetted.
5962,logical expression indicating elements or rows to keep:missing values are taken as false.
5963,expression, indicating columns to select from adata frame.
5964,passed on to <code>[</code> indexing operator.
5965,further arguments to be passed to or from other methods.
5966,object to be subsetted.
5967,logical expression indicating elements or rows to keep:missing values are taken as false.
5968,expression, indicating columns to select from adata frame.
5969,passed on to <code>[</code> indexing operator.
5970,further arguments to be passed to or from other methods.
5971,object to be subsetted.
5972,logical expression indicating elements or rows to keep:missing values are taken as false.
5973,expression, indicating columns to select from adata frame.
5974,passed on to <code>[</code> indexing operator.
5975,further arguments to be passed to or from other methods.
5976,object to be subsetted.
5977,logical expression indicating elements or rows to keep:missing values are taken as false.
5978,expression, indicating columns to select from adata frame.
5979,passed on to <code>[</code> indexing operator.
5980,further arguments to be passed to or from other methods.
5981,any syntactically valid <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression
5982,a <code>call</code>, i.e., an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object of<code>class</code> (and <code>mode</code>) <code>"call"</code>.
5983,an environment or a list object.  Defaults to thecurrent evaluation environment.
5984,a character vector.
5985,integer.  The first element to be replaced.
5986,integer.  The last element to be replaced.
5987,a character vector, recycled if necessary.
5988,a character vector.
5989,integer.  The first element to be replaced.
5990,integer.  The last element to be replaced.
5991,a character vector, recycled if necessary.
5992,a character vector.
5993,integer.  The first element to be replaced.
5994,integer.  The last element to be replaced.
5995,a character vector, recycled if necessary.
5996,a character vector.
5997,integer.  The first element to be replaced.
5998,integer.  The last element to be replaced.
5999,a character vector, recycled if necessary.
6000,numeric or complex or logical vectors.
6001,logical.  Should missing values (including <code>NaN</code>) beremoved?
6002,an object for which a summary is desired.
6003,a result of the <em>default</em> method of <code>summary()</code>.
6004,integer, indicating how many levels should be shown for<code>factor</code>s.
6005,integer, used for number formatting with<code>signif()</code> (for <code>summary.default</code>) or<code>format()</code> (for <code>summary.data.frame</code>).  In<code>summary.default</code>, if not specified (i.e.,<code>missing(.)</code>), <code>signif()</code> will <em>not</em> be calledanymore (since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &gt;= 3.4.0, where the default has been changed toonly round in the <code>print</code> and <code>format</code> methods).
6006,integer code used in <code>quantile(*, type=quantile.type)</code>for the default method.
6007,additional arguments affecting the summary produced.
6008,character string.  A description of the connection:see ‘Details’.
6009,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
6010,logical.  See the ‘Blocking’ section.
6011,The name of the encoding to be assumed.  See the‘Encoding’ section.
6012,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
6013,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
6014,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
6015,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
6016,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
6017,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
6018,a filename within a zip file.
6019,character string.  Host name for the port.
6020,integer.  The TCP port number.
6021,logical.  Should the socket be a client or a server?
6022,a server socket listening for connections.
6023,a connection.
6024,character string.  Currently ignored.
6025,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
6026,arguments passed to or from other methods.
6027,an object for which a summary is desired.
6028,a result of the <em>default</em> method of <code>summary()</code>.
6029,integer, indicating how many levels should be shown for<code>factor</code>s.
6030,integer, used for number formatting with<code>signif()</code> (for <code>summary.default</code>) or<code>format()</code> (for <code>summary.data.frame</code>).  In<code>summary.default</code>, if not specified (i.e.,<code>missing(.)</code>), <code>signif()</code> will <em>not</em> be calledanymore (since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &gt;= 3.4.0, where the default has been changed toonly round in the <code>print</code> and <code>format</code> methods).
6031,integer code used in <code>quantile(*, type=quantile.type)</code>for the default method.
6032,additional arguments affecting the summary produced.
6033,objects.
6034,further arguments passed to methods.
6035,logical: should missing values be removed?
6036,a <code>Date</code> object to be summarized or printed.
6037,number of significant digits for the computations.
6038,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
6039,further arguments to be passed from or to other methods.
6040,a <code>Date</code> object to be summarized or printed.
6041,number of significant digits for the computations.
6042,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
6043,further arguments to be passed from or to other methods.
6044,an object for which a summary is desired.
6045,a result of the <em>default</em> method of <code>summary()</code>.
6046,integer, indicating how many levels should be shown for<code>factor</code>s.
6047,integer, used for number formatting with<code>signif()</code> (for <code>summary.default</code>) or<code>format()</code> (for <code>summary.data.frame</code>).  In<code>summary.default</code>, if not specified (i.e.,<code>missing(.)</code>), <code>signif()</code> will <em>not</em> be calledanymore (since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &gt;= 3.4.0, where the default has been changed toonly round in the <code>print</code> and <code>format</code> methods).
6048,integer code used in <code>quantile(*, type=quantile.type)</code>for the default method.
6049,additional arguments affecting the summary produced.
6050,date-time or date objects.
6051,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
6052,character string.  Units in which the results aredesired.  Can be abbreviated.
6053,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
6054,character string or numeric value specifying a time interval.
6055,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
6056,an object inheriting from class <code>"difftime"</code>.
6057,arguments to be passed to or from other methods.
6058,an object for which a summary is desired.
6059,a result of the <em>default</em> method of <code>summary()</code>.
6060,integer, indicating how many levels should be shown for<code>factor</code>s.
6061,integer, used for number formatting with<code>signif()</code> (for <code>summary.default</code>) or<code>format()</code> (for <code>summary.data.frame</code>).  In<code>summary.default</code>, if not specified (i.e.,<code>missing(.)</code>), <code>signif()</code> will <em>not</em> be calledanymore (since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &gt;= 3.4.0, where the default has been changed toonly round in the <code>print</code> and <code>format</code> methods).
6062,integer code used in <code>quantile(*, type=quantile.type)</code>for the default method.
6063,additional arguments affecting the summary produced.
6064,a vector of data, usually taking a small number of distinctvalues.
6065,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
6066,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
6067,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
6068,logical flag to determine if the levels should be regardedas ordered (in the order given).
6069,an upper bound on the number of levels; see ‘Details’.
6070,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
6071,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
6072,an object for which a summary is desired.
6073,a result of the <em>default</em> method of <code>summary()</code>.
6074,integer, indicating how many levels should be shown for<code>factor</code>s.
6075,integer, used for number formatting with<code>signif()</code> (for <code>summary.default</code>) or<code>format()</code> (for <code>summary.data.frame</code>).  In<code>summary.default</code>, if not specified (i.e.,<code>missing(.)</code>), <code>signif()</code> will <em>not</em> be calledanymore (since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> &gt;= 3.4.0, where the default has been changed toonly round in the <code>print</code> and <code>format</code> methods).
6076,integer code used in <code>quantile(*, type=quantile.type)</code>for the default method.
6077,additional arguments affecting the summary produced.
6078,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
6079,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
6080,a vector of data, usually taking a small number of distinctvalues.
6081,an optional vector of the unique values (as character strings)that <code>x</code> might have taken.  The default is the unique set ofvalues taken by <code>as.character(x)</code>, sorted intoincreasing order <em>of <code>x</code></em>.  Note that this set can bespecified as smaller than <code>sort(unique(x))</code>.
6082,<em>either</em> an optional character vector oflabels for the levels (in the same order as <code>levels</code> afterremoving those in <code>exclude</code>), <em>or</em> a character string oflength 1.  Duplicated values in <code>labels</code> can be used to mapdifferent values of <code>x</code> to the same factor level.
6083,a vector of values to be excluded when forming theset of levels.  This may be factor with the same level set as <code>x</code>or should be a <code>character</code>.
6084,logical flag to determine if the levels should be regardedas ordered (in the order given).
6085,an upper bound on the number of levels; see ‘Details’.
6086,(in <code>ordered(.)</code>): any of the above, apart from<code>ordered</code> itself.
6087,only add an <code>NA</code> level if it is used, i.e.if <code>any(is.na(x))</code>.
6088,an object to be printed or summarized from one of thedate-time classes.
6089,for timezone formatting, passed to <code>format.POSIXct</code>.
6090,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
6091,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
6092,further arguments to be passed from or to other methods.
6093,date-time objects
6094,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
6095,a numeric vector (in seconds)
6096,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
6097,an object to be printed or summarized from one of thedate-time classes.
6098,for timezone formatting, passed to <code>format.POSIXct</code>.
6099,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
6100,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
6101,further arguments to be passed from or to other methods.
6102,date-time objects
6103,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
6104,a numeric vector (in seconds)
6105,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
6106,an object to be printed or summarized from one of thedate-time classes.
6107,for timezone formatting, passed to <code>format.POSIXct</code>.
6108,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
6109,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
6110,further arguments to be passed from or to other methods.
6111,date-time objects
6112,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
6113,a numeric vector (in seconds)
6114,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
6115,an object to be printed or summarized from one of thedate-time classes.
6116,for timezone formatting, passed to <code>format.POSIXct</code>.
6117,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
6118,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
6119,further arguments to be passed from or to other methods.
6120,date-time objects
6121,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
6122,a numeric vector (in seconds)
6123,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
6124, The name of a file. 
6125, The character encoding to assume for the file. 
6126, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
6127, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
6128, The timestamp to use on a copy of a file. 
6129, Is this <code>srcfilecopy</code> known to come from a file system file? 
6130, A <code>srcfile</code> object. 
6131, Line numbers. 
6132, A vector of four, six or eight values giving a source location; see‘Details’.
6133, An object of the appropriate class. 
6134, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
6135, An optional second <code>srcref</code> object to mark the endof the character range. 
6136, Additional arguments to the methods; these will be ignored. 
6137, The name of a file. 
6138, The character encoding to assume for the file. 
6139, The encoding with which to make strings: see the<code>encoding</code> argument of <code>parse</code>.
6140, A character vector of source lines.  Other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectswill be coerced to character. 
6141, The timestamp to use on a copy of a file. 
6142, Is this <code>srcfilecopy</code> known to come from a file system file? 
6143, A <code>srcfile</code> object. 
6144, Line numbers. 
6145, A vector of four, six or eight values giving a source location; see‘Details’.
6146, An object of the appropriate class. 
6147, Whether to read the <code>srcfile</code> to obtain thetext of a <code>srcref</code>. 
6148, An optional second <code>srcref</code> object to mark the endof the character range. 
6149, Additional arguments to the methods; these will be ignored. 
6150,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
6151,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
6152,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
6153,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
6154,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
6155,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
6156,a character vector giving the row names for the dataframe.
6157,The name to be used for the column of tableentries, usually counts.
6158,logical: should the classifying factors bereturned as factors (the default) or character vectors?
6159,passed to <code>provideDimnames</code>.
6160,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
6161,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
6162,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
6163,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
6164,a character string <code>cat()</code>ed before themessages are printed.
6165,zero or more objects which can be coerced to character(and which are pasted together with no separator) or (for<code>message</code> only) a single condition object.
6166,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated, see also the note in <code>stop</code>.
6167,logical: should messages given as a character stringhave a newline appended?
6168,expression to evaluate.
6169,character, indicating which classes of messages shouldbe suppressed.
6170,zero or more objects which can be coerced to character(and which are pasted together with no separator) or (for<code>message</code> only) a single condition object.
6171,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated, see also the note in <code>stop</code>.
6172,logical: should messages given as a character stringhave a newline appended?
6173,expression to evaluate.
6174,character, indicating which classes of messages shouldbe suppressed.
6175,zero or more objects which can be coerced to character(and which are pasted together with no separator) or a singlecondition object.
6176,logical, indicating if the call should become part of thewarning message.
6177,logical, indicating if the call should be outputimmediately, even if <code>getOption("warn") &lt;= 0</code>.
6178,logical, indicating as far as possible the message shouldbe output as a single line when <code>options(warn = 1)</code>.
6179,expression to evaluate.
6180,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated, see also the note in <code>stop</code>.
6181,character, indicating which classes of warnings shouldbe suppressed.
6182,a condition object.
6183,call expression.
6184,a condition object.
6185,expression to be evaluated.
6186,expression to be evaluated before returning or exiting.
6187,function.
6188,character string.
6189,character string.
6190,character string naming a restart.
6191,restart object.
6192,object.
6193,character string naming a condition class.
6194,additional arguments; see details below.
6195,a numeric or complex matrix whose SVD decompositionis to be computed.  Logical matrices are coerced to numeric.
6196,the number of left  singular vectors to be computed.This must between <code>0</code> and <code>n = nrow(x)</code>.
6197,the number of right singular vectors to be computed.This must be between <code>0</code> and <code>p = ncol(x)</code>.
6198,logical.  Defunct and an error.
6199,an array, including a matrix.
6200,a vector of indices giving the extent(s) of <code>x</code>which correspond to <code>STATS</code>.Where <code>x</code> has named dimnames, it can be a charactervector selecting dimension names.
6201,the summary statistic which is to be swept out.
6202,the function to be used to carry out the sweep.
6203,logical.  If <code>TRUE</code> (the default), warn if thelength or dimensions of <code>STATS</code> do not match the specifieddimensions of <code>x</code>.  Set to <code>FALSE</code> for a small speed gainwhen you <em>know</em> that dimensions match.
6204,optional arguments to <code>FUN</code>.
6205,an expression evaluating to a number or a characterstring.
6206,the list of alternatives.  If it is intended that<code>EXPR</code> has a character-string value these will benamed, perhaps except for one alternative to be used as a‘default’ value.
6207,the frame number if non-negative, the number of framesto go back if negative.
6208,the number of generations to go back.  (See the‘Details’ section.)
6209,the frame number if non-negative, the number of framesto go back if negative.
6210,the number of generations to go back.  (See the‘Details’ section.)
6211,a character vector containing a single path name.  Tildeexpansion (see <code>path.expand</code>) is done.
6212,character vectors containing file or directory paths.  Tildeexpansion (see <code>path.expand</code>) is done.
6213,logical; should the warnings on failure be shown?
6214,logical. Should elements of the path other than thelast be created?  If true, like the Unix command <code>mkdir -p</code>.
6215,the mode to be used on Unix-alikes: it will becoerced by <code>as.octmode</code>.  For <code>Sys.chmod</code> it isrecycled along <code>paths</code>.
6216,logical: should the mode be restricted by the<code>umask</code> setting?
6217,the frame number if non-negative, the number of framesto go back if negative.
6218,the number of generations to go back.  (See the‘Details’ section.)
6219,the frame number if non-negative, the number of framesto go back if negative.
6220,the number of generations to go back.  (See the‘Details’ section.)
6221,the frame number if non-negative, the number of framesto go back if negative.
6222,the number of generations to go back.  (See the‘Details’ section.)
6223,a character vector, or <code>NULL</code>.
6224,a character string.
6225,logical: should the result be named?  If <code>NA</code> (thedefault) single-element results are not named whereas multi-elementresults are.
6226,character string.  The following categories shouldalways be supported: <code>"LC_ALL"</code>, <code>"LC_COLLATE"</code>,<code>"LC_CTYPE"</code>, <code>"LC_MONETARY"</code>, <code>"LC_NUMERIC"</code> and<code>"LC_TIME"</code>.  Some systems (not Windows) will also support<code>"LC_MESSAGES"</code>, <code>"LC_PAPER"</code> and <code>"LC_MEASUREMENT"</code>.
6227,character string.  A valid locale name on the system inuse.  Normally <code>""</code> (the default) will pick up the defaultlocale for the system.
6228,character vector of patterns for relative or absolutefilepaths.  Missing values will be ignored.
6229,logical: should matches to directories from patternsthat do not already end in <code>/</code>have a slash appended?  May not be supported on all platforms.
6230,an object

6231,from which to extract elements.
6232,with a <code>"row.names"</code> attribute, typically adata frame.
6233,typically S3- or S4-classed.
6234,The full path to the‘<span class="file">DESCRIPTION</span>’ file of a package.
6235,the filepath to a lazyload database.
6236,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.
6237,logical.  Should this not give warnings or an errorif the package(s) are not found?
6238,logical.  Should  the <code>Imports</code> field be processed?
6239,logical: should message(s) be printed.
6240,A processed ‘<span class="file">DESCRIPTION</span>’ file of class<code>"packageDescription2"</code>.
6241,platform strings of the form <code>"cpu-vendor-os"</code>.
6242,name of image file to save or load.
6243,integer.  Currently <code>type = 0</code> returns the internal<code>"row.names"</code> attribute (possibly <code>NULL</code>), <code>type = 2</code>the number of rows implied by the attribute, and <code>type = 1</code> thelatter with a negative sign for ‘automatic’ row names.
6244,integer.  The number of rows.
6245,character string such as <code>"package:MASS"</code>.
6246,the object name in a lazyload database.
6247,logical: are the values in the database compressed?
6248,a load hook: see <code>unserialize</code>.
6249,a length-one integer vector.
6250,indices.
6251,logical.
6252,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6253,a classless object to be placed into the class.
6254,an optional time zone.
6255,an optional character vector of classes <em>containing</em>the default classes.
6256,see <code>difftime</code>.
6257,Arguments passed to non-dot name versions.
6258,an integer.
6259,character vector of <em>same</em> objects, masking others.
6260,character string naming the package which is masked from or by.
6261,logical indicating if the masking happens <em>by</em><code>pkg</code>, or (<code>by = FALSE</code>) from <code>pkg</code>.
6262,the frame number if non-negative, the number of framesto go back if negative.
6263,the number of generations to go back.  (See the‘Details’ section.)
6264,the frame number if non-negative, the number of framesto go back if negative.
6265,the number of generations to go back.  (See the‘Details’ section.)
6266,the frame number if non-negative, the number of framesto go back if negative.
6267,the number of generations to go back.  (See the‘Details’ section.)
6268,the frame number if non-negative, the number of framesto go back if negative.
6269,the number of generations to go back.  (See the‘Details’ section.)
6270,character vector of file paths.  Tilde expansion is done:see <code>path.expand</code>.
6271,an object

6272,from which to extract elements.
6273,with a <code>"row.names"</code> attribute, typically adata frame.
6274,typically S3- or S4-classed.
6275,The full path to the‘<span class="file">DESCRIPTION</span>’ file of a package.
6276,the filepath to a lazyload database.
6277,a character vector describing the location of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>library trees to search through, or <code>NULL</code>.  The default valueof <code>NULL</code> corresponds to all libraries currently known to<code>.libPaths()</code>.
6278,logical.  Should this not give warnings or an errorif the package(s) are not found?
6279,logical.  Should  the <code>Imports</code> field be processed?
6280,logical: should message(s) be printed.
6281,A processed ‘<span class="file">DESCRIPTION</span>’ file of class<code>"packageDescription2"</code>.
6282,platform strings of the form <code>"cpu-vendor-os"</code>.
6283,name of image file to save or load.
6284,integer.  Currently <code>type = 0</code> returns the internal<code>"row.names"</code> attribute (possibly <code>NULL</code>), <code>type = 2</code>the number of rows implied by the attribute, and <code>type = 1</code> thelatter with a negative sign for ‘automatic’ row names.
6285,integer.  The number of rows.
6286,character string such as <code>"package:MASS"</code>.
6287,the object name in a lazyload database.
6288,logical: are the values in the database compressed?
6289,a load hook: see <code>unserialize</code>.
6290,a length-one integer vector.
6291,indices.
6292,logical.
6293,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6294,a classless object to be placed into the class.
6295,an optional time zone.
6296,an optional character vector of classes <em>containing</em>the default classes.
6297,see <code>difftime</code>.
6298,Arguments passed to non-dot name versions.
6299,an integer.
6300,character vector of <em>same</em> objects, masking others.
6301,character string naming the package which is masked from or by.
6302,logical indicating if the masking happens <em>by</em><code>pkg</code>, or (<code>by = FALSE</code>) from <code>pkg</code>.
6303,named arguments with values coercible to a character string.
6304,a character vector, or an object coercible to character.
6305,A character vector containing file or directory paths.
6306,A date-time of class <code>"POSIXct"</code> or an object which can becoerced to one.  Fractions of a second may be ignored.  Recycled along<code>paths</code>.
6307,character string.  The following categories shouldalways be supported: <code>"LC_ALL"</code>, <code>"LC_COLLATE"</code>,<code>"LC_CTYPE"</code>, <code>"LC_MONETARY"</code>, <code>"LC_NUMERIC"</code> and<code>"LC_TIME"</code>.  Some systems (not Windows) will also support<code>"LC_MESSAGES"</code>, <code>"LC_PAPER"</code> and <code>"LC_MEASUREMENT"</code>.
6308,character string.  A valid locale name on the system inuse.  Normally <code>""</code> (the default) will pick up the defaultlocale for the system.
6309,The time interval to suspend execution for, in seconds.
6310,a character string naming the file to be read from
6311,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object specifying the environment in which theexpressions are to be evaluated.  May also be a list or an integer.The default value <code>NULL</code> corresponds to evaluation in the baseenvironment.  This is probably not what you want; you shouldtypically supply an explicit <code>envir</code> argument.
6312,logical; if <code>TRUE</code>, the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> working directory ischanged to the directory containing <code>file</code> for evaluating.
6313,logical.  If <code>TRUE</code>, functions keeptheir source including comments, see<code>options(keep.source = *)</code> for more details.
6314,logical.  If <code>TRUE</code> and <code>keep.source</code> isalso <code>TRUE</code>, functions keep parse data with their source, see<code>options(keep.parse.data = *)</code> for more details.
6315,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> environment to be used as top level whileevaluating the expressions.  This argument is useful for frameworksrunning package tests; the default should be used in other cases
6316,the frame number if non-negative, the number of framesto go back if negative.
6317,the number of generations to go back.  (See the‘Details’ section.)
6318,logical: defunct: ignored, with a warning for false values.
6319,The time-zone database to be used: the default is to tryknown locations until one is found.
6320,a character vector containing a single path name.  Tildeexpansion (see <code>path.expand</code>) is done.
6321,character vectors containing file or directory paths.  Tildeexpansion (see <code>path.expand</code>) is done.
6322,logical; should the warnings on failure be shown?
6323,logical. Should elements of the path other than thelast be created?  If true, like the Unix command <code>mkdir -p</code>.
6324,the mode to be used on Unix-alikes: it will becoerced by <code>as.octmode</code>.  For <code>Sys.chmod</code> it isrecycled along <code>paths</code>.
6325,logical: should the mode be restricted by the<code>umask</code> setting?
6326,named arguments with values coercible to a character string.
6327,a character vector, or an object coercible to character.
6328,Character vector of names or paths of possible executables.
6329,the system command to be invoked, as a character string.
6330,a logical (not <code>NA</code>) which indicates whether tocapture the output of the command as an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> character vector.
6331,a logical (not <code>NA</code>)indicating whether messages written to ‘<span class="file">stdout</span>’ or‘<span class="file">stderr</span>’ should be ignored.
6332,a logical (not <code>NA</code>) indicating whether the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>interpreter should wait for the command to finish, or run itasynchronously.  This will be ignored (and the interpreter willalways wait) if <code>intern = TRUE</code>. When running the commandasynchronously, no output will be displayed on the <code>Rgui</code>console in Windows (it will be dropped, instead).
6333,if a character vector is supplied, this is copied onestring per line to a temporary file, and the standard input of<code>command</code> is redirected to the file.
6334,timeout in seconds, ignored if 0.  This is a limit for theelapsed time running <code>command</code> in a separate process.  Fractionsof seconds are ignored.
6335,argumentsthat are accepted on Windows but ignored on this platform, with awarning.
6336,character vectors, specifying subdirectory and file(s)within some package.  The default, none, returns theroot of the package.  Wildcards are not supported.
6337,a character string with the name of a single package.An error occurs if more than one package name is given.
6338,a character vector with path names of <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> libraries.See ‘Details’ for the meaning of the default value of <code>NULL</code>.
6339,logical.  If <code>TRUE</code>, an error is given if thereare no matching files.
6340,Valid <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression to be timed.
6341,Logical - should a garbage collection be performedimmediately before the timing?  Default is <code>TRUE</code>.
6342,the system command to be invoked, as a character string.
6343,a character vector of arguments to <code>command</code>.
6344,where output to ‘<span class="file">stdout</span>’ or‘<span class="file">stderr</span>’ should be sent.  Possible values are <code>""</code>, to the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>console (the default), <code>NULL</code> or <code>FALSE</code> (discard output),<code>TRUE</code> (capture the output in a character vector) or acharacter string naming a file.
6345,should input be diverted?  <code>""</code> means the default,alternatively a character string naming a file.  Ignoredif <code>input</code> is supplied.
6346,if a character vector is supplied, this is copied onestring per line to a temporary file, and the standard input of<code>command</code> is redirected to the file.
6347,character vector of name=value strings to set environmentvariables.
6348,a logical (not <code>NA</code>) indicating whether the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>interpreter should wait for the command to finish, or run itasynchronously.  This will be ignored (and the interpreter willalways wait) if <code>stdout = TRUE</code> or <code>stderr = TRUE</code>. Whenrunning the command asynchronously, no output will be displayed onthe <code>Rgui</code> console in Windows (it will be dropped, instead).
6349,timeout in seconds, ignored if 0.  This is a limit for theelapsed time running <code>command</code> in a separate process.   Fractionsof seconds are ignored.
6350,arguments that are accepted on Windows butignored on this platform, with a warning.
6351,a matrix or data frame, typically.
6352,A non-negative integer specifying the desired length.Double values will be coerced to integer:supplying an argument of length other than one is an error.
6353,object to be coerced or tested.
6354,further arguments passed to or from other methods.
6355,a matrix or data frame, typically.
6356,a matrix or data frame, typically.
6357,one or more objects which can be interpreted as factors(including character strings), or a list (or data frame) whosecomponents can be so interpreted.  (For <code>as.table</code>, argumentspassed to specific methods; for <code>as.data.frame</code>, unused.)
6358,levels to remove for all factors in <code>...</code>.  Ifit does not contain <code>NA</code> and <code>useNA</code> is notspecified, it implies <code>useNA = "ifany"</code>.  See‘Details’ for its interpretation for non-factor arguments.
6359,whether to include <code>NA</code> values in the table.See ‘Details’.  Can be abbreviated.
6360,the names to be given to the dimensions in the result (the<em>dimnames names</em>).
6361,controls how the default <code>dnn</code> isconstructed.  See ‘Details’.
6362,an arbitrary <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, or an object inheriting from class<code>"table"</code> for the <code>as.data.frame</code> method. Note that<code>as.data.frame.table(x, *)</code> may be called explicitly fornon-table <code>x</code> for “reshaping” <code>array</code>s.
6363,a character vector giving the row names for the dataframe.
6364,The name to be used for the column of tableentries, usually counts.
6365,logical: should the classifying factors bereturned as factors (the default) or character vectors?
6366,passed to <code>provideDimnames</code>.
6367,a numeric vector (of positive integers), or a factor.Long vectors are supported.
6368,the number of bins to be used.
6369,numeric or complex vectors.
6370,a numeric or complex vector
6371,numeric or complex vectors.
6372,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object for which a <code>split</code> methodexists.  Typically vector-like, allowing subsetting with<code>[</code>.
6373,a <code>list</code> of one or more <code>factor</code>s,each of same length as <code>X</code>.  The elements are coerced tofactors by <code>as.factor</code>.
6374,a function (or name of a function) to be applied, or <code>NULL</code>.In the case of functions like <code>+</code>, <code>%*%</code>, etc.,the function name must be backquoted or quoted.  If <code>FUN</code> is<code>NULL</code>, tapply returns a vector which can be used to subscriptthe multi-way array <code>tapply</code> normally produces.
6375,optional arguments to <code>FUN</code>: the Note section.
6376,(only in the case of simplification to an array) thevalue with which the array is initialized as<code>array(default, dim = ..)</code>.  Before <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.4.0, thiswas hard coded to <code>array()</code>'s default <code>NA</code>.  If itis <code>NA</code> (the default), the missing value of the answer type,e.g. <code>NA_real_</code>, is chosen (<code>as.raw(0)</code> for<code>"raw"</code>).  In a numerical case, it may be set, e.g., to<code>FUN(integer(0))</code>, e.g., in the case of <code>FUN = sum</code> to<code>0</code> or <code>0L</code>.
6377,logical; if <code>FALSE</code>, <code>tapply</code> always returnsan array of mode <code>"list"</code>; in other words, a <code>list</code>with a <code>dim</code> attribute.  If <code>TRUE</code> (the default), then if<code>FUN</code> always returns a scalar, <code>tapply</code> returns an arraywith the mode of the scalar.
6378,this can be a list of callbacks in which each elementis a list with  an element  named <code>"f"</code>which is a callback function, and an optionalelement named <code>"data"</code> which is the 5-th argument to besupplied  to the callback when it is invoked.Typically this argument is not specified, and one uses <code>add</code> toregister callbacks after the manager is created.
6379,a logical value indicating whetherthe <code>evaluate</code> function has already been registeredwith the internal task callback mechanism.This is usually <code>FALSE</code> andthe first time a callback is addedvia the <code>add</code> function, the<code>evaluate</code> function is automatically registered.One can control when the function is registeredby specifying <code>TRUE</code> for this argumentand calling <code>addTaskCallback</code> manually.
6380,a logical value, which if <code>TRUE</code>,causes information to be printed to the consoleabout certain activities this dispatch manager performs.This is useful for debugging callbacks and the handleritself.
6381,numeric or complex matrices (or vectors): <code>y = NULL</code>is taken to be the same matrix as <code>x</code>.  Vectors are promoted tosingle-column or single-row matrices, depending on the context.
6382,a non-empty character vector giving the initial partof the name.
6383,a non-empty character vector giving the directory name
6384,a non-empty character vector giving the file extension
6385,<code>logical</code> indicating if <code>tmpdir()</code>should be checked and recreated if no longer valid.
6386,a non-empty character vector giving the initial partof the name.
6387,a non-empty character vector giving the directory name
6388,a non-empty character vector giving the file extension
6389,<code>logical</code> indicating if <code>tmpdir()</code>should be checked and recreated if no longer valid.
6390,character.  A description of the connection.For an input this is an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> character vector object, and for an outputconnection the name for the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> character vector to receive theoutput, or <code>NULL</code> (for none).
6391,character string.  Either <code>"r"</code> (or equivalently <code>""</code>)for an input connection or <code>"w"</code> or <code>"a"</code> for an outputconnection.
6392,logical.  Used only for output connections.  If <code>TRUE</code>,output is assigned to a variable in the calling environment.  Otherwisethe global environment is used.
6393,a <code>character</code> string specifying the connection name.
6394,character string, partially matched.  Used only for input connections.  Howmarked strings in <code>object</code> should be handled: converted to thecurrent locale, used byte-by-byte or translated to UTF-8.
6395,An output text connection.
6396,character.  A description of the connection.For an input this is an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> character vector object, and for an outputconnection the name for the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> character vector to receive theoutput, or <code>NULL</code> (for none).
6397,character string.  Either <code>"r"</code> (or equivalently <code>""</code>)for an input connection or <code>"w"</code> or <code>"a"</code> for an outputconnection.
6398,logical.  Used only for output connections.  If <code>TRUE</code>,output is assigned to a variable in the calling environment.  Otherwisethe global environment is used.
6399,a <code>character</code> string specifying the connection name.
6400,character string, partially matched.  Used only for input connections.  Howmarked strings in <code>object</code> should be handled: converted to thecurrent locale, used byte-by-byte or translated to UTF-8.
6401,An output text connection.
6402,a character vector, or an object that can be coerced tocharacter by <code>as.character</code>.
6403,a character string specifying the characters to betranslated.  If a character vector of length 2 or more is supplied,the first element is used with a warning.
6404,a character string specifying the translations. If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.
6405,logical: translate to upper or lower case?.
6406,environment.
6407,return this environment, if it matches beforeany other criterion is satisfied.  The default, the option<span class="samp">topLevelEnvironment</span>, is set by <code>sys.source</code>,which treats a specific environment as the top level environment.Supplying the argument as <code>NULL</code> or <code>emptyenv()</code> meansit will never match.
6408,The object to be converted.
6409,Suggestion for the maximum field width.  Values of<code>NULL</code> or <code>0</code> indicate no maximum.The minimum value accepted is 6 and smaller values are taken as 6.
6410,Optional arguments passed to or from methods.
6411,The object to be converted.
6412,Suggestion for the maximum field width.  Values of<code>NULL</code> or <code>0</code> indicate no maximum.The minimum value accepted is 6 and smaller values are taken as 6.
6413,Optional arguments passed to or from methods.
6414,a character vector, or an object that can be coerced tocharacter by <code>as.character</code>.
6415,a character string specifying the characters to betranslated.  If a character vector of length 2 or more is supplied,the first element is used with a warning.
6416,a character string specifying the translations. If acharacter vector of length 2 or more is supplied, the first elementis used with a warning.
6417,logical: translate to upper or lower case?.
6418,the name, possibly <code>quote()</code>d, of a functionto be traced or untraced.  For <code>untrace</code> or for <code>trace</code>with more than one argument, more than one name can be given in thequoted form, and the same action will be applied to each one.  For“hidden” functions such as S3 methods in a namespace,<code>where = *</code> typically needs to be specified as well.
6419,either a function or an unevaluated expression.  Thefunction will be called or the expression will be evaluated eitherat the beginning of the call, or before those steps in the callspecified by the argument <code>at</code>.See the details section.
6420,either a <code>function</code> or an unevaluated expression.  Thefunction will be called or the expression will be evaluated onexiting the function.See the details section.
6421,optional numeric vector or list.  If supplied, <code>tracer</code>will be called just before the corresponding step in the body of thefunction.See the details section. 
6422,If <code>TRUE</code> (as per default), a descriptive line isprinted before any trace expression is evaluated.
6423, If this argument is supplied, it should be asignature for a method for function <code>what</code>.  In this case, themethod, and <em>not</em> the function itself, is traced.
6424, For complicated tracing, such as tracing within a loopinside the function, you will need to insert the desired calls byediting the body of the function.  If so, supply the <code>edit</code>argument either as <code>TRUE</code>, or as the name of the editor youwant to use.  Then <code>trace()</code> will call <code>edit</code> anduse the version of the function after you edit it.  See the detailssection for additional information.
6425,where to look for the function to betraced; by default, the top-level environment of the call to<code>trace</code>.

6426,An important use of this argument is to trace functions from apackage which are “hidden” or called from another package.The namespace mechanism imports the functions to be called (with theexception of functions in the base package).  The functions beingcalled are <em>not</em> the same objects seen from the top-level (ingeneral, the imported packages may not even be attached).Therefore, you must ensure that the correct versions are beingtraced.  The way to do this is to set argument <code>where</code> to afunction in the namespace (or that namespace).  The tracingcomputations will then start looking in the environment of thatfunction (which will be the namespace of the corresponding package).(Yes, it's subtle, but the semantics here are central to hownamespaces work in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>.)
6427,logical; a call to the support function <code>tracingState</code> returns <code>TRUE</code>if tracing is globally turned on, <code>FALSE</code> otherwise.  Anargument of one or the other of those values sets the state.  If thetracing state is <code>FALSE</code>, none of the trace actions willactually occur (used, for example, by debugging functions to shutoff tracing during debugging).
6428,arguments to the support function <code>.doTrace</code>, calls towhich are inserted into the modified function or method:<code>expr</code> is the tracing action (such as a call to<code>browser()</code>), and <code>msg</code> is a string identifying theplace where the trace action occurs.
6429,If <code>returnValue</code> finds no return value (e.g.a function exited because of an error, restart or as a resultof evaluating a return from a caller function), it will return<code>default</code> instead.
6430,<code>NULL</code> (default, meaning <code>.Traceback</code>), or aninteger count of calls to skip in the current stack, or a list orpairlist of calls.  See the details.
6431,a number, the maximum number of lines to be printed<em>per call</em>.  The default is unlimited.  Applies only when <code>x</code>is <code>NULL</code>, a <code>list</code> or a <code>pairlist</code> ofcalls, see the details.
6432,An R object, not a function or environment or <code>NULL</code>.
6433,A value as returned by <code>tracemem</code> or <code>retracemem</code>.
6434,the name, possibly <code>quote()</code>d, of a functionto be traced or untraced.  For <code>untrace</code> or for <code>trace</code>with more than one argument, more than one name can be given in thequoted form, and the same action will be applied to each one.  For“hidden” functions such as S3 methods in a namespace,<code>where = *</code> typically needs to be specified as well.
6435,either a function or an unevaluated expression.  Thefunction will be called or the expression will be evaluated eitherat the beginning of the call, or before those steps in the callspecified by the argument <code>at</code>.See the details section.
6436,either a <code>function</code> or an unevaluated expression.  Thefunction will be called or the expression will be evaluated onexiting the function.See the details section.
6437,optional numeric vector or list.  If supplied, <code>tracer</code>will be called just before the corresponding step in the body of thefunction.See the details section. 
6438,If <code>TRUE</code> (as per default), a descriptive line isprinted before any trace expression is evaluated.
6439, If this argument is supplied, it should be asignature for a method for function <code>what</code>.  In this case, themethod, and <em>not</em> the function itself, is traced.
6440, For complicated tracing, such as tracing within a loopinside the function, you will need to insert the desired calls byediting the body of the function.  If so, supply the <code>edit</code>argument either as <code>TRUE</code>, or as the name of the editor youwant to use.  Then <code>trace()</code> will call <code>edit</code> anduse the version of the function after you edit it.  See the detailssection for additional information.
6441,where to look for the function to betraced; by default, the top-level environment of the call to<code>trace</code>.

6442,An important use of this argument is to trace functions from apackage which are “hidden” or called from another package.The namespace mechanism imports the functions to be called (with theexception of functions in the base package).  The functions beingcalled are <em>not</em> the same objects seen from the top-level (ingeneral, the imported packages may not even be attached).Therefore, you must ensure that the correct versions are beingtraced.  The way to do this is to set argument <code>where</code> to afunction in the namespace (or that namespace).  The tracingcomputations will then start looking in the environment of thatfunction (which will be the namespace of the corresponding package).(Yes, it's subtle, but the semantics here are central to hownamespaces work in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>.)
6443,logical; a call to the support function <code>tracingState</code> returns <code>TRUE</code>if tracing is globally turned on, <code>FALSE</code> otherwise.  Anargument of one or the other of those values sets the state.  If thetracing state is <code>FALSE</code>, none of the trace actions willactually occur (used, for example, by debugging functions to shutoff tracing during debugging).
6444,arguments to the support function <code>.doTrace</code>, calls towhich are inserted into the modified function or method:<code>expr</code> is the tracing action (such as a call to<code>browser()</code>), and <code>msg</code> is a string identifying theplace where the trace action occurs.
6445,If <code>returnValue</code> finds no return value (e.g.a function exited because of an error, restart or as a resultof evaluating a return from a caller function), it will return<code>default</code> instead.
6446,The object to be transformed
6447,Further arguments of the form <code>tag=value</code>
6448,The object to be transformed
6449,Further arguments of the form <code>tag=value</code>
6450,The object to be transformed
6451,Further arguments of the form <code>tag=value</code>
6452,non-negative numeric vectors.
6453,numeric vectors.
6454,integer vectors.
6455,a character vector
6456,a character string specifying whether to remove bothleading and trailing whitespace (default), or only leading(<code>"left"</code>) or trailing (<code>"right"</code>).  Can be abbreviated.
6457,a string specifying a regular expression to match(one character of) “white space”, see Details foralternatives to the default.
6458,a numeric vector.  Or, for <code>round</code> and <code>signif</code>, acomplex vector.
6459,integer indicating the number of decimal places(<code>round</code>) or significant digits (<code>signif</code>) to be used.Negative values are allowed (see ‘Details’).
6460,arguments to be passed to methods.
6461,an object inheriting from <code>"POSIXt"</code> or <code>"Date"</code>.
6462,one of the units listed.  Can be abbreviated.
6463,arguments to be passed to or from other methods, notably<code>digits</code> for <code>round</code>.
6464,an object inheriting from <code>"POSIXt"</code> or <code>"Date"</code>.
6465,one of the units listed.  Can be abbreviated.
6466,arguments to be passed to or from other methods, notably<code>digits</code> for <code>round</code>.
6467,a connection.
6468,numeric.  A file position (relative to the originspecified by <code>origin</code>), or <code>NA</code>.
6469,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
6470,character string.  One of <code>"start"</code>, <code>"current"</code>,<code>"end"</code>: see ‘Details’.
6471,further arguments passed to or from other methods.
6472,a connection.
6473,numeric.  A file position (relative to the originspecified by <code>origin</code>), or <code>NA</code>.
6474,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
6475,character string.  One of <code>"start"</code>, <code>"current"</code>,<code>"end"</code>: see ‘Details’.
6476,further arguments passed to or from other methods.
6477,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> expression to try.
6478,logical: should the report of error messages besuppressed?
6479,a connection, or a character string naming thefile to print to (via <code>cat(*, file = outFile)</code>);used only if <code>silent</code> is false, as by default.
6480,a condition object.
6481,call expression.
6482,a condition object.
6483,expression to be evaluated.
6484,expression to be evaluated before returning or exiting.
6485,function.
6486,character string.
6487,character string.
6488,character string naming a restart.
6489,restart object.
6490,object.
6491,character string naming a condition class.
6492,additional arguments; see details below.
6493,a condition object.
6494,call expression.
6495,a condition object.
6496,expression to be evaluated.
6497,expression to be evaluated before returning or exiting.
6498,function.
6499,character string.
6500,character string.
6501,character string naming a restart.
6502,restart object.
6503,object.
6504,character string naming a condition class.
6505,additional arguments; see details below.
6506,any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6507,a <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object
6508,a character vector naming classes.  <code>value</code>can also be <code>NULL</code>.
6509,logical affecting return value: see ‘Details’.
6510,any interpreted <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> function.
6511,a text string that can be retrieved when the browser is entered.
6512,a condition that can be retrieved when the browser isentered.
6513,an optional method signature. If specified, themethod is debugged, rather than its generic.
6514,logical; a call to the support function<code>debuggingState</code> returns <code>TRUE</code> if debugging is globallyturned on, <code>FALSE</code> otherwise.  An argument of one or the otherof those values sets the state.  If the debugging state is<code>FALSE</code>, none of the debugging actions will occur (but explicit<code>browser</code> calls in functions will continue to work).
6515,vectors (of the same mode) containing a sequenceof items (conceptually) with no duplicated values.
6516,a vector or a data frame or an array or <code>NULL</code>.
6517,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
6518,logical indicating if duplication should be consideredfrom the last, i.e., the last (or rightmost) of identical elements willbe kept.  This only matters for <code>names</code> or<code>dimnames</code>.
6519,the maximum number of unique items expected (greater than one).See <code>duplicated</code>.
6520,arguments for particular methods.
6521,the array margin to be held fixed: a single integer.
6522,a vector or a data frame or an array or <code>NULL</code>.
6523,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
6524,logical indicating if duplication should be consideredfrom the last, i.e., the last (or rightmost) of identical elements willbe kept.  This only matters for <code>names</code> or<code>dimnames</code>.
6525,the maximum number of unique items expected (greater than one).See <code>duplicated</code>.
6526,arguments for particular methods.
6527,the array margin to be held fixed: a single integer.
6528,a vector or a data frame or an array or <code>NULL</code>.
6529,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
6530,logical indicating if duplication should be consideredfrom the last, i.e., the last (or rightmost) of identical elements willbe kept.  This only matters for <code>names</code> or<code>dimnames</code>.
6531,the maximum number of unique items expected (greater than one).See <code>duplicated</code>.
6532,arguments for particular methods.
6533,the array margin to be held fixed: a single integer.
6534,a vector or a data frame or an array or <code>NULL</code>.
6535,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
6536,logical indicating if duplication should be consideredfrom the last, i.e., the last (or rightmost) of identical elements willbe kept.  This only matters for <code>names</code> or<code>dimnames</code>.
6537,the maximum number of unique items expected (greater than one).See <code>duplicated</code>.
6538,arguments for particular methods.
6539,the array margin to be held fixed: a single integer.
6540,a vector or a data frame or an array or <code>NULL</code>.
6541,a vector of values that cannot be compared.<code>FALSE</code> is a special value, meaning that all values can becompared, and may be the only value accepted for methods other thanthe default.  It will be coerced internally to the same type as<code>x</code>.
6542,logical indicating if duplication should be consideredfrom the last, i.e., the last (or rightmost) of identical elements willbe kept.  This only matters for <code>names</code> or<code>dimnames</code>.
6543,the maximum number of unique items expected (greater than one).See <code>duplicated</code>.
6544,arguments for particular methods.
6545,the array margin to be held fixed: a single integer.
6546,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
6547,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
6548,an object to be printed or summarized from one of thedate-time classes.
6549,for timezone formatting, passed to <code>format.POSIXct</code>.
6550,numeric or <code>NULL</code>, specifying the maximal number ofentries to be printed.  By default, when <code>NULL</code>,<code>getOption("max.print")</code> used.
6551,number of significant digits for the computations:should be high enough to represent the least important time unitexactly.
6552,further arguments to be passed from or to other methods.
6553,date-time objects
6554,date-time objects or character vectors.  (Charactervectors are converted by <code>as.POSIXct</code>.)
6555,a numeric vector (in seconds)
6556,one of <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>or <code>&gt;=</code>.
6557,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
6558,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
6559,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
6560,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
6561,a character string <code>cat()</code>ed before themessages are printed.
6562,date-time or date objects.
6563,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
6564,character string.  Units in which the results aredesired.  Can be abbreviated.
6565,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
6566,character string or numeric value specifying a time interval.
6567,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
6568,an object inheriting from class <code>"difftime"</code>.
6569,arguments to be passed to or from other methods.
6570,date-time or date objects.
6571,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
6572,character string.  Units in which the results aredesired.  Can be abbreviated.
6573,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
6574,character string or numeric value specifying a time interval.
6575,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
6576,an object inheriting from class <code>"difftime"</code>.
6577,arguments to be passed to or from other methods.
6578,date-time or date objects.
6579,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
6580,character string.  Units in which the results aredesired.  Can be abbreviated.
6581,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
6582,character string or numeric value specifying a time interval.
6583,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
6584,an object inheriting from class <code>"difftime"</code>.
6585,arguments to be passed to or from other methods.
6586,date-time or date objects.
6587,an optional time zone specification to be used for theconversion, mainly for <code>"POSIXlt"</code> objects.
6588,character string.  Units in which the results aredesired.  Can be abbreviated.
6589,character string.  Like <code>units</code>, except thatabbreviations are not allowed.
6590,character string or numeric value specifying a time interval.
6591,character specifying the format of <code>tim</code>: see<code>strptime</code>.  The default is a locale-specific time format.
6592,an object inheriting from class <code>"difftime"</code>.
6593,arguments to be passed to or from other methods.
6594,(arguments)
6595,a character vector with the names of the file(s) ordirectories to be deleted.
6596,logical.  Should directories be deleted recursively?
6597,logical.  Should permissions be changed (if possible) toallow the file or directory to be removed?
6598,logical. Should wildcards (see ‘Details’ below) andtilde (see <code>path.expand</code>) be expanded?
6599,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object, typically a list or vector.
6600,logical.  Should unlisting be applied to listcomponents of <code>x</code>?
6601,logical.  Should names be preserved?
6602,string or name space object.
6603,integer specifying position to attach.
6604,<code>NULL</code> or a character vector of dependencies to berecorded in object <code>.Depends</code> in the package.
6605,string naming the package/name space to load.
6606,character vector specifying library search path.
6607,Now ignored except during package installation.
6608,Ignored except during package installation.
6609,logical; if true, stop just after loading code.
6610,<code>NULL</code> or a version specification (a listwith components <code>op</code> and <code>version</code>).
6611,logical: should progress and error messages be suppressed?
6612,string or ‘name’, see <code>as.symbol</code>,of a package, e.g., <code>"stats"</code>.
6613,character vectors; see <code>library</code>.
6614,further arguments to be passed to <code>loadNamespace</code>.
6615,an environment.
6616,logical specifying whether bindings should be locked.
6617,a name object or character string.
6618,a function taking zero or one arguments.
6619,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6620,logical; if true, the <code>dimnames</code> (names and rownames) are removed even from <code>data.frame</code>s.
6621,<span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to serialize.
6622,an open connection or (for <code>serialize</code>)<code>NULL</code> or (for <code>unserialize</code>) a raw vector(see ‘Details’).
6623,a logical.  If <code>TRUE</code> or <code>NA</code>, an ASCIIrepresentation is written; otherwise (default) a binary one.See also the comments in the help for <code>save</code>.
6624,a logical: if a binary representation is used, should abig-endian one (XDR) be used?
6625,the workspace format version to use.  <code>NULL</code>specifies the current default version (3). The only other supportedvalue is 2, the default from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 1.4.0 to <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.5.0.
6626,a hook function for handling reference objects.
6627,vector or data frame containing values to be divided into groups.
6628,a ‘factor’ in the sense that <code>as.factor(f)</code>defines the grouping, or a list of such factors in which case theirinteraction is used for the grouping. If <code>x</code> is a data frame,<code>f</code> can also be a formula of the form <code> ~ g</code> to split bythe variable <code>g</code>, or more generally of the form <code> ~ g1 +      ... + gk</code> to split by the interaction of the variables<code>g1</code>, ..., <code>gk</code>, where these variables are evaluated inthe data frame <code>x</code> using the usual non-standard evaluationrules.
6629,logical indicating if levels that do not occur should be dropped(if <code>f</code> is a <code>factor</code> or a list).
6630,a list of vectors or data frames compatible with asplitting of <code>x</code>. Recycling applies if the lengths do not match.
6631,character string, passed to <code>interaction</code> in thecase where <code>f</code> is a <code>list</code>.
6632,logical, passed to <code>interaction</code> when<code>f</code> is a list.
6633,further potential arguments passed to methods.
6634,the name, possibly <code>quote()</code>d, of a functionto be traced or untraced.  For <code>untrace</code> or for <code>trace</code>with more than one argument, more than one name can be given in thequoted form, and the same action will be applied to each one.  For“hidden” functions such as S3 methods in a namespace,<code>where = *</code> typically needs to be specified as well.
6635,either a function or an unevaluated expression.  Thefunction will be called or the expression will be evaluated eitherat the beginning of the call, or before those steps in the callspecified by the argument <code>at</code>.See the details section.
6636,either a <code>function</code> or an unevaluated expression.  Thefunction will be called or the expression will be evaluated onexiting the function.See the details section.
6637,optional numeric vector or list.  If supplied, <code>tracer</code>will be called just before the corresponding step in the body of thefunction.See the details section. 
6638,If <code>TRUE</code> (as per default), a descriptive line isprinted before any trace expression is evaluated.
6639, If this argument is supplied, it should be asignature for a method for function <code>what</code>.  In this case, themethod, and <em>not</em> the function itself, is traced.
6640, For complicated tracing, such as tracing within a loopinside the function, you will need to insert the desired calls byediting the body of the function.  If so, supply the <code>edit</code>argument either as <code>TRUE</code>, or as the name of the editor youwant to use.  Then <code>trace()</code> will call <code>edit</code> anduse the version of the function after you edit it.  See the detailssection for additional information.
6641,where to look for the function to betraced; by default, the top-level environment of the call to<code>trace</code>.

6642,An important use of this argument is to trace functions from apackage which are “hidden” or called from another package.The namespace mechanism imports the functions to be called (with theexception of functions in the base package).  The functions beingcalled are <em>not</em> the same objects seen from the top-level (ingeneral, the imported packages may not even be attached).Therefore, you must ensure that the correct versions are beingtraced.  The way to do this is to set argument <code>where</code> to afunction in the namespace (or that namespace).  The tracingcomputations will then start looking in the environment of thatfunction (which will be the namespace of the corresponding package).(Yes, it's subtle, but the semantics here are central to hownamespaces work in <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>.)
6643,logical; a call to the support function <code>tracingState</code> returns <code>TRUE</code>if tracing is globally turned on, <code>FALSE</code> otherwise.  Anargument of one or the other of those values sets the state.  If thetracing state is <code>FALSE</code>, none of the trace actions willactually occur (used, for example, by debugging functions to shutoff tracing during debugging).
6644,arguments to the support function <code>.doTrace</code>, calls towhich are inserted into the modified function or method:<code>expr</code> is the tracing action (such as a call to<code>browser()</code>), and <code>msg</code> is a string identifying theplace where the trace action occurs.
6645,If <code>returnValue</code> finds no return value (e.g.a function exited because of an error, restart or as a resultof evaluating a return from a caller function), it will return<code>default</code> instead.
6646,An R object, not a function or environment or <code>NULL</code>.
6647,A value as returned by <code>tracemem</code> or <code>retracemem</code>.
6648,character string.  A description of the connection:see ‘Details’.
6649,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
6650,logical.  See the ‘Blocking’ section.
6651,The name of the encoding to be assumed.  See the‘Encoding’ section.
6652,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
6653,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
6654,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
6655,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
6656,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
6657,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
6658,a filename within a zip file.
6659,character string.  Host name for the port.
6660,integer.  The TCP port number.
6661,logical.  Should the socket be a client or a server?
6662,a server socket listening for connections.
6663,a connection.
6664,character string.  Currently ignored.
6665,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
6666,arguments passed to or from other methods.
6667,a matrix or other <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object with <code>length(dim(x)) == 2</code>.For back compatibility reasons, when the above is not fulfilled,<code>as.matrix(x)</code> is called first.
6668,logical.  Should the diagonal be included?
6669,character string.  A description of the connection:see ‘Details’.
6670,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
6671,logical.  See the ‘Blocking’ section.
6672,The name of the encoding to be assumed.  See the‘Encoding’ section.
6673,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
6674,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
6675,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
6676,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
6677,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
6678,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
6679,a filename within a zip file.
6680,character string.  Host name for the port.
6681,integer.  The TCP port number.
6682,logical.  Should the socket be a client or a server?
6683,a server socket listening for connections.
6684,a connection.
6685,character string.  Currently ignored.
6686,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
6687,arguments passed to or from other methods.
6688,a character string naming a function (and not abuilt-in operator).  Required for <code>UseMethod</code>.
6689,for <code>UseMethod</code>: an object whose class willdetermine the method to be dispatched.  Defaults to the firstargument of the enclosing function.
6690,further arguments to be passed to the next method.
6691,object to be converted.
6692,logical: should the conversion be to a singlecharacter string or multiple individual characters?
6693,logical: should interpretation ofsurrogate pairs be attempted?  (See ‘Details’.)Only supported for <code>multiple = FALSE</code>.
6694,a character vector.
6695,a character vector.
6696,a vector (atomic or list) or an <code>expression</code>object.  Other objects (including classed objects) will be coercedby <code>base::as.list</code>.
6697,the function to be applied to each element of <code>X</code>:see ‘Details’.  In the case of functions like<code>+</code>, <code>%*%</code>, the function name must be backquoted or quoted.
6698,optional arguments to <code>FUN</code>.
6699,logical or character string; should the result besimplified to a vector, matrix or higher dimensional array ifpossible?  For <code>sapply</code> it must be named and not abbreviated.The default value, <code>TRUE</code>, returns a vector or matrix if appropriate,whereas if <code>simplify = "array"</code> the result may be an<code>array</code> of “rank”(<i>=</i><code>length(dim(.))</code>) one higher than the resultof <code>FUN(X[[i]])</code>.
6700,logical; if <code>TRUE</code> and if <code>X</code> is character,use <code>X</code> as <code>names</code> for the result unless it had namesalready.  Since this argument follows <code>...</code> its name cannotbe abbreviated.
6701,a (generalized) vector; a template for the returnvalue from FUN.  See ‘Details’.
6702,integer: the number of replications.
6703,the expression (a language object, usually a call)to evaluate repeatedly.
6704,a list, typically returned from <code>lapply()</code>.
6705,logical; if true, <code>simplify2array()</code> will produce a(“higher rank”) array when appropriate, whereas<code>higher = FALSE</code> would return a matrix (or vector) only.These two cases correspond to <code>sapply(*, simplify = "array")</code> or<code>simplify = TRUE</code>, respectively.
6706,character string naming an atomic mode or<code>"list"</code> or <code>"expression"</code> or (except for <code>vector</code>)<code>"any"</code>.  Currently, <code>is.vector()</code> allows any type (see<code>typeof</code>) for <code>mode</code>, and when mode is not<code>"any"</code>, <code>is.vector(x, mode)</code> is almost the same as<code>typeof(x) == mode</code>.
6707,a non-negative integer specifying the desired length.  Fora long vector, i.e., <code>length &gt; .Machine$integer.max</code>, ithas to be of type <code>"double"</code>.  Supplying an argument of lengthother than one is an error.
6708,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6709,function to apply, found via <code>match.fun</code>.
6710,a character vector of arguments which should bevectorized.  Defaults to all arguments of <code>FUN</code>.
6711,logical or character string; attempt to reduce theresult to a vector, matrix or higher dimensional array; seethe <code>simplify</code> argument of <code>sapply</code>.
6712,logical; use names if the first ... argument hasnames, or if it is a character vector, use that character vector asthe names.
6713,zero or more objects which can be coerced to character(and which are pasted together with no separator) or a singlecondition object.
6714,logical, indicating if the call should become part of thewarning message.
6715,logical, indicating if the call should be outputimmediately, even if <code>getOption("warn") &lt;= 0</code>.
6716,logical, indicating as far as possible the message shouldbe output as a single line when <code>options(warn = 1)</code>.
6717,expression to evaluate.
6718,see <code>gettext</code>.  If <code>NA</code>, messages willnot be translated, see also the note in <code>stop</code>.
6719,character, indicating which classes of warnings shouldbe suppressed.
6720,a condition object.
6721,call expression.
6722,a condition object.
6723,expression to be evaluated.
6724,expression to be evaluated before returning or exiting.
6725,function.
6726,character string.
6727,character string.
6728,character string naming a restart.
6729,restart object.
6730,object.
6731,character string naming a condition class.
6732,additional arguments; see details below.
6733,arguments to be passed to <code>cat</code> (for<code>warnings()</code>).
6734,a <code>"warnings"</code> object as returned by<code>warnings()</code>.
6735,a <code>"warnings"</code> or <code>"summary.warnings"</code> object.
6736,if not <code>missing</code>, a <code>character</code>vector of the same <code>length</code> as <code>x</code>, to “label”the messages.  Defaults to <code>paste0(seq_len(n), ": ")</code> for<i>n &gt;= 2</i> where <code>n &lt;- length(x)</code>.
6737,a character string <code>cat()</code>ed before themessages are printed.
6738,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
6739,logical vector (possibly recycled).  Should the names beabbreviated?
6740,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
6741,arguments for other methods.
6742,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
6743,logical vector (possibly recycled).  Should the names beabbreviated?
6744,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
6745,arguments for other methods.
6746,an object inheriting from class <code>"POSIXt"</code> or <code>"Date"</code>.
6747,logical vector (possibly recycled).  Should the names beabbreviated?
6748,an length-one object inheriting from class<code>"POSIXt"</code> or <code>"Date"</code>.
6749,arguments for other methods.
6750,a <code>logical</code> vector or array.  <code>NA</code>sare allowed and omitted (treated as if <code>FALSE</code>).
6751,logical; should <b>arr</b>ay <b>ind</b>ices be returnedwhen <code>x</code> is an array?
6752,integer-valued index vector, as resulting from<code>which(x)</code>.
6753,<code>dim(.)</code> integer vector
6754,optional list of character <code>dimnames(.)</code>.If <code>useNames</code> is true, to be used for constructing dimnames for<code>arrayInd()</code> (and hence, <code>which(*, arr.ind=TRUE)</code>).If <code>names(.dimnames)</code> is not empty, these are used ascolumn names.  <code>.dimnames[[1]]</code> is used as row names.
6755,logical indicating if the value of <code>arrayInd()</code>should have (non-null) dimnames at all.
6756,numeric (logical, integer or double) vector or an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectfor which the internal coercion to <code>double</code> works whose<code>min</code> or <code>max</code> is searched for.
6757,numeric (logical, integer or double) vector or an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> objectfor which the internal coercion to <code>double</code> works whose<code>min</code> or <code>max</code> is searched for.
6758,A length-one logical vector that is not <code>NA</code>.Conditions of length greater than one are currently accepted with awarning, but only the first element is used.  An error is signalledinstead when the environment variable <span class="env">_R_CHECK_LENGTH_1_CONDITION_</span>is set to true.  Other types are coerced to logicalif possible, ignoring any class.
6759,A syntactical name for a variable.
6760,An expression evaluating to a vector (including a list andan expression) or to a pairlist or <code>NULL</code>.  Afactor value will be coerced to a character vector.  As from <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span>4.0.0 this can be a long vector.
6761,An <em>expression</em> in a formal sense.  This is either asimple expression or a so-called <em>compound expression</em>, usuallyof the form <code>{ expr1 ; expr2 }</code>.
6762,data to use for constructing an environment. For thedefault <code>with</code> method this may be an environment, a list, adata frame, or an integer as in <code>sys.call</code>. For <code>within</code>,it can be a list or a data frame.
6763,expression to evaluate; particularly for <code>within()</code>often a “compound” expression, i.e., of the form 

6764,for the <code>list</code> method of <code>within()</code>,a <code>logical</code> specifying if the resulting list should keepthe <code>attributes</code> from <code>data</code> and have its<code>names</code> in the same order.  Often this is unneeded asthe result is a <em>named</em> list anyway, and then <code>keepAttrs =      FALSE</code> is more efficient.
6765,arguments to be passed to (future) methods.
6766,data to use for constructing an environment. For thedefault <code>with</code> method this may be an environment, a list, adata frame, or an integer as in <code>sys.call</code>. For <code>within</code>,it can be a list or a data frame.
6767,expression to evaluate; particularly for <code>within()</code>often a “compound” expression, i.e., of the form 

6768,for the <code>list</code> method of <code>within()</code>,a <code>logical</code> specifying if the resulting list should keepthe <code>attributes</code> from <code>data</code> and have its<code>names</code> in the same order.  Often this is unneeded asthe result is a <em>named</em> list anyway, and then <code>keepAttrs =      FALSE</code> is more efficient.
6769,arguments to be passed to (future) methods.
6770,a connection or a character string giving thepathname of the file or URL to read from.  <code>""</code> indicates theconnection <code>stdin()</code>.
6771,<code>TRUE</code>, <code>FALSE</code> or an environment, determiningwhere the parsed expressions are evaluated.  <code>FALSE</code> (thedefault) corresponds to the user's workspace (the globalenvironment) and <code>TRUE</code> to the environment from which<code>source</code> is called.
6772,logical; if <code>TRUE</code>, each expression is printedafter parsing, before evaluation.
6773,logical; if <code>TRUE</code>, the result of<code>eval(i)</code> is printed for each expression <code>i</code>; defaultsto the value of <code>echo</code>.
6774,for <code>source()</code> and <code>withAutoprint(*, evaluated=TRUE)</code>:<em>instead</em> of specifying <code>file</code>, an<code>expression</code>, <code>call</code>, or <code>list</code>of <code>call</code>'s, but <em>not</em> an unevaluated “expression”.

6775,for <code>withAutoprint()</code> (with default <code>evaluated=FALSE</code>):one or more unevaluated “expressions”.
6776,logical indicating that <code>exprs</code> is passed to<code>source(exprs= *)</code> and hence must be evaluated, i.e., a formal<code>expression</code>, <code>call</code> or <code>list</code> of calls.
6777,logical indicating if newline (hence empty line) shouldbe printed before each expression (when <code>echo = TRUE</code>).
6778,if <code>TRUE</code>, more diagnostics (than just<code>echo = TRUE</code>) are printed during parsing and evaluation ofinput, including extra info for <b>each</b> expression.
6779,character; gives the prompt to be used if<code>echo = TRUE</code>.
6780,integer; is used only if <code>echo</code> is<code>TRUE</code> and gives the maximal number of characters output forthe deparse of a single expression.
6781,integer, passed to <code>deparse()</code> whichis used (only) when there are no source references.
6782,<code>character</code> vector, passed as<code>control</code> to <code>deparse()</code>, see also<code>.deparseOpts</code>.  In <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> version &lt;= 3.3.x, this washardcoded to <code>"showAttributes"</code>, which is the defaultcurrently; <code>deparseCtrl = "all"</code> may be preferable, when strictback compatibility is not of importance.
6783,logical; if <code>TRUE</code> and <code>file</code> is a pathname,the <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> working directory is temporarily changed to the directorycontaining <code>file</code> for evaluating.
6784,character vector.  The encoding(s) to be assumed when<code>file</code> is a character string: see <code>file</code>.  Apossible value is <code>"unknown"</code> when the encoding is guessed: seethe ‘Encodings’ section.
6785,character; gives the prompt to use oncontinuation lines if <code>echo = TRUE</code>.
6786,integer; how many comment lines at the start of thefile to skip if <code>echo = TRUE</code>.
6787,logical: should the source formatting be retainedwhen echoing expressions, if possible?
6788,(for <code>withAutoprint()</code>:) further (non-file related)arguments to be passed to <code>source(.)</code>.
6789,a condition object.
6790,call expression.
6791,a condition object.
6792,expression to be evaluated.
6793,expression to be evaluated before returning or exiting.
6794,function.
6795,character string.
6796,character string.
6797,character string naming a restart.
6798,restart object.
6799,object.
6800,character string naming a condition class.
6801,additional arguments; see details below.
6802,data to use for constructing an environment. For thedefault <code>with</code> method this may be an environment, a list, adata frame, or an integer as in <code>sys.call</code>. For <code>within</code>,it can be a list or a data frame.
6803,expression to evaluate; particularly for <code>within()</code>often a “compound” expression, i.e., of the form 

6804,for the <code>list</code> method of <code>within()</code>,a <code>logical</code> specifying if the resulting list should keepthe <code>attributes</code> from <code>data</code> and have its<code>names</code> in the same order.  Often this is unneeded asthe result is a <em>named</em> list anyway, and then <code>keepAttrs =      FALSE</code> is more efficient.
6805,arguments to be passed to (future) methods.
6806,data to use for constructing an environment. For thedefault <code>with</code> method this may be an environment, a list, adata frame, or an integer as in <code>sys.call</code>. For <code>within</code>,it can be a list or a data frame.
6807,expression to evaluate; particularly for <code>within()</code>often a “compound” expression, i.e., of the form 

6808,for the <code>list</code> method of <code>within()</code>,a <code>logical</code> specifying if the resulting list should keepthe <code>attributes</code> from <code>data</code> and have its<code>names</code> in the same order.  Often this is unneeded asthe result is a <em>named</em> list anyway, and then <code>keepAttrs =      FALSE</code> is more efficient.
6809,arguments to be passed to (future) methods.
6810,data to use for constructing an environment. For thedefault <code>with</code> method this may be an environment, a list, adata frame, or an integer as in <code>sys.call</code>. For <code>within</code>,it can be a list or a data frame.
6811,expression to evaluate; particularly for <code>within()</code>often a “compound” expression, i.e., of the form 

6812,for the <code>list</code> method of <code>within()</code>,a <code>logical</code> specifying if the resulting list should keepthe <code>attributes</code> from <code>data</code> and have its<code>names</code> in the same order.  Often this is unneeded asthe result is a <em>named</em> list anyway, and then <code>keepAttrs =      FALSE</code> is more efficient.
6813,arguments to be passed to (future) methods.
6814,a condition object.
6815,call expression.
6816,a condition object.
6817,expression to be evaluated.
6818,expression to be evaluated before returning or exiting.
6819,function.
6820,character string.
6821,character string.
6822,character string naming a restart.
6823,restart object.
6824,object.
6825,character string naming a condition class.
6826,additional arguments; see details below.
6827,an expression to be evaluated.
6828,the data to be written out, usually an atomic vector.
6829,a <code>connection</code>, or a character string namingthe file to write to.  If <code>""</code>, print to the standard outputconnection.

6830,When <code>.Platform$OS.type != "windows"</code>, and itis <code>"|cmd"</code>, the output is piped to the command givenby ‘<span class="file">cmd</span>’.
6831,the number of columns to write the data in.
6832,if <code>TRUE</code> the data <code>x</code> are appended to theconnection.
6833,a string used to separate columns.  Using <code>sep = "\t"</code>gives tab delimited output; default is <code>" "</code>.
6834,either a character string naming a file or a connection.<code>""</code> indicates output to the console.  For <code>read.dcf</code> thiscan name a compressed file (see <code>gzfile</code>).
6835,Fields to read from the DCF file.  Default is to read allfields.
6836,a logical indicating whether in case of multipleoccurrences of a field in a record, all these should be gathered.If <code>all</code> is false (default), only the last such occurrence isused.
6837,a character string with the names of the fields forwhich whitespace should be kept as is, or <code>NULL</code> (default)indicating that there are no such fields.  Coerced to character ifpossible.  For fields where whitespace is not to be kept as is,<code>read.dcf</code> removes leading and trailing whitespace, and<code>write.dcf</code> folds using <code>strwrap</code>.
6838,the object to be written, typically a data frame.  If not, itis attempted to coerce <code>x</code> to a data frame.
6839,logical.  If <code>TRUE</code>, the output is appended to thefile.  If <code>FALSE</code>, any existing file of the name is destroyed.
6840,logical to be passed to <code>writeLines()</code>,see there: “for expert use”.
6841,a positive integer specifying the indentation forcontinuation lines in output entries.
6842,a positive integer giving the target column for wrappinglines in the output.
6843,A connection object or a character string naming a file ora raw vector.
6844,Either an object whose mode will give the mode of thevector to be read, or a character vector of length one describingthe mode: one of <code>"numeric"</code>, <code>"double"</code>,<code>"integer"</code>, <code>"int"</code>, <code>"logical"</code>, <code>"complex"</code>,<code>"character"</code>, <code>"raw"</code>.
6845,numeric.  The (maximal) number of records to beread.  You can use an over-estimate here, but not too large asstorage is reserved for <code>n</code> items.
6846,integer.  The number of bytes per element in the bytestream.  The default, <code>NA_integer_</code>, uses the natural size.Size changing is not supported for raw and complex vectors.
6847,logical.  Only used for integers of sizes 1 and 2,when it determines if the quantity on fileshould be regarded as a signed or unsigned integer.
6848,The endian-ness (<code>"big"</code> or <code>"little"</code>) of thetarget system for the file.  Using <code>"swap"</code> will force swappingendian-ness.
6849,An <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object to be written to the connection.
6850,See <code>writeLines</code>.
6851,A connection object, or a character string naming a file,or a raw vector.
6852,integer vector, giving the lengths in characters of(unterminated) character strings to be read or written.  Elementsmust be &gt;= 0 and not <code>NA</code>.
6853,logical: For <code>readChar</code>, should <code>nchars</code> beregarded as a number of bytes not characters in a multi-bytelocale?  For <code>writeChar</code>, see <code>writeLines</code>.
6854,A character vector to be written to the connection, atleast as long as <code>nchars</code>.
6855,‘end of string’: character string.  The terminatorto be written after each string, followed by an ASCII <code>nul</code>;use <code>NULL</code> for no terminator at all.
6856,A character vector
6857,A connection object or a character string.
6858,character string.  A string to be written to the connectionafter each line of text.
6859,logical.  See ‘Details’.
6860,<code>raw</code>, <code>logical</code> or ‘number-like’ vectors (i.e., oftypes <code>double</code> (class <code>numeric</code>),<code>integer</code> and <code>complex</code>), or objects forwhich methods have been written.
6861,object of class <code>data.frame</code>.
6862,row names for old and new rows.
6863,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6864,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6865,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6866,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6867,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6868,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6869,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6870,a character vector with suitable numeric version strings (see‘Details’); for <code>package_version</code>, alternatively an Rversion object as obtained by <code>R.version</code>.
6871,a logical indicating whether invalid numeric versionsshould results in an error (default) or not.
6872,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6873,an <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> object.
6874,character string.  A description of the connection:see ‘Details’.
6875,character string.  A description of how to open the connection(if it should be opened initially).  See section ‘Modes’ forpossible values.
6876,logical.  See the ‘Blocking’ section.
6877,The name of the encoding to be assumed.  See the‘Encoding’ section.
6878,logical.  If true, a ‘raw’ interface is used whichwill be more suitable for arguments which are not regular files,e.g. character devices.  This suppresses the check for a compressedfile when opening for text-mode reading, and asserts that the‘file’ may not be seekable.
6879,character string, partially matched to<code>c("default", "internal", "wininet", "libcurl")</code>:see ‘Details’.
6880,named character vector of HTTP headers to use in HTTPrequests.  It is ignored for non-HTTP URLs.  The <code>User-Agent</code>header, coming from the <code>HTTPUserAgent</code> option (see<code>options</code>) is used as the first header, automatically.
6881,integer in 0–9.  The amount of compression to beapplied when writing, from none to maximal available.  For<code>xzfile</code> can also be negative: see the ‘Compression’section.
6882,numeric: the timeout (in seconds) to be used for thisconnection.  Beware that some OSes may treat very large values aszero: however the POSIX standard requires values up to 31 days to besupported.
6883,optional character vector with options. Currently only<code>"no-delay"</code> is supported on TCP sockets.
6884,a filename within a zip file.
6885,character string.  Host name for the port.
6886,integer.  The TCP port number.
6887,logical.  Should the socket be a client or a server?
6888,a server socket listening for connections.
6889,a connection.
6890,character string.  Currently ignored.
6891,character string.  Empty or <code>"read"</code> or <code>"write"</code>,partial matches allowed.
6892,arguments passed to or from other methods.
6893,a numeric or complex vector or any <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> number-like objectwhich has a <code>round</code> method and basic arithmetic methodsincluding <code>log10()</code>.
6894,integer indicating the precision to be used.
6895,the intercept and slope, single values.
6896,logical asking whether to <em>untransform</em>.  See‘Details’.
6897,the y-value(s) for horizontal line(s).
6898,the x-value(s) for vertical line(s).
6899,a vector of length two giving the intercept and slope.
6900,an object with a <code>coef</code> method.  See ‘Details’.
6901,graphical parameters such as<code>col</code>, <code>lty</code> and  <code>lwd</code> (possibly as vectors: see‘Details’) and <code>xpd</code> and the line characteristics<code>lend</code>, <code>ljoin</code> and <code>lmitre</code>.
6902,coordinates of points <b>from</b> which to draw.
6903,coordinates of points <b>to</b> which to draw.  At leastone must the supplied
6904,length of the edges of the arrow head (in inches).
6905,angle from the shaft of the arrow to the edge of thearrow head.
6906,integer code, determining <em>kind</em> of arrows to bedrawn.
6907,graphical parameters, possible vectors.<code>NA</code> values in <code>col</code> cause the arrow to be omitted.
6908,graphical parameters such as <code>xpd</code> and the linecharacteristics <code>lend</code>, <code>ljoin</code> and <code>lmitre</code>:see <code>par</code>.
6909,a two-dimensional contingency table in matrix form.
6910,a character vector of length two giving the colors used fordrawing positive and negative Pearson residuals, respectively.
6911,the amount of space (as a fraction of the averagerectangle width and height) left between each rectangle.
6912,overall title for the plot.
6913,a label for the x axis.  Defaults to the name (if any) ofthe row dimension in <code>x</code>.
6914,a label for the y axis.  Defaults to the name (if any) ofthe column dimension in <code>x</code>.
6915,an integer specifying which side of the plot the axis isto be drawn on.  The axis is placed as follows: 1=below,2=left, 3=above and 4=right.
6916,the points at which tick-marks are to be drawn.  Non-finite(infinite, <code>NaN</code> or <code>NA</code>) values are omitted.  By default(when <code>NULL</code>) tickmark locations are computed, see‘Details’ below.
6917,this can either be a logical value specifying whether(numerical) annotations are to be made at the tickmarks, or acharacter or expression vector of labels to be placed at thetickpoints.  (Other objects are coerced by <code>as.graphicsAnnot</code>.)If this is not logical, <code>at</code> should also be supplied and of thesame length.  If <code>labels</code> is of length zero after coercion,it has the same effect as supplying <code>TRUE</code>.
6918,a logical value specifying whether tickmarks and an axis lineshould be drawn.
6919,the number of lines into the margin at which the axis linewill be drawn, if not <code>NA</code>.
6920,the coordinate at which the axis line is to be drawn:if not <code>NA</code> this overrides the value of <code>line</code>.
6921,a logical value indicating whether the axis should bedrawn in the outer plot margin, rather than the standard plotmargin.
6922,font for text.  Defaults to <code>par("font")</code>.
6923,line type for both the axis line and the tick marks.
6924,line widths for the axis line and the tickmarks.  Zero or negative values will suppress the line or ticks.
6925,colors for the axis line and the tick marksrespectively.  <code>col = NULL</code> means to use <code>par("fg")</code>,possibly specified inline, and <code>col.ticks = NULL</code> means to usewhatever color <code>col</code> resolved to.
6926,adjustment (see <code>par("adj")</code>) for all labels<em>parallel</em> (‘horizontal’) to the reading direction.  Ifthis is not a finite value, the default is used (centring forstrings parallel to the axis, justification of the end nearest theaxis otherwise).
6927,adjustment for each tick label <em>perpendicular</em> to thereading direction.  For labels parallel to the axes, <code>padj = 0</code>means right or top alignment, and <code>padj = 1</code> means left or bottomalignment.  This can be a vector given a value for each string, andwill be recycled as necessary.

6928,If <code>padj</code> is not a finite value (the default), the value of<code>par("las")</code> determines the adjustment.  For strings plottedperpendicular to the axis the default is to centre the string.
6929,an optional (typically non-negative) numeric factor tobe multiplied with the size of an ‘m’ to determine theminimal gap between labels that are drawn, see ‘Details’.The default, <code>NA</code>, corresponds to <code>1</code> for tick labels drawn<em>parallel</em> to the axis and <code>0.25</code> otherwise, i.e., thedefault is equivalent to 

6930,<code>gap.axis</code> may typically be relevant when <code>at = ..</code>tick-mark positions are specified explicitly.
6931,other graphical parameters may also be passed asarguments to this function, particularly, <code>cex.axis</code>, <code>col.axis</code>and <code>font.axis</code> for axis annotation, i.e. tick labels, <code>mgp</code>and <code>xaxp</code> or <code>yaxp</code> for positioning, <code>tck</code> or<code>tcl</code> for tick mark length and direction, <code>las</code> forvertical/horizontal label orientation, or <code>fg</code> instead of<code>col</code>, and <code>xpd</code> for clipping.  See <code>par</code> on these.

6932,Parameters <code>xaxt</code> (sides 1 and 3) and <code>yaxt</code> (sides 2 and4) control if the axis is plotted at all.

6933,Note that <code>lab</code> will partial match to argument<code>labels</code> unless the latter is also supplied.  (Since thedefault axes have already been set up by <code>plot.window</code>,<code>lab</code> will not be acted on by <code>axis</code>.)
6934,an object which indicates the range over which an axis shouldbe drawn
6935,the points at which tick-marks are to be drawn.
6936,an integer specifying which side of the plot the axis isto be drawn on.  The axis is placed as follows: 1=below,2=left, 3=above and 4=right.
6937,this can either be a logical value specifying whether(numerical) annotations are to be made at the tickmarks, or acharacter or expression vector of labels to be placed at thetickpoints. If this is specified as a character or expressionvector, <code>at</code> should be supplied and they should be the samelength.
6938,arguments to be passed to methods and perhaps then to<code>axis</code>.
6939,A date-time or date object.
6940,See <code>axis</code>.
6941,See <code>strptime</code>.
6942,Either a logical value specifying whetherannotations are to be made at the tickmarks, or a vector of characterstrings to be placed at the tickpoints.
6943,Further arguments to be passed from or to other methods,typically graphical parameters.
6944,A date-time or date object.
6945,See <code>axis</code>.
6946,See <code>strptime</code>.
6947,Either a logical value specifying whetherannotations are to be made at the tickmarks, or a vector of characterstrings to be placed at the tickpoints.
6948,Further arguments to be passed from or to other methods,typically graphical parameters.
6949,integer in 1:4, as for <code>axis</code>.
6950,numeric vector of length three, defaulting to<code>par("xaxp")</code> or <code>par("yaxp")</code>depending on the <code>side</code> argument (<code>par("xaxp")</code>if <code>side</code> is 1 or 3, <code>par("yaxp")</code> if side is 2 or 4).
6951,numeric vector of length two giving user coordinatelimits, defaulting to the relevant portion of<code>par("usr")</code> (<code>par("usr")[1:2]</code> or<code>par("usr")[3:4]</code> for <code>side</code> in (1,3) or (2,4)respectively).
6952,logical indicating if log coordinates are active; defaultsto <code>par("xlog")</code> or <code>par("ylog")</code>depending on <code>side</code>.
6953,(only used when <code>log</code> is true): approximate (lowerbound for the) number of tick intervals; defaults to<code>par("lab")[j]</code> where <code>j</code> is 1 or 2 depending on<code>side</code>.  Set this to <code>Inf</code> if you want the same behavioras in earlier <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> versions (than 2.14.x).
6954,either a vector or matrix of values describing thebars which make up the plot.  If <code>height</code> is a vector, theplot consists of a sequence of rectangular bars with heightsgiven by the values in the vector.  If <code>height</code> is a matrixand <code>beside</code> is <code>FALSE</code> then each bar of the plotcorresponds to a column of <code>height</code>, with the values in thecolumn giving the heights of stacked sub-bars making up thebar.  If <code>height</code> is a matrix and <code>beside</code> is<code>TRUE</code>, then the values in each column are juxtaposedrather than stacked.
6955,optional vector of bar widths. Re-cycled to length thenumber of bars drawn.  Specifying a single value will have novisible effect unless <code>xlim</code> is specified.
6956,the amount of space (as a fraction of the average barwidth) left before each bar.  May be given as a single number orone number per bar.  If <code>height</code> is a matrix and<code>beside</code> is <code>TRUE</code>, <code>space</code> may be specified bytwo numbers, where the first is the space between bars in thesame group, and the second the space between the groups.  If notgiven explicitly, it defaults to <code>c(0,1)</code> if <code>height</code>is a matrix and <code>beside</code> is <code>TRUE</code>, and to 0.2otherwise.
6957,a vector of names to be plotted below each bar orgroup of bars.  If this argument is omitted, then the names aretaken from the <code>names</code> attribute of <code>height</code> if thisis a vector, or the column names if it is a matrix.
6958,a vector of text used to construct a legend forthe plot, or a logical indicating whether a legend should beincluded.  This is only useful when <code>height</code> is a matrix.In that case given legend labels should correspond to the rows of<code>height</code>; if <code>legend.text</code> is true, the row names of<code>height</code> will be used as labels if they are non-null.
6959,a logical value.  If <code>FALSE</code>, the columns of<code>height</code> are portrayed as stacked bars, and if <code>TRUE</code>the columns are portrayed as juxtaposed bars.
6960,a logical value.  If <code>FALSE</code>, the bars are drawnvertically with the first bar to the left.  If <code>TRUE</code>, thebars are drawn horizontally with the first at the bottom.
6961,a vector giving the density of shading lines, inlines per inch, for the bars or bar components.The default value of <code>NULL</code> means that no shading linesare drawn. Non-positive values of <code>density</code> also inhibit thedrawing of shading lines.
6962,the slope of shading lines, given as an angle indegrees (counter-clockwise), for the bars or bar components.
6963,a vector of colors for the bars or bar components.By default, grey is used if <code>height</code> is a vector, and agamma-corrected grey palette if <code>height</code> is a matrix.
6964,the color to be used for the border of the bars.Use <code>border = NA</code> to omit borders.  If there are shadinglines, <code>border = TRUE</code> means use the same colour forthe border as for the shading lines.
6965,overall and sub title for the plot.
6966,a label for the x axis.
6967,a label for the y axis.
6968,limits for the x axis.
6969,limits for the y axis.
6970,logical. Should bars be allowed to go outside region?
6971,string specifying if axis scales should be logarithmic; see<code>plot.default</code>.
6972,logical.  If <code>TRUE</code>, a vertical (or horizontal, if<code>horiz</code> is true) axis is drawn.
6973,logical.  If <code>TRUE</code>, and if there are<code>names.arg</code> (see above), theother axis is drawn (with <code>lty = 0</code>) and labeled.
6974,expansion factor for numeric axis labels (see<code>par('cex')</code>).
6975,expansion factor for axis names (bar labels).
6976,logical.  If <code>TRUE</code>, the lines which divideadjacent (non-stacked!) bars will be drawn.  Only applies when<code>space = 0</code> (which it partly is when <code>beside = TRUE</code>).
6977,logical.  If <code>FALSE</code>, nothing is plotted.
6978,the graphics parameter <code>lty</code> (see<code>par('lty')</code>) applied to the axisand tick marks of the categorical (default horizontal) axis.  Notethat by default the axis is suppressed.
6979,a vector indicating how much the bars should be shiftedrelative to the x axis.
6980,logical specifying if bars should be added to an alreadyexisting plot; defaults to <code>FALSE</code>.
6981,logical specifying if the default annotation (<code>main</code>,<code>sub</code>, <code>xlab</code>, <code>ylab</code>) should appear on the plot, see<code>title</code>.
6982,list of additional arguments to pass to<code>legend()</code>; names of the list are used as argumentnames.  Only used if <code>legend.text</code> is supplied.
6983,a formula where the <code>y</code> variables are numeric datato plot against the categorical <code>x</code> variables.  The formulacan have one of three forms:

6984,(see the examples).
6985,a data frame (or list) from which the variables in formulashould be taken.
6986,an optional vector specifying a subset of observationsto be used.
6987,a function which indicates what should happen whenthe data contain <code>NA</code> values.  The default is to ignore missingvalues in the given variables.
6988,arguments to be passed to/from other methods.  For thedefault method these can include further arguments (such as<code>axes</code>, <code>asp</code> and <code>main</code>) andgraphical parameters (see <code>par</code>) which are passed to<code>plot.window()</code>, <code>title()</code> and<code>axis</code>.
6989,either a vector or matrix of values describing thebars which make up the plot.  If <code>height</code> is a vector, theplot consists of a sequence of rectangular bars with heightsgiven by the values in the vector.  If <code>height</code> is a matrixand <code>beside</code> is <code>FALSE</code> then each bar of the plotcorresponds to a column of <code>height</code>, with the values in thecolumn giving the heights of stacked sub-bars making up thebar.  If <code>height</code> is a matrix and <code>beside</code> is<code>TRUE</code>, then the values in each column are juxtaposedrather than stacked.
6990,optional vector of bar widths. Re-cycled to length thenumber of bars drawn.  Specifying a single value will have novisible effect unless <code>xlim</code> is specified.
6991,the amount of space (as a fraction of the average barwidth) left before each bar.  May be given as a single number orone number per bar.  If <code>height</code> is a matrix and<code>beside</code> is <code>TRUE</code>, <code>space</code> may be specified bytwo numbers, where the first is the space between bars in thesame group, and the second the space between the groups.  If notgiven explicitly, it defaults to <code>c(0,1)</code> if <code>height</code>is a matrix and <code>beside</code> is <code>TRUE</code>, and to 0.2otherwise.
6992,a vector of names to be plotted below each bar orgroup of bars.  If this argument is omitted, then the names aretaken from the <code>names</code> attribute of <code>height</code> if thisis a vector, or the column names if it is a matrix.
6993,a vector of text used to construct a legend forthe plot, or a logical indicating whether a legend should beincluded.  This is only useful when <code>height</code> is a matrix.In that case given legend labels should correspond to the rows of<code>height</code>; if <code>legend.text</code> is true, the row names of<code>height</code> will be used as labels if they are non-null.
6994,a logical value.  If <code>FALSE</code>, the columns of<code>height</code> are portrayed as stacked bars, and if <code>TRUE</code>the columns are portrayed as juxtaposed bars.
6995,a logical value.  If <code>FALSE</code>, the bars are drawnvertically with the first bar to the left.  If <code>TRUE</code>, thebars are drawn horizontally with the first at the bottom.
6996,a vector giving the density of shading lines, inlines per inch, for the bars or bar components.The default value of <code>NULL</code> means that no shading linesare drawn. Non-positive values of <code>density</code> also inhibit thedrawing of shading lines.
6997,the slope of shading lines, given as an angle indegrees (counter-clockwise), for the bars or bar components.
6998,a vector of colors for the bars or bar components.By default, grey is used if <code>height</code> is a vector, and agamma-corrected grey palette if <code>height</code> is a matrix.
6999,the color to be used for the border of the bars.Use <code>border = NA</code> to omit borders.  If there are shadinglines, <code>border = TRUE</code> means use the same colour forthe border as for the shading lines.
7000,overall and sub title for the plot.
7001,a label for the x axis.
7002,a label for the y axis.
7003,limits for the x axis.
7004,limits for the y axis.
7005,logical. Should bars be allowed to go outside region?
7006,string specifying if axis scales should be logarithmic; see<code>plot.default</code>.
7007,logical.  If <code>TRUE</code>, a vertical (or horizontal, if<code>horiz</code> is true) axis is drawn.
7008,logical.  If <code>TRUE</code>, and if there are<code>names.arg</code> (see above), theother axis is drawn (with <code>lty = 0</code>) and labeled.
7009,expansion factor for numeric axis labels (see<code>par('cex')</code>).
7010,expansion factor for axis names (bar labels).
7011,logical.  If <code>TRUE</code>, the lines which divideadjacent (non-stacked!) bars will be drawn.  Only applies when<code>space = 0</code> (which it partly is when <code>beside = TRUE</code>).
7012,logical.  If <code>FALSE</code>, nothing is plotted.
7013,the graphics parameter <code>lty</code> (see<code>par('lty')</code>) applied to the axisand tick marks of the categorical (default horizontal) axis.  Notethat by default the axis is suppressed.
7014,a vector indicating how much the bars should be shiftedrelative to the x axis.
7015,logical specifying if bars should be added to an alreadyexisting plot; defaults to <code>FALSE</code>.
7016,logical specifying if the default annotation (<code>main</code>,<code>sub</code>, <code>xlab</code>, <code>ylab</code>) should appear on the plot, see<code>title</code>.
7017,list of additional arguments to pass to<code>legend()</code>; names of the list are used as argumentnames.  Only used if <code>legend.text</code> is supplied.
7018,a formula where the <code>y</code> variables are numeric datato plot against the categorical <code>x</code> variables.  The formulacan have one of three forms:

7019,(see the examples).
7020,a data frame (or list) from which the variables in formulashould be taken.
7021,an optional vector specifying a subset of observationsto be used.
7022,a function which indicates what should happen whenthe data contain <code>NA</code> values.  The default is to ignore missingvalues in the given variables.
7023,arguments to be passed to/from other methods.  For thedefault method these can include further arguments (such as<code>axes</code>, <code>asp</code> and <code>main</code>) andgraphical parameters (see <code>par</code>) which are passed to<code>plot.window()</code>, <code>title()</code> and<code>axis</code>.
7024,character, one of <code>"plot"</code>, <code>"figure"</code>,<code>"inner"</code> and <code>"outer"</code>.
7025,line type of the box.
7026,further graphical parameters, such as <code>bty</code>,<code>col</code>, or <code>lwd</code>, see <code>par</code>.  Note that<code>xpd</code> is not accepted as clipping is always to the deviceregion.
7027,a formula, such as <code>y ~ grp</code>, where <code>y</code> is anumeric vector of data values to be split into groups according tothe grouping variable <code>grp</code> (usually a factor).  Note that<code>~ g1 + g2</code> is equivalent to <code>g1:g2</code>.
7028,a data.frame (or list) from which the variables in<code>formula</code> should be taken.
7029,an optional vector specifying a subset of observationsto be used for plotting.
7030,a function which indicates what should happenwhen the data contain <code>NA</code>s.  The default is to ignore missingvalues in either the response or the group.
7031,x- and y-axis annotation, since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.6.0 with anon-empty default.  Can be suppressed by <code>ann=FALSE</code>.
7032,<code>logical</code> indicating if axes should be annotated (by<code>xlab</code> and <code>ylab</code>).
7033,passed to <code>split.default</code>, see there.
7034,for specifying data from which the boxplots are to beproduced. Either a numeric vector, or a single list containing suchvectors. Additional unnamed arguments specify further dataas separate vectors (each corresponding to a component boxplot).<code>NA</code>s are allowed in the data.
7035,For the <code>formula</code> method, named arguments to be passed tothe default method.

7036,For the default method, unnamed arguments are additional datavectors (unless <code>x</code> is a list when they are ignored), and namedarguments are arguments and graphical parameters to be passedto <code>bxp</code> in addition to the ones given by argument<code>pars</code> (and override those in <code>pars</code>). Note that<code>bxp</code> may or may not make use of graphical parameters it ispassed: see its documentation.
7037,this determines how far the plot whiskers extend outfrom the box.  If <code>range</code> is positive, the whiskers extendto the most extreme data point which is no more than<code>range</code> times the interquartile range from the box. A valueof zero causes the whiskers to extend to the data extremes.
7038,a vector giving the relative widths of the boxes makingup the plot.
7039,if <code>varwidth</code> is <code>TRUE</code>, the boxes aredrawn with widths proportional to the square-roots of the numberof observations in the groups.
7040,if <code>notch</code> is <code>TRUE</code>, a notch is drawn ineach side of the boxes.  If the notches of two plots do notoverlap this is ‘strong evidence’ that the two medians differ(Chambers <em>et al</em>, 1983, p. 62).  See <code>boxplot.stats</code>for the calculations used.
7041,if <code>outline</code> is not true, the outliers arenot drawn (as points whereas S+ uses lines).
7042,group labels which will be printed under each boxplot.Can be a character vector or an expression (seeplotmath).
7043,a scale factor to be applied to all boxes.  When thereare only a few groups, the appearance of the plot can be improvedby making the boxes narrower.
7044,staple line width expansion, proportional to boxwidth.
7045,outlier line width expansion, proportional to boxwidth.
7046,if <code>TRUE</code> (the default) then a boxplot isproduced.  If not, the summaries which the boxplots are based onare returned.
7047,an optional vector of colors for the outlines of theboxplots.  The values in <code>border</code> are recycled if thelength of <code>border</code> is less than the number of plots.
7048,if <code>col</code> is non-null it is assumed to contain colorsto be used to colour the bodies of the box plots. By default theyare in the background colour.
7049,character indicating if x or y or both coordinates shouldbe plotted in log scale.
7050,a list of (potentially many) more graphical parameters,e.g., <code>boxwex</code> or <code>outpch</code>; these are passed to<code>bxp</code> (if <code>plot</code> is true); for details, see there.
7051,logical indicating if the boxplots should behorizontal; default <code>FALSE</code> means vertical boxes.
7052,logical, if true <em>add</em> boxplot to current plot.
7053,numeric vector giving the locations where the boxplots shouldbe drawn, particularly when <code>add = TRUE</code>;defaults to <code>1:n</code> where <code>n</code> is the number of boxes.
7054,a formula, such as <code>y ~ grp</code>, where <code>y</code> is anumeric vector of data values to be split into groups according tothe grouping variable <code>grp</code> (usually a factor).  Note that<code>~ g1 + g2</code> is equivalent to <code>g1:g2</code>.
7055,a data.frame (or list) from which the variables in<code>formula</code> should be taken.
7056,an optional vector specifying a subset of observationsto be used for plotting.
7057,a function which indicates what should happenwhen the data contain <code>NA</code>s.  The default is to ignore missingvalues in either the response or the group.
7058,x- and y-axis annotation, since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 3.6.0 with anon-empty default.  Can be suppressed by <code>ann=FALSE</code>.
7059,<code>logical</code> indicating if axes should be annotated (by<code>xlab</code> and <code>ylab</code>).
7060,passed to <code>split.default</code>, see there.
7061,for specifying data from which the boxplots are to beproduced. Either a numeric vector, or a single list containing suchvectors. Additional unnamed arguments specify further dataas separate vectors (each corresponding to a component boxplot).<code>NA</code>s are allowed in the data.
7062,For the <code>formula</code> method, named arguments to be passed tothe default method.

7063,For the default method, unnamed arguments are additional datavectors (unless <code>x</code> is a list when they are ignored), and namedarguments are arguments and graphical parameters to be passedto <code>bxp</code> in addition to the ones given by argument<code>pars</code> (and override those in <code>pars</code>). Note that<code>bxp</code> may or may not make use of graphical parameters it ispassed: see its documentation.
7064,this determines how far the plot whiskers extend outfrom the box.  If <code>range</code> is positive, the whiskers extendto the most extreme data point which is no more than<code>range</code> times the interquartile range from the box. A valueof zero causes the whiskers to extend to the data extremes.
7065,a vector giving the relative widths of the boxes makingup the plot.
7066,if <code>varwidth</code> is <code>TRUE</code>, the boxes aredrawn with widths proportional to the square-roots of the numberof observations in the groups.
7067,if <code>notch</code> is <code>TRUE</code>, a notch is drawn ineach side of the boxes.  If the notches of two plots do notoverlap this is ‘strong evidence’ that the two medians differ(Chambers <em>et al</em>, 1983, p. 62).  See <code>boxplot.stats</code>for the calculations used.
7068,if <code>outline</code> is not true, the outliers arenot drawn (as points whereas S+ uses lines).
7069,group labels which will be printed under each boxplot.Can be a character vector or an expression (seeplotmath).
7070,a scale factor to be applied to all boxes.  When thereare only a few groups, the appearance of the plot can be improvedby making the boxes narrower.
7071,staple line width expansion, proportional to boxwidth.
7072,outlier line width expansion, proportional to boxwidth.
7073,if <code>TRUE</code> (the default) then a boxplot isproduced.  If not, the summaries which the boxplots are based onare returned.
7074,an optional vector of colors for the outlines of theboxplots.  The values in <code>border</code> are recycled if thelength of <code>border</code> is less than the number of plots.
7075,if <code>col</code> is non-null it is assumed to contain colorsto be used to colour the bodies of the box plots. By default theyare in the background colour.
7076,character indicating if x or y or both coordinates shouldbe plotted in log scale.
7077,a list of (potentially many) more graphical parameters,e.g., <code>boxwex</code> or <code>outpch</code>; these are passed to<code>bxp</code> (if <code>plot</code> is true); for details, see there.
7078,logical indicating if the boxplots should behorizontal; default <code>FALSE</code> means vertical boxes.
7079,logical, if true <em>add</em> boxplot to current plot.
7080,numeric vector giving the locations where the boxplots shouldbe drawn, particularly when <code>add = TRUE</code>;defaults to <code>1:n</code> where <code>n</code> is the number of boxes.
7081,a numeric matrix.
7082,logical indicating if columns (by default) or rows(<code>use.cols = FALSE</code>) should be plotted.
7083,Further arguments to <code>boxplot</code>.
7084,a list containing data summaries to be used in constructingthe plots.  These are usually the result of a call to<code>boxplot</code>, but can be generated in any fashion.
7085,if <code>notch</code> is <code>TRUE</code>, a notch is drawn in eachside of the boxes.  If the notches of two plots do not overlap thenthe medians are significantly different at the 5 percent level.
7086,a vector giving the relative widths of the boxes makingup the plot.
7087,if <code>varwidth</code> is <code>TRUE</code>, the boxes are drawnwith widths proportional to the square-roots of the number ofobservations in the groups.
7088,if <code>outline</code> is not true, the outliers are notdrawn.
7089,numeric in (0,1). When <code>notch = TRUE</code>, thefraction of the box width that the notches should use.
7090,character or numeric (vector), the color of the boxborders.  Is recycled for multiple boxes.  Is used as default forthe <code>boxcol</code>, <code>medcol</code>, <code>whiskcol</code>, <code>staplecol</code>,and <code>outcol</code> options (see below).
7091,character, indicating if any axis should be drawn inlogarithmic scale, as in <code>plot.default</code>.
7092,logical, indicating if a ‘frame’(<code>box</code>) should be drawn; defaults to <code>TRUE</code>, unless<code>axes = FALSE</code> is specified.
7093,logical indicating if the  boxplots should behorizontal; default <code>FALSE</code> means vertical boxes.
7094,a logical value indicating whether the default annotation(title and x and y axis labels) should appear on the plot.
7095,logical, if true <em>add</em> boxplot to current plot.
7096,numeric vector giving the locations where the boxplotsshould be drawn, particularly when <code>add = TRUE</code>; defaults to<code>1:n</code> where <code>n</code> is the number of boxes.
7097,Set to <code>TRUE</code> or <code>FALSE</code> to override thedefaults on whether an x-axis label is printed for each group.
7098,graphical parameters (etc) can be passed asarguments to this function, either as a list (<code>pars</code>) ornormally(<code>...</code>), see the following.  (Those in <code>...</code>take precedence over those in <code>pars</code>.)

7099,Currently, <code>yaxs</code> and <code>ylim</code> are used ‘along theboxplot’, i.e., vertically, when <code>horizontal</code> isfalse, and <code>xlim</code> horizontally.<code>xaxt</code>, <code>yaxt</code>, <code>las</code>, <code>cex.axis</code>,<code>gap.axis</code>, and<code>col.axis</code> are passed to <code>axis</code>, and <code>main</code>,<code>cex.main</code>, <code>col.main</code>, <code>sub</code>, <code>cex.sub</code>,<code>col.sub</code>, <code>xlab</code>, <code>ylab</code>, <code>cex.lab</code>, and<code>col.lab</code> are passed to <code>title</code>.

7100,In addition, <code>axes</code> is accepted (see<code>plot.window</code>), with default <code>TRUE</code>.

7101,The following arguments (or <code>pars</code> components) allow furthercustomization of the boxplot graphics.  Their defaults are typicallydetermined from the non-prefixed version (e.g., <code>boxlty</code> from<code>lty</code>), either from the specified argument or <code>pars</code>component or the corresponding <code>par</code> one.

7102,a scale factor to be applied to all boxes.  When thereare only a few groups, the appearance of the plot can be improved bymaking the boxes narrower.  The default depends on <code>at</code> andtypically is <i>0.8</i>.
7103,staple and outlier line width expansion,proportional to box width; both default to 0.5.
7104,box outline type, width,color, and fill color (which currently defaults to <code>col</code> and willin future default to <code>par("bg")</code>).
7105,median line type,line width, point character, point size expansion, color, andbackground color.  The default <code>medpch = NA</code> suppresses thepoint, and <code>medlty = "blank"</code> does so for the line.Note that <code>medlwd</code> defaults to<i>3x</i> the default <code>lwd</code>.
7106,whisker line type (default:<code>"dashed"</code>), width, and color.
7107,staple (= end of whisker) linetype, width, and color.
7108,outlier linetype, line width, point character, point size expansion, color, andbackground color.  The default <code>outlty =  "blank"</code> suppresses thelines and <code>outpch = NA</code> suppresses points.
7109,an object, the default method expects a single numericalvariable (or an object coercible to this).
7110,a <code>"factor"</code> interpreted to be the dependent variable
7111,a <code>"formula"</code> of type <code>y ~ x</code> with a single dependent<code>"factor"</code> and a single numerical explanatory variable.
7112,an optional data frame.
7113,logical. Should the computed conditional densities be plotted?
7114,convenience tolerance parameter for y-axis annotation.If the distance between two labels drops under this threshold, they areplotted equidistantly.
7115,a character or numeric vector specifying in which orderthe levels of the dependent variable should be plotted.
7116,arguments passed to <code>density</code>
7117,a vector of fill colors of the same length as <code>levels(y)</code>.The default is to call <code>gray.colors</code>.
7118,border color of shaded polygons.
7119,character strings for annotation
7120,character vector for annotation of y axis, defaults to<code>levels(y)</code>.
7121,the range of x and y values with sensible defaults.
7122,an optional vector specifying a subset of observationsto be used for plotting.
7123,user coordinates of clipping rectangle
7124,a two-element vector describing the number of rows and thenumber of columns in a screen matrix <em>or</em> a matrix with 4 columns.If a matrix, then each row describes a screen with values for the left,right, bottom, and top of the screen (in that order) in NDC units,that is 0 at the lower left corner of the device surface, and 1 atthe upper right corner.
7125,a number giving the screen to be split.  It defaults tothe current screen if there is one, otherwise the whole device region.
7126,logical: should the selected screen be cleared?
7127,a number indicating which screen to prepare for drawing(<code>screen</code>), erase (<code>erase.screen</code>), or close(<code>close.screen</code>).  (<code>close.screen</code> will accept a vector ofscreen numbers.)
7128,logical value indicating whether the screen should beerased as part of the preparation for drawing in the screen.
7129,logical value indicating whether all of thescreens should be closed.
7130,a formula describing the form of conditioning plot.  Aformula of the form <code>y ~ x | a</code> indicates that plots of<code>y</code> versus <code>x</code> should be produced conditional on thevariable <code>a</code>.  A formula of the form <code>y ~ x| a * b</code>indicates that plots of <code>y</code> versus <code>x</code> should be producedconditional on the two variables <code>a</code> and <code>b</code>.

7131,All three or four variables may be either numeric or factors.When <code>x</code> or <code>y</code> are factors, the result is almost as if<code>as.numeric()</code> was applied, whereas for factor<code>a</code> or <code>b</code>, the conditioning (and its graphics if<code>show.given</code> is true) are adapted.
7132,a data frame containing values for any variables in theformula.  By default the environment where <code>coplot</code> was calledfrom is used.
7133,a value or list of two values which determine howthe conditioning on <code>a</code> and <code>b</code> is to take place.

7134,When there is no <code>b</code> (i.e., conditioning only on <code>a</code>),usually this is a matrix with two columns each row of which gives aninterval, to be conditioned on, but is can also be a single vectorof numbers or a set of factor levels (if the variable beingconditioned on is a factor). In this case (no <code>b</code>),the result of <code>co.intervals</code> can be used directly as<code>given.values</code> argument.
7135,a <code>function(x, y, col, pch, ...)</code>which gives the action to be carried out ineach panel of the display.  The default is <code>points</code>.
7136,the panels of the plot are laid out in a <code>rows</code> by<code>columns</code> array.  <code>rows</code> gives the number of rows in thearray.
7137,the number of columns in the panel layout array.
7138,logical (possibly of length 2 for 2 conditioningvariables): should conditioning plots be shown for thecorresponding conditioning variables (default <code>TRUE</code>).
7139,a vector of colors to be used to plot the points.  If tooshort, the values are recycled.
7140,a vector of plotting symbols or characters.  If too short,the values are recycled.
7141,a named vector with components <code>"num"</code> and<code>"fac"</code> giving the background colors for the (shingle) bars,for <b>num</b>eric and <b>fac</b>tor conditioning variables respectively.
7142,character; labels to use for the x axis and thefirst conditioning variable.  If only one label is given, it is usedfor the x axis and the default label is used for the conditioningvariable.
7143,character; labels to use for the y axis and anysecond conditioning variable.
7144,logical: if true the panel function is given anadditional (third) argument <code>subscripts</code> giving the subscriptsof the data passed to that panel.
7145,function for creating axis (tick) labels when x or yare factors.
7146,integer; the number of conditioning intervals,for a and b, possibly of length 2.  It is only used if thecorresponding conditioning variable is not a <code>factor</code>.
7147,numeric &lt; 1; the fraction of overlap of theconditioning variables, possibly of length 2 for x and ydirection.  When overlap &lt; 0, there will be <em>gaps</em> betweenthe data slices.
7148,the range for the x axis.
7149,the range for the y axis.
7150,additional arguments to the panel function.
7151,a numeric vector.
7152,locations of grid lines at which the values in <code>z</code> aremeasured.  These must be in ascending order.  By default, equallyspaced values from 0 to 1 are used.  If <code>x</code> is a <code>list</code>,its components <code>x$x</code> and <code>x$y</code> are used for <code>x</code>and <code>y</code>, respectively. If the list has component <code>z</code> thisis used for <code>z</code>.
7153,a matrix containing the values to be plotted (<code>NA</code>s areallowed).  Note that <code>x</code> can be used instead of <code>z</code> forconvenience.
7154,number of contour levels desired <b>iff</b><code>levels</code> is not supplied.
7155,numeric vector of levels at which to draw contourlines.
7156,a vector giving the labels for the contour lines.  If<code>NULL</code> then the levels are used as labels, otherwise this iscoerced by <code>as.character</code>.
7157,<code>cex</code> for contour labelling.  This is an absolutesize, not a multiple of <code>par("cex")</code>.
7158,logical.  Contours are labelled if <code>TRUE</code>.
7159,character string specifying where the labels will belocated. Possible values are <code>"simple"</code>, <code>"edge"</code> and<code>"flattest"</code> (the default).  See the ‘Details’ section.
7160,if <code>NULL</code>, the current font family and face are usedfor the contour labels.  If a character vector of length 2 thenHershey vector fonts are used for the contour labels. The firstelement of the vector selects a typeface and the second elementselects a fontindex (see <code>text</code> for more information).The default is <code>NULL</code> on graphics devices with high-qualityrotation of text and <code>c("sans serif", "plain")</code> otherwise.
7161,x-, y- and z-limits for the plot.
7162,logical indicating whether axes or a boxshould be drawn, see <code>plot.default</code>.
7163,colour(s) for the lines drawn.
7164,line type(s) for the lines drawn.
7165,line width(s) for the lines drawn.
7166,logical. If <code>TRUE</code>, add to a current plot.
7167,additional arguments to <code>plot.window</code>,<code>title</code>, <code>Axis</code> and <code>box</code>,typically graphical parameters such as <code>cex.axis</code>.
7168,locations of grid lines at which the values in <code>z</code> aremeasured.  These must be in ascending order.  By default, equallyspaced values from 0 to 1 are used.  If <code>x</code> is a <code>list</code>,its components <code>x$x</code> and <code>x$y</code> are used for <code>x</code>and <code>y</code>, respectively. If the list has component <code>z</code> thisis used for <code>z</code>.
7169,a matrix containing the values to be plotted (<code>NA</code>s areallowed).  Note that <code>x</code> can be used instead of <code>z</code> forconvenience.
7170,number of contour levels desired <b>iff</b><code>levels</code> is not supplied.
7171,numeric vector of levels at which to draw contourlines.
7172,a vector giving the labels for the contour lines.  If<code>NULL</code> then the levels are used as labels, otherwise this iscoerced by <code>as.character</code>.
7173,<code>cex</code> for contour labelling.  This is an absolutesize, not a multiple of <code>par("cex")</code>.
7174,logical.  Contours are labelled if <code>TRUE</code>.
7175,character string specifying where the labels will belocated. Possible values are <code>"simple"</code>, <code>"edge"</code> and<code>"flattest"</code> (the default).  See the ‘Details’ section.
7176,if <code>NULL</code>, the current font family and face are usedfor the contour labels.  If a character vector of length 2 thenHershey vector fonts are used for the contour labels. The firstelement of the vector selects a typeface and the second elementselects a fontindex (see <code>text</code> for more information).The default is <code>NULL</code> on graphics devices with high-qualityrotation of text and <code>c("sans serif", "plain")</code> otherwise.
7177,x-, y- and z-limits for the plot.
7178,logical indicating whether axes or a boxshould be drawn, see <code>plot.default</code>.
7179,colour(s) for the lines drawn.
7180,line type(s) for the lines drawn.
7181,line width(s) for the lines drawn.
7182,logical. If <code>TRUE</code>, add to a current plot.
7183,additional arguments to <code>plot.window</code>,<code>title</code>, <code>Axis</code> and <code>box</code>,typically graphical parameters such as <code>cex.axis</code>.
7184,a formula describing the form of conditioning plot.  Aformula of the form <code>y ~ x | a</code> indicates that plots of<code>y</code> versus <code>x</code> should be produced conditional on thevariable <code>a</code>.  A formula of the form <code>y ~ x| a * b</code>indicates that plots of <code>y</code> versus <code>x</code> should be producedconditional on the two variables <code>a</code> and <code>b</code>.

7185,All three or four variables may be either numeric or factors.When <code>x</code> or <code>y</code> are factors, the result is almost as if<code>as.numeric()</code> was applied, whereas for factor<code>a</code> or <code>b</code>, the conditioning (and its graphics if<code>show.given</code> is true) are adapted.
7186,a data frame containing values for any variables in theformula.  By default the environment where <code>coplot</code> was calledfrom is used.
7187,a value or list of two values which determine howthe conditioning on <code>a</code> and <code>b</code> is to take place.

7188,When there is no <code>b</code> (i.e., conditioning only on <code>a</code>),usually this is a matrix with two columns each row of which gives aninterval, to be conditioned on, but is can also be a single vectorof numbers or a set of factor levels (if the variable beingconditioned on is a factor). In this case (no <code>b</code>),the result of <code>co.intervals</code> can be used directly as<code>given.values</code> argument.
7189,a <code>function(x, y, col, pch, ...)</code>which gives the action to be carried out ineach panel of the display.  The default is <code>points</code>.
7190,the panels of the plot are laid out in a <code>rows</code> by<code>columns</code> array.  <code>rows</code> gives the number of rows in thearray.
7191,the number of columns in the panel layout array.
7192,logical (possibly of length 2 for 2 conditioningvariables): should conditioning plots be shown for thecorresponding conditioning variables (default <code>TRUE</code>).
7193,a vector of colors to be used to plot the points.  If tooshort, the values are recycled.
7194,a vector of plotting symbols or characters.  If too short,the values are recycled.
7195,a named vector with components <code>"num"</code> and<code>"fac"</code> giving the background colors for the (shingle) bars,for <b>num</b>eric and <b>fac</b>tor conditioning variables respectively.
7196,character; labels to use for the x axis and thefirst conditioning variable.  If only one label is given, it is usedfor the x axis and the default label is used for the conditioningvariable.
7197,character; labels to use for the y axis and anysecond conditioning variable.
7198,logical: if true the panel function is given anadditional (third) argument <code>subscripts</code> giving the subscriptsof the data passed to that panel.
7199,function for creating axis (tick) labels when x or yare factors.
7200,integer; the number of conditioning intervals,for a and b, possibly of length 2.  It is only used if thecorresponding conditioning variable is not a <code>factor</code>.
7201,numeric &lt; 1; the fraction of overlap of theconditioning variables, possibly of length 2 for x and ydirection.  When overlap &lt; 0, there will be <em>gaps</em> betweenthe data slices.
7202,the range for the x axis.
7203,the range for the y axis.
7204,additional arguments to the panel function.
7205,a numeric vector.
7206,The name of a function, or a call or anexpression written as a function of <code>x</code> which willevaluate to an object of the same length as <code>x</code>.
7207,a ‘vectorizing’ numeric <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> function.
7208,alias for <code>from</code> for compatibility with <code>plot</code>
7209,the range over which the function will be plotted.
7210,integer; the number of x values at which to evaluate.
7211,logical; if <code>TRUE</code> add to an already existing plot; if<code>NA</code> start a new plot taking the defaults for the limits andlog-scaling of the x-axis from the previous plot.  Taken as<code>FALSE</code> (with a warning if a different value is supplied) if nographics device is open.
7212,<code>NULL</code> or a numeric vector of length 2;if non-<code>NULL</code> it provides the defaults for <code>c(from, to)</code>and, unless <code>add = TRUE</code>, selects the x-limits of the plot – see<code>plot.window</code>.
7213,plot type: see <code>plot.default</code>.
7214,character string giving the name to be used for the x axis.
7215,labels and graphical parameterscan also be specified as arguments.  See ‘Details’ for theinterpretation of the default for <code>log</code>.

7216,For the <code>"function"</code> method of <code>plot</code>, <code>...</code> caninclude any of the other arguments of <code>curve</code>, except<code>expr</code>.
7217,either a vector or matrix of numeric values (<code>NA</code>s areallowed).  If <code>x</code> is a matrix the overall plot consists ofjuxtaposed dotplots for each row.  Inputs which satisfy<code>is.numeric(x)</code> but not<code>is.vector(x) || is.matrix(x)</code> are coerced by<code>as.numeric</code>, with a warning.
7218,a vector of labels for each point.For vectors the default is to use <code>names(x)</code> and for matricesthe row labels <code>dimnames(x)[[1]]</code>.
7219,an optional factor indicating how the elements of<code>x</code> are grouped.If <code>x</code> is a matrix, <code>groups</code> will default to the columnsof <code>x</code>.
7220,data values for the groups.This is typically a summary such as the median or meanof each group.
7221,offset in inches of <code>ylab</code> and <code>labels</code>; washardwired to 0.4 before <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0.
7222,a <code>logical</code> value indicating whether the default annotation(title and x and y axis labels) should appear on the plot.
7223,a string indicating the x-axis style; use <code>"n"</code> tosuppress and see also <code>par("xaxt")</code>.
7224,a logical indicating whether a box should be drawnaround the plot.
7225,a character string indicating if one or the other axis shouldbe logarithmic, see <code>plot.default</code>.
7226,the character size to be used.  Setting <code>cex</code>to a value smaller than one can be a useful way of avoiding labeloverlap.  Unlike many other graphics functions, this sets the actualsize, not a multiple of <code>par("cex")</code>.
7227,the <code>cex</code> to be applied to plotting symbols.This behaves like <code>cex</code> in <code>plot()</code>.
7228,the plotting character or symbol to be used.
7229,the plotting character or symbol to be used for groupvalues.
7230,the background color of plotting characters or symbols to beused; use <code>par(bg= *)</code> to set the background color ofthe whole plot.
7231,the color(s) to be used for points and labels.
7232,the single color to be used for group labels andvalues.
7233,the color(s) to be used for the horizontal lines.
7234,horizontal range for the plot, see<code>plot.window</code>, for example.
7235,overall title for the plot, see <code>title</code>.
7236,axis annotations as in <code>title</code>.
7237,graphical parameters can also be specified as arguments.
7238,a two-element vector describing the number of rows and thenumber of columns in a screen matrix <em>or</em> a matrix with 4 columns.If a matrix, then each row describes a screen with values for the left,right, bottom, and top of the screen (in that order) in NDC units,that is 0 at the lower left corner of the device surface, and 1 atthe upper right corner.
7239,a number giving the screen to be split.  It defaults tothe current screen if there is one, otherwise the whole device region.
7240,logical: should the selected screen be cleared?
7241,a number indicating which screen to prepare for drawing(<code>screen</code>), erase (<code>erase.screen</code>), or close(<code>close.screen</code>).  (<code>close.screen</code> will accept a vector ofscreen numbers.)
7242,logical value indicating whether the screen should beerased as part of the preparation for drawing in the screen.
7243,logical value indicating whether all of thescreens should be closed.
7244,locations of grid lines at which the values in <code>z</code> aremeasured.  These must be in ascending order.  (The rest of thisdescription does not apply to <code>.filled.contour</code>.)By default, equally spaced values from 0 to 1 are used.  If <code>x</code>is a <code>list</code>, its components <code>x$x</code> and <code>x$y</code> are usedfor <code>x</code> and <code>y</code>, respectively.  If the list has component<code>z</code> this is used for <code>z</code>.
7245,a numeric matrix containing the values to be plotted..  Note that<code>x</code> can be used instead of <code>z</code> for convenience.
7246,x limits for the plot.
7247,y limits for the plot.
7248,z limits for the plot.
7249,a set of levels which are used to partition the rangeof <code>z</code>.  Must be <b>strictly</b> increasing (and finite).  Areaswith <code>z</code> values between consecutive levels are painted with thesame color.
7250,if <code>levels</code> is not specified, the range of <code>z</code>,values is divided into approximately this many levels.
7251,a color palette function to be used to assigncolors in the plot.
7252,an explicit set of colors to be used in the plot.This argument overrides any palette function specification.  Thereshould be one less color than levels
7253,statements which add titles to the main plot.
7254,statements which draw axes (and a <code>box</code>)on the main plot.  This overrides the default axes.
7255,statements which add titles for the plot key.
7256,statements which draw axes on the plot key.This overrides the default axis.
7257,the <i>y/x</i> aspect ratio, see <code>plot.window</code>.
7258,the x axis style.  The default is to use internallabeling.
7259,the y axis style.  The default is to use internallabeling.
7260,the style of labeling to be used.  The default is touse horizontal labeling.
7261,logicals indicating if axes and a box should bedrawn, as in <code>plot.default</code>.
7262,additional graphical parameters, currently only passed to<code>title()</code>.
7263,a 2 by 2 by <i>k</i> contingency table in array form, or as a2 by 2 matrix if <i>k</i> is 1.
7264,a vector of length 2 specifying the colors to use for thesmaller and larger diagonals of each 2 by 2 table.
7265,confidence level used for the confidence rings onthe odds ratios.  Must be a single nonnegative number less than 1;if set to 0, confidence rings are suppressed.
7266,a character string specifying how to standardize the table.Must match one of <code>"margins"</code>, <code>"ind.max"</code>, or<code>"all.max"</code>, and can be abbreviated to the initial letter.If set to <code>"margins"</code>, each 2 by 2 table is standardized toequate the margins specified by <code>margin</code> while preserving theodds ratio.  If <code>"ind.max"</code> or <code>"all.max"</code>, the tables areeither individually or simultaneously standardized to a maximal cellfrequency of 1.
7267,a numeric vector with the margins to equate.  Must beone of <code>1</code>, <code>2</code>, or <code>c(1, 2)</code> (the default), whichcorresponds to standardizing the row, column, or both margins ineach 2 by 2 table.Only used if <code>std</code> equals <code>"margins"</code>.
7268,the amount of space (as a fraction of the maximal radiusof the quarter circles) used for the row and column labels.
7269,character string for the fourfold title.
7270,a numeric vector of the form <code>c(nr, nc)</code>, indicatingthat the displays for the 2 by 2 tables should be arranged in an<code>nr</code> by <code>nc</code> layout, filled by rows.
7271,a numeric vector of the form <code>c(nr, nc)</code>, indicatingthat the displays for the 2 by 2 tables should be arranged in an<code>nr</code> by <code>nc</code> layout, filled by columns.
7272,numeric vector of coordinates.
7273,character strings giving the coordinate systems toconvert between.
7274,numeric vector of coordinates.
7275,character strings giving the coordinate systems toconvert between.
7276,number of cells of the grid in x and y direction.  When<code>NULL</code>, as per default, the grid aligns with the tick marks onthe corresponding <em>default</em> axis (i.e., tickmarks as computed by<code>axTicks</code>).  When <code>NA</code>, no grid lines aredrawn in the corresponding direction.
7277,character or (integer) numeric; color of the grid lines.
7278,character or (integer) numeric; line type of the grid lines.
7279,non-negative numeric giving line width of the grid lines.
7280,logical, only used when <em>log</em> coordinates andalignment with the axis tick marks are active.  Setting <code>equilogs =      FALSE</code> in that case gives <em>non equidistant</em> tick alignedgrid lines.
7281,a vector of values for which the histogram is desired.
7282,one of:

7283, a vector giving the breakpoints between histogram cells,
7284, a function to compute the vector of breakpoints,
7285, a single number giving the number of cells for the histogram,
7286, a character string naming an algorithm to compute thenumber of cells (see ‘Details’),
7287, a function to compute the number of cells.
7288,In the last three cases the number is a suggestion only; as thebreakpoints will be set to <code>pretty</code> values, the numberis limited to <code>1e6</code> (with a warning if it was larger).  If<code>breaks</code> is a function, the <code>x</code> vector is supplied to itas the only argument (and the number of breaks is only limited bythe amount of available memory).
7289,logical; if <code>TRUE</code>, the histogram graphic is arepresentation of frequencies, the <code>counts</code> component ofthe result; if <code>FALSE</code>, probability densities, component<code>density</code>, are plotted (so that the histogram has a total areaof one).  Defaults to <code>TRUE</code> <em>if and only if</em> <code>breaks</code> areequidistant (and <code>probability</code> is not specified).
7290,an <em>alias</em> for <code>!freq</code>, for S compatibility.
7291,logical; if <code>TRUE</code>, an <code>x[i]</code> equal tothe <code>breaks</code> value will be included in the first (or last, for<code>right = FALSE</code>) bar.  This will be ignored (with a warning)unless <code>breaks</code> is a vector.
7292,logical; if <code>TRUE</code>, the histogram cells areright-closed (left open) intervals.
7293,the density of shading lines, in lines per inch.The default value of <code>NULL</code> means that no shading linesare drawn. Non-positive values of <code>density</code> also inhibit thedrawing of shading lines.
7294,the slope of shading lines, given as an angle indegrees (counter-clockwise).
7295,a colour to be used to fill the bars.The default of <code>NULL</code> yields unfilled bars.
7296,the color of the border around the bars.  The defaultis to use the standard foreground color.
7297,main title and axis labels: these arguments to<code>title()</code> get “smart” defaults here, e.g., the default<code>ylab</code> is <code>"Frequency"</code> iff <code>freq</code> is true.
7298,the range of x and y values with sensible defaults.Note that <code>xlim</code> is <em>not</em> used to define the histogram (breaks),but only for plotting (when <code>plot = TRUE</code>).
7299,logical.  If <code>TRUE</code> (default), axes are draw if theplot is drawn.
7300,logical.  If <code>TRUE</code> (default), a histogram isplotted, otherwise a list of breaks and counts is returned.  In thelatter case, a warning is used if (typically graphical) argumentsare specified that only apply to the <code>plot = TRUE</code> case.
7301,logical or character string.  Additionally draw labels on topof bars, if not <code>FALSE</code>; see <code>plot.histogram</code>.
7302,numeric (integer).  For S(-PLUS) compatibility only,<code>nclass</code> is equivalent to <code>breaks</code> for a scalar orcharacter argument.
7303,logical.  If <code>plot = FALSE</code> and<code>warn.unused = TRUE</code>, a warning will be issued when graphicalparameters are passed to <code>hist.default()</code>.
7304,further arguments and graphical parameters passed to<code>plot.histogram</code> and thence to <code>title</code> and<code>axis</code> (if <code>plot = TRUE</code>).
7305,a vector of values for which the histogram is desired.
7306,one of:

7307, a vector giving the breakpoints between histogram cells,
7308, a function to compute the vector of breakpoints,
7309, a single number giving the number of cells for the histogram,
7310, a character string naming an algorithm to compute thenumber of cells (see ‘Details’),
7311, a function to compute the number of cells.
7312,In the last three cases the number is a suggestion only; as thebreakpoints will be set to <code>pretty</code> values, the numberis limited to <code>1e6</code> (with a warning if it was larger).  If<code>breaks</code> is a function, the <code>x</code> vector is supplied to itas the only argument (and the number of breaks is only limited bythe amount of available memory).
7313,logical; if <code>TRUE</code>, the histogram graphic is arepresentation of frequencies, the <code>counts</code> component ofthe result; if <code>FALSE</code>, probability densities, component<code>density</code>, are plotted (so that the histogram has a total areaof one).  Defaults to <code>TRUE</code> <em>if and only if</em> <code>breaks</code> areequidistant (and <code>probability</code> is not specified).
7314,an <em>alias</em> for <code>!freq</code>, for S compatibility.
7315,logical; if <code>TRUE</code>, an <code>x[i]</code> equal tothe <code>breaks</code> value will be included in the first (or last, for<code>right = FALSE</code>) bar.  This will be ignored (with a warning)unless <code>breaks</code> is a vector.
7316,logical; if <code>TRUE</code>, the histogram cells areright-closed (left open) intervals.
7317,the density of shading lines, in lines per inch.The default value of <code>NULL</code> means that no shading linesare drawn. Non-positive values of <code>density</code> also inhibit thedrawing of shading lines.
7318,the slope of shading lines, given as an angle indegrees (counter-clockwise).
7319,a colour to be used to fill the bars.The default of <code>NULL</code> yields unfilled bars.
7320,the color of the border around the bars.  The defaultis to use the standard foreground color.
7321,main title and axis labels: these arguments to<code>title()</code> get “smart” defaults here, e.g., the default<code>ylab</code> is <code>"Frequency"</code> iff <code>freq</code> is true.
7322,the range of x and y values with sensible defaults.Note that <code>xlim</code> is <em>not</em> used to define the histogram (breaks),but only for plotting (when <code>plot = TRUE</code>).
7323,logical.  If <code>TRUE</code> (default), axes are draw if theplot is drawn.
7324,logical.  If <code>TRUE</code> (default), a histogram isplotted, otherwise a list of breaks and counts is returned.  In thelatter case, a warning is used if (typically graphical) argumentsare specified that only apply to the <code>plot = TRUE</code> case.
7325,logical or character string.  Additionally draw labels on topof bars, if not <code>FALSE</code>; see <code>plot.histogram</code>.
7326,numeric (integer).  For S(-PLUS) compatibility only,<code>nclass</code> is equivalent to <code>breaks</code> for a scalar orcharacter argument.
7327,logical.  If <code>plot = FALSE</code> and<code>warn.unused = TRUE</code>, a warning will be issued when graphicalparameters are passed to <code>hist.default()</code>.
7328,further arguments and graphical parameters passed to<code>plot.histogram</code> and thence to <code>title</code> and<code>axis</code> (if <code>plot = TRUE</code>).
7329,coordinates of points in a scatter plot.  Alternatively, anyobject which defines coordinates (a plotting structure, timeseries etc: see <code>xy.coords</code>) can be given as <code>x</code>,and <code>y</code> left missing.
7330,an optional character vector giving labels for thepoints.  Will be coerced using <code>as.character</code>, andrecycled if necessary to the length of <code>x</code>.  Excess labels willbe discarded, with a warning.
7331,if <code>pos</code> is <code>TRUE</code>, a component is added to thereturn value which indicates where text was plotted relative to eachidentified point: see Value.
7332,the maximum number of points to be identified.
7333,logical: if <code>plot</code> is <code>TRUE</code>, the labels areprinted near the points and if <code>FALSE</code> they are omitted.
7334,logical: if <code>TRUE</code> and <code>plot = TRUE</code>, thelower-left corners of the labels are plotted at the points clickedrather than relative to the points.
7335,the distance (in character widths) which separates thelabel from identified points.  Negative values are allowed.  Notused if <code>atpen = TRUE</code>.
7336,the maximal distance (in inches) for the pointer to be‘close enough’ to a point.
7337,if <code>order</code> is <code>TRUE</code>, a component is added to thereturn value which indicates the order in which points wereidentified: see Value.
7338,further arguments passed to <code>par</code> such as<code>cex</code>, <code>col</code> and <code>font</code>.
7339,locations of grid lines at which the values in <code>z</code> aremeasured.  These must be finite, non-missing and in (strictly)ascending order.  By default, equallyspaced values from 0 to 1 are used.  If <code>x</code> is a <code>list</code>,its components <code>x$x</code> and <code>x$y</code> are used for <code>x</code>and <code>y</code>, respectively. If the list has component <code>z</code> thisis used for <code>z</code>.
7340,a numeric or logical matrix containing the values to be plotted(<code>NA</code>s are allowed).  Note that <code>x</code> can be used insteadof <code>z</code> for convenience.
7341,the minimum and maximum <code>z</code> values for which colorsshould be plotted, defaulting to the range of the finite values of<code>z</code>. Each of the given colors will be used to color anequispaced interval of this range. The <em>midpoints</em> of theintervals cover the range, so that values just outside the rangewill be plotted.
7342,ranges for the plotted <code>x</code> and <code>y</code> values,defaulting to the ranges of <code>x</code> and <code>y</code>.
7343,a list of colors such as that generated by<code>hcl.colors</code>, <code>gray.colors</code> or similarfunctions.
7344,logical; if <code>TRUE</code>, add to current plot (and disregardthe following four arguments).  This is rarely useful because<code>image</code> ‘paints’ over existing graphics.
7345,style of x and y axis.  The default <code>"i"</code> isappropriate for images.  See <code>par</code>.
7346,each a character string giving the labels for the x andy axis.  Default to the ‘call names’ of <code>x</code> or <code>y</code>, or to<code>""</code> if these were unspecified.
7347,a set of finite numeric breakpoints for the colours:must have one more breakpoint than colour and be in increasingorder.  Unsorted vectors will be sorted, with a warning.
7348,logical. If true the midpoints of the colour intervalsare equally spaced, and <code>zlim[1]</code> and <code>zlim[2]</code> were takento be midpoints.  The default is to have colour intervals of equallengths between the limits.
7349,logical; if <code>TRUE</code> a bitmap raster is used toplot the image instead of polygons. The grid must be regular in thatcase, otherwise an error is raised.   For the behaviour when this isnot specified, see ‘Details’.
7350,graphical parameters for <code>plot</code> may also bepassed as arguments to this function, as can the plot aspect ratio<code>asp</code> and <code>axes</code> (see <code>plot.window</code>).
7351,locations of grid lines at which the values in <code>z</code> aremeasured.  These must be finite, non-missing and in (strictly)ascending order.  By default, equallyspaced values from 0 to 1 are used.  If <code>x</code> is a <code>list</code>,its components <code>x$x</code> and <code>x$y</code> are used for <code>x</code>and <code>y</code>, respectively. If the list has component <code>z</code> thisis used for <code>z</code>.
7352,a numeric or logical matrix containing the values to be plotted(<code>NA</code>s are allowed).  Note that <code>x</code> can be used insteadof <code>z</code> for convenience.
7353,the minimum and maximum <code>z</code> values for which colorsshould be plotted, defaulting to the range of the finite values of<code>z</code>. Each of the given colors will be used to color anequispaced interval of this range. The <em>midpoints</em> of theintervals cover the range, so that values just outside the rangewill be plotted.
7354,ranges for the plotted <code>x</code> and <code>y</code> values,defaulting to the ranges of <code>x</code> and <code>y</code>.
7355,a list of colors such as that generated by<code>hcl.colors</code>, <code>gray.colors</code> or similarfunctions.
7356,logical; if <code>TRUE</code>, add to current plot (and disregardthe following four arguments).  This is rarely useful because<code>image</code> ‘paints’ over existing graphics.
7357,style of x and y axis.  The default <code>"i"</code> isappropriate for images.  See <code>par</code>.
7358,each a character string giving the labels for the x andy axis.  Default to the ‘call names’ of <code>x</code> or <code>y</code>, or to<code>""</code> if these were unspecified.
7359,a set of finite numeric breakpoints for the colours:must have one more breakpoint than colour and be in increasingorder.  Unsorted vectors will be sorted, with a warning.
7360,logical. If true the midpoints of the colour intervalsare equally spaced, and <code>zlim[1]</code> and <code>zlim[2]</code> were takento be midpoints.  The default is to have colour intervals of equallengths between the limits.
7361,logical; if <code>TRUE</code> a bitmap raster is used toplot the image instead of polygons. The grid must be regular in thatcase, otherwise an error is raised.   For the behaviour when this isnot specified, see ‘Details’.
7362,graphical parameters for <code>plot</code> may also bepassed as arguments to this function, as can the plot aspect ratio<code>asp</code> and <code>axes</code> (see <code>plot.window</code>).
7363,a matrix object specifying the location of the next<i>N</i> figures on the output device.  Each value in the matrix mustbe <code>0</code> or a positive integer.  If <i>N</i> is the largestpositive integer in the matrix, then the integers<i>{1, …, N-1}</i> must also appear at least once in the matrix.
7364,a vector of values for the widths of columns on thedevice.  Relative widths are specified with numeric values.    Absolutewidths (in centimetres) are specified with the <code>lcm()</code> function (seeexamples).
7365,a vector of values for the heights of rows on the device.Relative and absolute heights can be specified, see <code>widths</code> above.
7366,either a logical value or a matrix object.  If thelatter, then it must have the same dimensions as <code>mat</code> andeach value in the matrix must be either <code>0</code> or <code>1</code>.
7367,number of figures to plot.
7368,a dimension to be interpreted as a number of centimetres.
7369,a matrix object specifying the location of the next<i>N</i> figures on the output device.  Each value in the matrix mustbe <code>0</code> or a positive integer.  If <i>N</i> is the largestpositive integer in the matrix, then the integers<i>{1, …, N-1}</i> must also appear at least once in the matrix.
7370,a vector of values for the widths of columns on thedevice.  Relative widths are specified with numeric values.    Absolutewidths (in centimetres) are specified with the <code>lcm()</code> function (seeexamples).
7371,a vector of values for the heights of rows on the device.Relative and absolute heights can be specified, see <code>widths</code> above.
7372,either a logical value or a matrix object.  If thelatter, then it must have the same dimensions as <code>mat</code> andeach value in the matrix must be either <code>0</code> or <code>1</code>.
7373,number of figures to plot.
7374,a dimension to be interpreted as a number of centimetres.
7375,a matrix object specifying the location of the next<i>N</i> figures on the output device.  Each value in the matrix mustbe <code>0</code> or a positive integer.  If <i>N</i> is the largestpositive integer in the matrix, then the integers<i>{1, …, N-1}</i> must also appear at least once in the matrix.
7376,a vector of values for the widths of columns on thedevice.  Relative widths are specified with numeric values.    Absolutewidths (in centimetres) are specified with the <code>lcm()</code> function (seeexamples).
7377,a vector of values for the heights of rows on the device.Relative and absolute heights can be specified, see <code>widths</code> above.
7378,either a logical value or a matrix object.  If thelatter, then it must have the same dimensions as <code>mat</code> andeach value in the matrix must be either <code>0</code> or <code>1</code>.
7379,number of figures to plot.
7380,a dimension to be interpreted as a number of centimetres.
7381,the x and y co-ordinates to be used to position the legend.They can be specified by keyword or in any way which is accepted by<code>xy.coords</code>: See ‘Details’.
7382,a character or expression vectorof length <i>≥ 1</i> to appear in the legend.  Otherobjects will be coerced by <code>as.graphicsAnnot</code>.
7383,if specified, this argument will cause boxes filledwith the specified colors (or shaded in the specified colors)to appear beside the legend text.
7384,the color of points or lines appearing in the legend.
7385,the border color for the boxes (used only if<code>fill</code> is specified).
7386,the line types and widths for lines appearing in thelegend.  One of these two <em>must</em> be specified for line drawing.
7387,the plotting symbols appearing in the legend, asnumeric vector or a vector of 1-character strings (see<code>points</code>). Unlike <code>points</code>, this can all bespecified as a single multi-character string.  <em>Must</em> bespecified for symbol drawing.
7388,angle of shading lines.
7389,the density of shading lines, if numeric andpositive. If <code>NULL</code> or negative or <code>NA</code> color fillingis assumed.
7390,the type of box to be drawn around the legend.  The allowedvalues are <code>"o"</code> (the default) and <code>"n"</code>.
7391,the background color for the legend box.  (Note that this isonly used if <code>bty != "n"</code>.)
7392,the line type, width and color forthe legend box (if <code>bty = "o"</code>).
7393,the background color for the <code>points</code>,corresponding to its argument <code>bg</code>.
7394,character expansion factor <b>relative</b> to current<code>par("cex")</code>.  Used for text, and provides the default for<code>pt.cex</code>.
7395,expansion factor(s) for the points.
7396,line width for the points, defaults to the one forlines, or if that is not set, to <code>par("lwd")</code>.
7397,how the legend is to be justified relative to the legendx location.  A value of 0 means left justified, 0.5 means centeredand 1 means right justified.
7398,the same as <code>xjust</code> for the legend y location.
7399,character interspacing factor for horizontal (x) spacing.
7400,the same for vertical (y) line distances.
7401,numeric of length 1 or 2; the string adjustment for legendtext.  Useful for y-adjustment when <code>labels</code> areplotmath expressions.
7402,the width of the legend text in x (<code>"user"</code>)coordinates.  (Should be a single positive number even for a reversedx axis.)  Defaults to the proper value computed by<code>strwidth(legend)</code>.
7403,the color used for the legend text.
7404,the font used for the legend text, see <code>text</code>.
7405,logical; if <code>TRUE</code>, merge points and lines butnot filled boxes.  Defaults to <code>TRUE</code> if there are points and lines.
7406,logical; if <code>TRUE</code>, shows how <code>legend</code> does allits magical computations.
7407,logical.  If <code>FALSE</code>, nothing is plotted but thesizes are returned.
7408,the number of columns in which to set the legend items(default is 1, a vertical legend).
7409,logical; if <code>TRUE</code>, set the legend horizontallyrather than vertically (specifying <code>horiz</code> overrides the <code>ncol</code>specification).
7410,a character string or length-one expression giving atitle to be placed at the top of the legend.  Other objects will becoerced by <code>as.graphicsAnnot</code>.
7411,inset distance(s) from the margins as a fraction of theplot region when legend is placed by keyword.
7412,if supplied, a value of the graphical parameter <code>xpd</code>to be used while the legend is being drawn.
7413,color for <code>title</code>.
7414,horizontal adjustment for <code>title</code>: see the helpfor <code>par("adj")</code>.
7415,the length of lines drawn to illustrate <code>lty</code>and/or <code>lwd</code> (in units of character widths).
7416,coordinate vectors of points to join.
7417,character indicating the type of plotting; actually any ofthe <code>type</code>s as in <code>plot.default</code>.
7418,Further graphical parameters (see <code>par</code>) mayalso be supplied as arguments, particularly, line type, <code>lty</code>,line width, <code>lwd</code>, color, <code>col</code> and for <code>type = "b"</code>,<code>pch</code>.  Also the line characteristics <code>lend</code>, <code>ljoin</code>and <code>lmitre</code>.
7419,coordinate vectors of points to join.
7420,character indicating the type of plotting; actually any ofthe <code>type</code>s as in <code>plot.default</code>.
7421,Further graphical parameters (see <code>par</code>) mayalso be supplied as arguments, particularly, line type, <code>lty</code>,line width, <code>lwd</code>, color, <code>col</code> and for <code>type = "b"</code>,<code>pch</code>.  Also the line characteristics <code>lend</code>, <code>ljoin</code>and <code>lmitre</code>.
7422,the maximum number of points to locate.  Valid values startat 1.
7423,One of <code>"n"</code>, <code>"p"</code>, <code>"l"</code> or<code>"o"</code>. If <code>"p"</code> or <code>"o"</code> the points areplotted; if <code>"l"</code>  or <code>"o"</code> they are joined by lines.
7424,additional graphics parameters used if <code>type != "n"</code>for plotting the locations.
7425,vectors or matrices of data for plotting.  The number ofrows should match.  If one of them are missing, the other is takenas <code>y</code> and an <code>x</code> vector of <code>1:n</code> is used.Missing values (<code>NA</code>s) are allowed.  Since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0,<code>class(.)</code>es of <code>x</code> and <code>y</code> such as<code>"Date"</code> are typically preserved.
7426,character string (length 1 vector) or vector of 1-characterstrings indicating the type of plot for eachcolumn of <code>y</code>, see <code>plot</code> for all possible<code>type</code>s.  The first character of <code>type</code>defines the first plot, the second character the second, etc.Characters in <code>type</code> are cycled through; e.g., <code>"pl"</code>alternately plots points and lines.
7427,vector of line types, widths, and end styles.The first element is for the first column, the second element forthe second column, etc., even if lines are not plotted for allcolumns. Line types will be used cyclically until all plots aredrawn.
7428,character string or vector of 1-characters or integers forplotting characters, see <code>points</code> for details.The first character is the plotting-character for the first plot,the second for the second, etc.  The default is the digits (1through 9, 0) then the lowercase and uppercase letters.
7429,vector of colors.  Colors are used cyclically.
7430,vector of character expansion sizes, used cyclically.This works as a multiple of <code>par("cex")</code>.  <code>NULL</code> isequivalent to <code>1.0</code>. 
7431,vector of background (fill) colors for the open plotsymbols given by <code>pch = 21:25</code> as in <code>points</code>.  Thedefault <code>NA</code> corresponds to the one of the underlying function<code>plot.xy</code>.
7432,titles for x and y axes, as in <code>plot</code>.
7433,ranges of x and y axes, as in <code>plot</code>.
7434,Graphical parameters (see <code>par</code>) and any furtherarguments of <code>plot</code>, typically <code>plot.default</code>, may also besupplied as arguments to this function; even <code>panel.first</code> etcnow work.  Hence, the high-levelgraphics control arguments described under <code>par</code> and thearguments to <code>title</code> may be supplied to this function.
7435,logical.  If <code>TRUE</code>, plots are added to current one,using <code>points</code> and <code>lines</code>.
7436,logical.  If <code>TRUE</code>, write one line of what isdone.
7437,vectors or matrices of data for plotting.  The number ofrows should match.  If one of them are missing, the other is takenas <code>y</code> and an <code>x</code> vector of <code>1:n</code> is used.Missing values (<code>NA</code>s) are allowed.  Since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0,<code>class(.)</code>es of <code>x</code> and <code>y</code> such as<code>"Date"</code> are typically preserved.
7438,character string (length 1 vector) or vector of 1-characterstrings indicating the type of plot for eachcolumn of <code>y</code>, see <code>plot</code> for all possible<code>type</code>s.  The first character of <code>type</code>defines the first plot, the second character the second, etc.Characters in <code>type</code> are cycled through; e.g., <code>"pl"</code>alternately plots points and lines.
7439,vector of line types, widths, and end styles.The first element is for the first column, the second element forthe second column, etc., even if lines are not plotted for allcolumns. Line types will be used cyclically until all plots aredrawn.
7440,character string or vector of 1-characters or integers forplotting characters, see <code>points</code> for details.The first character is the plotting-character for the first plot,the second for the second, etc.  The default is the digits (1through 9, 0) then the lowercase and uppercase letters.
7441,vector of colors.  Colors are used cyclically.
7442,vector of character expansion sizes, used cyclically.This works as a multiple of <code>par("cex")</code>.  <code>NULL</code> isequivalent to <code>1.0</code>. 
7443,vector of background (fill) colors for the open plotsymbols given by <code>pch = 21:25</code> as in <code>points</code>.  Thedefault <code>NA</code> corresponds to the one of the underlying function<code>plot.xy</code>.
7444,titles for x and y axes, as in <code>plot</code>.
7445,ranges of x and y axes, as in <code>plot</code>.
7446,Graphical parameters (see <code>par</code>) and any furtherarguments of <code>plot</code>, typically <code>plot.default</code>, may also besupplied as arguments to this function; even <code>panel.first</code> etcnow work.  Hence, the high-levelgraphics control arguments described under <code>par</code> and thearguments to <code>title</code> may be supplied to this function.
7447,logical.  If <code>TRUE</code>, plots are added to current one,using <code>points</code> and <code>lines</code>.
7448,logical.  If <code>TRUE</code>, write one line of what isdone.
7449,vectors or matrices of data for plotting.  The number ofrows should match.  If one of them are missing, the other is takenas <code>y</code> and an <code>x</code> vector of <code>1:n</code> is used.Missing values (<code>NA</code>s) are allowed.  Since <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> 4.0.0,<code>class(.)</code>es of <code>x</code> and <code>y</code> such as<code>"Date"</code> are typically preserved.
7450,character string (length 1 vector) or vector of 1-characterstrings indicating the type of plot for eachcolumn of <code>y</code>, see <code>plot</code> for all possible<code>type</code>s.  The first character of <code>type</code>defines the first plot, the second character the second, etc.Characters in <code>type</code> are cycled through; e.g., <code>"pl"</code>alternately plots points and lines.
7451,vector of line types, widths, and end styles.The first element is for the first column, the second element forthe second column, etc., even if lines are not plotted for allcolumns. Line types will be used cyclically until all plots aredrawn.
7452,character string or vector of 1-characters or integers forplotting characters, see <code>points</code> for details.The first character is the plotting-character for the first plot,the second for the second, etc.  The default is the digits (1through 9, 0) then the lowercase and uppercase letters.
7453,vector of colors.  Colors are used cyclically.
7454,vector of character expansion sizes, used cyclically.This works as a multiple of <code>par("cex")</code>.  <code>NULL</code> isequivalent to <code>1.0</code>. 
7455,vector of background (fill) colors for the open plotsymbols given by <code>pch = 21:25</code> as in <code>points</code>.  Thedefault <code>NA</code> corresponds to the one of the underlying function<code>plot.xy</code>.
7456,titles for x and y axes, as in <code>plot</code>.
7457,ranges of x and y axes, as in <code>plot</code>.
7458,Graphical parameters (see <code>par</code>) and any furtherarguments of <code>plot</code>, typically <code>plot.default</code>, may also besupplied as arguments to this function; even <code>panel.first</code> etcnow work.  Hence, the high-levelgraphics control arguments described under <code>par</code> and thearguments to <code>title</code> may be supplied to this function.
7459,logical.  If <code>TRUE</code>, plots are added to current one,using <code>points</code> and <code>lines</code>.
7460,logical.  If <code>TRUE</code>, write one line of what isdone.
7461,a contingency table in array form, with optional categorylabels specified in the <code>dimnames(x)</code> attribute.  The table isbest created by the <code>table()</code> command.
7462,character string for the mosaic title.
7463,character string for the mosaic sub-title (at bottom).
7464,x- and y-axis labels used for the plot; by default,the first and second element of <code>names(dimnames(X))</code> (i.e., thename of the first and second variable in <code>X</code>).
7465,vector ordering of the variables, containing a permutationof the integers <code>1:length(dim(x))</code> (the default).
7466,vector of offsets to determine percentage spacing at eachlevel of the mosaic (appropriate values are between 0 and 20,and the default is 20 times the number of splits for 2-dimensionaltables, and 10 otherwise).  Rescaled to maximally 50, and recycled ifnecessary.
7467,vector of split directions (<code>"v"</code> for vertical and<code>"h"</code> for horizontal) for each level of the mosaic, onedirection for each dimension of the contingency table.  Thedefault consists of alternating directions, beginning with avertical split.
7468,logical or (recycling) vector of colors for colorshading, used only when <code>shade</code> is <code>FALSE</code>, or <code>NULL</code>(default).  By default, grey boxes are drawn.  <code>color = TRUE</code>uses a gamma-corrected grey palette.  <code>color = FALSE</code> gives emptyboxes with no shading.
7469,a logical indicating whether to produce extended mosaicplots, or a numeric vector of at most 5 distinct positive numbersgiving the absolute values of the cut points for the residuals.  Bydefault, <code>shade</code> is <code>FALSE</code>, and simple mosaics arecreated.  Using <code>shade = TRUE</code> cuts absolute values at 2 and4.
7470,a list of vectors with the marginal totals to be fit inthe log-linear model.  By default, an independence model is fitted.See <code>loglin</code> for further information.
7471,The magnification to be used for axis annotation,as a multiple of <code>par("cex")</code>.
7472,numeric; the style of axis labels, see <code>par</code>.
7473,colour of borders of cells: see <code>polygon</code>.
7474,a character string indicating the type of residual to berepresented.  Must be one of <code>"pearson"</code> (giving components ofPearson's <i>chi-squared</i>), <code>"deviance"</code> (givingcomponents of the likelihood ratio <i>chi-squared</i>), or<code>"FT"</code> for the Freeman-Tukey residuals.  The value of thisargument can be abbreviated.
7475,a formula, such as <code>y ~ x</code>.
7476,a data frame (or list), or a contingency table from whichthe variables in <code>formula</code> should be taken.
7477,further arguments to be passed to or from methods.
7478,an optional vector specifying a subset of observationsin the data frame to be used for plotting.
7479,a function which indicates what should happenwhen the data contains variables to be cross-tabulated, and thesevariables contain <code>NA</code>s.  The default is to omit cases whichhave an <code>NA</code> in any variable.  Since the tabulation will omitall cases containing missing values, this will only be useful if the<code>na.action</code> function replaces missing values.
7480,a character or expression vector specifyingthe <em>text</em> to be written.  Other objects are coerced by<code>as.graphicsAnnot</code>.
7481,on which side of the plot (1=bottom, 2=left, 3=top, 4=right).
7482,on which MARgin line, starting at 0 counting outwards.
7483,use outer margins if available.
7484,give location of each string in user coordinates.  If thecomponent of <code>at</code> corresponding to a particular text item isnot a finite value (the default), the location will be determinedby <code>adj</code>.
7485,adjustment for each string in reading direction.  Forstrings parallel to the axes, <code>adj = 0</code> means left or bottomalignment, and <code>adj = 1</code> means right or top alignment.

7486,If <code>adj</code> is not a finite value (the default), the value of<code>par("las")</code> determines the adjustment.  For strings plottedparallel to the axis the default is to centre the string.
7487,adjustment for each string perpendicular to the readingdirection (which is controlled by <code>adj</code>).  For strings parallelto the axes, <code>padj = 0</code> means right or top alignment,and <code>padj = 1</code> means left or bottom alignment.

7488,If <code>padj</code> is not a finite value (the default), the value of<code>par("las")</code> determines the adjustment.  For strings plottedperpendicular to the axis the default is to centre the string.
7489,character expansion factor.  <code>NULL</code> and <code>NA</code>are equivalent to <code>1.0</code>.  This is an absolute measure, notscaled by <code>par("cex")</code> or by setting <code>par("mfrow")</code>or <code>par("mfcol")</code>.  Can be a vector.
7490,color to use.  Can be a vector.  <code>NA</code> values (thedefault) mean use <code>par("col")</code>.
7491,font for text.  Can be a vector.  <code>NA</code> values (thedefault) mean use <code>par("font")</code>.
7492,Further graphical parameters (see <code>par</code>),including <code>family</code>, <code>las</code> and <code>xpd</code>. (The latterdefaults to the figure region unless <code>outer = TRUE</code>, otherwisethe device region.  It can only be increased.)
7493,the coordinates of points given as numeric columns of amatrix or data frame.  Logical and factor columns are converted tonumeric in the same way that <code>data.matrix</code> does.
7494,a formula, such as <code>~ x + y + z</code>.  Each term willgive a separate variable in the pairs plot, so terms should benumeric vectors.  (A response will be interpreted as anothervariable, but not treated specially, so it is confusing to use one.)
7495,a data.frame (or list) from which the variables in<code>formula</code> should be taken.
7496,an optional vector specifying a subset of observationsto be used for plotting.
7497,a function which indicates what should happenwhen the data contain <code>NA</code>s.  The default is to pass missingvalues on to the panel functions, but <code>na.action = na.omit</code>will cause cases with missing values in any of the variables to beomitted entirely.
7498,the names of the variables.
7499,<code>function(x, y, ...)</code> which is used to plotthe contents of each panel of the display.
7500,arguments to be passed to or from methods.

7501,Also, graphical parameters can be given as can arguments to<code>plot</code> such as <code>main</code>.  <code>par("oma")</code> will be setappropriately unless specified.
7502,The (numerical) indices of the variables to beplotted on the horizontal and vertical axes respectively.
7503,separate panel functions (or<code>NULL</code>) to be used below and above the diagonal respectively.
7504,optional <code>function(x, ...)</code> to beapplied on the diagonals.
7505,optional <code>function(x, y, labels, cex,      font, ...)</code> to be applied on the diagonals.
7506,<code>y</code> position of labels in the text panel.
7507,if <code>main</code> is specified, <code>line.main</code> givesthe <code>line</code> argument to <code>mtext()</code> which draws thetitle.  You may want to specify <code>oma</code> when changing <code>line.main</code>.
7508,graphics parameters for the text panel.
7509,logical.  Should the layout be matrix-like with row 1at the top, or graph-like with row 1 at the bottom?  The latter (nondefault) leads to a basically symmetric scatterplot matrix.
7510,distance between subplots, in margin lines.
7511,a character string indicating if logarithmic axes are to beused, see <code>plot.default</code> <em>or</em> a numeric vector ofindices specifying the indices of those variables where logarithmicaxes should be used for both x and y.  <code>log = "xy"</code> specifieslogarithmic axes for all variables.
7512,<code>logical</code> (or integer) determininghow the horizontal and vertical axis labeling happens.  If true, theaxis labelling starts at the first (from top left) row or column,respectively.
7513,the coordinates of points given as numeric columns of amatrix or data frame.  Logical and factor columns are converted tonumeric in the same way that <code>data.matrix</code> does.
7514,a formula, such as <code>~ x + y + z</code>.  Each term willgive a separate variable in the pairs plot, so terms should benumeric vectors.  (A response will be interpreted as anothervariable, but not treated specially, so it is confusing to use one.)
7515,a data.frame (or list) from which the variables in<code>formula</code> should be taken.
7516,an optional vector specifying a subset of observationsto be used for plotting.
7517,a function which indicates what should happenwhen the data contain <code>NA</code>s.  The default is to pass missingvalues on to the panel functions, but <code>na.action = na.omit</code>will cause cases with missing values in any of the variables to beomitted entirely.
7518,the names of the variables.
7519,<code>function(x, y, ...)</code> which is used to plotthe contents of each panel of the display.
7520,arguments to be passed to or from methods.

7521,Also, graphical parameters can be given as can arguments to<code>plot</code> such as <code>main</code>.  <code>par("oma")</code> will be setappropriately unless specified.
7522,The (numerical) indices of the variables to beplotted on the horizontal and vertical axes respectively.
7523,separate panel functions (or<code>NULL</code>) to be used below and above the diagonal respectively.
7524,optional <code>function(x, ...)</code> to beapplied on the diagonals.
7525,optional <code>function(x, y, labels, cex,      font, ...)</code> to be applied on the diagonals.
7526,<code>y</code> position of labels in the text panel.
7527,if <code>main</code> is specified, <code>line.main</code> givesthe <code>line</code> argument to <code>mtext()</code> which draws thetitle.  You may want to specify <code>oma</code> when changing <code>line.main</code>.
7528,graphics parameters for the text panel.
7529,logical.  Should the layout be matrix-like with row 1at the top, or graph-like with row 1 at the bottom?  The latter (nondefault) leads to a basically symmetric scatterplot matrix.
7530,distance between subplots, in margin lines.
7531,a character string indicating if logarithmic axes are to beused, see <code>plot.default</code> <em>or</em> a numeric vector ofindices specifying the indices of those variables where logarithmicaxes should be used for both x and y.  <code>log = "xy"</code> specifieslogarithmic axes for all variables.
7532,<code>logical</code> (or integer) determininghow the horizontal and vertical axis labeling happens.  If true, theaxis labelling starts at the first (from top left) row or column,respectively.
7533,numeric vectors of the same length
7534,numeric or character codes for the color(s),point type and size of <code>points</code>; see also<code>par</code>.
7535,color to be used by <code>lines</code> for drawing thesmooths.
7536,smoothing parameter <code>f</code> for <code>lowess</code>, seethere.
7537,number of robustness iterations for <code>lowess</code>.
7538,further arguments to <code>lines</code>.
7539,arguments in <code>tag = value</code> form, or a list of taggedvalues.  The tags must come from the names of graphical parametersdescribed in the ‘Graphical Parameters’ section.
7540,logical; if <code>TRUE</code> and there are no otherarguments, only parameters are returned which can be set by asubsequent <code>par()</code> call <em>on the same device</em>.
7541,locations of grid lines at which the values in <code>z</code> aremeasured.  These must be in ascending order.  By default, equallyspaced values from 0 to 1 are used.  If <code>x</code> is a <code>list</code>,its components <code>x$x</code> and <code>x$y</code> are used for <code>x</code>and <code>y</code>, respectively.
7542,a matrix containing the values to be plotted (<code>NA</code>s areallowed).  Note that <code>x</code> can be used instead of <code>z</code> forconvenience.
7543,x-, y-  and z-limits.  These should be chosento cover the range of values of the surface: see ‘Details’.
7544,titles for the axes.  N.B. These must becharacter strings; expressions are not accepted.  Numbers will becoerced to character strings.
7545,main and sub title, as for <code>title</code>.
7546,angles defining the viewing direction.<code>theta</code> gives the azimuthal direction and <code>phi</code>the colatitude.
7547,the distance of the eyepoint from the centre of the plotting box.
7548,a value which can be used to vary the strength ofthe perspective transformation.  Values of <code>d</code> greaterthan 1 will lessen the perspective effect and values lessand 1 will exaggerate it.
7549,before viewing the x, y and z coordinates of thepoints defining the surface are transformed to the interval[0,1].  If <code>scale</code> is <code>TRUE</code> the x, y and z coordinatesare transformed separately.  If <code>scale</code> is <code>FALSE</code>the coordinates are scaled so that aspect ratios are retained.This is useful for rendering things like DEM information.
7550,a expansion factor applied to the <code>z</code>coordinates. Often used with <code>0 &lt; expand &lt; 1</code> to shrink theplotting box in the <code>z</code> direction.
7551,the color(s) of the surface facets.  Transparent colours areignored.  This is recycled to the <i>(nx-1)(ny-1)</i> facets.
7552,the color of the line drawn around the surface facets.The default, <code>NULL</code>, corresponds to <code>par("fg")</code>.A value of <code>NA</code> will disable the drawing of borders: this issometimes useful when the surface is shaded.
7553,if finite values are specified for <code>ltheta</code>and <code>lphi</code>, the surface is shaded as though it was beingilluminated from the direction specified by azimuth <code>ltheta</code>and colatitude <code>lphi</code>.
7554,the shade at a surface facet is computed as<code>((1+d)/2)^shade</code>, where <code>d</code> is the dot product ofa unit vector normal to the facet and a unit vector in thedirection of a light source.  Values of <code>shade</code> closeto one yield shading similar to a point light source modeland values close to zero produce no shading.  Values in therange 0.5 to 0.75 provide an approximation to daylightillumination.
7555,should the bounding box for the surface be displayed.The default is <code>TRUE</code>.
7556,should ticks and labels be added to the box.  Thedefault is <code>TRUE</code>.  If <code>box</code> is <code>FALSE</code> then noticks or labels are drawn.
7557,character: <code>"simple"</code> draws just an arrowparallel to the axis to indicate direction of increase;<code>"detailed"</code> draws normal ticks as per 2D plots.
7558,the (approximate) number of tick marks to draw on theaxes.  Has no effect if <code>ticktype</code> is <code>"simple"</code>.
7559,additional graphical parameters (see <code>par</code>).
7560,a vector of non-negative numerical quantities.The values in <code>x</code> are displayed as the areas of pie slices.
7561,one or more expressions or character strings givingnames for the slices.  Other objects are coerced by<code>as.graphicsAnnot</code>.  For empty or <code>NA</code>(after coercion to character) labels, no label nor pointing lineis drawn.
7562,the circular outline of the pie is approximated by apolygon with this many edges.
7563,the pie is drawn centered in a square box whose sidesrange from <i>-1</i> to <i>1</i>.  If the character strings labelingthe slices are long it may be necessary to use a smaller radius.
7564,logical indicating if slices are drawn clockwise orcounter clockwise (i.e., mathematically positive direction), thelatter is default.
7565,number specifying the <em>starting angle</em> (indegrees) for the slices. Defaults to 0 (i.e., ‘3 o'clock’)unless <code>clockwise</code> is true where <code>init.angle</code>defaults to 90 (degrees), (i.e., ‘12 o'clock’).
7566,the density of shading lines, in lines per inch.The default value of <code>NULL</code> means that no shading linesare drawn. Non-positive values of <code>density</code> also inhibit thedrawing of shading lines.
7567,the slope of shading lines, given as an angle indegrees (counter-clockwise).
7568,a vector of colors to be used in filling or shadingthe slices. If missing a set of 6 pastel colours is used,unless <code>density</code> is specified when <code>par("fg")</code> is used.
7569,(possibly vectors) arguments passed to<code>polygon</code> which draws each slice.
7570,an overall title for the plot.
7571,graphical parameters can be given as arguments to<code>pie</code>.  They will affect the main title and labels only.
7572,the <code>x</code> and <code>y</code> arguments provide the x and ycoordinates for the plot.  Any reasonable way of defining thecoordinates is acceptable.  See the function <code>xy.coords</code>for details.  If supplied separately, they must be of the same length.
7573,1-character string giving the type of plot desired.  Thefollowing values are possible, for details, see <code>plot</code>:<code>"p"</code> for points, <code>"l"</code> for lines,<code>"b"</code> for both points and lines,<code>"c"</code> for empty points joined by lines,<code>"o"</code> for overplotted points and lines,<code>"s"</code> and <code>"S"</code> for stair steps and<code>"h"</code> for histogram-like vertical lines.  Finally,<code>"n"</code> does not produce any points or lines.
7574,the x limits (x1, x2) of the plot.  Note that <code>x1 &gt; x2</code>is allowed and leads to a ‘reversed axis’.

7575,The default value, <code>NULL</code>, indicates that the range of thefinite values to be plotted should be used.
7576,the y limits of the plot.
7577,a character string which contains <code>"x"</code> if the x axisis to be logarithmic, <code>"y"</code> if the y axis is to be logarithmicand <code>"xy"</code> or <code>"yx"</code> if both axes are to be logarithmic.
7578,a main title for the plot, see also <code>title</code>.
7579,a sub title for the plot.
7580,a label for the x axis, defaults to a description of <code>x</code>.
7581,a label for the y axis, defaults to a description of <code>y</code>.
7582,a logical value indicating whether the default annotation(title and x and y axis labels) should appear on the plot.
7583,a logical value indicating whether both axes should be drawn onthe plot.  Use graphical parameter <code>"xaxt"</code> or <code>"yaxt"</code>to suppress just one of the axes.
7584,a logical indicating whether a box should be drawnaround the plot.
7585,an ‘expression’ to be evaluated after theplot axes are set up but before any plotting takes place.  This canbe useful for drawing background grids or scatterplot smooths.  Notethat this works by lazy evaluation: passing this argument from other<code>plot</code> methods may well not work since it may be evaluated tooearly.
7586,an expression to be evaluated after plotting hastaken place but before the axes, title and box are added.  See thecomments about <code>panel.first</code>.
7587,the <i>y/x</i> aspect ratio, see <code>plot.window</code>.
7588,the <i>x/y</i> axis gap factors, passed as<code>gap.axis</code> to the two <code>axis()</code> calls (when<code>axes</code> is true, as per default).
7589,other graphical parameters (see <code>par</code> andsection ‘Details’ below).
7590,the <code>x</code> and <code>y</code> arguments provide the x and ycoordinates for the plot.  Any reasonable way of defining thecoordinates is acceptable.  See the function <code>xy.coords</code>for details.  If supplied separately, they must be of the same length.
7591,1-character string giving the type of plot desired.  Thefollowing values are possible, for details, see <code>plot</code>:<code>"p"</code> for points, <code>"l"</code> for lines,<code>"b"</code> for both points and lines,<code>"c"</code> for empty points joined by lines,<code>"o"</code> for overplotted points and lines,<code>"s"</code> and <code>"S"</code> for stair steps and<code>"h"</code> for histogram-like vertical lines.  Finally,<code>"n"</code> does not produce any points or lines.
7592,the x limits (x1, x2) of the plot.  Note that <code>x1 &gt; x2</code>is allowed and leads to a ‘reversed axis’.

7593,The default value, <code>NULL</code>, indicates that the range of thefinite values to be plotted should be used.
7594,the y limits of the plot.
7595,a character string which contains <code>"x"</code> if the x axisis to be logarithmic, <code>"y"</code> if the y axis is to be logarithmicand <code>"xy"</code> or <code>"yx"</code> if both axes are to be logarithmic.
7596,a main title for the plot, see also <code>title</code>.
7597,a sub title for the plot.
7598,a label for the x axis, defaults to a description of <code>x</code>.
7599,a label for the y axis, defaults to a description of <code>y</code>.
7600,a logical value indicating whether the default annotation(title and x and y axis labels) should appear on the plot.
7601,a logical value indicating whether both axes should be drawn onthe plot.  Use graphical parameter <code>"xaxt"</code> or <code>"yaxt"</code>to suppress just one of the axes.
7602,a logical indicating whether a box should be drawnaround the plot.
7603,an ‘expression’ to be evaluated after theplot axes are set up but before any plotting takes place.  This canbe useful for drawing background grids or scatterplot smooths.  Notethat this works by lazy evaluation: passing this argument from other<code>plot</code> methods may well not work since it may be evaluated tooearly.
7604,an expression to be evaluated after plotting hastaken place but before the axes, title and box are added.  See thecomments about <code>panel.first</code>.
7605,the <i>y/x</i> aspect ratio, see <code>plot.window</code>.
7606,the <i>x/y</i> axis gap factors, passed as<code>gap.axis</code> to the two <code>axis()</code> calls (when<code>axes</code> is true, as per default).
7607,other graphical parameters (see <code>par</code> andsection ‘Details’ below).
7608,either a data frame containing the design factors andoptionally the response, or a <code>formula</code> or<code>terms</code> object.
7609,the response, if not given in x.
7610,a function (or name of one) to be applied to eachsubset.  It must return one number for a numeric (vector) input.
7611,data frame containing the variables referenced by <code>x</code>when that is formula-like.
7612,graphical parameters such as <code>col</code>,see <code>par</code>.
7613,range of y values, as in <code>plot.default</code>.
7614,x axis label, see <code>title</code>.
7615,y axis label with a ‘smart’ default.
7616,main title, see <code>title</code>.
7617,logical indicating if the user should be asked before a newpage is started – in the case of multiple y's.
7618,character giving the type of x axis.
7619,logical indicating if axes should be drawn.
7620,logical indicating if ticks (one per factor) shouldbe drawn on the x axis.
7621,The name of a function, or a call or anexpression written as a function of <code>x</code> which willevaluate to an object of the same length as <code>x</code>.
7622,a ‘vectorizing’ numeric <span style="font-family: Courier New, Courier; color: #666666;"><b>R</b></span> function.
7623,alias for <code>from</code> for compatibility with <code>plot</code>
7624,the range over which the function will be plotted.
7625,integer; the number of x values at which to evaluate.
7626,logical; if <code>TRUE</code> add to an already existing plot; if<code>NA</code> start a new plot taking the defaults for the limits andlog-scaling of the x-axis from the previous plot.  Taken as<code>FALSE</code> (with a warning if a different value is supplied) if nographics device is open.
7627,<code>NULL</code> or a numeric vector of length 2;if non-<code>NULL</code> it provides the defaults for <code>c(from, to)</code>and, unless <code>add = TRUE</code>, selects the x-limits of the plot – see<code>plot.window</code>.
7628,plot type: see <code>plot.default</code>.
7629,character string giving the name to be used for the x axis.
7630,labels and graphical parameterscan also be specified as arguments.  See ‘Details’ for theinterpretation of the default for <code>log</code>.

7631,For the <code>"function"</code> method of <code>plot</code>, <code>...</code> caninclude any of the other arguments of <code>curve</code>, except<code>expr</code>.
7632,numeric vectors of length 2, giving the x and ycoordinates ranges.
7633,character; indicating which axes should be in log scale.
7634,numeric, giving the <b>asp</b>ect ratio y/x,see ‘Details’.
7635,further graphical parameters as in <code>par</code>.The relevant ones are <code>xaxs</code>, <code>yaxs</code> and <code>lab</code>.
7636,A four-element list as results from<code>xy.coords</code>.
7637,1 character code: see <code>plot.default</code>.<code>NULL</code> is accepted as a synonym for <code>"p"</code>.
7638,character or integer code for kind of points, see<code>points.default</code>.
7639,line type code, see <code>lines</code>.
7640,color code or name, see <code>colors</code>,<code>palette</code>.  Here <code>NULL</code> means colour 0.
7641,background (fill) color for the open plot symbols 21:25:see <code>points.default</code>.
7642,character expansion.
7643,line width, also used for (non-filled) plot symbols, see<code>lines</code> and <code>points</code>.
7644,further graphical parameters such as <code>xpd</code>,<code>lend</code>, <code>ljoin</code> and <code>lmitre</code>.
7645,coordinate vectors of points to plot.
7646,character indicating the type of plotting; actually any ofthe <code>type</code>s as in <code>plot.default</code>.
7647,Further graphical parameters may also be supplied asarguments.  See ‘Details’.
7648,coordinate vectors of points to plot.
7649,character indicating the type of plotting; actually any ofthe <code>type</code>s as in <code>plot.default</code>.
7650,Further graphical parameters may also be supplied asarguments.  See ‘Details’.
7651,vectors containing the coordinates of the verticesof the polygon.
7652,the density of shading lines, in lines per inch.  Thedefault value of <code>NULL</code> means that no shading lines are drawn.A zero value of <code>density</code> means no shading nor filling whereasnegative values and <code>NA</code> suppress shading (and so allowcolor filling).
7653,the slope of shading lines, given as an anglein degrees (counter-clockwise).
7654,the color for filling the polygon. The default,<code>NA</code>, is to leave polygons unfilled, unless <code>density</code> isspecified.  (For back-compatibility, <code>NULL</code> is equivalent to<code>NA</code>.)  If <code>density</code> is specified with a positive valuethis gives the color of the shading lines.
7655,the color to draw the border.  The default, <code>NULL</code>,means to use <code>par("fg")</code>.  Use <code>border = NA</code> toomit borders.

7656,For compatibility with S, <code>border</code> can also be logical, inwhich case <code>FALSE</code> is equivalent to <code>NA</code> (borders omitted)and <code>TRUE</code> is equivalent to <code>NULL</code> (use the foreground colour),
7657,the line type to be used, as in <code>par</code>.
7658,graphical parameters such as <code>xpd</code>, <code>lend</code>,<code>ljoin</code> and <code>lmitre</code> can be given as arguments.
7659,logical controlling the polygon shading mode:  seebelow for details.  Default <code>FALSE</code>.
7660,vectors containing the coordinates of the verticesof the path.
7661,the color for filling the path. The default,<code>NA</code>, is to leave paths unfilled.
7662,the color to draw the border.  The default, <code>NULL</code>,means to use <code>par("fg")</code>.  Use <code>border = NA</code> toomit borders.

7663,For compatibility with S, <code>border</code> can also be logical, inwhich case <code>FALSE</code> is equivalent to <code>NA</code> (borders omitted)and <code>TRUE</code> is equivalent to <code>NULL</code> (use the foreground colour),
7664,the line type to be used, as in <code>par</code>.
7665,character value specifying the path fill mode: either<code>"winding"</code> or <code>"evenodd"</code>.
7666,graphical parameters such as <code>xpd</code>, <code>lend</code>,<code>ljoin</code> and <code>lmitre</code> can be given as arguments.
7667,a <code>raster</code> object, or an object that can becoerced to one by <code>as.raster</code>.
7668,a vector (or scalar) of left x positions.
7669,a vector (or scalar) of bottom y positions.
7670,a vector (or scalar) of right x positions.
7671,a vector (or scalar) of top y positions.
7672,angle of rotation (in degrees, anti-clockwisefrom positive x-axis, about the bottom-left corner).
7673,a logical vector (or scalar) indicatingwhether to apply linear interpolation to the image when drawing. 
7674,graphical parameters.
7675,a vector (or scalar) of left x positions.
7676,a vector (or scalar) of bottom y positions.
7677,a vector (or scalar) of right x positions.
7678,a vector (or scalar) of top y positions.
7679,the density of shading lines, in lines per inch.The default value of <code>NULL</code> means that no shading lines are drawn.A zero value of <code>density</code> means no shading lines whereasnegative values (and <code>NA</code>) suppress shading (and so allowcolor filling).
7680,angle (in degrees) of the shading lines.
7681,color(s) to fill or shade the rectangle(s) with.The default <code>NA</code> (or also <code>NULL</code>) means do not fill,i.e., draw transparent rectangles, unless <code>density</code> is specified.
7682,color for rectangle border(s).  The default means<code>par("fg")</code>.  Use <code>border = NA</code> to omit borders.If there are shadinglines, <code>border = TRUE</code> means use the same colour forthe border as for the shading lines.
7683,line type for borders and shading; defaults to <code>"solid"</code>.
7684,line width for borders and shading.  Note that the use of<code>lwd = 0</code> (as in the examples) is device-dependent.
7685,graphical parameters such as <code>xpd</code>, <code>lend</code>,<code>ljoin</code> and <code>lmitre</code> can be given asarguments.
7686,A numeric vector
7687,The length of the ticks making up the ‘rug’.Positive lengths give inwards ticks.
7688,On which side of the plot box the rug will beplotted. Normally 1 (bottom) or 3 (top).
7689,The line width of the ticks.  Some devices will round thedefault width up to <code>1</code>.
7690,The colour the ticks are plotted in.
7691,logical indicating if there should be a warning aboutclipped values.
7692,further arguments, passed to <code>axis</code>, suchas <code>line</code> or <code>pos</code> for specifying the location of the rug.
7693,a two-element vector describing the number of rows and thenumber of columns in a screen matrix <em>or</em> a matrix with 4 columns.If a matrix, then each row describes a screen with values for the left,right, bottom, and top of the screen (in that order) in NDC units,that is 0 at the lower left corner of the device surface, and 1 atthe upper right corner.
7694,a number giving the screen to be split.  It defaults tothe current screen if there is one, otherwise the whole device region.
7695,logical: should the selected screen be cleared?
7696,a number indicating which screen to prepare for drawing(<code>screen</code>), erase (<code>erase.screen</code>), or close(<code>close.screen</code>).  (<code>close.screen</code> will accept a vector ofscreen numbers.)
7697,logical value indicating whether the screen should beerased as part of the preparation for drawing in the screen.
7698,logical value indicating whether all of thescreens should be closed.
7699,coordinates of points <b>from</b> which to draw.
7700,coordinates of points <b>to</b> which to draw.  At leastone must be supplied.
7701,graphical parameters as in<code>par</code>, possibly vectors.  <code>NA</code> values in <code>col</code>cause the segment to be omitted.
7702,further graphical parameters (from <code>par</code>),such as <code>xpd</code> and the line characteristics <code>lend</code>, <code>ljoin</code>and <code>lmitre</code>.
7703,the <code>x</code> and <code>y</code> arguments provide the x and ycoordinates for the plot.  Any reasonable way of defining thecoordinates is acceptable.  See the function <code>xy.coords</code>for details.  If supplied separately, they must be of the same length.
7704,numeric vector of length one (for both directions) or two(for x and y separately) specifying the number of equally spacedgrid points for the density estimation; directly used as<code>gridsize</code> in <code>bkde2D()</code>.
7705,numeric vector (length 1 or 2) of smoothing bandwidth(s).If missing, a more or less useful default is used. <code>bandwidth</code>is subsequently passed to function<code>bkde2D</code>.
7706,function accepting an integer <code>n</code> as an argument andreturning <code>n</code> colors.
7707,number of points to be superimposed on the densityimage.  The first <code>nrpoints</code> points from those areas of lowestregional densities will be plotted.  Adding points to the plotallows for the identification of outliers.  If all points are to beplotted, choose <code>nrpoints = Inf</code>.
7708,<code>logical</code> indicating to return theordered indices of “low density” points if <code>nrpoints &gt; 0</code>.
7709,arguments passed to <code>points</code>,when <code>nrpoints &gt; 0</code>: point symbol, character expansion factorand color, see also <code>par</code>.
7710,function mapping the density scale to the color scale.
7711,either <code>NULL</code> or a function which will becalled (with no arguments) after <code>image</code>.
7712,character strings to be used as axis labels, passedto <code>image</code>.
7713,numeric vectors of length 2 specifying axis limits.
7714,further arguments passed to <code>image</code>,e.g., <code>add=TRUE</code> or <code>useRaster=TRUE</code>.
7715,an object, the default method expects either a single variable(interpreted to be the explanatory variable) or a 2-way table.  Seedetails.
7716,a <code>"factor"</code> interpreted to be the dependent variable
7717,a <code>"formula"</code> of type <code>y ~ x</code> with a singledependent <code>"factor"</code> and a single explanatory variable.
7718,an optional data frame.
7719,if the explanatory variable is numeric, this controls howit is discretized. <code>breaks</code> is passed to <code>hist</code> and canbe a list of arguments.
7720,convenience tolerance parameter for y-axis annotation.If the distance between two labels drops under this threshold, they areplotted equidistantly.
7721,vertical offset between the bars (in per cent). It is fixed to<code>0</code> for spinograms and defaults to <code>2</code> for spine plots.
7722,a character or numeric vector specifying in which orderthe levels of the dependent variable should be plotted.
7723,a vector of fill colors of the same length as <code>levels(y)</code>.The default is to call <code>gray.colors</code>.
7724,character strings for annotation
7725,character vectors for annotation of x and y axis.Default to <code>levels(y)</code> and <code>levels(x)</code>, respectively for thespine plot. For <code>xaxlabels</code> in the spinogram, the breaks areused.
7726,the range of x and y values with sensible defaults.
7727,logical.  If <code>FALSE</code> all axes (including those givinglevel names) are suppressed.
7728,additional arguments passed to <code>rect</code>.
7729,an optional vector specifying a subset of observationsto be used for plotting.
7730,should factors have unused levels dropped?Defaults to <code>FALSE</code>.
7731,a two-element vector describing the number of rows and thenumber of columns in a screen matrix <em>or</em> a matrix with 4 columns.If a matrix, then each row describes a screen with values for the left,right, bottom, and top of the screen (in that order) in NDC units,that is 0 at the lower left corner of the device surface, and 1 atthe upper right corner.
7732,a number giving the screen to be split.  It defaults tothe current screen if there is one, otherwise the whole device region.
7733,logical: should the selected screen be cleared?
7734,a number indicating which screen to prepare for drawing(<code>screen</code>), erase (<code>erase.screen</code>), or close(<code>close.screen</code>).  (<code>close.screen</code> will accept a vector ofscreen numbers.)
7735,logical value indicating whether the screen should beerased as part of the preparation for drawing in the screen.
7736,logical value indicating whether all of thescreens should be closed.
7737,matrix or data frame of data.One star or segment plot will be produced for each row of <code>x</code>.Missing values (<code>NA</code>) are allowed, but they are treated as ifthey were 0 (after scaling, if relevant).
7738,logical flag: if <code>TRUE</code>, the segment plots willoccupy a full circle.  Otherwise, they occupy the (upper) semicircleonly.
7739,logical flag: if <code>TRUE</code>, the columns of the datamatrix are scaled independently so that the maximum value in eachcolumn is 1 and the minimum is 0.  If <code>FALSE</code>, the presumptionis that the data have been scaled by some other algorithm to therange <i>[0, 1]</i>.
7740,logical flag: in <code>TRUE</code>, the radii corresponding toeach variable in the data will be drawn.
7741,vector of character strings for labeling the plots.Unlike the S function <code>stars</code>, no attempt is made to constructlabels if <code>labels = NULL</code>.
7742,Either two column matrix with the x and y coordinates usedto place each of the segment plots; or numeric of length 2 when allplots should be superimposed (for a ‘spider plot’).By default, <code>locations = NULL</code>, the segment plots will beplaced in a rectangular grid.
7743,integers giving the number of rows and columns to use when<code>locations</code> is <code>NULL</code>.  By default, <code>nrow == ncol</code>, a squarelayout will be used.
7744,scale factor for the length of radii or segments.
7745,vector with x and y coordinates of the unit key.
7746,vector of character strings for labeling thesegments of the unit key.  If omitted, the second component of<code>dimnames(x)</code> is used, if available.
7747,clipping switch for the unit key (drawing andlabeling), see <code>par("xpd")</code>.
7748,vector with the range of x coordinates to plot. 
7749,vector with the range of y coordinates to plot.
7750,logical indicating if the label locations shouldflip up and down from diagram to diagram. Defaults to a somewhatsmart heuristic.
7751,logical. If <code>TRUE</code> draw a segment diagram.
7752,color vector (integer or character, see<code>par</code>), each specifying a color for one of the segments(variables).  Ignored if <code>draw.segments = FALSE</code>.
7753,color vector (integer or character, see<code>par</code>), each specifying a color for one of the stars(cases).  Ignored if <code>draw.segments = TRUE</code>.
7754,color vector (integer or character, see<code>par</code>), each specifying a color for one of the lines(cases).  Ignored if <code>draw.segments = TRUE</code>.
7755,logical flag: if <code>TRUE</code> axes are added to the plot.
7756,logical flag: if <code>TRUE</code>, the plot region is framed.
7757,a main title for the plot.
7758,a sub title for the plot.
7759,a label for the x axis.
7760,a label for the y axis.
7761,character expansion factor for the labels.
7762,line width used for drawing.
7763,line type used for drawing.
7764,logical or NA indicating if clipping should be done, see<code>par(xpd = .)</code>.
7765,argument to <code>par(mar = *)</code>, typically choosingsmaller margins than by default.
7766,further arguments, passed to the first call of<code>plot()</code>, see <code>plot.default</code> and to<code>box()</code> if <code>frame.plot</code> is true.
7767,logical, if <code>TRUE</code> <em>add</em> stars to current plot.
7768,logical, if <code>FALSE</code>, nothing is plotted.
7769,a numeric vector.
7770,This controls the plot length.
7771,The desired width of plot.
7772,a tolerance.
7773,a character or expression vector whose dimensionsare to be determined.  Other objects are coerced by<code>as.graphicsAnnot</code>.
7774,character indicating in which units <code>s</code> is measured;should be one of <code>"user"</code>, <code>"inches"</code>, <code>"figure"</code>;partial matching is performed.
7775,numeric <b>c</b>haracter <b>ex</b>pansion factor; multipliedby <code>par("cex")</code> yields the final character size; thedefault <code>NULL</code> is equivalent to <code>1</code>.
7776,additional information about the font,possibly including the graphics parameter <code>"family"</code>:see <code>text</code>.
7777,the data from which the plots are to be produced.  In thedefault method the data can be specified as a single numericvector, or as list of numeric vectors, each corresponding toa component plot.  In the <code>formula</code> method, a symbolicspecification of the form <code>y ~ g</code> can be given,indicating the observations in the vector <code>y</code> are to begrouped according to the levels of the factor<code>g</code>.  <code>NA</code>s are allowed in the data.
7778,a data.frame (or list) from which the variables in<code>x</code> should be taken.
7779,an optional vector specifying a subset of observationsto be used for plotting.
7780,a function which indicates what should happenwhen the data contain <code>NA</code>s.  The default is to ignore missingvalues in either the response or the group.
7781,additional parameters passed to the default method, or byit to <code>plot.window</code>, <code>points</code>,<code>axis</code> and <code>title</code> to control the appearanceof the plot.
7782,the method to be used to separate coincident points.The default method <code>"overplot"</code> causes such points to beoverplotted, but it is also possible to specify <code>"jitter"</code> tojitter the points, or <code>"stack"</code> have coincident pointsstacked.  The last method only makes sense for very granular data.
7783,when <code>method = "jitter"</code> is used, <code>jitter</code>gives the amount of jittering applied.
7784,when stacking is used, points are stacked this manyline-heights (symbol widths) apart.
7785,when vertical is <code>TRUE</code> the plots are drawnvertically rather than the default horizontal.
7786,group labels which will be printed alongside(or underneath) each plot.
7787,logical, if true <em>add</em> the chart to the current plot.
7788,numeric vector giving the locations where the charts shouldbe drawn, particularly when <code>add = TRUE</code>;defaults to <code>1:n</code> where <code>n</code> is the number of boxes.
7789,labels: see <code>title</code>.
7790,alternate way to specify axis labels: see ‘Details’.
7791,plot limits: see <code>plot.window</code>.
7792,on which axes to use a log scale:  see<code>plot.default</code>
7793,Graphical parameters: see <code>par</code>.
7794,Axis control:  see <code>plot.default</code>.
7795,a character or expression vector whose dimensionsare to be determined.  Other objects are coerced by<code>as.graphicsAnnot</code>.
7796,character indicating in which units <code>s</code> is measured;should be one of <code>"user"</code>, <code>"inches"</code>, <code>"figure"</code>;partial matching is performed.
7797,numeric <b>c</b>haracter <b>ex</b>pansion factor; multipliedby <code>par("cex")</code> yields the final character size; thedefault <code>NULL</code> is equivalent to <code>1</code>.
7798,additional information about the font,possibly including the graphics parameter <code>"family"</code>:see <code>text</code>.
7799,numeric vector of <code>x</code>-coordinates of length <code>n</code>,say, or another valid plotting structure, as for<code>plot.default</code>, see also <code>xy.coords</code>.
7800,numeric vector of <code>y</code>-coordinates of length <code>n</code>.
7801,integer vector of length <code>n</code>. <code>number[i]</code> = numberof replicates for <code>(x[i], y[i])</code>, may be 0.<br>Default (<code>missing(number)</code>): compute the exact multiplicity ofthe points <code>x[], y[]</code>, via<code>xyTable()</code>.
7802,character indicating log coordinate scale, see<code>plot.default</code>.
7803,when <code>number</code> is computed (i.e., not specified),<code>x</code> and <code>y</code> are rounded to <code>digits</code> significantdigits before multiplicities are computed.
7804,character label for x-, or y-axis, respectively.
7805,<code>numeric(2)</code> limiting the extents of the x-,or y-axis.
7806,logical; should the plot be added on a previous one ?Default is <code>FALSE</code>.
7807,logical; if <code>TRUE</code>, randomly rotate thesunflowers (preventing artefacts).
7808,plotting character to be used for points(<code>number[i]==1</code>) and center of sunflowers.
7809,numeric; character size expansion of center points(s. <code>pch</code>).
7810,numeric <em>shrinking</em> factor to be used for thecenter points <em>when there are flower leaves</em>,i.e., <code>cex / cex.fact</code> is used for these.
7811,colors for the plot symbols, passed to<code>plot.default</code>.
7812,of sunflower leaves in inches, 1[in] := 2.54[cm].Default: 1/8\", approximately 3.2mm.
7813,color to be used for the <b>seg</b>ments which make thesunflowers leaves, see <code>par(col=)</code>;<code>col = "gold"</code> reminds of real sunflowers.
7814,numeric; the line width for the leaves' segments.
7815,further arguments to <code>plot</code> [if<code>add = FALSE</code>], or to be passed to or from another method.
7816,a <code>formula</code>, such as <code>y ~ x</code>.
7817,a data.frame (or list) from which the variables in<code>formula</code> should be taken.
7818,an optional vector specifying a subset of observationsto be used in the fitting process.
7819,a function which indicates what should happenwhen the data contain <code>NA</code>s.  The default is to ignore casewith missing values.
7820,the x and y co-ordinates for the centres of the symbols.They can be specified in any way which is accepted by<code>xy.coords</code>.
7821,a vector giving the radii of the circles.
7822,a vector giving the length of the sides of the squares.
7823,a matrix with two columns.  The first column giveswidths and the second the heights of rectangles.
7824,a matrix with three or more columns giving the lengthsof the rays from the center of the stars.  <code>NA</code> valuesare replaced by zeroes.
7825,a matrix with three or four columns.  The firsttwo columns give the width and height of the thermometer symbols. Ifthere are three columns, the third is taken as a proportion: thethermometers are filled (using colour <code>fg</code>) from their base tothis proportion of their height.  If there are four columns, thethird and fourth columns are taken as proportions and thethermometers are filled between these two proportions of theirheights.  The part of the box not filled in <code>fg</code> will be filledin the background colour (default transparent) given by <code>bg</code>.
7826,a matrix with five columns.  The first two columnsgive the width and height of the boxes, the next two columnsgive the lengths of the lower and upper whiskers and the fifththe proportion (with a warning if not in [0,1]) of the way upthe box that the median line is drawn.
7827,<code>TRUE</code>, <code>FALSE</code> or a positive number.See ‘Details’.
7828,if <code>add</code> is <code>TRUE</code>, the symbols are added toan existing plot, otherwise a new plot is created.
7829,colour(s) the symbols are to be drawn in.
7830,if specified, the symbols are filled with colour(s), thevector <code>bg</code> being recycled to the number of symbols.The default is to leave the symbols unfilled.
7831,the x label of the plot if <code>add</code> is not true.Defaults to the <code>deparse</code>d expression used for <code>x</code>.
7832,the y label of the plot.  Unused if <code>add = TRUE</code>.
7833,a main title for the plot.  Unused if <code>add = TRUE</code>.
7834,numeric vector of length 2 giving the x limits for theplot.  Unused if <code>add = TRUE</code>.
7835,numeric vector of length 2 giving the y limits for theplot.  Unused if <code>add = TRUE</code>.
7836,graphics parameters can also be passed to this function,as can the plot aspect ratio <code>asp</code> (see <code>plot.window</code>).
7837,numeric vectors of coordinates where the text<code>labels</code> should be written.  If the length of <code>x</code> and<code>y</code> differs, the shorter one is recycled.
7838,a character vector or expression specifyingthe <em>text</em> to be written.  An attempt is made to coerce otherlanguage objects (names and calls) to expressions, and vectors andother classed objects to character vectors by <code>as.character</code>.If <code>labels</code> is longer than <code>x</code> and<code>y</code>, the coordinates are recycled to the length of <code>labels</code>.
7839,one or two values in <i>[0, 1]</i> which specify the x(and optionally y) adjustment (‘justification’) of thelabels, with 0 for left/bottom, 1 for right/top, and 0.5 forcentered.  On most devices values outside <i>[0, 1]</i> will alsowork.  See below.
7840,a position specifier for the text.  If specified thisoverrides any <code>adj</code> value given.  Values of <code>1</code>,<code>2</code>, <code>3</code> and <code>4</code>, respectively indicatepositions below, to the left of, above and to the right ofthe specified <code>(x,y)</code> coordinates.
7841,when <code>pos</code> is specified, this value controls thedistance (‘offset’) of the text label from the specifiedcoordinate in fractions of a character width.
7842,<code>NULL</code> for the current font family, or a charactervector of length 2 for Hershey vector fonts.  The first element ofthe vector selects a typeface and the second element selects astyle.  Ignored if <code>labels</code> is an expression.
7843,numeric <b>c</b>haracter <b>ex</b>pansion factor; multipliedby <code>par("cex")</code> yields the final character size.<code>NULL</code> and <code>NA</code> are equivalent to <code>1.0</code>.
7844,the color and (if <code>vfont = NULL</code>) font to beused, possibly vectors.  These default to the values of the globalgraphical parameters in <code>par()</code>.
7845,further graphical parameters (from <code>par</code>),such as <code>srt</code>, <code>family</code> and <code>xpd</code>.
7846,numeric vectors of coordinates where the text<code>labels</code> should be written.  If the length of <code>x</code> and<code>y</code> differs, the shorter one is recycled.
7847,a character vector or expression specifyingthe <em>text</em> to be written.  An attempt is made to coerce otherlanguage objects (names and calls) to expressions, and vectors andother classed objects to character vectors by <code>as.character</code>.If <code>labels</code> is longer than <code>x</code> and<code>y</code>, the coordinates are recycled to the length of <code>labels</code>.
7848,one or two values in <i>[0, 1]</i> which specify the x(and optionally y) adjustment (‘justification’) of thelabels, with 0 for left/bottom, 1 for right/top, and 0.5 forcentered.  On most devices values outside <i>[0, 1]</i> will alsowork.  See below.
7849,a position specifier for the text.  If specified thisoverrides any <code>adj</code> value given.  Values of <code>1</code>,<code>2</code>, <code>3</code> and <code>4</code>, respectively indicatepositions below, to the left of, above and to the right ofthe specified <code>(x,y)</code> coordinates.
7850,when <code>pos</code> is specified, this value controls thedistance (‘offset’) of the text label from the specifiedcoordinate in fractions of a character width.
7851,<code>NULL</code> for the current font family, or a charactervector of length 2 for Hershey vector fonts.  The first element ofthe vector selects a typeface and the second element selects astyle.  Ignored if <code>labels</code> is an expression.
7852,numeric <b>c</b>haracter <b>ex</b>pansion factor; multipliedby <code>par("cex")</code> yields the final character size.<code>NULL</code> and <code>NA</code> are equivalent to <code>1.0</code>.
7853,the color and (if <code>vfont = NULL</code>) font to beused, possibly vectors.  These default to the values of the globalgraphical parameters in <code>par()</code>.
7854,further graphical parameters (from <code>par</code>),such as <code>srt</code>, <code>family</code> and <code>xpd</code>.
7855,The main title (on top) using font, size (characterexpansion) and color <code>par(c("font.main", "cex.main", "col.main"))</code>.
7856,Sub-title (at bottom) using font, size and color <code>par(c("font.sub", "cex.sub", "col.sub"))</code>.
7857,X axis label using font, size and color <code>par(c("font.lab", "cex.lab", "col.lab"))</code>.
7858,Y axis label, same font attributes as <code>xlab</code>.
7859,specifying a value for <code>line</code> overrides thedefault placement of labels, and places them this many linesoutwards from the plot edge.
7860,a logical value.  If <code>TRUE</code>, the titles areplaced in the outer margins of the plot.
7861,further graphical parameters from <code>par</code>.  Usee.g., <code>col.main</code> or <code>cex.sub</code> instead of just <code>col</code>or <code>cex</code>.  <code>adj</code> controls the justification of the titles.<code>xpd</code> can be used to set the clipping region:this defaults to the figure region unless <code>outer = TRUE</code>,otherwise the device region and can only be increased.  <code>mgp</code>controls the default placing of the axis titles.
7862,numeric vector
7863,numeric of length 1 or 2.
7864,logical; if <code>TRUE</code>, a warning is printed in caseof active log scale.
7865,vectors containing the coordinates of the verticesof the polygon. See <code>xy.coords</code> for alternatives.
7866,A numeric vector of values between -1 and 1, whichcontrol the shape of the spline relative to the control points.
7867,A logical value indicating whether the spline isan open or a closed shape.
7868,For open X-splines, a logical value indicating whetherthe first and last control points should be replicated for drawingthe curve. Ignored for closed X-splines.
7869,logical: should the X-spline be drawn?  If false, a set ofline segments to draw the curve is returned, and nothing is drawn.
7870,the color to draw the curve.   Use <code>border = NA</code> toomit borders.
7871,the color for filling the shape. The default,<code>NA</code>, is to leave unfilled.
7872,graphical parameters such as <code>lty</code>, <code>xpd</code>,<code>lend</code>, <code>ljoin</code> and <code>lmitre</code> can be given as arguments.
7873,numeric vector
7874,numeric of length 1 or 2.
7875,logical; if <code>TRUE</code>, a warning is printed in caseof active log scale.
7876,numeric vector
7877,numeric of length 1 or 2.
7878,logical; if <code>TRUE</code>, a warning is printed in caseof active log scale.
7879, a n by m matrix (or vector if m= 1)
7880, a m by p matrix (or vector if m = 1)
7881,A rectangular matrix or data frame (probably a correlation matrix)
7882,A data matrix or data frame or a vector depending upon the function.
7883,A data matrix or data frame or a vector
7884,The object returned from either a factor analysis (fa) or a principal components analysis (principal) 
7885,round to digits
7886,Abbreviate to minlength in lowerCor
7887,Should pairwise deletion be done, or one of the other options to cor
7888,Should we check for NA on the diagonal of a correlation matices
7889,"pearson", "kendall", "spearman"
7890,the current value of some looping variable
7891,The maximum value the loop will achieve
7892,what function is looping
7893,The factor or components to be reversed keyed (by factor number)
7894,flag=TRUE in char2numeric will flag those variables that had been numeric
7895,Correct for the maximum possible information in this item
7896,What is the base for the log function (default=2, e implies base = exp(1))
7897,The name of a package (or list of packages) to be activated and then have all      the examples tested.
7898,Find the dependencies for this package, e.g., psych
7899,Which type of dependency to examine?
7900,Look up the dependencies, and then test all of their examples
7901,Do not test these dependencies
7902,A dataframe or matrix to choose from
7903,select from column with name from to column with name to
7904,select from column from to column to
7905,Any string of legitimate objects
7906,A data.frame or matrix of data, or a covariance or correlation matrix 
7907,If some items are to be reversed keyed, then either specify the direction of all items or just a vector of which items to reverse 
7908,Any text string to identify this run
7909,should means reflect the sum of items or the mean of the items.  The default value is means.
7910,the number of categories/item to consider if reporting category frequencies.  Defaults to 10, passed to <code>link{response.frequencies}</code> 
7911,The default is to remove missing values and find pairwise correlations
7912,if TRUE, then find the first principal component and reverse key items with negative loadings.  Give a warning if this happens. 
7913,Number of iterations if bootstrapped confidence intervals are desired
7914,Delete items with no variance and issue a warning
7915,Options to pass to the cor function: "everything", "all.obs", "complete.obs", "na.or.complete", or "pairwise.complete.obs". The default is "pairwise"
7916,By default print a warning and a message that items were reversed. Suppress the message if warnings = FALSE
7917,The value to use for confidence intervals
7918,If using correlation matrices as input, by specify the number of observations, we can find confidence intervals
7919,How should we impute missing data? Not at all, medians, or means
7920,Number of items in the scale (to find r.bar)
7921,width of confidence interval (pval/2 to 1-p.val/2) 
7922,How many digits to use for alpha.ci
7923,A data.frame or matrix of data, or a covariance or correlation matrix 
7924,If some items are to be reversed keyed, then either specify the direction of all items or just a vector of which items to reverse 
7925,Any text string to identify this run
7926,should means reflect the sum of items or the mean of the items.  The default value is means.
7927,the number of categories/item to consider if reporting category frequencies.  Defaults to 10, passed to <code>link{response.frequencies}</code> 
7928,The default is to remove missing values and find pairwise correlations
7929,if TRUE, then find the first principal component and reverse key items with negative loadings.  Give a warning if this happens. 
7930,Number of iterations if bootstrapped confidence intervals are desired
7931,Delete items with no variance and issue a warning
7932,Options to pass to the cor function: "everything", "all.obs", "complete.obs", "na.or.complete", or "pairwise.complete.obs". The default is "pairwise"
7933,By default print a warning and a message that items were reversed. Suppress the message if warnings = FALSE
7934,The value to use for confidence intervals
7935,If using correlation matrices as input, by specify the number of observations, we can find confidence intervals
7936,How should we impute missing data? Not at all, medians, or means
7937,Number of items in the scale (to find r.bar)
7938,width of confidence interval (pval/2 to 1-p.val/2) 
7939,How many digits to use for alpha.ci
7940,An object from <code>setCor</code>,  <code>mediate</code>, <code>omega</code>, or <code>fa</code>.
7941,More objects of the same type may be supplied here
7942,a 4 x 1 vector or a 2 x2 table of TP, FP, FN, TN values (see below) May be counts or proportions.
7943,Base Rate of successful outcomes or actual symptom (if t is not specified)
7944,Selection Rate for  candidates or diagnoses (if t is not specified)
7945,The Phi correlation coefficient between the predictor and the outcome variable(if t is not specified)
7946,The number of Valid Positives (selected applicants who succeed; correct diagnoses).(if t and Phi are not specified)
7947,Names of variables  1 and 2
7948,"b" (both), "d" (decision theory), "a" (auc), or "n" neither
7949,If True, then the noise distribution is centered at zero
7950,Cell values of 0 are replaced with correct. (See <code>tetrachoric</code> for a discussion of why this is needed.)
7951,The color choice for the VP and FP, defaults to =c("blue","red") but could be c("grey","black") if we want to avoid colors 
7952,a vector, data.frame or matrix
7953,the lag to use when finding <code>diff</code>     
7954,A column of the x data.frame to be used for grouping
7955,Should missing data be removed?
7956,How to handle missing data in autoR
7957,.
7958,A correlation matrix or a data matrix suitable for factoring
7959,Factors from 1 to nfactors will be extracted. If nfactors is a a vector, then just the number of factors specified in the vector will be extracted. (See examples).
7960,Factor method.  The default is 'minres' factoring.  Although to be consistent with the original Goldberg article, we can also do principal components (fm ="pca").
7961,What type of rotation to apply.  The default for factors is oblimin.  Unlike the normal call to pca where the default is varimax, in bassAckward the default for pca is oblimin.
7962,What factor scoring algorithm should be used. The default is "tenBerge", other possibilities include "regression", or "bartlett"
7963,If using any other scoring proceure that "tenBerge" should we adjust the correlations for the lack of factor score fit?
7964,By default draw a bassAckward diagram
7965,How to treat missing data.  Use='pairwise" finds pairwise complete correlations.
7966,What kind of correlation to find.  The default is Pearson.
7967,Should cases be weighted?  Default, no.
7968,If finding tetrachoric or polychoric correlations, what correction should be applied to empty cells (defaults to .5)
7969,The object returned by bassAckward
7970,Number of digits to display on each path
7971,Values greater than the abs(cut) will be displayed in a path diagram.
7972,Labels may be taken from the output of the bassAckward function or can be specified as a list.
7973,Margins are set to be slightly bigger than normal to allow for a cleaner diagram
7974,The main title for the figure
7975,if TRUE, show the items associated with the factors
7976,if TRUE, sort the items by factor loadings
7977,Should the graphic be drawn left to right or top to bottom
7978,Should we show the correlations between factors at the same level
7979,Rename and sort the factors at two lowest levels for a more pleasing figure
7980,If TRUE, then show the percent variance accounted for by this factor.
7981,Other graphic parameters (e.g., cex)
7982,A correlation matrix or a data matrix suitable for factoring
7983,Factors from 1 to nfactors will be extracted. If nfactors is a a vector, then just the number of factors specified in the vector will be extracted. (See examples).
7984,Factor method.  The default is 'minres' factoring.  Although to be consistent with the original Goldberg article, we can also do principal components (fm ="pca").
7985,What type of rotation to apply.  The default for factors is oblimin.  Unlike the normal call to pca where the default is varimax, in bassAckward the default for pca is oblimin.
7986,What factor scoring algorithm should be used. The default is "tenBerge", other possibilities include "regression", or "bartlett"
7987,If using any other scoring proceure that "tenBerge" should we adjust the correlations for the lack of factor score fit?
7988,By default draw a bassAckward diagram
7989,How to treat missing data.  Use='pairwise" finds pairwise complete correlations.
7990,What kind of correlation to find.  The default is Pearson.
7991,Should cases be weighted?  Default, no.
7992,If finding tetrachoric or polychoric correlations, what correction should be applied to empty cells (defaults to .5)
7993,The object returned by bassAckward
7994,Number of digits to display on each path
7995,Values greater than the abs(cut) will be displayed in a path diagram.
7996,Labels may be taken from the output of the bassAckward function or can be specified as a list.
7997,Margins are set to be slightly bigger than normal to allow for a cleaner diagram
7998,The main title for the figure
7999,if TRUE, show the items associated with the factors
8000,if TRUE, sort the items by factor loadings
